--- a/android/src/main/cpp/ghostty_jni.cpp
+++ b/android/src/main/cpp/ghostty_jni.cpp
@@ -66,6 +66,8 @@
   GhosttyRenderStateRowCells cells = nullptr;
   GhosttyKeyEncoder encoder = nullptr;
   GhosttyKeyEvent keyEvent = nullptr;
+  GhosttyMouseEncoder mouseEncoder = nullptr;
+  GhosttyMouseEvent mouseEvent = nullptr;
   // Query responses emitted by the terminal during vt_write; forwarded to the
   // PTY by the caller after each write.
   std::vector<uint8_t> ptyOut;
@@ -125,6 +127,8 @@
 
 void destroySession(Session* session) {
   if (session == nullptr) return;
+  ghostty_mouse_event_free(session->mouseEvent);
+  ghostty_mouse_encoder_free(session->mouseEncoder);
   ghostty_key_event_free(session->keyEvent);
   ghostty_key_encoder_free(session->encoder);
   ghostty_render_state_row_cells_free(session->cells);
@@ -243,7 +247,9 @@
       ghostty_render_state_row_iterator_new(nullptr, &session->rowIter) != GHOSTTY_SUCCESS ||
       ghostty_render_state_row_cells_new(nullptr, &session->cells) != GHOSTTY_SUCCESS ||
       ghostty_key_encoder_new(nullptr, &session->encoder) != GHOSTTY_SUCCESS ||
-      ghostty_key_event_new(nullptr, &session->keyEvent) != GHOSTTY_SUCCESS) {
+      ghostty_key_event_new(nullptr, &session->keyEvent) != GHOSTTY_SUCCESS ||
+      ghostty_mouse_encoder_new(nullptr, &session->mouseEncoder) != GHOSTTY_SUCCESS ||
+      ghostty_mouse_event_new(nullptr, &session->mouseEvent) != GHOSTTY_SUCCESS) {
     __android_log_print(ANDROID_LOG_ERROR, kLogTag, "failed to create terminal session");
     destroySession(session);
     return 0;
@@ -826,4 +832,75 @@
   return outArray;
 }
 
+// A TUI that asked for mouse tracking owns the scroll gesture: the finger must
+// become a wheel report, not a scrollback move. Reported as a bitmask so one
+// JNI call answers a whole gesture rather than one per touch sample.
+JNIEXPORT jint JNICALL
+Java_expo_modules_libghostty_GhosttyVt_nativeTerminalModes(JNIEnv*, jobject, jlong handle) {
+  auto* session = fromHandle(handle);
+  if (session == nullptr) return 0;
+  jint flags = 0;
+  bool value = false;
+  if (ghostty_terminal_mode_get(session->term, GHOSTTY_MODE_NORMAL_MOUSE, &value) ==
+          GHOSTTY_SUCCESS && value) flags |= 1 << 0;
+  if (ghostty_terminal_mode_get(session->term, GHOSTTY_MODE_BUTTON_MOUSE, &value) ==
+          GHOSTTY_SUCCESS && value) flags |= 1 << 1;
+  if (ghostty_terminal_mode_get(session->term, GHOSTTY_MODE_ANY_MOUSE, &value) ==
+          GHOSTTY_SUCCESS && value) flags |= 1 << 2;
+  if (ghostty_terminal_mode_get(session->term, GHOSTTY_MODE_SGR_MOUSE, &value) ==
+          GHOSTTY_SUCCESS && value) flags |= 1 << 3;
+  if (ghostty_terminal_mode_get(session->term, GHOSTTY_MODE_ALT_SCREEN, &value) ==
+          GHOSTTY_SUCCESS && value) flags |= 1 << 4;
+  if (ghostty_terminal_mode_get(session->term, GHOSTTY_MODE_ALT_SCROLL, &value) ==
+          GHOSTTY_SUCCESS && value) flags |= 1 << 5;
+  return flags;
+}
+
+// Position is surface-space PIXELS; the encoder converts to cells using the
+// cell size handed to nativeResize. Buttons follow X11 numbering, so wheel up
+// and down are FOUR and FIVE.
+JNIEXPORT jbyteArray JNICALL
+Java_expo_modules_libghostty_GhosttyVt_nativeEncodeMouse(
+    JNIEnv* env, jobject, jlong handle, jint button, jint action, jfloat x, jfloat y,
+    jint metaState) {
+  auto* session = fromHandle(handle);
+  if (session == nullptr) return nullptr;
+
+  // Mouse modes change as programs run (Claude Code enables 1000/1002/1003/1006
+  // in its first chunk); sync before every encode so the format follows.
+  ghostty_mouse_encoder_setopt_from_terminal(session->mouseEncoder, session->term);
+
+  GhosttyMouseAction mouseAction = GHOSTTY_MOUSE_ACTION_PRESS;
+  if (action == 1) mouseAction = GHOSTTY_MOUSE_ACTION_RELEASE;
+  if (action == 2) mouseAction = GHOSTTY_MOUSE_ACTION_MOTION;
+
+  ghostty_mouse_event_set_action(session->mouseEvent, mouseAction);
+  ghostty_mouse_event_set_button(session->mouseEvent,
+                                 static_cast<GhosttyMouseButton>(button));
+  ghostty_mouse_event_set_mods(session->mouseEvent, mapMods(metaState));
+  GhosttyMousePosition position{x, y};
+  ghostty_mouse_event_set_position(session->mouseEvent, position);
+
+  char stackBuf[64];
+  size_t written = 0;
+  GhosttyResult result = ghostty_mouse_encoder_encode(session->mouseEncoder,
+                                                      session->mouseEvent, stackBuf,
+                                                      sizeof(stackBuf), &written);
+  std::vector<char> heapBuf;
+  const char* out = stackBuf;
+  if (result == GHOSTTY_OUT_OF_SPACE) {
+    heapBuf.resize(written);
+    result = ghostty_mouse_encoder_encode(session->mouseEncoder, session->mouseEvent,
+                                          heapBuf.data(), heapBuf.size(), &written);
+    out = heapBuf.data();
+  }
+
+  if (result != GHOSTTY_SUCCESS || written == 0) return nullptr;
+  jbyteArray outArray = env->NewByteArray(static_cast<jsize>(written));
+  if (outArray == nullptr) return nullptr;
+  env->SetByteArrayRegion(outArray, 0, static_cast<jsize>(written),
+                          reinterpret_cast<const jbyte*>(out));
+  return outArray;
+}
+
 }  // extern "C"
--- a/android/src/main/java/expo/modules/libghostty/GhosttyVt.kt
+++ b/android/src/main/java/expo/modules/libghostty/GhosttyVt.kt
@@ -96,4 +96,22 @@
 
   /** Encode clipboard bytes for the PTY (control-byte strip + mode-2004 wrap). */
   external fun nativeEncodePaste(handle: Long, data: ByteArray): ByteArray?
+
+  /** Active DEC private modes as a bitmask: 1000, 1002, 1003, 1006, alt screen,
+   *  alt scroll. A TUI with mouse tracking on owns the scroll gesture. */
+  @JvmStatic
+  external fun nativeTerminalModes(handle: Long): Int
+
+  /** Encodes one mouse event for the PTY. Position is in surface-space pixels;
+   *  buttons are X11-numbered, so 4 is wheel up and 5 is wheel down. Actions:
+   *  0 press, 1 release, 2 motion. */
+  @JvmStatic
+  external fun nativeEncodeMouse(
+    handle: Long,
+    button: Int,
+    action: Int,
+    x: Float,
+    y: Float,
+    metaState: Int
+  ): ByteArray?
 }
--- a/android/src/main/java/expo/modules/libghostty/GhosttyTerminalView.kt
+++ b/android/src/main/java/expo/modules/libghostty/GhosttyTerminalView.kt
@@ -45,6 +45,13 @@
  * snapshots from JNI once per Choreographer frame and patches them onto a
  * grid-sized bitmap; [onDraw] blits the bitmap and overlays the cursor.
  */
+/** nativeTerminalModes bits 0..2: DEC 1000, 1002, 1003. */
+private const val MODE_MOUSE_TRACKING = 0b111
+/** X11 wheel buttons and the press action, as nativeEncodeMouse takes them. */
+private const val MOUSE_WHEEL_UP = 4
+private const val MOUSE_WHEEL_DOWN = 5
+private const val MOUSE_ACTION_PRESS = 0
+
 internal class GhosttyTerminalView(context: Context) : View(context) {
   var onInputBytes: ((ByteArray) -> Unit)? = null
   var onGridResize: ((cols: Int, rows: Int) -> Unit)? = null
@@ -166,14 +173,17 @@
       val deltaRows = (scrollAccum / cellHeight).toInt()
       if (deltaRows != 0) {
         scrollAccum -= deltaRows * cellHeight
-        GhosttyVt.nativeScroll(handle, deltaRows)
-        markScrollActivity()
-        scheduleFrame()
+        scrollRows(deltaRows, lastTouchX, lastTouchY)
       }
       postOnAnimation(this)
     }
   }
 
+  /** When false the host owns focus and the IME: a tap neither grabs focus nor
+   *  opens this view's keyboard. A React Native host drives input through its
+   *  own TextInput, and two keyboards fight over the same surface. */
+  var managesFocus: Boolean = true
+
   private val gestureDetector = GestureDetector(
     context,
     object : GestureDetector.SimpleOnGestureListener() {
@@ -193,9 +203,11 @@
           clearSelection()
           return true
         }
-        requestFocus()
-        context.getSystemService(InputMethodManager::class.java)
-          ?.showSoftInput(this@GhosttyTerminalView, 0)
+        if (managesFocus) {
+          requestFocus()
+          context.getSystemService(InputMethodManager::class.java)
+            ?.showSoftInput(this@GhosttyTerminalView, 0)
+        }
         return true
       }
 
@@ -225,13 +237,13 @@
         distanceY: Float
       ): Boolean {
         if (handle == 0L || scaleDetector.isInProgress) return false
+        lastTouchX = e2.x
+        lastTouchY = e2.y
         scrollAccum += distanceY
         val deltaRows = (scrollAccum / cellHeight).toInt()
         if (deltaRows != 0) {
           scrollAccum -= deltaRows * cellHeight
-          GhosttyVt.nativeScroll(handle, deltaRows)
-          markScrollActivity()
-          scheduleFrame()
+          scrollRows(deltaRows, e2.x, e2.y)
         }
         return true
       }
@@ -367,6 +379,45 @@
     }
   }
 
+  /** Current DEC private modes as nativeTerminalModes' bitmask; 0 once freed. */
+  fun terminalModes(): Int = if (handle == 0L) 0 else GhosttyVt.nativeTerminalModes(handle)
+
+  private var lastTouchX = 0f
+  private var lastTouchY = 0f
+
+  /** Move the view by whole rows, or hand the gesture to the program.
+   *
+   *  A TUI that enabled mouse tracking (Claude Code does, in its first chunk,
+   *  and it runs on the alternate screen where there is no scrollback) owns
+   *  the scroll: the finger becomes wheel reports and the program repaints.
+   *  Everything else scrolls our own history. Decided per gesture step, so a
+   *  program toggling modes mid-session is followed without a rebuild. */
+  private fun scrollRows(deltaRows: Int, x: Float, y: Float) {
+    if (handle == 0L || deltaRows == 0) return
+    val modes = GhosttyVt.nativeTerminalModes(handle)
+    val mouseTracking = modes and MODE_MOUSE_TRACKING != 0
+    if (mouseTracking && !finished) {
+      // X11 numbering: 4 is wheel up, 5 is wheel down. deltaRows > 0 means the
+      // finger moved up, which the user reads as scrolling DOWN the content.
+      val button = if (deltaRows > 0) MOUSE_WHEEL_DOWN else MOUSE_WHEEL_UP
+      val steps = kotlin.math.abs(deltaRows)
+      var out: ByteArray? = null
+      for (i in 0 until steps) {
+        val report = GhosttyVt.nativeEncodeMouse(handle, button, MOUSE_ACTION_PRESS, x, y, 0)
+          ?: break
+        out = if (out == null) report else out + report
+      }
+      // Straight to the PTY: sendBytes would clear a selection and jump to the
+      // live view, neither of which a scroll should do.
+      if (out != null) onInputBytes?.invoke(out)
+      markScrollActivity()
+      return
+    }
+    GhosttyVt.nativeScroll(handle, deltaRows)
+    markScrollActivity()
+    scheduleFrame()
+  }
+
   private fun sendBytes(bytes: ByteArray) {
     if (!finished && bytes.isNotEmpty()) {
       if (selectionMode) clearSelection()
--- a/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyView.kt
+++ b/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyView.kt
@@ -19,6 +19,10 @@
 
   private val onInput by EventDispatcher<Map<String, Any>>()
   private val onResize by EventDispatcher<Map<String, Any>>()
+  // The host gates gesture bytes on the program's mouse and alt-screen modes,
+  // so it has to hear about them; emitted only when the mask changes.
+  private val onModes by EventDispatcher<Map<String, Any>>()
+  private var lastModes = -1
 
   private val terminal = GhosttyTerminalView(context).also { view ->
     view.onInputBytes = { bytes ->
@@ -55,9 +59,43 @@
     }
   }
 
+  /** The library's accessory bar and keyboard padding exist for hosts that do
+   *  not handle the IME themselves. A React Native host that draws its own
+   *  accessory row and does its own keyboard avoidance gets two bars and a
+   *  doubled inset, so both are opt-out and both default to the old behaviour. */
+  var showsAccessoryBar: Boolean = true
+    set(value) {
+      field = value
+      if (!value) accessoryBar.visibility = GONE
+    }
+
+  var managesKeyboardInsets: Boolean = true
+    set(value) {
+      field = value
+      if (!value && paddingBottom != 0) {
+        setPadding(0, 0, 0, 0)
+        requestLayout()
+      }
+    }
+
+  fun setManagesFocus(value: Boolean) {
+    terminal.managesFocus = value
+  }
+
   private fun syncAccessoryBar(insets: WindowInsetsCompat) {
     val imeVisible = insets.isVisible(WindowInsetsCompat.Type.ime())
-    accessoryBar.visibility = if (imeVisible) VISIBLE else GONE
+    if (showsAccessoryBar) {
+      accessoryBar.visibility = if (imeVisible) VISIBLE else GONE
+    } else if (accessoryBar.visibility != GONE) {
+      accessoryBar.visibility = GONE
+    }
+    if (!managesKeyboardInsets) {
+      if (paddingBottom != 0) {
+        setPadding(0, 0, 0, 0)
+        requestLayout()
+      }
+      return
+    }
     // Edge-to-edge windows don't resize for the keyboard — it just covers us.
     // Pad our bottom by the covered amount so the bar sits above the IME and
     // the terminal shrinks (regaining the hidden rows). Hosts that already
@@ -93,7 +131,14 @@
   )
 
   /** Feed PTY output (terminal.output on the wire) into the grid. */
-  fun write(data: ByteArray) = terminal.write(data)
+  fun write(data: ByteArray) {
+    terminal.write(data)
+    val modes = terminal.terminalModes()
+    if (modes != lastModes) {
+      lastModes = modes
+      onModes(mapOf("mask" to modes))
+    }
+  }
 
   /** Mark the underlying PTY as exited (terminal.exit on the wire). */
   fun finish(exitCode: Int) = terminal.finish(exitCode)
--- a/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyModule.kt
+++ b/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyModule.kt
@@ -12,7 +12,7 @@
     Name("ExpoLibghostty")
 
     View(ExpoLibghosttyView::class) {
-      Events("onInput", "onResize")
+      Events("onInput", "onResize", "onModes")
 
       // Base font size in dp (default 14); applied live, the grid reflows.
       Prop("fontSize") { view: ExpoLibghosttyView, size: Float ->
@@ -24,6 +24,20 @@
         view.setTheme(theme)
       }
 
+      // Hand focus, the IME and the accessory row back to the host. Every
+      // default keeps the library's own behaviour, so these are inert unset.
+      Prop("managesFocus") { view: ExpoLibghosttyView, value: Boolean ->
+        view.setManagesFocus(value)
+      }
+
+      Prop("showsAccessoryBar") { view: ExpoLibghosttyView, value: Boolean ->
+        view.showsAccessoryBar = value
+      }
+
+      Prop("managesKeyboardInsets") { view: ExpoLibghosttyView, value: Boolean ->
+        view.managesKeyboardInsets = value
+      }
+
       // PTY output bytes (base64) → terminal grid.
       AsyncFunction("write") { view: ExpoLibghosttyView, base64: String ->
         val data = try {
--- a/src/ExpoLibghostty.types.ts
+++ b/src/ExpoLibghostty.types.ts
@@ -14,6 +14,15 @@
   rows: number;
 };
 
+export type TerminalModesEvent = {
+  /**
+   * DEC private modes as a bitmask: bit0 1000 (normal mouse), bit1 1002
+   * (button), bit2 1003 (any), bit3 1006 (SGR), bit4 alt screen, bit5 1007
+   * (alt scroll). Emitted when the mask changes after a write.
+   */
+  mask: number;
+};
+
 export type TerminalViewRef = {
   /** Feed base64-encoded PTY output into the terminal grid. */
   write(base64: string): Promise<void>;
@@ -61,6 +70,14 @@
   onInput?: (event: { nativeEvent: TerminalInputEvent }) => void;
   /** Grid resized (layout, rotation, font change); forward to the PTY. */
   onResize?: (event: { nativeEvent: TerminalResizeEvent }) => void;
+  /** Program changed its mouse/alt-screen modes; see TerminalModesEvent. */
+  onModes?: (event: { nativeEvent: TerminalModesEvent }) => void;
+  /** False hands focus and the IME to the host (default true). */
+  managesFocus?: boolean;
+  /** False hides the library's own accessory bar (default true). */
+  showsAccessoryBar?: boolean;
+  /** False stops the view padding itself for the keyboard (default true). */
+  managesKeyboardInsets?: boolean;
   ref?: Ref<TerminalViewRef>;
   style?: StyleProp<ViewStyle>;
 };
--- a/build/ExpoLibghostty.types.d.ts
+++ b/build/ExpoLibghostty.types.d.ts
@@ -11,6 +11,10 @@
     cols: number;
     rows: number;
 };
+export type TerminalModesEvent = {
+    /** DEC private modes bitmask: bit0 1000, bit1 1002, bit2 1003, bit3 1006, bit4 alt screen, bit5 1007. */
+    mask: number;
+};
 export type TerminalViewRef = {
     /** Feed base64-encoded PTY output into the terminal grid. */
     write(base64: string): Promise<void>;
@@ -60,6 +64,10 @@
     onResize?: (event: {
         nativeEvent: TerminalResizeEvent;
     }) => void;
+    onModes?: (event: { nativeEvent: TerminalModesEvent }) => void;
+    managesFocus?: boolean;
+    showsAccessoryBar?: boolean;
+    managesKeyboardInsets?: boolean;
     ref?: Ref<TerminalViewRef>;
     style?: StyleProp<ViewStyle>;
 };
