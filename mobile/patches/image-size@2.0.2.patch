diff --git a/dist/detector.cjs b/dist/detector.cjs
index 0898608dbeca36722dfd795341b1c3c1448f58aa..3ce613de0acdfd3ead3e13bb83b6a5438fca5a3a 100644
--- a/dist/detector.cjs
+++ b/dist/detector.cjs
@@ -28,9 +28,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -232,11 +234,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -247,10 +251,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/detector.mjs b/dist/detector.mjs
index 67afcab02d627f95e98c938499e07a934b832a26..ee658173ae8ebbaf0d7bcbb6b8fe5762166c046c 100644
--- a/dist/detector.mjs
+++ b/dist/detector.mjs
@@ -26,9 +26,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -230,11 +232,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -245,10 +249,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/fromFile.cjs b/dist/fromFile.cjs
index c226d3dac07f235db456c774b26cca88f655e1af..ef12421b7bee975aaf78ef229df35adc18e99123 100644
--- a/dist/fromFile.cjs
+++ b/dist/fromFile.cjs
@@ -54,9 +54,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -258,11 +260,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -273,10 +277,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
diff --git a/dist/fromFile.mjs b/dist/fromFile.mjs
index 4a3c44432982397204f7a9d04fc66ef65a8e7ba4..6f3ab93370ca433f5bcb3e981e5031e92b4b80cb 100644
--- a/dist/fromFile.mjs
+++ b/dist/fromFile.mjs
@@ -31,9 +31,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -235,11 +237,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -250,10 +254,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210eb131db06ccca30a5c466c7252a03af50..67abaf463d4ba17fca40684361cc5a50bdcc5613 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -30,9 +30,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -234,11 +236,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -249,10 +253,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17afad4e406eba013fff7858366cb5f8e3ba4..e41a6debf394bd2e6fe17ea530618ecb0056478a 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -26,9 +26,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -230,11 +232,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -245,10 +249,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
diff --git a/dist/lookup.cjs b/dist/lookup.cjs
index 9731ad8bb0949f8fdf9297851bdb312e91282f01..d2fd15eb604e0c31946fe10debadf81967e5f25b 100644
--- a/dist/lookup.cjs
+++ b/dist/lookup.cjs
@@ -28,9 +28,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -232,11 +234,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -247,10 +251,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
diff --git a/dist/lookup.mjs b/dist/lookup.mjs
index f787d1c8408d7b681fec8617595d058f66959869..6515ddcf5e3ca34db9b352eae8cebd82f6edec26 100644
--- a/dist/lookup.mjs
+++ b/dist/lookup.mjs
@@ -26,9 +26,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -230,11 +232,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -245,10 +249,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f490788ea654ea9896060a5c0253d81e348af92..91220b7b1c36125838f3de81e7ec0b6dc3bcbcc3 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -6,9 +6,11 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 01330919e01b3c034a96a7b4b1dbf5a7f658bb2d..478dd82a8a8d9be89bd281856bd7acef4089c9ef 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -4,9 +4,11 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d370dcf80a10f14a6deebfdbe04d3c70585c..3c847d128a77382f7575f078bad9e399c08f4af5 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -53,11 +53,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -68,10 +70,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027bc5890a79d05a7fdb87de3018e556729e..f5cf5a77be8e3b57d0e28d0023ca8333214378b4 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -51,11 +51,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -66,10 +68,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/index.cjs b/dist/types/index.cjs
index 6949cd134db7ab59dae7b804b36c7bacb4e387cf..4eb9cb5119d88b7f9ae91e48e7e6d51cf71675fa 100644
--- a/dist/types/index.cjs
+++ b/dist/types/index.cjs
@@ -28,9 +28,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -232,11 +234,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -247,10 +251,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/index.mjs b/dist/types/index.mjs
index b026dda4aa16c6077a76e4b3cfe3f54fe1d6d1d0..54f05a2d90e8149662311558464a3a3cf8b79f65 100644
--- a/dist/types/index.mjs
+++ b/dist/types/index.mjs
@@ -26,9 +26,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
@@ -230,11 +232,13 @@ var ICON_TYPE_SIZE = {
   ic10: 1024
 };
 function readImageHeader(input, imageOffset) {
+  if (input.length - imageOffset < 8) throw new TypeError("Truncated ICNS entry");
   const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-  return [
-    toUTF8String(input, imageOffset, imageLengthOffset),
-    readUInt32BE(input, imageLengthOffset)
-  ];
+  const length = readUInt32BE(input, imageLengthOffset);
+  if (length < 8 || length > input.length - imageOffset) {
+    throw new TypeError("Invalid ICNS entry length");
+  }
+  return [toUTF8String(input, imageOffset, imageLengthOffset), length];
 }
 function getImageSize2(type) {
   const size = ICON_TYPE_SIZE[type];
@@ -245,10 +249,12 @@ var ICNS = {
   calculate(input) {
     const inputLength = input.length;
     const fileLength = readUInt32BE(input, FILE_LENGTH_OFFSET);
+    if (fileLength < 8 || fileLength > inputLength) throw new TypeError("Invalid ICNS file length");
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] > fileLength - imageOffset) throw new TypeError("ICNS entry exceeds file length");
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/jp2.cjs b/dist/types/jp2.cjs
index cdd7c884efeb940b99f69a8f1d6ed361a1979211..dc2ce7531f76b4cde2c4e8f07ecc8f058709536f 100644
--- a/dist/types/jp2.cjs
+++ b/dist/types/jp2.cjs
@@ -6,9 +6,11 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
diff --git a/dist/types/jp2.mjs b/dist/types/jp2.mjs
index 63a537635b4f91250431a53225d4521ed60cf67d..da0f7218fc71f7e9947ae932ba6783fe31402852 100644
--- a/dist/types/jp2.mjs
+++ b/dist/types/jp2.mjs
@@ -4,9 +4,11 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095be85c7a8fd25763895a47c46b4292ebd9c..bde45a1b4e092f2e5ea349fc3582e1e2fc9558c4 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -47,9 +47,11 @@ var toHexString = (input, start = 0, end = input.length) => input.slice(start, e
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4cac6abb366378e5feed745f16981b441c0..e01c11b83ed16cd27f198364ec902571ed87bea4 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -45,9 +45,11 @@ var toHexString = (input, start = 0, end = input.length) => input.slice(start, e
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
diff --git a/dist/types/utils.cjs b/dist/types/utils.cjs
index 23d52e4cbf271e667feb2d7f040c07f69678d3aa..f00306bd8492000c29de1736159da230bf0f01e0 100644
--- a/dist/types/utils.cjs
+++ b/dist/types/utils.cjs
@@ -28,9 +28,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
diff --git a/dist/types/utils.mjs b/dist/types/utils.mjs
index 3f09dceb036e40be8538dbb5588d88c951339158..80905fa1cc2ab369a121b3f6b8fbf19d604193ff 100644
--- a/dist/types/utils.mjs
+++ b/dist/types/utils.mjs
@@ -26,9 +26,11 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   return methods[methodName](input, offset);
 }
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
-  const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  if (input.length - offset < 8) return;
+  const declaredSize = readUInt32BE(input, offset);
+  // ISO BMFF size 0 extends to EOF. Never return a non-advancing box.
+  const boxSize = declaredSize === 0 ? input.length - offset : declaredSize;
+  if (boxSize < 8 || input.length - offset < boxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
