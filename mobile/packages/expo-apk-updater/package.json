{
  "name": "@codeui/expo-apk-updater",
  "version": "0.1.0",
  "private": true,
  "description": "Downloads a Code UI release through Android's DownloadManager (which survives the app being closed) and installs it through the PackageInstaller session API, so a self-update runs in the background and lands like a system update.",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "peerDependencies": {
    "expo": "*",
    "react-native": "*"
  }
}
