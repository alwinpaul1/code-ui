# Upstream port inventory

Every Orca commit since the vendor base (`ef428d879e09a02daa3bb9ed977a7802d89da289`)
that can reach the phone, and what was done about it. "Reaches the phone" means it
changes a file under `mobile/`, or a file under `src/shared/` that `mobile/` imports.
143 commits touch shared or mobile code; 69 of them reach the phone. The rest are
desktop and host work whose code this repo does not vendor.

Regenerate the candidate list with the script in `docs/upstream-port-inventory.sh`.

Status values: **done** landed here; **already** Code UI solved it independently
before the port; **skip** with a reason; **in flight** an agent is on it now;
**pending** queued.

## Landed

| Commit | What | Note |
|---|---|---|
| a65332a8b #18560 | Claude structured chat onto the Agent SDK | one line taken: the tab contract's agent union |
| cc07249e7 #18697 | definitive-refusal allowlist | came in with the structured port |
| d07c47593 #18741 | structured native Claude chat | the core port |
| 546fd9b21 #19147 | remember structured model and effort picks | eleven shared files moved with it |
| 09ee4c1b1 #18926 | stop host streams after a cancelled relay subscription | |
| 992360f12 #18940 | cancel direct probes when their owner stops | Code UI had the abort half; the stop fence was missing |
| 61b09b7a0 #18959 | fail a dead direct probe fast | mobile half; the rest is relay-server code |
| e9d9d42cc #18914 | Markdown placeholder prefixes in one scan | |
| 4e8e14424 #18919 | stop splitting every path in file autocomplete | mobile half only |
| ddc5b75ac #18760 | label a Codex tool row by what it did | summary half; the icon half has nothing to attach to here |
| 53852c9ca #18126 | configurable terminal contrast floor | mobile mirror; found and fixed a theme-equality bug on the way |
| e89deb63c #18757, f8780a2c8 #18807, 5868fdc9e #19346, 2bf298d1d #19311 | background tasks over the wire | shared contract hand-applied; the structured lane now reads the host's roster and the transcript reader keeps terminal tabs. `#18757`'s journal-unchanged short-circuit came with it |
| b0c67eaf8 #18761 (remainder) | the chat lane wiring out of the controller | the extraction skipped when the rest of #18761 landed; taken verbatim for the room |
| 2f828e446 #19822 (working half) | a structured send reads as working before the provider echoes it, and Stop waits for a turn | the optimistic-bubble half stays unported; see `scratchpad/19822-assessment.md` |
| 172aa1ac3 #18765 | agent file edits as inline diff cards | upstream shipped it desktop-only; the five shared edit modules are vendored verbatim and the phone got its own card. `native-chat-types.ts` and `structured-agent-session-projection.ts` took hand-applied hunks |
| 6a9c5d8ce #19496, 44eb95fc6 #19468, 063c1caa6 #19469, 2c5cd845a #19465 | the shared-path allocation work | one commit; pure refactors, guarded rather than test-first. `native-chat-tool-fold.ts` took hand-applied hunks (whole-file would drag in unported #18773). Upstream's own tests are vendored but never run here, so the running guards live under `mobile/src/` |
| dda103d2c #19832 | one `/` picker for every agent, anywhere in the prompt | mobile already had the grouped menu and the draft-leading dispatch rule; the mid-prompt trigger was the real gap. `native-chat-agent-profiles.ts` re-vendored to drop `groupedSlash`, which nothing here read |
| d0506bf5d #19226 | execution details and tool row identity | the shared half plus a mobile annotations row. Unblocked the `mcpIdentity` field `native-chat-tool-summary.ts` had been dropping, so that file and its test went back to a clean c1e15c400. The `src/main/` Codex translation that fills `exitCode`/`durationMs` is not vendored — the host supplies those |
| 9f044031f #19228 | a compaction, a plan and a toned line drawn as what they are | landed in 8d80263 as the `presentation`/`tone` hints, which is what LOCAL-FILES.md records; the rest of #19228 is `src/main/` |
| f2d5711b2 #19845 | an older page that no longer touches the transcript is refused, not merged | landed in 8f7bbaf. The mobile half came over whole; the reducer carries local hunks, so its two changes were hand-applied. Covers the structured lane only — a terminal-driven tab is a different path, so it is not yet an answer to the missing-replies report |
| 0b60b0dcb #19841 | bound retained items on the structured session path | the live-batch head trim that #19845's older-page retry exists for. Hand-applied onto the local reducer (activity, background tasks, `requestedCursor`). A long session now keeps 1024 items, sets `hasOlder`, and paging raises the cap so a live batch cannot collapse it |
| d15a6df22 #19230 | task checklists with update diffs, and a composer progress panel | both agents' plans as checklists, in both themes. A later turn diffs against the earlier message's plan, not only the current tool run. The run header names the plan by progress (`1/3 · Writing the test`) instead of the input JSON. A collapsed **Tasks n/m** strip sits above the composer and expands to the checklist. Shared `native-chat-task-list.ts` vendored; mobile surface is `MobileNativeChatTaskList.tsx` plus the row builder, composer strip, and tool-run header rewrite. Live Codex `projectNativeChatTaskListFrames` (notification→update_plan) was not ported |
| e80fae0c4 #19229 | summarize turn file changes and preserve resolved prompt receipts | **the shared half only, and it changes nothing the phone draws.** Both visible halves — the turn diff rollup and the resolution receipt — are `src/renderer/` components this fork does not vendor, and `summarizeUnifiedPatch` exists only to feed the rollup. What the phone does get is the per-item render cache in `structured-agent-session-projection.ts`: mobile re-projects the WHOLE transcript on every stream frame, and unchanged rows now come back as the same objects. `native-chat-edit-normalize.ts` and `native-chat-unified-patch.ts` re-vendored at e80fae0c4, `native-chat-edit-patch-files.ts` vendored new (`native-chat-edit-normalize.ts` imports it, so mobile typechecks it). The extraction is behaviour-preserving and was proved so: upstream's own 50 pre-extraction `native-chat-edit-normalize.test.ts` cases, vendored here at 172aa1ac3, all pass against the extracted code |
| 8096cb280 #18743 | render Claude structured chat through the same UI as Codex | already in: `structured-agent-session-composer.ts` is pinned at f1d854502, a later commit that contains it, and the file was verified byte-identical |
| 1b4159a31 #18638 | skip host-label work on a single-host install | `host-context-labels.ts` re-vendored. Mobile imports it for the worktree list. |
| fb9d08f5f #19476 | index project table option and iteration order | `project-group-sort.ts` re-vendored. Mobile `groupRows` / `sortRows` call it. |
| d3501f7ad #18456 | `unavailableReason` on a non-authoritative worktree list | the two-line type only. The scan-failure host and sidebar are `src/main/` / `src/renderer/` |
| cd8e98fdf #20313 | stop the mobile Markdown parser stalling on an unsupported block | hand-applied; our parser carries the escaped-pipe table hunks but the defect was identical. The paragraph loop guarded with `startsWith('```')` and `/^(#{1,6})\s+/` while the dispatchers used stricter patterns, so a ```c++ fence or a bare `# ` stopped the paragraph at a line nothing would consume and the index never advanced. Upstream's `mobile-markdown-parser-progress.test.ts` came over verbatim and hangs 12 of its 13 cases on the old parser |
| 3bc631dad #20317 | validate review table delimiters by cell | taken verbatim; `markdown-blocks.ts` matched upstream here. The one `TABLE_DELIM` regex backtracked exponentially on a long whitespace run, so `splitTableRow` now does the split and each cell is tested on its own. Upstream's test pins the new check against the old regex over 2,197 generated rows |
| 1b5092492 #20322 | scan mobile Markdown links without repeated suffix searches | taken verbatim across all four files; both inline tokenizers matched upstream. `markdown-inline-matcher.ts` is new here. Link scanning leaves the regex and walks brackets with `indexOf`, so `[` x 60,000 no longer re-searches the suffix per opener. Upstream's differential test pins the merged matcher against the old regexes over 10,000 generated strings |
| 42a2c6510 #20430 | release consumed terminal write-queue slots | one-line source change taken verbatim. Upstream's 305-line differential test came over with the harness extended by this fork's scroll-gesture write-hold vars (`scrollGestureActive`, `scrollGestureStartedAt`, `heldWritePumpTimer`, `nowMs`), which upstream's queue does not carry. The payload-hash pin moved 757011 → 757055 |
| 20fb3e7d1 #20210 | normalize history scope paths once per candidate | applied verbatim; the file matched upstream. The dedupe re-normalized every accepted path per candidate, so a project with many same-repo worktrees paid O(n²) normalizations. A `Set` of comparison paths carries the answer forward |
| baa1cb135 #20220 | avoid materializing input characters on backspace | applied verbatim. `Array.from(text).slice(0, -1).join('')` built an array of every code point per keystroke; the last character's width now comes from `readUtf8CodePointAt` and the rest is one `slice`. `src/shared/utf8-byte-limits.ts` was already vendored and exports it |
| 56aefb542 #20226 | bound autocomplete substring retention | upstream's two hunks applied verbatim, plus a third: `rankSkillSuggestions` is Code UI's own and carries the same unbounded substring list (and matches descriptions too, so it retained more). The grep for the shape (`substring.push`) found all three. Upstream's test comes over as-is; the skill ranker got its own pair |
| 9b2b02bb3 #20358 | reuse UTF-8 prefix truncation for diagnostics | applied verbatim; both files matched upstream. Two hand-rolled truncators encoded every character separately through a fresh `TextEncoder`; both now call the vendored `clampUtf8TextPrefix` |
| 7a440b1c8 #20252 | skip successful duplicate connection log saves | applied verbatim; `connection-log-buffer.ts` matched upstream. Several save requests between two appends each copied the entries and each wrote them; a per-host revision now carries one snapshot and one `saved` flag, and a failed revision still retries |
| ef3b7e83b #20224 | reuse numeric collators across source control sorts | applied verbatim across the three files, which matched upstream. `localeCompare(…, { numeric: true })` builds a collator per comparison, so an O(n log n) sort built O(n log n) collators; one `Intl.Collator` per sort now. Upstream's `config/scripts/mobile-source-control-collation-benchmark.mjs` was not taken — this fork does not carry the benchmark scripts |
| 701dc2211 #20233 | precompute task sort keys and reuse repository collation | applied verbatim; the tasks screens here match upstream. Sort keys were derived inside the comparator, so each was rebuilt O(log n) times per item, and the repository sort built a collator per comparison |
| a045af361 #20249 | precompute Linear issue sort keys | applied verbatim; this fork has the Linear task screens and the files matched upstream |
| 7e9ade7c7 #20431 | reuse Linear grouping between list and board | applied verbatim, before df375cdd8 (it creates the projection test the later commit edits) |
| df375cdd8 #20314 | reuse Linear issue ordering when grouping list and board | applied verbatim on top of 7e9ade7c7 |
| 131d5ab07 #20310 | reuse current workspace on notification taps | applied verbatim; push-receive.test.ts is not in this fork, the other two files matched upstream byte for byte |
| 9a5679748 #20125 | surface host create warnings and terminal-create errors | mostly verbatim. `use-mobile-session-terminal-create-actions.ts` was hand-merged — this fork's structured-provider fallback and HUD launch config sit above the two failure branches, which themselves matched upstream, so only those were replaced. `onCreated`'s two signatures were hand-widened (upstream's context differed). The session screen here already reads a `warning` search param through `use-mobile-session-foundation.ts`, so the chain completes. Both route-parity hashes re-pinned, the same two upstream refreshed |
| 58ff95bec #19960 | name the RPC acceptance policies its call sites hand-rolled | applied verbatim across all nine files; they matched upstream. `rpc-acceptance-policies.ts` is new here and each policy preserves one call site's acceptance exactly — the predicates did not agree with each other before |
| f7d521601 #19055 | provider activity in chat turn tails | the wire already carried `activity`; the reducer and coalescer now keep it, and the live turn status row shows the host copy instead of a generic Working |
| c84007c54 #19961 | a shared params catalog generated from the host registry | **done, mobile and shared only.** Taken: `mobile/src/transport/rpc-params-contract.ts` (type-only door) and its boundary test, plus `src/shared/rpc-contract/` vendored whole (84 files) and the 8 shared files the catalog imports that this fork lacked (`mobile-push-contract.ts`, the six `orchestration-fleet-*`, `worker-terminal-host-scope.ts`). All 92 pinned in `UPSTREAM.txt`. **Not taken:** the generator (`config/scripts/generate-rpc-params-catalog.mjs`), its parse-parity gate in `.github/workflows/pr.yml`, `.gitattributes`, root `.oxlintrc.json`, `package.json` and the ~90 `src/main/runtime/rpc/methods/` files whose schemas were moved out — all host and CI machinery this fork does not vendor. The catalog is vendored as generated; the generator was not run. **2026-09-18:** the catalog lists what the desktop registers, not what a mobile-scope token may call — see "Catalogued ≠ reachable" below before reading a row here as reachable |
| eedd35645 #20018 | typed RPC operations, and the raw request port fenced behind an inventory | **done.** All 15 mobile files applied verbatim; `rpc-client.ts` hand-merged (its two hunks matched upstream). `rpc-params-catalog.generated.ts` and the new `rpc-send-params.ts` re-vendored at this commit. The port inventory was **regenerated for this fork** rather than taken: upstream lists its own 153 files, this fork has 170 (161 pending, 9 owners) — same ratchet, same AST counter, our call sites. To re-derive the list, run `rawRequestPortReferences` from `unvalidated-rpc-request-port-boundary.test.ts` over `app/` and `src/`; the test itself reports every drift. One local change: `rpc-params-contract-type-only-boundary.test.ts` does its file sweep at module scope, the way the sibling boundary test already does — parsing this fork's 1243 files took over vitest's 5s per-test budget under the full suite. Cast fence unmodified |
| b0070e372 #20499 | migrate settings reads to RpcOperation | **done.** 9 of the 10 call-site and test files were byte-identical to upstream's parent and were applied verbatim, `mobile-tasks-refactor-parity.test.ts` included — its refreshed hashes pass here unchanged, so this fork's tasks screen still matches upstream's on every hooked declaration. `mobile-new-tab-agent-loader.ts` was hand-merged: it carries the local folder-workspace hunk (Orca #16215) above the settings read. Port inventory regenerated: 168 files, the 7 migrated call sites each down one |
| ecd7b19ad #19929 | pass agent-implemented slash commands through to the agent | **done.** This fork did NOT already do it: `native-chat-agent-profiles.ts` and `structured-agent-session-composer.ts` were byte-identical to upstream's parent, so a Claude structured chat answered "/init is not available in chat sessions" for a command Claude does run, and Codex's `/goal` was typable but never offered. Both shared files re-vendored whole at `ecd7b19ad`, with `native-chat-agent-profiles.test.ts` — which this fork vendors but never collects, vitest's root being `mobile/`. `use-mobile-structured-native-chat-send-bridge.ts` took the patch verbatim through `git apply --3way`, and its new upstream test came with it. Upstream's `MobileNativeChatComposer.tsx` hunk has no counterpart here: this fork's composer builds the `/` menu through `mobile-native-chat-session-catalog.ts`, so the `agent` argument was threaded there instead. The controller's bridge call gained `agent`; upstream's `legacyNativeChatQuestion` rename was skipped as lint-budget churn on a file that has diverged. Because the shared tests never run here, both behaviours are re-pinned where the gate does run them — `/init` and `/goal` reach the agent (`mobile-structured-composer-command.test.ts`), Codex's fallback menu offers `/goal` (`mobile-native-chat-session-catalog.test.ts`), and a both-agents guard keeps `/clear` host-claimed |
| 7ce8e18d0 #20521 | the RPC migration's recording oracle | **done, whole.** All 104 mobile files verbatim: 73 goldens under `mobile/rpc-foundation/`, `pilot-scenarios.json`, `settings-recording-coverage.json`, `live-probe/`, the 26-module recorder under `mobile/src/test-support/rpc-recording/`, and `mobile/scripts/rpc-recording.mts`. **Upstream's goldens replay green against this fork's own product source** — 115 passed, 3 skipped — which is the whole question a vendored oracle raises. 11 of the 16 modules the mount adapters load are byte-identical to upstream; the two that are not move no recorded observation (`use-mobile-native-chat-file-search.ts` adds `nativeChatFileSearchPending` and a `limit` that defaults to the same 16, and the adapter projects `nativeChatFilePaths` only; `mobile-new-tab-agent-loader.ts` adds the folder-workspace branch, and the scenario's `repo-1::/folder` is not one). The three `native-host-*-operations.ts` modules this fork lacks are reached only under `RPC_FOUNDATION_REFERENCE_ROOT`, which needs the archived `bcba08b3e4` tree — those are upstream's own 3 skips. Port inventory: upstream's two OWNERS lines taken as-is, because they name the recorder's own files and the boundary test confirms the same counts here; 168 files → 170 |
| 7d98c8e2f #20544 | the source-control domain through typed RpcOperations | **done, whole.** All 22 product files: 8 new operation modules, `rpc-reader-payload.ts` and `rpc-refusal-message.ts`, and 12 migrated call sites. 20 of the 22 were byte-identical to upstream's parent; `MobileGitHistoryList.tsx` (pull-to-refresh) and `mobile-git-history.ts` (`commitTimestampToMs`, Orca #17729) were 3-way merged and keep their local hunks. The recorder and `rpc-foundation/` were taken **at the commit rather than as a diff** — `recorderSha256` digests the whole recorder directory, so it also has to carry the two guard inversions main made between the two commits, which a path-scoped diff would have missed. Goldens 73 → 153. Port inventory: upstream's hunk verbatim — the boundary test named the same 13 stale source-control entries here as upstream, and `use-mobile-git-requests.ts` stays at 1 for the same reason (a `(method, params)` dispatcher five hooks feed action steps). 170 files → 157; pending references 414 → 370, upstream's 44. **One fork-local recorder edit:** `unhandled-recording.ts` casts `process.rawListeners`, which is `Function[]` — not callable — under the `@types/node` 22 this fork resolves, so `process.on` below it rejects every element. That one line is inside `recorderSha256`, so all 153 goldens had that single header field rewritten to `d31a6743…`; **no recorded observation was touched**, the run is still upstream's, and `compareGolden` re-checks every checkpoint field by field: 201 passed, 3 skipped. Outside the recorder, `mobile/vitest.config.ts` sets `testTimeout: 30_000`: the oracle transpiles product source through the TypeScript compiler API per scenario, so the first test in a cold process pays the warmup (`b1` records in under a second warm and timed out at 5277 ms cold), and it is CPU-bound across four files for half a minute, which starved `e2ee-base64` to 7.0 s and `agent-hud-launch-args` to 5.9 s in a full pass. Both measured clean alone; a timeout that fires only on a busy box names the wrong file |
| 50e752fc6 #20562 | pin each RPC golden to its own scenario input, not the whole manifest | **done, whole.** **Applied second, after #20563, because it is that commit's child in upstream's history — the PR numbers do not follow merge order, and both commits edit `pilot-scenarios.json` and every golden.** `recorderSha256` covered the recorder directory *plus* `pilot-scenarios.json`, so every golden's header was a function of every other family's scenarios and adding one family re-digested all 153. It now covers the recorder directory only; a new per-golden `scenarioSha256` pins the scenarios that golden was recorded from, derived through the new `derived-goldens.ts` so the digest and the recording agree by construction rather than by a restated expansion. `GOLDEN_FORMAT_VERSION` 3 → 4. Taken at the commit, every file whole: 4 new modules (`derived-goldens.ts` + test, `golden-header-digest.test.ts`, `scenario-digest.ts`), 6 edited (`family-recordings.test.ts`, `golden-recording.ts`, `pilot-recordings.test.ts`, `recorder-digest.ts`, `recording-runner.test.ts`, README), `pilot-scenarios.json` and all 153 goldens. After the #20563 port this fork's recorder was byte-identical to upstream's parent **except** `unhandled-recording.ts`, whose `process.rawListeners` cast is kept. Goldens 153 → 153, and `git diff -U0` over `mobile/rpc-foundation` filtered for the four moved header names leaves **zero** lines: `scenarioSha256` added (153 distinct values, upstream's own — the manifest is taken whole), `goldenFormatVersion` 3 → 4, `baseline` `5ec0b269…` → `e53f1557…`, and `recorderSha256`, this fork's `a9293486…` against upstream's `58a461db…` for the cast; the digest module reproduces upstream's value exactly over upstream's tree. No product file. Gate: tsc clean, vitest 730 files / 6069 passed / 17 skipped, oxlint clean; recorder suite alone 207 passed, 3 skipped — the 210 the README now states |
| e53f1557e #20563 | two known main bugs the RPC migration preserved | **done, whole.** Both are reply-side, no wire change. `hostReplyErrorTextOrFallback` passed a truthy non-string through under a `string` annotation, and its one caller is the in-band `git.commit` failure whose every consumer is display copy — `summarizeCommitFailure` opens with `raw.slice(…).replace(…)`, so an object error threw. It now reads a non-string as absent. `useNewWorkspaceRuntimeContext` read settings through `settingsRead`, whose reader preserves main's `boxed!.settings` throw, so a null or absent `settings.get` **or** `ui.get` reply threw a TypeError out of the effect and lost the trusted-hooks publish and the provider computation, not just the settings; it reads through `optionalSettingsRead` and degrades a null `ui.get` to untrusted hooks. Taken whole: `use-new-workspace-runtime-context.ts` + its new test, `rpc-refusal-message.ts` + its new test, the recorder README, `unhandled-recording.test.ts` (with the last unhandled-rejection golden gone, nothing else pins the capture), `pilot-scenarios.json` and all 153 goldens. Every parent file was byte-identical here **except** `settings-read-operations.ts`, which carries this fork's local `terminalCopyTrimsGutterRead` operation below upstream's two comment lines; the two-hunk patch applied around it and the local operation is untouched. Goldens 153 → 153: three move an observation — `matrix-hostedreview.create-intent-git.commit-1` (`inner-false-object-error`), `matrix-settings.workspace-context-settings.get-1` and `matrix-settings.workspace-context-ui.get-1` (`result-null`, `result-absent`) lose the TypeError effect and commit `providers: ["github"]` — and the rest move `baseline` and `recorderSha256` only. As on #20544, `recorderSha256` is this fork's `5ca89784…` rather than upstream's `70aa6f59…` because of the `unhandled-recording.ts` cast; the digest module reproduces upstream's value over upstream's tree, so the cast is the whole gap, and **no recorded observation was hand-edited**. Gate: tsc clean, vitest 728 files / 6064 passed / 17 skipped, oxlint clean; recorder suite alone 202 passed, 3 skipped |
| fc525c355 #20568 | the task workspace-creation domain through typed RpcOperations | **done, whole.** 22 operations over 20 methods in four new modules — workspace create, workspace source, task runtime, and the Smart picker's provider reads — plus the migrated call sites: 12 of `src/tasks/`'s raw-port files go to zero references. All 31 non-golden files taken whole **except** `unvalidated-rpc-request-port-inventory.ts`, which is this fork's own list (#20018 precedent) and was regenerated rather than copied: the boundary test named exactly the 12 stale `src/tasks/` entries upstream deleted, with the same counts, so the deletions match and only upstream's expanded `// src/tasks/` comment was taken verbatim — its three "cannot reach zero" files all exist here. 157 files → 145; pending references 370 → 334, upstream's 306. Every other parent file was byte-identical here, this fork's `use-mobile-tasks-*` workspace hooks and `mobile-tasks-refactor-parity.test.ts` included, so upstream's refreshed parity hashes pass unchanged. Recorder again taken **at the commit**: the two new task-workspace mount-adapter files, the three edited adapters/loader and `workspace-settings-mounts.ts`. Goldens 153 → 208 (55 added: 24 `matrix-*`, 31 `tw-*`/`settings-task-workspace-*`). The 153 pre-existing goldens move `baseline` (`e53f1557…` → `50e752fc…`) and `recorderSha256` and **nothing else** — filtering those two names out of `git diff -U0` leaves zero lines, and no `scenarioSha256` moved, which is exactly the property #20562 was built to give. `recorderSha256` is this fork's `d4284ed4…` against upstream's `5ec2faff…`, the cast again; upstream's value reproduces over upstream's tree. No `.oxlintrc.json` override was needed — the largest migrated file, `use-mobile-tasks-runtime-hydration.tsx` at 401 raw lines, is under the 400 `**/*.tsx` cap once blanks and comments are skipped. Gate: tsc clean, vitest 730 files / 6126 passed / 17 skipped, oxlint clean; recorder suite alone 262 passed, 3 skipped |
| c21c08322 #20535 | report auth callback failures instead of cancellation | **done, mobile half — it was already carried.** The fix itself is one branch in `src/main/orca-profiles/profile-cloud-pkce.ts`, the Electron loopback server for the Orca Cloud PKCE sign-in: a callback whose `error` is not `access_denied` now rejects as `orca_cloud_auth_callback_failed` and answers "sign-in failed" instead of "cancelled". That half, and its two desktop test files, are **skipped** — `ls src/` here returns `shared` only, this fork vendors no `src/main/`, and the phone pairs with a desktop rather than signing in to Orca Cloud. The commit's other 75 files are the mobile recorder: a lint-shaped rewrite of `family-recordings.test.ts` and `pilot-recordings.test.ts` (conditional `it()` registration replaced by a `continue` guard) and the `recorderSha256` refresh those edits force across 73 goldens. **Both test files already sit at their post-#20535 form here.** The #20544 port above is a descendant of this commit and took the recorder at that commit, so `pilot-recordings.test.ts` is byte-identical to upstream's post-fix file and the `if (!second) { continue }` guard is at `family-recordings.test.ts:121`. The golden headers likewise already carry this fork's own post-#20535 digest `d31a6743…` — upstream's is `9b12a8ad…`, the gap being the `unhandled-recording.ts` cast noted on #20544 — and `recorder-digest.ts` recomputes exactly that over the working tree; swapping either pre-#20535 test file back moves it to `021724db…`, which would fail every golden's header check. **Skipped goldens:** the 13 upstream `matrix-<family>.json` files — `matrix-legacy-inventory`, `matrix-linear-detail-barrier`, `matrix-project-explicit-false`, `matrix-settings-agent-read`, `matrix-settings-best-effort` and `matrix-settings.{bot-overrides,home-providers,repo-metadata,resume-metadata,task-hydration,task-workspace,workspace-context,workspace-submit}`. #20544 replaced one golden per family with one per matrix site, so this fork carries `matrix-<family>-<request>-N.json` and those 13 names do not exist here. Net diff on the port: none. Verified with the oracle itself — `family-recordings`, `pilot-recordings`, `recording-runner` and `probe-hole-witness` at 201 passed, 3 skipped |
| 6c3b97b95 #19952 | a scope refusal is not a missing method on the Relay pairing probes | **done, merged.** The desktop's mobile allowlist gate answers before its dispatcher, so an old desktop refuses `pairing.getEndpoints` / `pairing.provisionRelay` with `forbidden`, never `method_not_found`, and the "too old for Relay, stay on LAN" fallback keyed on absence alone never fired: in `upgradeDirectMobileRelay` the refusal threw into the controller's swallowing catch and the write-once journal (with its pending resume secret) was never retired. New `pairing-relay-rpc-unavailable.ts` (verbatim; its comment cites upstream's `docs/reference/remote-wire-compatibility.md`, which this fork does not carry) reads both codes at the three probe sites. Both tests verbatim (the files matched upstream's parent); both product files **hand-merged** because this fork still calls the probes through raw `client.sendRequest` — the #20667 operations module `mobile-relay-pairing-operations.ts` is Group D, not landed here — so the same three guard swaps, the `'relay-pairing-unavailable'` sentinel rename and the coordinator's breadcrumb log were applied onto the raw-call shape. `isMethodNotFoundRefusal` keeps its four other consumers. Failing-first: with the fix reverted both new tests reject with `forbidden: …` instead of resolving (2 failed / 14 passed), green after. Recorder untouched: no golden scenario drives the pairing probes |
| 5287c5cdb #20069 | stop a created tab from jumping when the host snapshot lands | **done, merged.** The strip appended a created tab and then jumped it: the request anchored on the active tab (`afterTabId`, host `splice(insertAfter + 1)`) while the optimistic paint appended, two placements that disagree by construction. New shared `session-tab-placement.ts` + test (verbatim, pinned 5287c5cdb) is the one rule both sides use; `use-mobile-session-foundation.ts` (verbatim, matched parent) binds `useHostProtocolGates` for `hostCapabilities`; `protocol-version.ts` takes the `SESSION_TABS_SPLIT_GROUP_PLACEMENT_RUNTIME_CAPABILITY` constant and its `RUNTIME_CAPABILITIES` entry as a **hand-applied hunk** on its d07c47593 pin (LOCAL-FILES.md updated). `use-mobile-session-terminal-create-actions.ts` **hand-merged** onto this fork's raw `client.sendRequest('session.tabs.createTerminal', …)` shape, which sits below the fork's HUD `launchConfig` resolution and the Claude-falls-through-to-terminal branch: one captured `afterTabId` feeds the request and the paint, the paint goes through `placeCreatedSessionTab(…, { afterParentGroup: true })`, and against a host that does not advertise the capability the phone paints nothing and lets the snapshot place it (an older headed host places after the split parent, an older headless one after the leaf). `mobile-runtime-client-capabilities.ts` + test hand-merged (this fork's list lacks the #19849/#19695 entries upstream's has). The create-actions test took upstream's six-case `optimistic placement` block verbatim on top of this fork's Claude-fallback cases. Failing-first: 3 of the 6 fail on the pre-port hook (`paints … after the anchor`, `paints after the active split parent`, `waits for an older host snapshot`: append vs anchor, and setSessionTabs called against an older host), green after. Route parity re-pinned: hooks 281 → 282 (`useHostProtocolGates`), `HEAD_HOOK_BINDING_SHA256`, `HEAD_NESTED_FUNCTION_SHA256` (`handleCreateTerminal`'s body); the same three upstream moved, dated notes in the test. **Recorder:** the ten upstream goldens and `session-terminal-create-mount-adapters.ts` are Group D's #20891 recorder mount, which this fork does not carry yet, and its `create-after-tab-id-null` mutant likewise; when that lands the mutant's `before`/`after` must match the `const afterTabId = …` line, as upstream's does |
| 6da72383d #19849 | one executor for agent launches, exposed as `agent.launch` | **done, shared half, in one commit with #19850** (its first and only consumer; the constant and the intent type have no reader until the create sheet routes through them). `agent-launch-intent.ts` vendored whole at 97aa5ff19 rather than 6da72383d, because #19850 is the next commit to touch it (moves `warning` to the top level of the receipt, makes `withoutReservedAgentCreateFields` generic) and landing the intermediate shape for one commit would pin nothing. `protocol-version.ts`: `AGENT_LAUNCH_RUNTIME_CAPABILITY` and its two array entries **hand-applied** on the d07c47593 pin (upstream's hunk context names `NOTIFICATIONS_REMOTE_PUSH_RUNTIME_CAPABILITY`, not vendored here). `rpc-params-catalog.generated.ts`: this commit's `RPC_METHODS_WITHOUT_SHARED_PARAMS` entry is superseded by #19850's catalog row, so only the net is applied (see that row). The executor itself, `src/main/agent-launch/`, and the `agent.launch` handler are host code this fork does not vendor |
| 97aa5ff19 #19850 | open native chat when a new worktree launches a default agent | **done, mostly verbatim.** Picking an agent on the create sheet always produced a terminal: every create path put `startupAgent` on `worktree.create`, which means "create agent-first", so the startup terminal IS the agent and the host's structured branch is unreachable. The blank, branch and new-branch creates now send the same payload through `agent.launch` when the host advertises `agent.launch.v1` on `status.get`, and let the host settle the surface; work-item creates stay on `worktree.create` (their issue/PR URL is an unsent `startupDraft`). **Verbatim (matched upstream's parent):** `use-new-workspace-create-submit.ts`, `blank-workspace-create.ts` + test, `source-workspace-create.ts` + test, `workspace-create-params.ts` + test (`agentLaunchCreateFields` → `startupAgentCreateFields`), `worktree-create-capability.ts` + test, `worktree-create-retry.ts`, `mobile-workspace-create-operations.ts` (`agentLaunchRun`), `active-session-tab.test.ts`, the recorder's `workspace-settings-mounts.ts` (`getAgentLaunchSupport: async () => false`, so the baseline stays on `worktree.create`); **new:** `agent-launch-worktree-create.ts` + test, shared `rpc-contract/agent-launch-params.ts` (pinned 97aa5ff19). **Hand-merged:** `NewWorktreeModal.tsx` (two one-line hunks; the file carries fork chrome), `mobile-runtime-client-capabilities.ts` + test (advertise `agent.launch.v1`; this fork's list lacks the #19695 turn-item entry upstream's has), the catalog's `agent.launch` row (LOCAL-FILES.md). **Not taken:** `unchecked-rpc-reader-inventory.ts` is Group D's #20950 and does not exist here. Failing-first: with `worktree-create-retry.ts` reverted to the pre-port file, 8 tests across `blank-workspace-create.test.ts` and `source-workspace-create.test.ts` fail (`expected ['worktree.create'] to deeply equal ['agent.launch']`, and the no-workspace reply throwing instead of returning "Failed to create workspace"); green after. **Recorder re-recorded:** 17 goldens move, the same 17 upstream's commit lists — 5 behaviourally (`tw-capabilities-*` and `matrix-worktree.runtime-capabilities-status.get-1` now project `agentLaunch: false` off the probe; `matrix-worktree.create-retry-worktree.create-1` turns three `TypeError` rejections off an unguarded `result.worktree` read into `{ error: 'Failed to create workspace' }`, the guard the launch route needs because a receipt can arrive without a `worktreeId`) and 12 `settings-*`/`matrix-settings.*` by `adapterSha256` only (the mount adapter gained the member). The other 238 moved `lockfileSha256` alone and were restored to HEAD bytes, as a0fb9f8 did; the wrapper's baseline fence names an upstream sha this repo does not hold, so the two recording drivers were run directly, as 54ee8cb did. **Allowlist ratchet:** `agent.launch` is not on the 1.4.205 fixture and the installed 1.4.205 bundle registers neither the method nor the capability, so the phone never sends it to that desktop. `orca-mobile-rpc-allowlist.test.ts` gains a fourth exception kind, `capability`, checked on the real pieces rather than a marker: the gate file must read the capability through its `protocol-version` constant (`capabilities.includes(AGENT_LAUNCH_RUNTIME_CAPABILITY)`; a copied string fails it, proven by ablation), and the downgrade reader must accept the gate's verbatim refusal and `method_not_found` while refusing `invalid_argument` |
| 4b87bc718 #20999 | redefine the `agent.launch` contract | **done, verbatim where it reaches.** Contract only, redefined in place because the method had no shipped client yet: the capability becomes `agent.launch.v2`, a launch warning lives at the top level of the receipt only, the prompt receipt becomes an outcome union (`journaled` / `handed-to-terminal` / `not-delivered`), the dead `customization` field goes, and the mode-reason union is declared once in shared. `agent-launch-intent.ts` re-vendored at 4b87bc718; `agent-launch-worktree-create.ts` + test applied verbatim (the reader drops the nested-warning fallback, and the test its two cases for it); `protocol-version.ts` hand-bumped to `v2` with upstream's comment on the d07c47593 pin. The `operation.id` / `clientOperationId` params half is `src/main/runtime/rpc/methods/agent-launch-schemas.ts`, host code; it reaches the shared params file in #21106. Refactor oracle: the touched tests are green and no golden moved (recorder 321 passed, 3 skipped) |
| 0bf815a48 #21106 | make a lost launch safe to retry | **done, the reachable half.** The host admits `agent.launch` through its durable operation ledger when the caller names the launch with `operationId`, and advertises `agent.launch.replay.v1` so a client may only treat a retry as safe once told. Taken, because #21137 reads them from the phone: `agent-launch-intent.ts` re-vendored at 0bf815a48 (`isAgentLaunchResult` and its three narrowers), `rpc-contract/agent-launch-params.ts` verbatim (optional `operationId`, refined through `parseAgentSessionOperationTimestamp`), and `AGENT_LAUNCH_REPLAY_RUNTIME_CAPABILITY` hand-applied on `protocol-version.ts`'s d07c47593 pin. **Not taken, with the grep:** `agent-launch-operation.ts` (new), `agent-session-operation-ledger.ts` (`claimAgentSessionOperation`, monotone settlement, the `launch` payload), `agent-session-mutation-envelope.ts` (`canonicalAgentSessionDigest`) and the `agent_session_operation_unknown` member of `AGENT_SESSION_RPC_ERROR_CODES` in `agent-session-host-authority.ts` — `grep -rln "agent-session-operation-ledger\|agent-session-mutation-envelope\|agent-launch-operation" mobile/src mobile/app` is empty, and `AGENT_SESSION_RPC_ERROR_CODES` has no mobile reader (`mobile-structured-agent-session-rpc.ts:143` compares the literal against a string, so the union does not reach it). The ledger, the claim and the handler are `src/main/`, host code. No mobile behaviour changes here: shipped mobile sends no `operationId` until #21137 |
| abc8386e1 #21137 | name a create's launch so a lost reply cannot build two workspaces | **done, mostly verbatim; ported after #19849/#19850/#20999/#21106, which it needs, rather than in the backlog table's slot.** Mobile retries a lost create by design, and a retried `agent.launch` built a second agent in a second workspace because no client sent the `operationId` the host's ledger admits. The retry loop now mints one durable id per create candidate (beside `clientMutationId`, never re-minted inside a candidate: the fingerprint folds the workspace name, so carrying one id across a collision bump would refuse `agent_session_operation_conflict`) and sends it through the distinct `agent.launchReplay` method whenever the probe saw `agent.launch.replay-required.v1`; a host that advertises `agent.launch` without the ledger gets the unnamed call as before. **Verbatim (matched upstream's parent):** `worktree-create-retry.ts`, `agent-launch-worktree-create.ts`, `worktree-create-capability.ts` + test (`agentLaunch` becomes `{ replay } | false`), `mobile-workspace-create-operations.ts` (`agentLaunchReplayRun`, a checked `rpcResultVariant` reader over `isAgentLaunchResult`), `blank-workspace-create.ts` + test, `source-workspace-create.ts` + test, `use-new-workspace-create-submit.ts`, `rpc-contract/agent-launch-params.ts` (`AgentLaunchReplay`); **new:** `session/structured-session-operation-id.ts` (the mint, in its own module so `tasks/` need not import the native-chat write graph) and `worktree-create-launch-operation.test.ts`. **Hand-merged:** `mobile-structured-agent-session-rpc.ts` and `mobile-structured-agent-session-launch.ts` (both diverged here; only the two mint functions move out to the new module — this fork's `structuredSessionOperationId()` had no `now` parameter, the shared mint already accepted one), `protocol-version.ts` (`AGENT_LAUNCH_REPLAY_REQUIRED_RUNTIME_CAPABILITY`, hand-applied on the pin), the catalog row. **One fork-local extraction:** the replay route's inline `method_not_found || forbidden || agent_launch_replay_unsupported` downgrade test became `isAgentLaunchReplayUnsupportedRefusal` in `agent-launch-worktree-create.ts`, behaviour-identical, so the allowlist ratchet's second `capability` exception (`agent.launchReplay`, gated on the replay-required capability) checks the reader the loop really uses. **Not taken:** `agent-launch-mobile-replay.test.ts` and `mobile-agent-launch-architecture.test.ts` mount the real host (`src/main/runtime/rpc/methods/agent-launch`, `AgentSessionRecordStore`, the dispatcher) under the phone's retry loop; this fork vendors no `src/main/`, so neither can run here — the end-to-end claim (one tap, one workspace, across a cutover and a legacy replacement host) is unproven on this fork and stays so until a host-side harness exists. `new-workspace/worktree-create-collision.ts` is only those tests' import. Failing-first: with `worktree-create-retry.ts` reverted to the pre-port file, 12 of `worktree-create-launch-operation.test.ts`'s 15 cases fail (no operation id minted, no `agent.launchReplay` sent, the name search restarted after a collision); green after. **Recorder:** the module loader is handed the real `zod` — `mobile-workspace-create-operations.ts` now builds a schema at module load, and the fork's loader (which predates upstream's substitute table from Group D's #20667) answered every non-relative import with a throwing proxy, so all seven `tw-create-retry-*` scenarios failed to send at all. That is a recorder-engine edit, so `recorderSha256` moved and all 255 goldens were re-recorded (headers only; `recording` and `values` byte-identical in every one — upstream's own invariant for this commit, which moves no golden). The 238 that still carried the pre-#19850 `lockfileSha256` picked up the current one on the way |
| 889c2b562 #21566 | say why a host is unreachable instead of "Connecting via Relay…" | **done, merged; 827 upstream files of which 788 are goldens this fork never copies.** The home host row and the host header said "Connecting via Relay…" for as long as the desktop stayed unreachable, even when every relay dial had ended with the cell's 4404 host-offline close (a user's export: 25 such dials over 25 hours), and the diagnostics report said "No single failure cause" because relay dial failures were not recognised and every app resume emptied the evidence window. Relay close codes now map to a closed `RelayHostReachability` verdict (`signed-out` / `host-offline` / `credential-refused` / `unreachable`, else `connecting`), latched after two consecutive identical dial failures and cleared only by an authenticated session; the old signed-out boolean becomes a member of it; `classifyConnection` renders each verdict as a label plus a detail line ("Host 1 is offline" / "Check it's awake, Orca is running, and you're signed in"); a failed dial carries its close code as a structured `relayCloseCode` on the log entry; and the analysis reports evidence from before the last resume or network change with a "Before …" qualifier instead of dropping it. **New (verbatim):** `relay-host-reachability.ts`, `relay-host-reachability-latch.ts`, `relay-recovery-evidence.ts` and their three tests, `connection-diagnostics-relay-dial.test.ts`. **Merged clean by `git merge-file` (upstream's hunks whole, fork drift untouched):** `mobile-relay-reconnect-controller.ts` + test, `stable-logical-rpc-client.ts`, `logical-client-connection-path.ts`, `types.ts`, `mobile-relay-session-establisher.ts`, `mobile-relay-diagnostic-log.ts`, `mobile-relay-recovery-log.ts`, `relay-credential-eligibility.ts`, `client-context-connection-metrics.ts`, `client-context.test.ts`, `relay-host-signed-out-*.test.ts`, `MobileHostCard.test.ts`, `home-host-connection-projection.test.ts`. **Hand-resolved (13 conflicts), every one against this fork's liveness probe or chrome:** the context contract, its state, `use-all-host-clients.ts` and the two test fakes swap `isHostSignedOut` for `getRelayHostReachability` and keep `isLivenessProbing`; `home-host-connection-projection.ts` takes upstream's one-record shape (`HomeHostConnections`) with `livenessProbing` as one more field on the entry, and `MobileHomeHostList.tsx` / `MobileHomeScreen.tsx` / `use-mobile-home-data.ts` read it off `connection?.livenessProbing`; `connection-health.ts` keeps the "Checking…" branch inside `connected` and adds upstream's verdict block below it; `MobileHostCard.tsx` adds the verdict's detail line as one more themed `Txt` caption under the fork's chrome; `host-screen-header.tsx` passes `hostName`. **`connection-diagnostics-analysis.ts` is a real merge, not a pick:** upstream's structure (`selectDiagnosticFailure` with the session/resume split and stale flag, `diagnoseFailure`, relay-first `newestFailure`) is taken whole; this fork's 401-over-503 rule (`hasRejectedCredential`, over the run the selected failure came from) sits at the top of `diagnoseFailure`; and the relay-dial reader is upstream's structured one with the fork's close-code copy folded in — the code is read from `relayCloseCode` when the log wrote one and from `relay_outer_NNNN` text otherwise (older exports), 4503 keeps the fork's "the Relay service itself" copy and its `orca-relay` reportability, the 503-upgrade text keeps its copy, 4404 and 4401 take upstream's (they pass both suites and match the host row's detail line), 4403 routes to the credential copy, 1006 keeps the fork's "either this phone cannot reach the Relay, or the Relay is down" line — upstream's `blames the phone's connection on a transport close` case is re-pinned to it, with the reason in the test — and the text fallback is gated to entries with no code at all, so a session close naming `relay_outer_4408` in prose reaches its own branch (upstream's ordering test caught the fork's old regex taking it). The fork-only `mobile-relay-diagnostic-log.test.ts` gains the two e2ee mocks upstream's latch test uses, because the log now reads `RelayOuterError` off the link. Failing-first: with `connection-health.ts` reverted to the pre-port file, 6 of `relay-host-reachability-verdict.test.ts` fail ("Connecting via Relay…" where the verdict names the offline desktop); green after. **Not taken:** `pilot-scenarios.json` (upstream's baseline pin), 788 goldens (fork re-records its own corpus; none moved here — the twelve upstream re-digested belong to screen-mount adapters this fork does not carry), the three Group D screen-mount adapters and `client-context.web.tsx` (Group E). Recorder 321 passed / 3 skipped, unchanged. **Second review (Opus, read-only, 16 differential inputs old-vs-merged):** one real regression and two small ones, each fixed with its own failing-first test in the same pass. (1) The 401-over-503 rule read the *current* window while the selector could pick a *stale* failure, so `[401 ×4, 503, app-resumed]` wrapped "keep Orca open, recovery will retry" in the stale qualifier — `selectDiagnosticFailure` now hands back the run the failure came from and the sweep reads that (`keeps the refused credential ahead of a 503 when both sit before the last resume`). (2) The 401/403 socket-text branch had ended up behind the numeric map, and a real dial entry always carries a close code, so an Android `'403 Forbidden'` upgrade refusal read as the 1006 copy; it now precedes the map like the 503 text does (`reads a 403 in the socket error text as a refused device`). (3) A comment said the liveness row "draws a spinner"; it prints "Checking…". Accepted as deliberate: a live session close carrying 4503 reads "closed unexpectedly" (reportable) until the next dial fails 4503, because upstream's ordering test requires the code gate |
| 85d1ffc07 #20450 | accept enterprise managed GitHub owner logins | **done, verbatim.** GitHub appends `_<shortcode>` to Enterprise Managed User logins, and three drifting owner regexes (main, renderer, mobile) all rejected the underscore, so an EMU user could not pick a project by `owner/N` or URL. New shared `github/owner-slug.ts` + test (pinned 85d1ffc07) is the one source; `tasks/github-project-reference.ts` and its test matched upstream's parent and took the hunk whole (`OWNER_RE` and the shorthand regex now come from shared). Failing-first: the new case `accepts Enterprise Managed User owners` fails on the old regex (`octocat_acme/1` parses to null), green after. The main and renderer halves are not vendored here |
| eabfbaab8 #21293 | drop the unreachable dispose-before-ready notifications arm | **done, merged.** `disposed` is set only on the disposer's first line and the next statement detaches the stream listener in every transport, so the `ready` handler's `if (disposed)` arm could never observe it. Removed here the same way (three lines, with upstream's comment) from this fork's much larger `mobile-notifications.ts` (epoch/watermark catch-up, 289 lines of drift). Upstream's two transport pins applied clean by 3-way merge: `mobile-relay-rpc-streams.test.ts` (matched parent) and `stable-logical-rpc-client.test.ts` (a live/cancelled differential against a physical session with an inert disposer, so only the logical guard can fence the late event). Upstream's two registry-driven notification cases — the real `RpcClientStreamRegistry`, dispose before and after the `ready` reply — are adapted to this fork's catch-up shape (no `requestNotificationCatchup` mock here; the assertion is that no request at all is sent) and live in a new `mobile-notifications-dispose.test.ts`, because the fork's `mobile-notifications.test.ts` sits at oxlint's 800-line ceiling. Refactor oracle: both new cases pass with the arm present (verified by swapping the pre-port file back in) and with it gone; no golden moved |
| c35a47958 #20303 | reuse computed project identity in projection | applied verbatim; `project-host-setup-projection.ts` matched upstream's parent. `createProjectFromRepo` now takes the already-computed `projectId` argument instead of recomputing it a second time via `getProjectId(repo)`. Mobile calls `projectHostSetupProjectionFromRepos` from `NewWorktreeModal.tsx` and `new-workspace-project-targets.ts` |
| b7a1aeda5 #20305 | avoid per-item arrays in journal projection | 3-way merged (`git merge-file`) onto this fork's many local hunks in `structured-agent-session-projection.ts` (Orca #19226, #18765, #19845, #19841, #19230, e80fae0c4's per-item render cache — see LOCAL-FILES.md). `projectStructuredItemsToNativeChat` builds its result with `forEach`/`push` instead of `flatMap` allocating a one-or-zero-element array per item; none of the local hunks touch that function, so the merge applied clean. Mobile reaches it through `mobile-structured-transcript-projection-reuse.test.ts`'s render-cache reuse guard |
| 0d4d059ae #20321 | stop building chat diff sections beyond preview limit | applied verbatim; `native-chat-diff.ts` matched upstream's parent (no drift since its ddc5b75ac vendor pin — see UPSTREAM.txt). The internal `patchTextFromToolInput` now breaks out of its section loop once the joined length passes `MAX_DIFF_CHARS`, instead of building every section through `flatMap` first and truncating after. Reachable through the exported `diffFromToolCall`, which `MobileNativeChatDiffCard.tsx` calls. Upstream's own `src/renderer/.../native-chat-diff.test.ts` addition is desktop-only and not vendored |
| 13c0c8b53 #20334 | select matching model IDs without sorting | 3-way merged (`git merge-file`); `native-chat-session-option-state.ts` carries this fork's local hunk (LOCAL-FILES.md: carry a reported-model's own non-reported option picks onto the newly reported model) below where this commit's hunk lands, so both merged clean. `matchNativeChatCatalogModelId` now walks `catalog.models` once, tracking the longest containing id, instead of sorting a copy of the array by id length on every call. `native-chat-session-option-state.test.ts` was byte-identical to upstream's parent; its new case taken verbatim |
| dcc1f9012 #20343 | count bot comments without a filtered array | applied verbatim; `pr-comment-audience.ts` matched upstream's parent (this file is at the vendor base, unlisted in UPSTREAM.txt because it was never re-vendored past it). `getPRCommentAudienceCounts` now counts with a `forEach` instead of `.filter(...).length`. Mobile calls it from `PRCommentsSection.tsx` |
| fbab61ec0 #20329 | skip unclosed suffixes when stripping review markdown tags | 3-way merged (`git merge-file`); `mobile/src/components/pr-sidebar/markdown-blocks.ts` carries two fork-local features upstream's parent lacks (a wrapped-list-item continuation join, and the per-cell table-delimiter check from #20317's port) — neither overlaps `stripHtmlTags`, so the merge applied clean. `stripHtmlTags` now regex-replaces only the prefix up to the text's last `>`, leaving any unclosed `<a ` suffix untouched instead of retrying the tag pattern against a suffix no tag can close in. New `markdown-tag-stripping-performance.test.ts` taken whole (a `runInNewContext` 250ms budget over a 20,000-token unclosed run, plus a differential fuzz against the original always-retry regex) |
| 2d1bd1eb4 #20332 | bound whitespace normalization for tool previews | `native-chat-tool-summary.ts` 3-way merged (`git merge-file`, base = upstream's parent, other = upstream's new): this fork sits one commit behind that parent because #20328's `countToolCalls` rewrite was skipped as unreachable (see Skipped), and that region does not overlap this commit's hunks, so the merge carried both changes without a conflict. New `native-chat-tool-preview-prefix.ts` (`collapsedToolInputPrefix`/`MAX_TOOL_PREVIEW_LENGTH`: a single bounded scan with a sticky-regex whitespace matcher, instead of `.replace(/\s+/g,' ').trim()` over the whole string) and its `native-chat-tool-preview-scan.test.ts` vendored whole; `summarizeToolInput`, `normalizedToolInputHasDetail` and `summarizeToolPath` now call the shared helper. Upstream's `config/scripts/tool-preview-whitespace-benchmark.mjs` was not taken — this fork carries no `config/scripts/` (same precedent as #20224) |
| f107499e4 #20786 | fix(lint): enable anti-slop/no-reflect-get | **code only, rule skipped.** This fork has no `config/oxlint-anti-slop.json` and no `config/oxlint-plugins/anti-slop/` directory — the `anti-slop` plugin does not exist here at all, so "enable the rule" has nothing to enable; recorded as skipped for that reason. The three mobile-reachable files this commit's fix touches were byte-identical to upstream's parent and applied verbatim: `settings-read-operations.ts` gained a named `settingsField(settings, key)` reader (boxes once, indexes a `Record<string, unknown>`) replacing two `Reflect.get(Object(settings), '...')` calls (for `prBotAuthorOverrides` and this fork's own `terminalCopyTrimsGutter`, ported at #20545 above), and the two `mobile/src/test-support/rpc-recording/` mutation-harness files (`mutants/operation-mutations.ts`, `recording-runner.test.ts`) update their anchor text to match, so the `bot-overrides-envelope` mutant still finds exactly one site |
| bfdec2635 #20781 | fix(lint): enable anti-slop/no-object-parameters | **code only, rule skipped** (same anti-slop absence as #20786). The six mobile files' `object`-typed parameters became named identity types (`Record<string, never>`, a branded `unique symbol`, or a real domain type), which this fork's local hunks in four of the six files did not touch, so `git merge-file` applied clean: `connection-diagnostics-screen-data.ts` and `host-client-acquisition-registry.ts` were byte-identical to upstream's parent (applied verbatim); `mobile-terminal-viewport-resubscribe.ts` (this fork's `emptySnapshotNeedsResubscribe`), `direct-rpc-client.ts` (this fork's liveness-foreground-profile arrow methods and the `deliveryUnknown`/`stateAfterClose` local hunks), `relay-dial-stage.ts` (this fork's `RelayDialTimings`/`formatRelayDialTimings`) and `rpc-session-liveness-watchdog.ts` (this fork's `isProbing`/`onProbingChange`/`setProfile`) 3-way merged. One follow-up fix this port required that upstream's own diff didn't touch: `app/connection-log.tsx` declared its own inline `routeKey: object` / `{ hostId, requestedHostId, routeKey: object }` shape instead of importing the screen-data module's types, so tsc refused `object` against the new `DiagnosticsRouteKey`; replaced with the exported `DiagnosticsHostSelection`/`DiagnosticsRouteKey` types directly |
| 231e805b1 #20785 | fix(lint): enable anti-slop/no-shape-in-symbol-names | **code only, rule skipped** (same anti-slop absence as #20786/#20781). The mechanical renames — nothing wire-visible, no exported symbol's name a caller keys off — were applied wherever they touch a file mobile reaches: `mobile/src/components/MobileRepoIcon.tsx` (a comment-only exemption note for lucide's own `Shapes` export) and `mobile/src/tasks/use-mobile-tasks-provider-view-projection.test.tsx` (`shape()` → `summarizeSections()`, its local test helper) applied verbatim, byte-identical to upstream's parent. Shared: `native-chat-ask.ts` (`parseQuestionsShape` → `parseCanonicalQuestionsInput`, internal, not exported) and the three RPC-contract type helpers `repo-update-params.ts`/`rpc-send-params.ts`/`ui-update-value-tolerance-params.ts` (`ZodRawShape` generic constraints spelled out as `Readonly<Record<string, z.ZodType>>`, internal type-parameter renames only) applied verbatim. `agent-session-record.ts` 3-way merged onto this fork's local `rewind`/`conversationName` fields (from the rewind port, ce4a3a418 #19235) — the renamed local `shapeValid` → `fieldsValid` variable sits inside `isAgentSessionRecord`'s validation chain, which the local hunk only extends with two more conjuncts, so the merge applied clean |
| 40b223050 #21298 | typecheck the test files on a ratchet, and pin the reply enums where tsc looks | **done, ratchet infrastructure only; the enum-pin half is skipped.** Ported: `mobile/tsconfig.test.json` (puts test files back into a tsc program, `typecheck:tests`) and `mobile/scripts/check-tests-typecheck-ratchet.mjs` + its test, both self-contained tooling with no product-specific logic — the `TESTS_OUTSIDE_PROGRAM` map was set to empty rather than upstream's four Node-side file names, none of which exist here (`grep -rl "from '.*/src/main\|shared/child-process'" mobile/src mobile/scripts | grep '\.test\.ts'` returns nothing), and `tsconfig.test.json`'s `exclude` matches (just `node_modules`) so the two stay in sync, which the vendored test itself verifies. **Baseline regenerated for this fork, not copied from upstream** (per the port brief): `node scripts/check-tests-typecheck-ratchet.mjs --init` against this fork's own 904 test files produced 197 grandfathered entries (upstream's is 126, a different number for a different test suite). One census failure the `--init` run surfaced and fixed: `MobileHostCard.test.tsx` was shadowed by `MobileHostCard.test.ts` the same way upstream's was (a wildcard `include` keeps only the higher-priority extension) — renamed to `MobileHostCard-truthful-status.test.tsx`, the same name upstream chose, since its own content (never asserting a count nothing proved) fits. Pulled forward from a **later** commit in this same group, f949d5fcc #21156 (confirmed an ancestor of 40b223050 in upstream's actual history, despite the backlog listing it after): `vitest.config.ts`'s `include` gained `'scripts/**/*.test.ts'`, 3-way merged onto this fork's local `resolve.alias`/`testTimeout`/`exclude` hunks — without it the new ratchet test would typecheck but never actually run under vitest. `mobile/package.json` gained the two scripts (`typecheck:tests`, `check:tests-typecheck`), byte-identical hunk. `mobile/README.md`'s Validation section describes the same mechanism with this fork's own 197 count instead of upstream's 127. **Skipped:** `src/shared/zod-salvage.ts`'s `openEnum`/`hostUnionArms` refinement and the five schema files it feeds (`new-workspace-reply-schema.ts`, `notification-reply-schema.ts`, `task-project-board-reply-schema.ts`, `task-provider-entity-reply-schema.ts`, `workspace-source-reply-schema.ts`) — none of `openEnum`, `hostUnionArms`, or any of those five files exist in this fork at all (confirmed absent; this fork's reply-schema layer for these domains, and the #21269 `hostUnionArms` helper this commit builds on, were never ported here — out of scope for this commit alone). `mobile/src/test-support/rpc-recording/README.md`'s corpus-count correction (368→393 scenarios, 727→778 goldens) also skipped: those are upstream-specific measurements of upstream's own corpus, not this fork's (this fork's own count moves with each RPC-migration port and is tracked in that port's own commit message, e.g. "Goldens 153 → 208"); whether this fork's README carries a similarly stale count is a separate, unaudited question left as a known gap. The 18-test-file mechanical fixes and the reply-schema pin moves were not replicated file-by-file — the ratchet's own baseline already captures whatever pre-existing type debt this fork's tests carry, which is the mechanism's whole purpose |
| 2b34255d9 #20755 | fix(ci): stop defining pilot mutant tests inside a conditional | applied verbatim; `pilot-mutants.test.ts` matched upstream's parent. Each pilot now pairs with its pinned mutant and reference state before the loops, so every iteration defines exactly one `it()` unconditionally instead of a variable count gated on a runtime `if`. Same 14 tests as before (11 mutant-kill, 3 `skipIf`-gated reference tests): 11 passed, 3 skipped, verified |
| f949d5fcc #21156 | ci(mobile): fail CI when the RPC recording pin leaves main's history or the corpus does not reproduce | **mostly skipped; one hunk already landed.** `vitest.config.ts`'s `include` gaining `'scripts/**/*.test.ts'` was pulled forward and applied as part of the #21298 port above (needed there for the new ratchet test to actually run under vitest — recorded on that row). The new `mobile/scripts/rpc-recording-pin-guard.mts` + its test (594 lines) are **skipped**: the guard's whole premise is that `mobile/rpc-foundation/goldens/*.json`'s `baseline` field names a commit reachable in `git log` on this repo's own main branch, so a squash-merge that drops the branch cannot silently orphan the pin. Checked: every golden's `baseline` (e.g. `c6a72169843ececf3a21da370ac50c5c5a4e6462`) is an **upstream Orca** commit sha that `git cat-file -t` confirms does not exist as an object in this repo at all — this fork's recorder inherited the field from vendoring rather than rewriting it to a Code UI commit, and each port's own commit message (not a CI ancestry check) is what actually tracks the baseline moving here (e.g. "baseline `e53f1557…` → `50e752fc…`", #20568's row above). This fork also has no PR-gating GitHub Actions workflow at all — `.github/workflows/` holds only `mobile-android-release.yml`, a tag-triggered release build, with no `pr.yml`/`main.yml` for the guard to run under. Porting the script would add ~600 lines of dead CI tooling with no home and a check that would always fail for the wrong reason (a foreign commit sha) rather than the one it exists to catch |
| cf7408a37 #19750, f31bfa8fb #20023 (revert), fa3d29fa1 #20029 (re-land) | session search query engine over the index | **done, one shared file's worth.** The engine itself (SQLite FTS index, `SessionSearchEngine`, the route ladder, benchmarks) is entirely `src/main/`, not vendored. What reaches the phone: `ai-vault-session-filters.ts`'s `parseVaultQuery` now delegates to a new shared tokenizer, `splitAiVaultSearchQuery` (new `ai-vault-search-query-operators.ts` + test, vendored whole — no external deps), instead of its own hand-rolled `tokenizeQuery` (removed, confirmed unreferenced: `grep -rln 'tokenizeQuery' mobile/ src/` returns nothing). `matchesAiVaultQueryOperators` was extracted so the panel and the (unvendored) engine share one definition of `repo:`/`path:`. Mobile calls `filterAiVaultSessions` → `matchesQuery` → `parseVaultQuery`/`matchesAiVaultQueryOperators` from `agent-history-sections.ts`'s Agent History search box, so this is a real behaviour change reaching the phone: quoted operator values (`path:"/Users/ada/My Project"`), case handling and the repo/path matching semantics now match the desktop exactly. `ai-vault-session-filters.ts` was byte-identical to upstream's parent (cf7408a37^) and to the re-landed fa3d29fa1's version (the revert-then-reland nets to the same file), so applied verbatim. The revert (f31bfa8fb) itself has nothing to port |
| 1f7655f3e #20277, a01027697 #20886 | public session search contract and transports; enable indexing on paired servers from a client | **done, catalog-only.** The session-search panel, settings UI, IPC/relay/web transports and the paired-server indexing toggle are desktop-only (`src/renderer`, `src/main`, `src/preload`, `src/relay`) and were not ported — mobile has no session-search UI. What reaches the phone: `rpc-contract/rpc-params-catalog.generated.ts` gained three `RPC_PARAMS_BY_METHOD` rows (`aiVault.searchSessions`, `aiVault.searchStatus`, `aiVault.setSearchEnabled`), applied verbatim from both commits' isolated hunks (the file was byte-identical to 1f7655f3e's parent; a01027697's own hunk to the same file is a clean, context-independent 2-line addition, taken directly without pulling in two intervening non-group commits — 46ed53b88 and 0d2f7bcea — that also touched files in this chain but are outside this port). Pulled in only because the catalog imports them (same precedent as c84007c54's rpc-contract dependencies): new `ai-vault-search-contract.ts` (hand-assembled from 1f7655f3e's version plus a01027697's isolated `AiVaultSetSearchEnabledParamsSchema` addition — verified as a clean, non-conflicting insert), its test, and new `ai-vault-search-limit.ts`. Both new schemas' dependencies (`AI_VAULT_AGENTS`, `AI_VAULT_SCOPE_PATHS_MAX_COUNT`) already exist in this fork's `ai-vault-types.ts`. This closes a gap `orca-mobile-rpc-allowlist.test.ts` had pinned since 2026-09-18 — the real installed desktop (1.4.205) already allowed these two `aiVault.search*` methods for a mobile-scope token, ahead of what the vendored catalog knew; that test's comment said "re-vendoring the catalog past them shrinks this list", and this port does exactly that (the expected catalog-gap list is now `[]`). Nothing on the phone sends any of the three methods (no session-search UI here), so no RPC allowlist exception was needed |
| 09073086a #19512 | inline terminal images via @xterm/addon-image (perf-first) | **done, one shared file only.** The feature itself — the lazy xterm addon, DA1 Sixel advertisement, Kitty/SIXEL/iTerm2 decode, the Settings toggle — is entirely `src/renderer/`/`src/main/`/`src/relay/` desktop code (an xterm.js addon has no counterpart on a React Native terminal view) and was not ported. `src/shared/terminal-query-reply.ts` applied verbatim (byte-identical to upstream's parent): a new `KITTY_GRAPHICS_PREFIX_RE` recognizes a complete `ESC _G ... OK/E...:...  ESC \` Kitty graphics acknowledgement as a query reply, so it bypasses user-input activity and the query-reply debounce the way a CPR or DECRPM reply already does. Mobile imports `isTerminalQueryReply`/`extractOnlyTerminalQueryReplies`/`needsCookedEchoSafeQueryReply` from this file (`mobile-terminal-query-reply.ts`, `terminal-webview-query-reply-routing.ts`, `use-mobile-session-terminal-input.ts`) to keep a PTY-emitted ack out of the visible transcript even though the phone never renders the image itself. `terminal-query-reply.test.ts`'s new case vendored (not run here). `default-global-settings.ts`/`global-settings-types.ts`/`terminal-image-protocol.ts`/`terminal-inline-images-settings.ts` are desktop-only, mobile imports none of them |
| 55b339201 #20545, #19770 | drop the agent gutter from copied selections | **done, mobile half.** New `src/shared/terminal-selection-gutter.ts` (+ test) vendored whole — `stripTerminalSelectionGutter` measures the run of leading spaces every non-blank selected line shares (CRLF-safe: the carriage return is split off a line before its indent is measured, so a blank CRLF row cannot read as a zero-indent content row and cancel the gutter on Windows) and trims only that. Mobile now mirrors the desktop's "Trim Gutter on Copy" setting instead of always stripping: new `settings-read-operations.ts` export `terminalCopyTrimsGutterRead` (3-way merged around this fork's already-reworded acceptance-comment hunks from #20563/b30debd — an unreachable read, since this fork never had the gutter-strip feature to begin with, so nothing was actually "always on" here before this port), and new `mobile/src/terminal/terminal-copy-gutter-preference.ts` (`useTerminalCopyTrimsGutter`: reads once per connection, defaults to on so an unreachable host or an older desktop still trims). `use-mobile-session-accessory-selection.ts` 3-way merged onto this fork's local accessory-repeat-fired hunk (Orca #12251, unrelated function) — the selection Copy button now trims through the shared helper when the mirrored setting is on. `mobile-session-route-parity.test.ts` re-pinned to **this fork's own computed hashes**, not upstream's (per the port brief): hooks 281 → 282, `HEAD_MAIN_HOOK_SHA256`, `HEAD_HOOK_BINDING_SHA256` and `HEAD_CALLBACK_BODY_SHA256` recomputed from the merged source. Desktop-only pieces not vendored: every terminal-pane/xterm clipboard seam, the Settings UI section, and `default-global-settings.ts`/`global-settings-types.ts` (mobile never imports the global-settings object directly, only through `settings.get`'s RPC projection) |
| f2af92b2f #19705 | show live background work and name each row by kind | **done, the wire and the phone's half.** Upstream's visible change is three `src/main/` trackers (Claude and Codex) that now publish foreground work while a turn is open, plus the desktop strip; the phone already names each row by kind and reads the roster off the wire (484323a), so what it needed was the contract: `stoppable?: boolean` on a task (absent means yes), equality that sees it, and the two capabilities the host gates on. `agent-session-background-task-wire.ts` vendored new at f2af92b2f — upstream split the task types out of `agent-session-wire.ts` there — and `agent-session-wire.ts` took the re-export hunk by 3-way merge (only the import line conflicted, on the rewind import this fork lacks). `remote-runtime-client-capabilities.ts` re-vendored whole at f2af92b2f: its whole base→f2af92b2f history is #19346's and #19705's one-line adds. `protocol-version.ts` hand-applied: `AGENT_SESSION_BACKGROUND_TASK_STOP_CAPABILITY` (#19346, never taken with 484323a — so an upstream host was nulling a Codex roster with `supportsStopAll: false` for this phone) and `AGENT_SESSION_BACKGROUND_TASK_ROW_STOP_CAPABILITY`. Advertising the second is what makes the host send foreground rows at all; `mobile-structured-background-tasks.ts` carries an explicit `stoppable: false` onto the projected row and `MobileBackgroundTasksSheet.tsx` withholds that row's Stop. Five tests written from the symptom, all red on the pre-port code (equality judged a withdrawn stop equal; the reducer dropped the republish; the projection lost the flag; the sheet drew `Stop Explore the repo`; the capability list lacked both), green after; the sheet case runs in both themes. The upstream reducer test is mirrored in `mobile-structured-session-background-tasks.test.ts` because `src/shared/*.test.ts` is never collected here. `docs/mobile-background-tasks.md` says how a row can now be unstoppable |
| 2626e2eca #19695 | make the structured turn lifecycle row durable so completed durations survive | **done, whole for the phone.** The turn record becomes its own journal item (`kind: 'turn'`, revised in place through running → completed / interrupted / unverifiable, never tombstoned) carrying the host's `startedAt`/`completedAt`, the provider's own `durationMs` and the `userItemId` that opened it; every frame carries `hostNow`, so a client attaching mid-turn anchors its live counter on the real start, and a settled "Worked for" comes off the journal instead of what this phone happened to clock. A host publishes the typed item only to a client advertising `agent-session.turn-item.v1`; the phone now does. **Shared, new and verbatim at 2626e2eca:** `agent-session-turn-record.ts` (one reader for both journal shapes), `agent-turn-lifecycle-text.ts`, `structured-agent-session-turn-timing.ts` (settled-turn selectors, the skew-free local anchor), `agent-session-background-task-state-equality.ts` (the reducer's roster comparator, moved out), and their four test files; `remote-runtime-client-capabilities.ts` re-vendored whole. **Merged** (3-way onto the local hunks, see LOCAL-FILES.md): `agent-session-journal-types.ts`, `agent-session-journal-schemas.ts`, `structured-agent-session-projection.ts`, `structured-agent-session-reducer.ts`, `agent-session-wire.ts` (`hostNow`), `protocol-version.ts` (the capability, hand-applied). **Not taken:** `agent-session-wire-refusals.ts` — the split exists for upstream's 300-line cap and imports `agent-session-rewind.ts`, which this fork does not vendor; the refusal codes stay inline in `agent-session-wire.ts`. **Completed on the way:** `structured-agent-session-live-turn.ts` and its test, partial since #19977 for want of the typed item, are now vendored whole at fab78c766 and the projection re-exports its two selectors, as upstream's does. **Mobile:** `use-mobile-structured-agent-turn-timing.ts` + test new verbatim; `use-mobile-structured-agent-state.ts` and `use-mobile-native-chat-turn-disclosure.test.tsx` verbatim (both matched upstream's parent); `use-mobile-native-chat-turn-status.ts` re-vendored at fab78c766, which is exactly this fork's copy plus the #19695 hunk; the disclosure hook, view props, view, overlay, both controller contracts, the controller and the structured session hook hand-threaded (`workingStartedAt`, `settledTurns`), keeping this fork's `thinking`/`turnActivity` props beside them. Upstream's extraction of the composer-lock settle hook into the lease file was skipped — this fork had already moved it to `use-mobile-native-chat-input-lock.ts` — and its new test came over adapted as `use-mobile-native-chat-input-lock.test.ts`. The fork-local `mobile-native-chat-turn-activity.ts` now finds the turn start through `readAgentJournalTurn`, so a typed row bounds the scan. **Failing-first:** reverting the twenty modified source files fails 8 tests — the two upstream disclosure cases, the capability pin, and five in the new `mobile-structured-session-turn-record.test.ts` (a typed row is not the live turn; it paints as a bubble; the previous turn's activity copy leaks into the live one; the reasoning check runs past it; no `hostClock`). Green after. Upstream's reducer/projection/turn-record/turn-timing suites are vendored but never collected here, so that file mirrors them. One fork-local fixture corrected: `mobile-structured-older-page-hole.test.ts` fed items with no `body`, which the reducer's submission retention now reads. `MobileNativeChatView.tsx` ceiling 410 → 412 for the two threaded props. Gate: tsc clean; vitest 904 files / 7864 passed / 17 skipped; oxlint 0 errors |
| 027acb4ef #19863 | settle a structured send on admission, not on the provider echo | **done, the phone's half.** Upstream's defect was the desktop outbox: the host held `agentSession.send`'s reply up to 10 s for the provider to echo the message, an echo that cannot come until the turn ahead ends, and the renderer read the timeout as "delivery unconfirmed" with a Retry that re-delivered (one message reached the model five times). The fix's dispatch and outbox halves are `src/main/` and `src/renderer/`; this phone never read the reply's dispatch state — an `ok` is "accepted" and its own pending-echo system draws the bubble until the journal's submission row lands — so it never showed the false banner. What it did pay was the wait: a mobile-scope client without `agent-session.pending-send-result.v1` still went through `waitForSendSettlement`, up to 10 s against this phone's 15 s send budget over the relay. The capability is now advertised (`protocol-version.ts` hand-applied, incl. the Electron list line for fidelity; `mobile-runtime-client-capabilities.ts`), so the host answers as soon as the message is written. `structured-agent-session-outbox.ts` re-vendored whole (it matched upstream's parent; the new `pending` arm of `reconcileStructuredAgentSessionOutbox` has no caller here), the projection's comment hunk merged, `rpc-client-capabilities.test.ts` verbatim — red before (the capability was not on the wire), green after. **Not taken:** `structured-agent-session-send-disposition.ts`, the renderer outbox's pure send-result classifier; nothing under `mobile/` imports the outbox reconciler or would import this (`grep -rn "reconcileStructuredAgentSessionOutbox\|SendDisposition" mobile/src` → 0). The Codex 10 s stopwatch upstream names as debt is host-side |
| 9b83f976f #19928 | describe slash commands from the provider's own report | **done.** The Claude session reports a description and argument hint for every command it can run; the catalog kept only the name, so the `/` menu described the handful the curated map covers and left the rest — `/goal` included — blank. The producer is `src/main/claude/claude-slash-command-catalog.ts` (not vendored; a host past this commit sends the fields). Shared: `AgentSessionSlashCommand.description` / `argumentHint` on `agent-session-wire.ts` and the `sessionSlashCommandSuggestions` change on `native-chat-slash-commands.ts` (a reported description wins, the curated one is the fallback for a names-only report; the hint carried through), both hand-applied — both files carry local hunks — and the test's two new cases by 3-way merge. Mobile: `MobileNativeChatComposerSuggestions.tsx` shows the hint beside the token in the muted mono tier, capped at 80 characters with whitespace collapsed, the same bound upstream's `native-chat-picker-items.ts` applies; the phone's own row already drew the description. Four tests, all red on the pre-port code: the two upstream shared cases mirrored in `mobile-native-chat-session-catalog.test.ts` (with this fork's own `/compact` text, since the curated catalog here is Claude Code 2.1.261's) and a new `MobileNativeChatComposerSuggestions.test.tsx` that renders the row in light and dark and pins the cap |
| 8999a0028 #20133, with e7206f62a #20868 folded in | give each structured dispatch state exactly one meaning | **done, whole for the phone.** `unknown` meant five things; now `pending` is written-and-awaiting, `accepted` the provider has it, `rejected` provably did not happen, `unknown` genuinely cannot tell — and Retry never re-delivers an `unknown`. The mobile half is the larger one: a send's operation id was released whenever the reply came back `unknown`, so the user's next send of the same text went out under a fresh id the host had no ledger row for — a second delivery of the one message the host might already hold — and a replayed `unknown` came back `ok`, which the phone called "accepted" and cleared the composer on. The send path now lives in `mobile-structured-agent-session-send.ts`: the id is kept in a durable AsyncStorage journal keyed by session and intent (`mobile-structured-send-operation-journal.ts`, bounded at the host's 4 096-row ledger limit and fail-closed at capacity), `mobile-structured-send-delivery.ts` reads the reply's `dispatchState` in one pure function (accepted/pending spend the id; rejected spends it and shows `structuredAgentSessionRejectionNotice` copy instead of an internal `provider_write_failed: …` token; unknown keeps it), and `use-mobile-structured-send-operation-reconciliation.ts` clears an ack-lost id once the journal settles its submission. `retryUnknown` is gone from the wire. Transport: a relay request is marked delivery-unknown only once its frame was written (`relay-pending-requests.ts` `written`), the direct client settles a correlated `unauthorized` before marking the rest unknown, and the stable logical client resolves a correlated response even when close or cutover wins the callback race (this fork's error-arm cutover wrap is kept). An uploaded chip carries a `contentFingerprint` so a re-pick of the same bytes is the same send. **Verbatim** (matched upstream's parent, or new): `mobile-structured-agent-session-rpc.ts`, the five new session modules and their three tests plus the send-result fixture, `use-mobile-structured-agent-session.test.tsx`, `mobile-structured-session-operation-retention.test.ts`, `use-mobile-structured-native-chat-send-bridge.ts`, `relay-pending-requests.ts`; shared `agent-session-host-authority.ts`, `structured-agent-session-mutation.ts` + test, `structured-agent-session-outbox.ts`, and new `structured-agent-session-dispatch-rejection.ts` and `structured-agent-session-send-disposition.ts` + test (the latter skipped at #19863 as unreachable; `mobile-structured-send-delivery.ts` imports its notice now). **Merged** by 3-way: the controller (`callerIdentity` from the device token), the session lane, both attachment hooks, the chip type (beside this fork's `kind`/`name`/`uploading`/`batch`), the session hook (its send arm replaced by the new module; the fork's rewind, cancel, background-task and `/` catalog wiring untouched), the composer-command test, the direct and relay clients and their tests. **#20868 folded in:** #20133 maps a cancel's `agent_session_operation_unknown` to `unknown`, and this fork's `mobile-structured-cancel-id-retirement.test.ts` — written against upstream's #20868 before the defect could reach here — went red on the port exactly as upstream's did; the `hostReportedOperationUnknown` flag and the cancel dispatcher's condition are #20868's hunks on this fork's `mobile-structured-agent-cancel.ts` (upstream's `mobile-structured-agent-session-cancel.ts` and its test did not exist here until the #20601 port below took both). **Failing-first:** reverting the 17 modified source files fails 28 tests across 8 files — the ack-loss id kept across two `unknown` replays, the journal's persistence/capacity/identity guards, the composer command retaining its id after a host failure, the chip fingerprint, the relay's pre-write failure staying definite, the direct client's auth-recovery marking, the stable client's committed response, and the cancel retirement. Green after. `use-mobile-native-chat-image-attachments.test.ts` ceiling 800 → 806 (this fork's copy carries its own batch and document cases). Gate: tsc clean; vitest 908 files / 7902 passed / 17 skipped; oxlint 0 errors **Fork-local divergence after the second review (2026-09-19):** an unreadable send-operation journal (not JSON, wrong shape, malformed id) starts over with a warning instead of refusing every later send; upstream's fail-closed test for that case is replaced by `starts over from an unreadable journal…`. A transient storage read/write error still fails closed, as upstream pins. Also fork-local: `useMobileStructuredAgentTurnTiming` hands the settled-turns map on only when its content changed, so settled rows stop re-rendering per token batch |
| c287a5d9b #20506 | provider-aware Fast mode | **done, the phone's half.** The producers — Claude's `list_models` support check and the `fastModeState` frame, Codex's `serviceTier` routing — are `src/main/`; a host past this commit reports `fastModeSupport`, `supportsFastMode` per model and `current.fastMode`/`fastModeState`, and the phone now draws and sets it. Shared: `structured-agent-session-option-codec.ts` (new, the one boundary that writes the boolean as `'true'`/`'false'` on the string-valued wire), `structured-agent-session-options.ts` (the Fast row appears only when the session AND the model say so; a boolean pick is admissible; a `fastMode` the host stops reporting is cleared), `native-chat-session-options.ts` (`currentValue` required on the boolean arm, `sessionOptionValueMarker`), `native-chat-session-option-defaults.ts` (`fastMode` joins the launch seed through the codec) — all four re-vendored whole with their tests, the defaults file moving past 852495d35's pure `narrowStructuredLaunchSeedOptions` extraction on the way; `agent-session-wire.ts` and `native-chat-session-option-snapshot.ts` took their hunks by 3-way merge onto the local ones. Mobile: `use-mobile-structured-agent-options.ts` merged (generation fencing, the pending-write guard, the post-write options refresh, the codec on the wire; this fork's rewind-support read kept beside it), `mobile-native-chat-session-option-labels.ts` merged (a boolean summary says On/Off and lets the sheet's marker carry provenance), and `MobileNativeChatSessionOptionRows.tsx` hand-written: upstream's On/Off radio pair becomes one `Switch` row that owns its label with a `Default` / `Not reported` marker, drawn from this fork's theme rather than upstream's static palette (the track and thumb take the palette every other switch in the app uses). Upstream's two new mobile tests came over verbatim (`MobileNativeChatSessionOptionRows.test.ts`, `use-mobile-structured-agent-options.test.ts`, 398 lines pinning the hook's concurrency contract and the boolean round trip); `MobileNativeChatSessionOptionRows.theme.test.tsx` is this fork's, pinning the row's colours in light and dark and the flip reaching the setter. **Failing-first:** reverting the eight modified source files fails 12 tests across the three files (one switch instead of two radios, the marker's two arms, the resolved value on the switch, the theme colours, the boolean round trip on the wire, the refresh after an accepted write and its fence gate, the superseded read, the overlapping-write refusal). Green after. Not verified against a live host: no desktop here is past c287a5d9b, so the Fast row has not been watched appearing; a host that reports nothing draws nothing, which is the pre-port view |
| 4634d2c03 #20518, a4c11f188 #20581 | let the provider reopen a Claude turn it resumed itself; stop a bounded tail read from moving the chat cursor past unapplied rows | **done as alignment; neither defect reaches the phone.** #20518 is `src/main/claude/` (a lifecycle row may key itself when provider output opened a turn with no user item); its one shared line is the `userItemId` doc comment on `AgentJournalTurnLifecycle`, merged, and the phone's `selectStructuredAgentTurnTimings` already accepts a row keyed to its own id (`itemIds.has(key)`). #20581's symptom — a pane latched "Working for N" after the agent finished — came from the desktop's `tail-page` refresh on window focus, a third reducer operation that took the cursor from the journal head and the items from a bounded page, so a send's `pending` submission survived past the acceptance row that would have settled it. **This phone never dispatched `tail-page`**: `use-mobile-structured-agent-state.ts` moves the cursor only through the live subscription and reads history only as `older-page` (`grep -rn "tail-page" mobile/src` → tests only). The reducer hunk is merged anyway so the vendored file tracks upstream: `history-page` replaces the page whole (roster, activity and catalog carried from state), `replacePage` takes a nullable fence, `shouldAdvanceStructuredResumeCursor` is gone (0 callers here). The fork's three reducer mirror tests that used `tail-page` to prove state survives a refresh now say `history-page` and still hold. The vendored `structured-agent-session-reducer.test.ts` is left at its pre-#20581 shape — seven conflicting hunks on a file this fork never collects. No failing-first test: nothing changed that a phone test can observe; the four mirror files stay green, which is the refactor's oracle |
| c6a721698 #20496 | hide activity while awaiting input | **done, adapted.** While a structured prompt (approval, question, ask) waits on the user, the live turn's "Working for N" row said the agent was working beside a card that said it was waiting. Upstream gates its list footer on `!hasPendingStructuredInteraction`; this fork draws the live row under the user message (and in the list header when unanchored), so the gate went where both readers get it — `awaitingInput` on `use-mobile-native-chat-turn-disclosure.ts` nulls `active` and the row's `turnStatus` while leaving `activeTurnIsWorking`, the timing state and Stop untouched. Withheld, not settled: the clock keeps counting and the row returns when the prompt resolves. The view computes the flag exactly as upstream (`structuredActivityUi && (ask ?? permission ?? question) != null`). Upstream's three-card test (`ChatAsk`, `ChatQuestion`, `ChatPermission`) came over adapted to this fork's row-level assertions, red on the pre-port code (three cases: the row still said working), green after. The commit's other twelve files are `src/main/codex/` prompt-registry settlement and the desktop renderer, not vendored. `MobileNativeChatView.tsx` ceiling 412 → 413 |
| 59d29af40 #20655 | a verified OMP native-chat mock scenario | **done, verbatim.** Both files matched upstream's parent byte for byte: `mobile/scripts/mock-server-native-chat-scenario.ts` gains `MOCK_CHAT_AGENT=omp` (an OMP tab with no `transcriptPath`, to exercise legacy-hook readability discovery, and four decoded transcript messages including a tool call and its result), and `mobile/README.md` documents the variable. Dev tooling only — the mock server is not part of the app or the gate; tsc and oxlint clean on the script |
| f55b7ba68 #20601, with 955051ded #20138's shared line | cancel pending prompts precisely | **done, whole for the phone.** A structured approval or question card gets an X that cancels the prompt itself: on a host advertising `agent-session.prompt-cancel.v1` the cancel names the item (`prompt: { itemId, expectedRevision }` on `agentSession.cancel`, a strict schema an older host would reject whole), and on any other host it degrades to the turn stop it always was. The capability comes off the same runtime probe as the other session surfaces (`use-mobile-session-feedback-capabilities.ts`, `use-mobile-session-tab-reconciliation.ts`, verbatim) and is threaded through the dictation hook, the controller and the session lane to `use-mobile-structured-agent-session.ts`. **New** `mobile-structured-agent-session-cancel.ts`, taken at e7206f62a (#20868) so it carries `hostReportedOperationUnknown` from the start, replaces this fork's own `mobile-structured-agent-cancel.ts`; `mobile-structured-cancel-id-retirement.test.ts` (this fork's #20868 witness) goes with it, and upstream's `mobile-structured-operation-id-retirement.test.ts` takes its place. `mobile-structured-agent-prompts.ts`, `mobile-structured-grouped-question.ts` and `mobile-native-chat-question.ts` verbatim (the projected card now carries its prompt identity); `MobileNativeChatOverlay.tsx`, the controller and its test, the session lane, the controller contract, `agent-session-wire.ts` (`AGENT_SESSION_ID_MAX_LENGTH`) merged; `protocol-version.ts` hand-applied. The cards are this fork's themed ones, so the X was hand-written on `MobileNativeChatPermission.tsx` and `MobileNativeChatQuestion.tsx` (muted glyph from the theme, disabled while a response is in flight), the prompt card and the view props threaded by hand, and upstream's two card tests came over adapted to run in both themes. Shared: `structured-agent-session-dispatch-rejection.ts` re-vendored at f55b7ba68, which also brings **#20138** (955051ded) — `DISPATCH_REJECTED_CODEX_QUEUE_FULL` is internal copy the notice must not print, a one-line shared change the backlog did not list because the file did not exist here when the backlog was cut — and #20601's `DISPATCH_REJECTED_CANCELLED`; `structured-agent-session-outbox.ts`, its disposition test and `rpc-contract/structured-agent-session-params.ts` (the `prompt` field on `CancelParams`) verbatim. **Failing-first:** reverting the 23 modified source files and restoring the old dispatcher fails 6 tests — upstream's four in `use-mobile-structured-agent-session-prompt-cancel.test.tsx` (the identity reaches the wire on a capable host, an old host gets turn-only, a question card cancels by identity, the rendered identity wins over a journal that moved) and the two card tests. Green after. Route-parity re-pinned: hooks 281 → 282, capabilities 5 → 6, and the four hashes upstream's own re-pin moved. Ceilings: the session hook 300 → 307, the dictation hook 300 → 302, the controller 496 → 502 and its test 800 → 805, the view 413 → 414. Gate: tsc clean; vitest 912 files / 7931 passed / 17 skipped; oxlint 0 errors |
| 533b0bd02 #21086 | count a turn from the send that opened it | **done, whole for the phone.** The live indicator switched on at the send but anchored its clock at the provider turn-open, so it climbed to "Working for 25s", reset to "Working for 0s" when the turn opened, and settled "Worked for 26s" — three readings of one turn from two instants (measured upstream on a real Claude session). The host now publishes `requestedAt` on the turn lifecycle row (both providers; omitted when no send can be named); readers take one origin, `requestedAt ?? startedAt`, for the live counter and the settled duration, exact host endpoints outrank the provider's own measure, and the live reducer keeps the earlier anchor so publication order can never run the clock backwards. New `structured-agent-turn-clock-anchor.ts` latches one host-to-local conversion per turn at first sight, so a later, jittered host sample cannot move the anchor later. Shared: `native-chat-turn-status.ts` + test, `structured-agent-session-turn-timing.ts`, the new clock-anchor module + test, all re-vendored whole (each matched upstream's parent); `agent-session-journal-types.ts`, `agent-session-journal-schemas.ts` and the turn-timing test by clean 3-way merge. Mobile: `use-mobile-structured-agent-turn-timing.ts` re-vendored whole (matched the parent) — it now drives the shared latch instead of its own anchor state; its test is unchanged upstream and still passes. **Failing-first:** `mobile-structured-turn-origin.test.ts` mirrors upstream's shared cases where the gate runs them; reverting the five modified files fails three of its four (the settled interval counted from the send, the live anchor on the request, the reducer's no-later rule); the latch case passes either way because the module is new. Green after |
| aad41b1a4 #21087 | render approvals from the harness presentation, not serialized tool input | **done, adapted.** Upstream's symptom had two halves. The unbounded detail — `JSON.stringify(input)` in an element with no height bound, pushing the buttons off the screen — this fork had already fixed on its own card (a reading area capped at 16 % of the window, a diff scroller, and choices that scroll), so that half was already solved here. The other half is new: the Agent SDK's own presentation (`displayName`, `description`, `decisionReason`, `blockedPath`, `matchedAskRule`) threaded through the journal item, so the card says why a request was raised, not only what it was. Shared: `agent-session-journal-types.ts` and `agent-session-journal-schemas.ts` (+ test) by clean 3-way merge onto their local hunks. Mobile: `mobile-structured-agent-prompts.ts` re-vendored whole (matched the parent), `mobile-native-chat-permission.ts` merged (the five optional fields), and `MobileNativeChatPermission.tsx` hand-written — upstream's card is the static-palette one, this fork's is themed and already scrolls, so the harness context is one more bounded scroller above the diff/prose, labelled `Reason:`, `Blocked path:` and `Ask rule:` in the theme's tones. Upstream's `keeps oversized provider context in a bounded scroller above the actions` came over adapted to this card's own bound and runs in both themes; `mobile-structured-agent-prompts-presentation.test.ts` pins the projection carrying every field and carrying nothing extra for an older host. **Failing-first:** reverting the five modified files fails both (the projection dropped the fields; the card had no context block). Green after |
| 68ea3b92e #21151 | stop a collapsed run claiming success when a tool call failed | **done, the half the phone can show.** Upstream's collapsed run drew its completion mark whenever nothing was `running`, so a run whose call `failed` took the mark. This fork's collapsed header (`MobileNativeChatToolRun.tsx`) never drew a completion mark, so the false claim did not exist here; what it shared was the silence — a run with a failed call read exactly like a clean one until expanded. The new `native-chat-tool-run-outcome.ts` (+ test) is vendored whole, `native-chat-tool-activity.ts` re-vendored for its two copy entries (it matched the parent), and the phone's header now says `N failed` in its muted member-arg type with a spoken `Failed tool calls: N`, counted over every call by the same composite test (a call's own `failed` verdict or an error result, a structured lane's two signals for one failure counted once). Text only, no danger tint, as upstream. Three tests on the header, red before (no mark at all), green after: the count in light and in dark, a legacy lane with no lifecycle state, and silence on a clean run |
| e42f7c00b #21090 | render a proposed plan as a plan, not a generic approval | **done, adapted.** A finished plan arrives as an ExitPlanMode approval whose detail is the tool input — thousands of characters of escaped JSON. The host now classifies it and carries a typed `subject: { kind: 'plan', text, filePath? }` on the approval item (the approval kind is kept on purpose: this phone filters pending approvals on it, and a new kind would have made the prompt vanish here silently). Shared: `agent-session-journal-types.ts`, `agent-session-journal-schemas.ts` (+ test) by clean 3-way merge. Mobile: `mobile-structured-agent-prompts.ts` re-vendored whole, `mobile-native-chat-permission.ts` merged, and `MobileNativeChatPermission.tsx` hand-written on this fork's themed card: a plan renders through `MobileMarkdown` in a scroller that takes the diff's share of the window (it reads line by line), with `Plan file: <path>` in mono below, and it replaces the raw detail — the proposed-edit preview, the description and the command are all skipped for a plan — instead of rendering beside it. Upstream's card test adapted to this card's own region and bound, in both themes; the projection test pins the subject carried. **Failing-first:** reverting the five modified files fails all three (the projection dropped the subject; the card drew the raw JSON). Green after |
| 434365d2d #21096 (one line of it) | offer to reconnect native chats that were working when Orca restarted | **done, the one phone-visible line; the feature is skipped.** The restart resume — teardown markers, `agent-session-restart-continuation.ts`, `agent-session-resume-marker.ts`, the `agentSession.restart*` RPCs and the `nativeChatResumeWorkOnRestart` setting — is the desktop's launch flow, `src/main/` and `src/renderer/`; the phone re-acquires a chat at its cursor by opening it and has no launch to resume from. What the commit also changed is `structured-agent-session-message-projection.ts`: a `rejected` submission's journal row is ledger evidence, not conversation history, so it leaves the visible transcript instead of standing as a message the agent never received beside the banner that says it was not sent. That file is re-vendored whole at 434365d2d, which also brings #19364's optional `projectItems` argument (skipped earlier as unused; the phone still calls the default). `mobile-structured-session-refused-send.test.ts` pins it — red on the pre-port projection (the refused row stayed), green after, with the no-refusal and no-items cases. **Skipped from the same commit:** `newestStructuredAgentSessionTurn` on the live-turn module and `latestStructuredAgentSessionUserItem` on the projection (`grep -rn` over `mobile/src mobile/app` → 0), the two new shared modules, the settings fields, and the params catalog's four `agentSession.restart*` rows — the generated catalog is re-vendored by whichever group carries the commit that adds a method the phone calls, and these it never calls |
| f1a901e97 #20672 | suggest verified OMP terminal commands | **done, adapted.** The `/` menu had no catalog for OMP, so it fell back to the two-entry common set. Upstream's `OMP_COMMANDS` (22 entries, its built-in registry) is hand-applied onto `native-chat-slash-commands.ts`, which carries this fork's own catalogs and cannot be re-vendored whole. One adaptation: the ten entries upstream describes as "in Terminal" (`model`, `switch`, `resume`, `fork`, `branch`, `tree`, `git`, `settings`, `extensions`, `hotkeys`) carry this fork's `opensOverlay` flag, so the phone shows the terminal for a selector the chat cannot drive — exactly what its Codex catalog already does through `slashCommandOpensOverlay`; upstream's desktop has no such lane and needs no flag. Upstream's mobile classification test hunk merged clean; the shared `native-chat-slash-commands.omp.test.ts` vendored (never collected here). Two tests on the phone, red on the pre-port catalog (OMP fell back to the common set, so `/switch` was an unknown token and every OMP command read as an overlay), green after: the classification case and one pinning which commands open the terminal and which keep the chat |



The sweep on 2026-09-13 covered every upstream commit to `origin/main`
(fe4237cd4) whose subject carries `(mobile)`, plus the three mobile Markdown
and terminal fixes that do not. `UPSTREAM.txt` is unchanged by that sweep: all
twelve ports are mobile-only, and its pin list records `src/shared/` files.

The sweep on 2026-09-19 took `docs/upstream-port-backlog-2026-09-19.md`'s
Group A — the 29 native-chat commits on the structured lane from f2af92b2f
(#19705) to 7f5141ae2 (#20977), in the order the table lists them — plus
955051ded (#20138) and e7206f62a (#20868), two commits the backlog did not list
because the files they change did not exist here when it was cut. Seventeen
landed (rows above, each with its failing-first evidence), twelve are in the
Skipped table with the grep that proved no phone reader. Every landed port ran
`tsc`, its own tests red-then-green, and the full suite; the branch was gated
whole after the large ones (#19695, #20133, #20601) and at the end. `UPSTREAM.txt`
gained or moved 41 `src/shared/` pins, and `LOCAL-FILES.md` records every
hand-applied hunk. Not verified against a live host: no desktop here is past
c287a5d9b, so the Fast row, the prompt-cancel X's precise path and the
`stoppable` foreground rows have not been watched arriving; each falls back to
the pre-port view on a host that does not send them.

**This table is behind `main` for work outside the chat-rendering and
shared-path batches.** It was last rewritten at 588b199, and several ports have
landed since without adding a row — the pins in `UPSTREAM.txt` for
`native-chat-tool-activity.ts` (c1e15c400), `native-chat-turn-status.ts`
(b0c67eaf8), `structured-agent-session-*` (1ae7aa8bb, f1d854502) and the
per-session `/` catalog (bf4e27050) all name commits no row here mentions.
Whoever owns those batches should fill them in; they are not guessed at here.

## Already solved here

| Commit | What | Evidence |
|---|---|---|
| c300913f9 #17731 | double-scaled commit timestamps | Code UI accepts both units, for older hosts too |
| 1dae024ab #19380 | xmldom security patch | the fixed versions are already pinned through workspace overrides |
| 5a1acfec1 #18712 | clickable document paths and links in chat | `mobile/src/components/markdown-file-path-detection.ts` (241 lines, 312 lines of test) already turns bare paths in prose and path-shaped code spans into tappable text, and `markdown-href-routing.ts` already routes a markdown link through `routeNativeChatHref` — file targets to the viewer, web and mailto to the system handler. Upstream's shared delta is only `createNativeChatFileHref` and its decode loop, an encoding internal to desktop's remark pipeline. Mobile has its own parser and no remark, so porting it would add an unreachable export to a vendored file |

## Skipped, with the reason

| Commit(s) | Why |
|---|---|
| 3160b54c6 #18554, reverted by d53cbed43 #19203 | upstream pulled its own push-notification feature |
| 74cc9b503 #19935, d33354cfd #19951, 341b13cf6 #20068, reverted in part by e187c8267 #20040 | the re-landed desktop/mobile push integration. Code UI has no push feature and no paired-desktop push transport; the desktop half is `src/main/` this fork does not vendor. Upstream itself pulled the rollout once already |
| afce0c85c #19115 | `mobile/src/sync-runtime-graph/` does not exist here. The agent-status projection join it short-circuits is part of the runtime graph this fork does not carry |
| 4e1681338 #19675 | a 5,200-line pure refactor that moves settings, diagnostics and editor-document screens out of their routes. These are among the most diverged files in this fork — Code UI wrote its own settings, voice and connection-log screens — so the extraction has no common ground to land on and no behaviour to gain |
| 1798786d4 #19869 | transcript row windowing. Every visible file is `src/renderer/` (NativeChatMessageList and 14 new modules beside it); mobile draws its own list. Its `src/shared/` delta is two optional fields on journal types owned by another agent this session. Windowing the phone's transcript is a Code UI decision, not a port |
| 23df74d85, e628090ad, c37413271, ceafdcad2, 83b1558ec, 0ba7f8dc8, 643571def | the relay speed pass, reverted upstream by d74f8cb78 and d936d8da8 pending a smaller re-land. Code UI already has the dial-stage timing and the network-type gate |
| b51bbf3fc #12772, 4b4acf26a #19769 | iOS only; this app is Android |
| 821c8b7df, da48ad2b4, 2265fce59, 36d209f51 | version bumps and repo chores |
| 06a607a1d, 8f78c2824, 12f53da54, 852495d35, 1c1cb7115 | Orca's orchestration-worker feature, which this app does not implement |
| 51eed5a1b, 14e0d40e0, da836faee | host-side agent and terminal bookkeeping with no mobile surface |
| 2513e2139 #18776, c7bcfa750 #19137 | the host-published status feed for the DESKTOP sidebar. Its wire type (`AgentSessionStatusSummary`) and its capability are already vendored, because `agent-session-wire.ts` is pinned at f1d854502 and `protocol-version.ts` at d07c47593 — both later than these commits. What is left is `projectStructuredAgentSessionStatusSummary`, which nothing on the phone reads: mobile learns a structured tab's status from `sessionTabs` and from its own subscribe |
| fa5ef9988 #19122 | host-side restart settlement (`adjudicateAgentSessionRestart`). The symptom — a turn stranded working after a desktop restart — is mobile-visible, but the phone reads the journal and the host writes the settlement. `agent-session-lease-adjudication.ts` has no mobile importer |
| 39cbc68f1 #19040 | desktop launch routing. Its shared half (`resolveStructuredLaunchSeedOptions`, and the `agent-session-record.ts` line) is already vendored at 546fd9b21 / f1d854502; the phone builds its own create params and has no persisted launch seed |
| 149732df6 #18739 | a native `encodeInto` fast path in `isUtf8ByteLengthWithinLimit` for the desktop's session-write scrollback scan. The phone imports `utf8-byte-limits.ts` for `readUtf8CodePointAt` and `clampUtf8TextPrefix` only; nothing under `mobile/` calls the changed function (grep 2026-09-19, 0 non-test uses). Found while checking the v0.0.50 release notes against this table — the script lists the file, not the symbol |
| cb7f7dd11 #18756 | host-side fallback that retitles a structured tab for a client which does NOT advertise `agent-session.structured.v1`. Code UI advertises it, so it never sees the row. Its `protocol-version.ts` half is already vendored at d07c47593 |
| 6494f2a4f #18933 | resume from the desktop's Agent Session History panel, which this app does not have. The `reveal` capability and `isAgentSessionWireRefusalCode` are already vendored at d07c47593 / f1d854502 |
| ce4a3a418 #19235 | the rewind backend, by its own commit title. **Consumed, not vendored** (2026-09-18): the phone now calls `agentSession.rewind` from `mobile/src/session/mobile-structured-agent-rewind.ts` ("Rewind to here" on a sent prompt, structured lane only, conversation only — the host wraps no file restore). The reply type, the `rewind` field on `agentSession.options` and the `agent_session_rewind:<reason>` refusal shape are copied from upstream's `agent-session-rewind.ts` / `structured-rewind-refusal.ts` into that file and pinned by its test, because `agent-session-wire.ts` cannot be re-vendored past f1d854502 (see LOCAL-FILES.md). Verified against the desktop's own bundle: Orca 1.4.205 carries the method, the `agent-session.rewind.v1` capability and those strings. **Same day, later:** carrying it is not the same as letting a phone call it — 1.4.205's mobile-scope allowlist refuses `agentSession.rewind`, so the control is now probe-gated (see "Catalogued ≠ reachable" below) and does not appear on that host |
| 20eea184c #19130 | the link-action popover. Entirely desktop renderer: the popover component, `http-link-destinations.ts` and the chat link owner all live under `src/renderer/`, and the one `src/shared/` line is a doc-comment on `terminalLinkActionPopoverEnabled` — a setting `mobile/` never imports. The phone has no link-routing preference and no popover; giving it one is a Code UI product decision, not a port |
| 0252fe5c3 #18773 | Codex subagent activity. Upstream designed this so a client without a roster renderer needs nothing: the host freezes a plain-text twin (`Ran 3 subagents (1 failed)`) into the journal beside the block, and `native-chat-subagent-summary.ts` says in its own header that mobile shows exactly that sentence. So the phone already reads a new host correctly. The producer is ~1,400 lines of `src/main/codex/` this fork does not vendor, and `worker-transcript-text.ts` belongs to the orchestration-worker feature already skipped above. **Residual:** without #18773's `native-chat-tool-fold.ts` hunk, a roster row landing mid-turn ends the tool run it sits inside, so one run draws as two. Cosmetic, and the fix is that hunk plus the `subagent-group` block type |
| d15a6df22 #19230 (`update_plan` icon, in effect) | the one-line `native-chat-tool-icon.ts` map entry is vendored so the pin moves, but it draws nothing here. Mobile imports only `isShellActivityToolCall` from that file and picks between a terminal and a wrench; it never calls `nativeChatToolCategory` or `nativeChatToolRunIconName`, and the new entry does not change `isShellActivityToolCall`. The checklist's own `list-checks` glyph comes from the mobile component, not from the category map |
| a567e33bf #17795 | GitHub Projects roadmap timeline | desktop renderer plus `project-roadmap-timeline.ts`, which nothing under `mobile/` imports. The `fieldName` wire field on a date cell is unused here |
| 72befaf36 #18806 | Claude subagent activity on the shared carrier | host-side Claude translation, same residual as #18773. The shared delta is a test fixture for `subagent-group`. Mobile still shows the host's frozen sentence |
| fab78c766 #19977 | one live-turn indicator, and Thinking means reasoning | **done, partial.** Shared: `native-chat-turn-status.ts` re-vendored whole (adds `describeNativeChatActiveTurnLabel`/`formatNativeChatActiveTurnLabel` and the host-settled turn map, drops `nativeChatTurnHasResponse`); `structured-agent-session-live-turn.ts` ported partially for `isStructuredAgentSessionThinking`. Mobile: the live turn's `thinking` now comes from the journal's reasoning tail instead of "this turn has produced nothing yet", and the live label goes through the shared resolver. **Not ported:** upstream also moves the live row from the user row to the list footer — this fork's inverted list already owns that placement through its own header/footer, and the restructure is unrelated to the reasoning fix. The `src/main` halves (Claude `thinking` journalled as a `reasoning` message, Codex streamed-reasoning markers) and `src/renderer` are desktop, not vendored — see the note below on what the installed desktop can send |
| ef6ad2243 #19364 | renderer work plus one optional `projectItems` argument in `structured-agent-session-message-projection.ts`. The phone already calls that function with the default projector. (2026-09-19: the argument is now vendored anyway — the file was re-vendored whole at 434365d2d for #21096's refused-send line — and still unused here) |
| 85d7cf3cc #20280 | preserve delivery ambiguity across a transport cutover | hand-applied; `rpc-client.ts` and `stable-logical-rpc-client.ts` had diverged, the three test files matched upstream and took the patch verbatim. `migrateTo` no longer blanket-rejects its own pending set (the set and its `PendingRequest` type are gone) — `previous.close()` is now the only settlement path, and `sendRequest`'s error arm wraps a stale-generation rejection in `LogicalClientCutoverError(error)` so the physical error survives as `cause` and its delivery-unknown mark is copied onto the cutover error. `RpcClient.close()` gained the settlement-contract doc comment. Our local hunks kept: the success arm still rejects a post-cutover correlated response (upstream resolves it — an older divergence, out of scope here), and the `closed`/`requestGeneration` branches, the liveness-probe surface, and the existing close()/suspendActiveSession() "let the physical close settle" comments are untouched. Nothing skipped |
| 2fc84cb49 #20493 | one tail-follow owner so streaming stops jumping | **done, adapted.** Upstream's symptom was two tail-followers racing: a delayed *animated* `scrollToEnd` easing toward a stale endpoint while a non-animated content-size pin yanked past it. **This fork never had that race** — its inverted FlashList has no timer-driven scroll — but it had the same disease in another form: **three owners of the list's offset.** The view pinned on content growth, `use-mobile-chat-dock.ts` pinned on the dock's height, and `use-mobile-chat-scroll-handlers.ts` jumped. The hook is now vendored as `use-mobile-native-chat-tail-follow.ts`, **adapted to offset-0 tail semantics**: upstream owns a non-inverted list whose tail is a measured content height, so it pins with `scrollToEnd` / `scrollToOffset({ offset: measuredHeight })`; here the tail is the constant offset 0, so `pinToTailAfterContentResize` takes the measured height the list hands it and ignores it — pinning at that height would land the reader at the far end of history. It absorbed `use-mobile-chat-scroll-handlers.ts` (deleted, comments carried over) and composes `use-mobile-chat-following.ts`, which keeps the reader's hold, the long-press ownership and the text-selection flag upstream has no counterpart for; the follow gate moved inside it; `use-mobile-chat-dock.ts` re-pins by calling `pinToTail` instead of reaching for the list. **Newly ported in this pass:** single ownership — nothing outside the hook may move the list, pinned by *leaves the chat list with exactly one caller that can move it*; the intent/geometry split (`following` vs `atTail`, one writer each, metrics deciding only where a RELEASED gesture leaves us — `atTail` is a ref here, not state, because nothing renders off it); `detachFromTail` before a history page, which is inert under inverted geometry (older rows append past the far end, so the tail does not move) and is kept for the contract; and `onLayout={pinToTail}`, which an inverted list only needs as a re-assertion. The momentum fence and the one-frame settle landed earlier and are unchanged. Upstream's 80px slop measured up from the bottom of the content stays this fork's 40px measured from offset 0 — the value tuned on the device, against an offset that does not move. **Still NOT ported, each pinned by a test here:** `showJumpToTail = !following && !atTail` — this fork raises the control the instant a drag starts (*stops following on a drag, so nothing yanks the reader while they scroll* in `use-mobile-chat-following.test.ts`; *does not resume following during a drag near the live edge* in `MobileNativeChatView.test.ts`), and that same flag drives `maintainVisibleContentPosition` (*uses history anchoring only while the reader is away from the live edge*), so upstream's rule would also disable scroll anchoring for a reader who dragged but stayed near the edge. And upstream's `jumpToTail()` on send: this fork leaves a reader in history exactly where they are (*preserves the reading position when a new message is sent from history*) and lets the content growth that follows do the pinning, which is upstream's own "content growth owns tail-follow" principle |
| c21c08322 #20535 | report auth callback failures instead of cancellation. The whole fix is one branch in `src/main/orca-profiles/profile-cloud-pkce.ts`, the Electron loopback server for the Orca Cloud PKCE sign-in: a callback carrying `error != access_denied` now rejects as `orca_cloud_auth_callback_failed` rather than `orca_cloud_auth_denied`. This fork vendors no `src/main/` at all — only `src/shared/` — and does not sign in to Orca Cloud; the phone pairs with a desktop. Its `mobile/` portion is not this app: `mobile/rpc-foundation/` is upstream's RPC recorder harness, absent here, and its 72 touched files are `recorderSha256` fingerprint churn plus two recorder tests. Nothing to port |
| 754134fd6 #21155 | deliver a launch prompt from the host. The host now commits a `submit` prompt to the structured session `agent.launch` just created, through its own `agentSession.send` path, and reports `journaled` with the row id instead of `not-delivered` always. That is `src/main/agent-launch/` and `src/main/runtime/rpc/methods/agent-launch-structured-prompt.ts`, host code. Its one shared hunk splits `structuredAgentSessionSendRequest` into a typed `structuredAgentSessionSendMutation` plus a wire-shaped wrapper; the phone imports only `StructuredAgentSessionAttachment` and `structuredAgentSessionSendBody` from that file (`grep -rn "structuredAgentSessionSendRequest\|structuredAgentSessionSendMutation" mobile/src mobile/app` → 0, 2026-09-19), and neither changed. The mobile create sheet sends no launch prompt today (`agentLaunchCreateParams` builds `agent` + `target` only), so nothing here is observable from the phone until it does |
| 2531dc9d5 #20053 | bound the connect phase against an unreachable host, at the transport. A host whose TCP SYN is black-holed left every `orca <cmd> --environment <unreachable>` silent for 60 s; the fix passes `handshakeTimeout` at the three Node-side remote-runtime WebSocket sites and the relay control socket, all `src/shared/remote-runtime-*` transports the phone does not use (it has its own `rpc-client.ts` with its own connect bound). The phone imports two of the nine files: `remote-runtime-client-capabilities` (unchanged) and `remote-runtime-tailscale-hint`, from which it takes only `isTailscaleEndpoint` — unchanged; the changed export there is `withRemoteRuntimeTailscaleHint` (`grep -rn withRemoteRuntimeTailscaleHint mobile/src mobile/app` → 0, 2026-09-19) |
| f7b2736d6 #20153 | block worktree removal when the archive hook fails. Host-side gate (`archive-hook-removal-gate.ts`, the `worktree.remove` handler) plus an `allowFailedArchiveHook` waiver on the params and an `archiveHookOverride` receipt on `RemoveWorktreeResult`, advertised as `worktree.archive-failure-blocking.v1`. The phone imports `SparsePreset`, `CreateSparseCheckoutRequest` and `SetupDecision` from `worktree/create-types.ts`, none of which changed, and its `worktree.remove-or-skip` operation reads the reply unchecked and offers no waiver (`grep -rn "allowFailedArchiveHook\|archiveHookOverride\|WORKTREE_ARCHIVE_FAILURE_BLOCKING" mobile/src mobile/app` → 0, 2026-09-19). A host with a failing hook now refuses the phone's removal with its own message, which the existing error path already shows |
| dd85e5fc8 #20653 | keep OMP terminals when folder workspaces become Git repos. Host bookkeeping: one optional `folderUpgradeGitRootPath` on `Repo` that the folder-upgrade path records so the original checkout locator stays stable. Nothing under `mobile/` reads or writes it (`grep -rn folderUpgradeGitRootPath mobile/src mobile/app` → 0, 2026-09-19); the phone's six `repo-types` imports take other fields |
| c45b2c94c #21291 | make worktree scan failures actionable. Adds `worktree-scan-failure.ts` and a structured `failureKind` on `DetectedWorktreeListResult`, read by the desktop's worktree scan UI. The phone never lists detected worktrees (`grep -rn "DetectedWorktreeListResult\|unavailableReason\|failureKind" mobile/src mobile/app` → 0, 2026-09-19) |
| 2ed89b878 #20588 | name an unfiltered empty project view instead of blaming a filter. Twelve renderer files (`ProjectViewStates.tsx` and the six locale files) and one doc comment on `GitHubProjectView.filter` in `project-types.ts`. The phone's tasks board does not carry the "No items match this view's filter" copy (`grep -rn "match this view" mobile/src` → 0), and it imports only `GitHubProjectTable` and `GitHubProjectSortDirection` from that file |
| c34b94413 #13664 | bind projects to a specific gh account. Desktop Settings UI plus `github.listBindableAccounts` / `github.validateAccountBinding`, a `ghAccount` binding on `Repo` and on `repo.update`'s schema. The phone's one `repo.update` sends `issueSourcePreference` only, and nothing under `mobile/` names the binding or the two methods (`grep -rn "ghAccount\|accountBinding\|listBindableAccounts" mobile/src mobile/app` → 0, 2026-09-19) |
| aee98ccaa #20767 | make the browser identity one process-wide choice. Removes `BrowserSessionUserAgentMode` / `ProfileCreate` from the browser contracts and adds `browser.identity.v1` with its own params file. The phone imports one browser module, `browser-screencast-protocol.ts`, which the commit does not touch (`grep -rn "userAgentMode\|BrowserSessionUserAgentMode\|browser.identity" mobile/src mobile/app` → 0, 2026-09-19) |
| 13ba649c2 #20825 | let a runtime-created Windows terminal BE the requested shell. Adds a `shell` override to `terminal.create`'s params, `terminal.create-shell-selection.v1`, and the canonical Windows shell-name map in `windows-terminal-shell.ts`. The phone's one `terminal.create` (the transcript tail) sends no `shell`, and its one import from that file is `resolveWindowsShellStartupFamily`, unchanged (`grep -rn "shell:" mobile/src/session/transcript-tail` → 0, 2026-09-19) |
| 241fb9ed9 #20463 | batch file-link checks on their owning host. `files.pathsExist` with `PATH_EXISTENCE_BATCH_MAX`, for the desktop terminal's link underlining. The phone's terminal routes taps through `files.resolveTerminalPath` one at a time and never batches existence checks (`grep -rn "pathsExist\|path-existence" mobile/src mobile/app` → 0, 2026-09-19) |
| 16d1ab81d #12314 | bound WSL installed skill discovery. Host-side: `names` / `sourceKinds` filters on `SkillDiscoveryTarget` and a CR fix in the metadata parser. The phone sends `skills.discover` with `{ worktreeId, refresh }` only and imports `DiscoveredSkill`, `SkillProvider` and `SkillDiscoveryResult`, none changed (`grep -rn SkillDiscoveryTarget mobile/src` → 0, 2026-09-19) |
| 49274394f #21299 | put the branch-compare leg on the lifecycle owner, with a currency probe (step 5). **Blocked on Group D:** it moves `use-mobile-source-control-loaders.ts`'s compare onto `GenerationScopedRequestOwner` (`transport/generation-scoped-request-owner.ts`, `lifecycle-owner.test.ts`, the lease compile fence), which arrives with #20914 and does not exist here yet. Port it after that one lands; the fork's loader file drifts by 16 lines (pull-to-refresh), so expect a 3-way merge, not a verbatim apply |
| 1249be3c8 #20286 | perf: accept UTF-8 text that fits the worst-case byte bound. `isUtf8ByteLengthWithinLimit` in `utf8-byte-limits.ts` is the changed export; mobile imports only `readUtf8CodePointAt` and `clampUtf8TextPrefix` from that file, same finding as #18739's `149732df6` row above (`grep -rn 'isUtf8ByteLengthWithinLimit' mobile/` returns nothing, 2026-09-19) |
| 627b7432c #20312 | perf: find path basename without splitting all segments. `getRuntimePathBasename` in `cross-platform-path.ts` is the changed export; mobile imports only `isPathInsideOrEqual` and `isRuntimePathAbsolute` from that file (`grep -rn 'getRuntimePathBasename' mobile/` returns nothing, 2026-09-19) |
| 3d648003e #20328 | perf: count chat tool calls without a filtered array. `countToolCalls` in `native-chat-tool-summary.ts` is the changed export; mobile imports `createToolInputDisplay`, `truncateToolDetail`, `isShellActivityToolCall` and `MAX_TOOL_DETAIL_LENGTH` from that file but never `countToolCalls` (`grep -rn 'countToolCalls' mobile/` returns nothing, 2026-09-19). Left the file at its c1e15c400 pin; #20332's later hunks to the same file merge around this gap cleanly, see that row |
| 2569a71ce | fix(deps): update vulnerable dependencies without new overrides. Upstream's diff touches no `mobile/package.json` at all — only `mobile/pnpm-lock.yaml` (transitive re-resolution within existing declared ranges) plus `cloud/**` and the root lockfile, none of which this fork vendors. The named packages (react-native 0.83.10, expo 55.0.30, eslint 9.39.4, the jest/babel-preset-expo toolchain) are upstream's own dependency baseline, substantially different from this fork's (react-native 0.86.3 here). Checked whether the equivalent action applies to this fork's own tree: `pnpm audit` in `mobile/` reports "No known vulnerabilities found", and `pnpm install --lockfile-only` produces **zero diff** to `mobile/pnpm-lock.yaml` — nothing to re-resolve within the current declared ranges. Nothing to port |
| 1cd296450 #21311 | perf(mobile): build the two projected git enums once, not per parse. The two files this commit touches, `mobile/src/session/diff-review-reply-schema.ts` and `mobile/src/source-control/git-status-reply-schema.ts`, do not exist in this fork. This fork's git status and branch-compare reads (`mobile-git-status.ts`, `mobile-branch-compare.ts`) have no `conflictOperation`/`compareStatus` zod-schema layer to optimize — the `zod-salvage` reply-schema pattern is used elsewhere here (`workspace-session-schema.ts`, `rpc-operation-result-reader.ts`) but was never ported for git status. Confirmed: `grep -rln 'readProjectedCompareStatus\|readProjectedConflictOperation\|GIT_BRANCH_COMPARE_STATUS\|GIT_CONFLICT_OPERATION' mobile/` returns nothing, 2026-09-19 |
| 4408fe897 #19807 | native-chat subagents as desktop sidebar child rows. Eleven of its thirteen files are `src/renderer/` — the status bridge that maps agent-kind background tasks into the sidebar's `subagents`, and the per-connection `live`/`unverifiable` confirmation. The shared delta is `'unverifiable'` added to `AgentSubagentState`, accepted by `normalizeSubagentSnapshot`, and a renderer-local `subagentObservation` on `AgentStatusEntry` ("absent on hook rows", by its own comment). Nothing under `mobile/` names any of them: `grep -rn "AgentSubagentState\|subagentObservation\|normalizeSubagentSnapshot\|normalizeAgentStatusPayload" mobile/src mobile/app` → 0 (2026-09-19, tests included). The phone's one reader of `subagents[].state`, `liveSubagentRoster` in `mobile-background-tasks.ts`, keys on `!== 'idle'` and reads hook rows off `sessionTabs`, which the desktop never marks unverifiable. Not ported |
| 37603a6cf #19645 | derive `WellKnownAgentType` from `TuiAgent` instead of a hand-copied 22-member union that had drifted past `qwen-code`, `mistral-vibe` and `claude-agent-teams`. Type-only, and nothing under `mobile/` names the union: `grep -rn "WellKnownAgentType" mobile/src mobile/app` → 0 (2026-09-19, tests included). The phone reads `AgentType`, which stays an open string, and its agent ids come from `tui-agent.ts` directly. `agent-status-types.ts` here is also not at upstream's parent (it lacks #19807's `subagentObservation` and the fleet `attention` field), so a whole-file re-vendor would drag in unported changes for a type nothing reads. Not ported |
| 60d793956 #20724 | replace the raw question tool row with an awaiting-input row. Desktop renderer work (`NativeChatAwaitingInputRow.tsx`, the tool run and receipt) plus three shared additions: `native-chat-ask-row.ts` (the row's copy and subject, "read by mobile directly" per its header — but upstream's own `mobile/` never imports it, at ea02d9070 either), `parseAskFromToolInput` on `native-chat-ask.ts`, and a `message-square-more` glyph name on the tool-icon table. Nothing under `mobile/` names any of them: `grep -rn "parseAskFromToolInput\|native-chat-ask-row\|message-square-more\|nativeChatAskRunSubject\|hasNativeChatAskCall" mobile/src mobile/app` → 0, and the phone never reads `NATIVE_CHAT_TOOL_ICON_NAMES` (0). The phone answers a question through its own card above the composer (`MobileNativeChatAsk.tsx`) and draws the transcript's tool rows itself; whether it should also stand a question call in for an "Awaiting user input" row is a Code UI decision, not a port. Not ported |
| 1457d3966 #20502, f02d09c1b #20659 | release sessions after provider root exit; deliver queued messages while the chat pane is hidden. Both are the desktop's outbox — `source?: 'launch'` on an outbox entry, and `admitStructuredAgentSessionOutboxEntry` plus the refusal-index fix in `disposeStructuredAgentSessionSendResult`. The phone has no outbox: it imports `structured-agent-session-outbox.ts` for `structuredAgentSessionSendBody` and the attachment type only, and `structured-agent-session-send-disposition.ts` for `structuredAgentSessionRejectionNotice` only; `grep -rn "reconcileStructuredAgentSessionOutbox\|parseStructuredAgentSessionOutboxEntry\|StructuredAgentSessionOutboxEntry\|admitStructuredAgentSessionOutboxEntry\|disposeStructuredAgentSessionSendResult" mobile/src mobile/app` finds one hit, a comment in `mobile-structured-send-delivery.ts` naming the desktop function it mirrors. The rest of both commits is `src/main/` and `src/renderer/`. Not ported |
| 9ed561c1d #20921 | judge Stop against the turn the journal published. `src/main/claude/`; its shared line is `activeStructuredAgentSessionTurnIdBySequence` on `structured-agent-session-live-turn.ts`, a host-side reader over unordered items. `grep -rn "activeStructuredAgentSessionTurnIdBySequence" mobile/src mobile/app` → 0. Not ported |
| c702e77bc #20944, 7f5141ae2 #20977 | stop reading the terminal arguments field on the structured chat route; make the Agent Permissions toggle apply to Codex chat. Desktop launch routing: `tui-agent-launch-customization.ts` (never vendored here) gives way to `tui-agent-launch-command-override.ts`, and `tui-agent-launch-defaults.ts` gains `tuiAgentArgsBypassPermissions` / `resolvedTuiAgentArgsBypassPermissions`, which #20977 then makes tokenize the argv so quoted text and operands after `--` cannot authorize a structured session. The phone imports `resolveTuiAgentLaunchArgs`, `resolveTuiAgentLaunchEnv` and `isResumableTuiAgent` from that file, none of which either commit changes; `grep -rn "tuiAgentArgsBypassPermissions\|resolvedTuiAgentArgsBypassPermissions\|hasExplicitTuiLaunchCommand\|hasExplicitTuiLaunchCustomization" mobile/src mobile/app` → 0. The phone has no Agent Permissions toggle of its own; the structured create it sends carries no arguments field. Not ported |
| 7a1f55c52 #20519 | a failed Claude background task as a typed row instead of an opcode. The same shape as #18773 (skipped above): a new `background-task` block on the dual carrier, where the host freezes a plain-text twin beside the typed block for clients without the block type — `backgroundTaskFallbackText`'s own comment says "mobile renders only this". So a host past this commit already reads correctly here: the phone shows the provider's own sentence (or `Background command "x" failed`) where before it showed the wire opcode twice. The producer is ~5,000 lines of `src/main/claude/` this fork does not vendor; the phone's own background-task surface reads the host's roster (`mobile-structured-background-tasks.ts`), not transcript rows. Nothing under `mobile/` names the block: `grep -rn "isBackgroundTaskBlock\|native-chat-background-task-row\|type: 'background-task'" mobile/src` → 0 (a plain `background-task` grep hits only the app's own expo background-task modules), and `native-chat-types.ts` carries hand-applied hunks that keep it off a whole-file re-vendor. **Residual, as for #18773:** without the `native-chat-tool-fold.ts` hunk a task row landing mid-turn ends the tool run it sits inside, so one run draws as two. Cosmetic; the fix is that hunk plus the block type. Not ported |
| 990711756 #21278 | record an explicit provider outcome on every structured turn. A failed turn was journalled `completed`, identically to one that succeeded; the host now writes `outcome: success / failure / cancellation` on the turn record (absent means unknown, never success). Shared: the field on the lifecycle type and both zod shapes, and `readAgentJournalTurnOutcome` on the turn-record reader. Nothing on this phone, and nothing in upstream's own `mobile/` at ea02d9070, reads it: `grep -rn "readAgentJournalTurnOutcome\|AgentJournalTurnOutcome" mobile/src mobile/app` → 0. The phone's zod `turn` shape is a plain `z.object`, which strips the unknown key rather than refusing the row, so a host past this commit still parses. Drawing a failed turn as failed is a Code UI decision that would start from this reader; not ported |
| 4d3b0cca8 #20615 | publish parent transcript paths for native chat (OMP). Host-side: `extractAgentProviderSession` now reads OMP's `session_file` hook field into `providerSession.transcriptPath`, which the host publishes on the tab; the phone already reads that field off `agentStatus.providerSession` (`mobile-native-chat-eligibility.ts`) and needs no change to receive it. The other shared line is a comment on `nativeChatRequiresLocalTranscript`, whose body is unchanged (OMP stays gated on a local transcript). `agent-session-resume.ts` also differs from upstream's parent by an orchestration-worker field this fork does not carry. Nothing to port; the #20655 mock scenario's "current OMP hooks may report a path" is this commit |
| ea02d9070 #20620 | answer startup Kitty queries before renderer handoff (OMP). The host's PTY startup ingress (`pty-startup-ingress.ts`, the new `pty-startup-query.ts` and `pty-startup-color-query-answer.ts`) answers OMP's `ESC [ ? u` Kitty keyboard probe when the client's terminal declared support, negotiated by a new `agent-session.keyboard.v1` capability and a `terminalKittyKeyboardProtocol` flag on `terminal.create` / `agentSession.create`. This phone's terminal is xterm.js in a WebView, which does not implement the Kitty keyboard protocol, so it must not declare it; it sends no `terminalColorQueryReplies` either (`grep -rn "terminalColorQueryReplies\|terminalKitty" mobile/src` → 0 outside the generated engine). None of the ingress modules is imported here. Not ported |


## Pending

**Assess before porting** — f4c282116 #18652 moves the session journal onto
SQLite. That is host storage; confirm nothing on the phone depends on the old
shape before touching it. The #19822 verdict lives in
`scratchpad/19822-assessment.md`; its working-state half is already ported.

## Catalogued ≠ reachable: the mobile-scope RPC allowlist (2026-09-18)

**The RPC catalog says which methods the desktop registers. It says nothing
about which of them a phone may call.** Orca's WebSocket dispatch checks every
request from a `mobile`-scope device token — the only kind the phone ever
holds — against a hardcoded allowlist before dispatch (`if (u.scope ===
'mobile' && !iRa.has(s.method))` in the 1.4.205 bundle) and refuses the rest
with `{ code: 'forbidden', message: "Method '<m>' is not available to mobile
clients" }`. Nothing in `src/shared/rpc-contract/` records this, tsc cannot see
it, and there is no RPC that lists it. The only way to learn a host's answer is
to call.

That is how two of the day's ports and three editors shipped dead controls:
`files.write` ("Revert this hunk", and Save on MCP Servers / Permission Rules
/ Project Memory) and `agentSession.rewind` ("Rewind to here") are both
catalogued and neither is on the list, so every tap got the refusal. A third,
`skills.discover` (the `/` menu's installed-skills read), had been refused
silently since it landed and re-asked every 3 s.

- **The list, as read from the installed bundle:**
  `mobile/src/transport/fixtures/orca-mobile-rpc-allowlist-1.4.205.json` (289
  names, version in the file name; a newer Orca means a new fixture under its
  own name, never an edit to this one). `files.read`, `files.createFile`,
  `files.resolveTerminalPath`, `terminal.*`, `nativeChat.*`, `worktree.set` and
  the `agentSession.*` mutations the chat already uses are on it. The fixture
  also carries two `aiVault.search*` methods the vendored catalog predates.
- **The probe:** `mobile/src/transport/host-mobile-capabilities.ts` asks each
  gated method once per connection (keyed on `useLastConnectedAt`, like the
  reconnect refetch) with parameters the host must refuse without side effects,
  and reads the refusal: the gate's own text, or a host with no such method →
  hidden; any other refusal or a result → shown; a transport failure → logged
  (`[capabilities] probe failed`), the last settled answer kept if there is
  one, asked again next connection.
  The verdict is a property of the host, not the socket: a reconnect re-probes
  in the background while the last answer stands, so nothing blinks off for a
  round trip (a review of the first cut found it did, and a half-typed rule
  went with it). The four surfaces read `useHostMobileCapability(hostId, key)`
  and offer nothing tappable until it is true; the three editors read the
  tri-state verdict and draw neither Save nor a line until the host has
  answered, then become viewers with one line, "Read-only from the phone on
  this Orca version." The feature modules stay — they are correct for a host
  that allows the call. **The exception is per method, not per surface:**
  `useProjectConfigFile().save` and `revertDiffCardHunk` still send
  `files.write` ungated, the gate sits on the buttons, and a new caller of
  either helper passes the ratchet because the method is already excepted.
- **A fourth kind, `capability` (2026-09-19, with #19850):** a method the
  phone sends only after `status.get` advertised a runtime capability the
  fixture's host does not have, whose sender downgrades to the older method
  on the gate's refusal. `agent.launch` is the one entry: the 1.4.205 bundle
  registers neither the method nor `agent.launch.v1`, so that desktop never
  receives it. The ratchet checks the real gate and reader rather than a
  marker — the named file must read the capability through its
  `protocol-version` constant, and the reader must accept the gate's
  verbatim refusal and `method_not_found` while refusing `invalid_argument`.
- **The ratchet:** `mobile/src/transport/orca-mobile-rpc-allowlist.test.ts`
  walks every catalogued method literal in `app/` and `src/` and fails on any
  that is neither on the fixture nor an explicit, typed exception (probe-gated,
  fails-open with a sender that reads the refusal, or provably never sent —
  "provably" meaning every occurrence sits in a position that cannot send: an
  array element that is not itself a call argument, a type literal, a `case`
  label; a wrapper call fails the claim, because the send shapes the scan
  recognises are a floor, not the tree's whole set). A new operation on an
  unlisted method fails it until it is one of those.
- **Prior art the map did not read:** the phone already recognised this exact
  refusal in three "old desktop" fallbacks — `files/file-list-fallback.ts`,
  `source-control/mobile-git-status.ts`, `dictation/mobile-dictation-setup.ts`
  — each hand-matching `not available to mobile clients` for a method that was
  off the list on an older desktop. The shape was known; the rule was not
  written down. `transport/mobile-scope-refusal.ts` now names it; those three
  keep their own match because each also folds in `method_not_found` for a
  desktop that predates the method.
- **The rule for the next port:** before promising a feature on the strength
  of a catalog row, check the fixture. "In catalog: YES" is necessary, not
  sufficient; the feasibility map's column of that name was written as if it
  were sufficient, and it was corrected the same day.

## Known gaps left behind

- `skills.discover` is refused for every phone on Orca 1.4.205, so the `/`
  menu never lists the user's own skills there. The phone now asks once per
  client and stops (it used to re-ask every 3 s); the list is empty, not
  wrong. Nothing on the phone can change the host's answer.

- The background-task roster has **not been watched arriving from a live host**.
  The contract and the projection are tested; no real desktop has published one
  to this phone. The fallback is the safe direction: a host that publishes
  nothing leaves the tab on the transcript reader.
- The wire's per-task `totalTokens` is read by the equality check and then
  dropped, because the sheet has nowhere to show it.
- The Windows status line and Stop hook run for real under PowerShell 7 in
  tests, and have never run on Windows.
- Live Codex `projectNativeChatTaskListFrames` (a `plan` notification rewritten
  as an `update_plan` tool call so the checklist can draw it) is still desktop
  only. The phone already draws a real `update_plan` call.

## Claude `thinking` on the terminal-driven transcript lane (2026-09-13)

The reported bug — the model's summarized thoughts drawn as its reply — is
**not fixed by #19977 and cannot be fixed on the phone today.**

- Orca's transcript reader (`src/main/native-chat/transcript-record-blocks.ts`)
  turns a Claude `thinking` content block into a plain `{type:'text'}` block on
  an ordinary assistant message. `transcript-line-decoders-claude.ts` has a
  comment saying a thinking-only message becomes a `reasoning`-role message, but
  `claudeMessageRole` returns `assistant` unchanged, and the test named
  "marks thinking-only assistant content as a reasoning surface" only asserts
  the block, never the role. Verified at upstream `origin/main` fe4237cd4
  (2026-09-13) and in the installed Orca 1.4.200 bundle. So on this lane a
  thought and a reply are byte-identical to the phone: same role, same block
  type, same `source: 'transcript'`. Per this repo's rule, the phone shows the
  row as it arrives rather than guessing.
- #19977 fixes the **structured** lane only: `claude-structured-journal-translation.ts`
  stops appending a Claude thought as `kind: 'status'` and appends
  `kind: 'message', role: 'reasoning'` instead. The installed 1.4.200 still
  writes `kind: 'status'` (checked in the bundle), so even the structured lane
  cannot deliver reasoning as reasoning until the desktop is updated past
  fab78c766 (2026-09-11). No Orca tag contains that commit yet; `package.json`
  reads 1.4.197 both at fab78c766 and at `origin/main`.
