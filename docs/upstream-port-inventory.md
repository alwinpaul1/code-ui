# Upstream port inventory

Every Orca commit since the vendor base (`ef428d879e09a02daa3bb9ed977a7802d89da289`)
that can reach the phone, and what was done about it. "Reaches the phone" means it
changes a file under `mobile/`, or a file under `src/shared/` that `mobile/` imports.
143 commits touch shared or mobile code; 69 of them reach the phone. The rest are
desktop and host work whose code this repo does not vendor.

Regenerate the candidate list with the script in `docs/upstream-port-inventory.sh`.

Status values: **done** landed here; **already** Code UI solved it independently
before the port; **skip** with a reason; **in flight** an agent is on it now;
**pending** queued.

## Landed

| Commit | What | Note |
|---|---|---|
| a65332a8b #18560 | Claude structured chat onto the Agent SDK | one line taken: the tab contract's agent union |
| cc07249e7 #18697 | definitive-refusal allowlist | came in with the structured port |
| d07c47593 #18741 | structured native Claude chat | the core port |
| 546fd9b21 #19147 | remember structured model and effort picks | eleven shared files moved with it |
| 09ee4c1b1 #18926 | stop host streams after a cancelled relay subscription | |
| 992360f12 #18940 | cancel direct probes when their owner stops | Code UI had the abort half; the stop fence was missing |
| 61b09b7a0 #18959 | fail a dead direct probe fast | mobile half; the rest is relay-server code |
| e9d9d42cc #18914 | Markdown placeholder prefixes in one scan | |
| 4e8e14424 #18919 | stop splitting every path in file autocomplete | mobile half only |
| ddc5b75ac #18760 | label a Codex tool row by what it did | summary half; the icon half has nothing to attach to here |
| 53852c9ca #18126 | configurable terminal contrast floor | mobile mirror; found and fixed a theme-equality bug on the way |
| e89deb63c #18757, f8780a2c8 #18807, 5868fdc9e #19346, 2bf298d1d #19311 | background tasks over the wire | shared contract hand-applied; the structured lane now reads the host's roster and the transcript reader keeps terminal tabs. `#18757`'s journal-unchanged short-circuit came with it |
| b0c67eaf8 #18761 (remainder) | the chat lane wiring out of the controller | the extraction skipped when the rest of #18761 landed; taken verbatim for the room |
| 2f828e446 #19822 (working half) | a structured send reads as working before the provider echoes it, and Stop waits for a turn | the optimistic-bubble half stays unported; see `scratchpad/19822-assessment.md` |
| 172aa1ac3 #18765 | agent file edits as inline diff cards | upstream shipped it desktop-only; the five shared edit modules are vendored verbatim and the phone got its own card. `native-chat-types.ts` and `structured-agent-session-projection.ts` took hand-applied hunks |
| 6a9c5d8ce #19496, 44eb95fc6 #19468, 063c1caa6 #19469, 2c5cd845a #19465 | the shared-path allocation work | one commit; pure refactors, guarded rather than test-first. `native-chat-tool-fold.ts` took hand-applied hunks (whole-file would drag in unported #18773). Upstream's own tests are vendored but never run here, so the running guards live under `mobile/src/` |
| dda103d2c #19832 | one `/` picker for every agent, anywhere in the prompt | mobile already had the grouped menu and the draft-leading dispatch rule; the mid-prompt trigger was the real gap. `native-chat-agent-profiles.ts` re-vendored to drop `groupedSlash`, which nothing here read |
| d0506bf5d #19226 | execution details and tool row identity | the shared half plus a mobile annotations row. Unblocked the `mcpIdentity` field `native-chat-tool-summary.ts` had been dropping, so that file and its test went back to a clean c1e15c400. The `src/main/` Codex translation that fills `exitCode`/`durationMs` is not vendored — the host supplies those |
| 9f044031f #19228 | a compaction, a plan and a toned line drawn as what they are | landed in 8d80263 as the `presentation`/`tone` hints, which is what LOCAL-FILES.md records; the rest of #19228 is `src/main/` |
| f2d5711b2 #19845 | an older page that no longer touches the transcript is refused, not merged | landed in 8f7bbaf. The mobile half came over whole; the reducer carries local hunks, so its two changes were hand-applied. Covers the structured lane only — a terminal-driven tab is a different path, so it is not yet an answer to the missing-replies report |
| 0b60b0dcb #19841 | bound retained items on the structured session path | the live-batch head trim that #19845's older-page retry exists for. Hand-applied onto the local reducer (activity, background tasks, `requestedCursor`). A long session now keeps 1024 items, sets `hasOlder`, and paging raises the cap so a live batch cannot collapse it |
| d15a6df22 #19230 | task checklists with update diffs, and a composer progress panel | both agents' plans as checklists, in both themes. A later turn diffs against the earlier message's plan, not only the current tool run. The run header names the plan by progress (`1/3 · Writing the test`) instead of the input JSON. A collapsed **Tasks n/m** strip sits above the composer and expands to the checklist. Shared `native-chat-task-list.ts` vendored; mobile surface is `MobileNativeChatTaskList.tsx` plus the row builder, composer strip, and tool-run header rewrite. Live Codex `projectNativeChatTaskListFrames` (notification→update_plan) was not ported |
| e80fae0c4 #19229 | summarize turn file changes and preserve resolved prompt receipts | **the shared half only, and it changes nothing the phone draws.** Both visible halves — the turn diff rollup and the resolution receipt — are `src/renderer/` components this fork does not vendor, and `summarizeUnifiedPatch` exists only to feed the rollup. What the phone does get is the per-item render cache in `structured-agent-session-projection.ts`: mobile re-projects the WHOLE transcript on every stream frame, and unchanged rows now come back as the same objects. `native-chat-edit-normalize.ts` and `native-chat-unified-patch.ts` re-vendored at e80fae0c4, `native-chat-edit-patch-files.ts` vendored new (`native-chat-edit-normalize.ts` imports it, so mobile typechecks it). The extraction is behaviour-preserving and was proved so: upstream's own 50 pre-extraction `native-chat-edit-normalize.test.ts` cases, vendored here at 172aa1ac3, all pass against the extracted code |
| 8096cb280 #18743 | render Claude structured chat through the same UI as Codex | already in: `structured-agent-session-composer.ts` is pinned at f1d854502, a later commit that contains it, and the file was verified byte-identical |
| 1b4159a31 #18638 | skip host-label work on a single-host install | `host-context-labels.ts` re-vendored. Mobile imports it for the worktree list. |
| fb9d08f5f #19476 | index project table option and iteration order | `project-group-sort.ts` re-vendored. Mobile `groupRows` / `sortRows` call it. |
| d3501f7ad #18456 | `unavailableReason` on a non-authoritative worktree list | the two-line type only. The scan-failure host and sidebar are `src/main/` / `src/renderer/` |
| cd8e98fdf #20313 | stop the mobile Markdown parser stalling on an unsupported block | hand-applied; our parser carries the escaped-pipe table hunks but the defect was identical. The paragraph loop guarded with `startsWith('```')` and `/^(#{1,6})\s+/` while the dispatchers used stricter patterns, so a ```c++ fence or a bare `# ` stopped the paragraph at a line nothing would consume and the index never advanced. Upstream's `mobile-markdown-parser-progress.test.ts` came over verbatim and hangs 12 of its 13 cases on the old parser |
| 3bc631dad #20317 | validate review table delimiters by cell | taken verbatim; `markdown-blocks.ts` matched upstream here. The one `TABLE_DELIM` regex backtracked exponentially on a long whitespace run, so `splitTableRow` now does the split and each cell is tested on its own. Upstream's test pins the new check against the old regex over 2,197 generated rows |
| 1b5092492 #20322 | scan mobile Markdown links without repeated suffix searches | taken verbatim across all four files; both inline tokenizers matched upstream. `markdown-inline-matcher.ts` is new here. Link scanning leaves the regex and walks brackets with `indexOf`, so `[` x 60,000 no longer re-searches the suffix per opener. Upstream's differential test pins the merged matcher against the old regexes over 10,000 generated strings |
| 42a2c6510 #20430 | release consumed terminal write-queue slots | one-line source change taken verbatim. Upstream's 305-line differential test came over with the harness extended by this fork's scroll-gesture write-hold vars (`scrollGestureActive`, `scrollGestureStartedAt`, `heldWritePumpTimer`, `nowMs`), which upstream's queue does not carry. The payload-hash pin moved 757011 → 757055 |
| 20fb3e7d1 #20210 | normalize history scope paths once per candidate | applied verbatim; the file matched upstream. The dedupe re-normalized every accepted path per candidate, so a project with many same-repo worktrees paid O(n²) normalizations. A `Set` of comparison paths carries the answer forward |
| baa1cb135 #20220 | avoid materializing input characters on backspace | applied verbatim. `Array.from(text).slice(0, -1).join('')` built an array of every code point per keystroke; the last character's width now comes from `readUtf8CodePointAt` and the rest is one `slice`. `src/shared/utf8-byte-limits.ts` was already vendored and exports it |
| 56aefb542 #20226 | bound autocomplete substring retention | upstream's two hunks applied verbatim, plus a third: `rankSkillSuggestions` is Code UI's own and carries the same unbounded substring list (and matches descriptions too, so it retained more). The grep for the shape (`substring.push`) found all three. Upstream's test comes over as-is; the skill ranker got its own pair |
| 9b2b02bb3 #20358 | reuse UTF-8 prefix truncation for diagnostics | applied verbatim; both files matched upstream. Two hand-rolled truncators encoded every character separately through a fresh `TextEncoder`; both now call the vendored `clampUtf8TextPrefix` |
| 7a440b1c8 #20252 | skip successful duplicate connection log saves | applied verbatim; `connection-log-buffer.ts` matched upstream. Several save requests between two appends each copied the entries and each wrote them; a per-host revision now carries one snapshot and one `saved` flag, and a failed revision still retries |
| ef3b7e83b #20224 | reuse numeric collators across source control sorts | applied verbatim across the three files, which matched upstream. `localeCompare(…, { numeric: true })` builds a collator per comparison, so an O(n log n) sort built O(n log n) collators; one `Intl.Collator` per sort now. Upstream's `config/scripts/mobile-source-control-collation-benchmark.mjs` was not taken — this fork does not carry the benchmark scripts |
| 701dc2211 #20233 | precompute task sort keys and reuse repository collation | applied verbatim; the tasks screens here match upstream. Sort keys were derived inside the comparator, so each was rebuilt O(log n) times per item, and the repository sort built a collator per comparison |
| a045af361 #20249 | precompute Linear issue sort keys | applied verbatim; this fork has the Linear task screens and the files matched upstream |
| 7e9ade7c7 #20431 | reuse Linear grouping between list and board | applied verbatim, before df375cdd8 (it creates the projection test the later commit edits) |
| df375cdd8 #20314 | reuse Linear issue ordering when grouping list and board | applied verbatim on top of 7e9ade7c7 |
| 131d5ab07 #20310 | reuse current workspace on notification taps | applied verbatim; push-receive.test.ts is not in this fork, the other two files matched upstream byte for byte |
| 9a5679748 #20125 | surface host create warnings and terminal-create errors | mostly verbatim. `use-mobile-session-terminal-create-actions.ts` was hand-merged — this fork's structured-provider fallback and HUD launch config sit above the two failure branches, which themselves matched upstream, so only those were replaced. `onCreated`'s two signatures were hand-widened (upstream's context differed). The session screen here already reads a `warning` search param through `use-mobile-session-foundation.ts`, so the chain completes. Both route-parity hashes re-pinned, the same two upstream refreshed |
| 58ff95bec #19960 | name the RPC acceptance policies its call sites hand-rolled | applied verbatim across all nine files; they matched upstream. `rpc-acceptance-policies.ts` is new here and each policy preserves one call site's acceptance exactly — the predicates did not agree with each other before |
| f7d521601 #19055 | provider activity in chat turn tails | the wire already carried `activity`; the reducer and coalescer now keep it, and the live turn status row shows the host copy instead of a generic Working |
| c84007c54 #19961 | a shared params catalog generated from the host registry | **done, mobile and shared only.** Taken: `mobile/src/transport/rpc-params-contract.ts` (type-only door) and its boundary test, plus `src/shared/rpc-contract/` vendored whole (84 files) and the 8 shared files the catalog imports that this fork lacked (`mobile-push-contract.ts`, the six `orchestration-fleet-*`, `worker-terminal-host-scope.ts`). All 92 pinned in `UPSTREAM.txt`. **Not taken:** the generator (`config/scripts/generate-rpc-params-catalog.mjs`), its parse-parity gate in `.github/workflows/pr.yml`, `.gitattributes`, root `.oxlintrc.json`, `package.json` and the ~90 `src/main/runtime/rpc/methods/` files whose schemas were moved out — all host and CI machinery this fork does not vendor. The catalog is vendored as generated; the generator was not run. **2026-09-18:** the catalog lists what the desktop registers, not what a mobile-scope token may call — see "Catalogued ≠ reachable" below before reading a row here as reachable |
| eedd35645 #20018 | typed RPC operations, and the raw request port fenced behind an inventory | **done.** All 15 mobile files applied verbatim; `rpc-client.ts` hand-merged (its two hunks matched upstream). `rpc-params-catalog.generated.ts` and the new `rpc-send-params.ts` re-vendored at this commit. The port inventory was **regenerated for this fork** rather than taken: upstream lists its own 153 files, this fork has 170 (161 pending, 9 owners) — same ratchet, same AST counter, our call sites. To re-derive the list, run `rawRequestPortReferences` from `unvalidated-rpc-request-port-boundary.test.ts` over `app/` and `src/`; the test itself reports every drift. One local change: `rpc-params-contract-type-only-boundary.test.ts` does its file sweep at module scope, the way the sibling boundary test already does — parsing this fork's 1243 files took over vitest's 5s per-test budget under the full suite. Cast fence unmodified |
| b0070e372 #20499 | migrate settings reads to RpcOperation | **done.** 9 of the 10 call-site and test files were byte-identical to upstream's parent and were applied verbatim, `mobile-tasks-refactor-parity.test.ts` included — its refreshed hashes pass here unchanged, so this fork's tasks screen still matches upstream's on every hooked declaration. `mobile-new-tab-agent-loader.ts` was hand-merged: it carries the local folder-workspace hunk (Orca #16215) above the settings read. Port inventory regenerated: 168 files, the 7 migrated call sites each down one |
| ecd7b19ad #19929 | pass agent-implemented slash commands through to the agent | **done.** This fork did NOT already do it: `native-chat-agent-profiles.ts` and `structured-agent-session-composer.ts` were byte-identical to upstream's parent, so a Claude structured chat answered "/init is not available in chat sessions" for a command Claude does run, and Codex's `/goal` was typable but never offered. Both shared files re-vendored whole at `ecd7b19ad`, with `native-chat-agent-profiles.test.ts` — which this fork vendors but never collects, vitest's root being `mobile/`. `use-mobile-structured-native-chat-send-bridge.ts` took the patch verbatim through `git apply --3way`, and its new upstream test came with it. Upstream's `MobileNativeChatComposer.tsx` hunk has no counterpart here: this fork's composer builds the `/` menu through `mobile-native-chat-session-catalog.ts`, so the `agent` argument was threaded there instead. The controller's bridge call gained `agent`; upstream's `legacyNativeChatQuestion` rename was skipped as lint-budget churn on a file that has diverged. Because the shared tests never run here, both behaviours are re-pinned where the gate does run them — `/init` and `/goal` reach the agent (`mobile-structured-composer-command.test.ts`), Codex's fallback menu offers `/goal` (`mobile-native-chat-session-catalog.test.ts`), and a both-agents guard keeps `/clear` host-claimed |
| 7ce8e18d0 #20521 | the RPC migration's recording oracle | **done, whole.** All 104 mobile files verbatim: 73 goldens under `mobile/rpc-foundation/`, `pilot-scenarios.json`, `settings-recording-coverage.json`, `live-probe/`, the 26-module recorder under `mobile/src/test-support/rpc-recording/`, and `mobile/scripts/rpc-recording.mts`. **Upstream's goldens replay green against this fork's own product source** — 115 passed, 3 skipped — which is the whole question a vendored oracle raises. 11 of the 16 modules the mount adapters load are byte-identical to upstream; the two that are not move no recorded observation (`use-mobile-native-chat-file-search.ts` adds `nativeChatFileSearchPending` and a `limit` that defaults to the same 16, and the adapter projects `nativeChatFilePaths` only; `mobile-new-tab-agent-loader.ts` adds the folder-workspace branch, and the scenario's `repo-1::/folder` is not one). The three `native-host-*-operations.ts` modules this fork lacks are reached only under `RPC_FOUNDATION_REFERENCE_ROOT`, which needs the archived `bcba08b3e4` tree — those are upstream's own 3 skips. Port inventory: upstream's two OWNERS lines taken as-is, because they name the recorder's own files and the boundary test confirms the same counts here; 168 files → 170 |
| 7d98c8e2f #20544 | the source-control domain through typed RpcOperations | **done, whole.** All 22 product files: 8 new operation modules, `rpc-reader-payload.ts` and `rpc-refusal-message.ts`, and 12 migrated call sites. 20 of the 22 were byte-identical to upstream's parent; `MobileGitHistoryList.tsx` (pull-to-refresh) and `mobile-git-history.ts` (`commitTimestampToMs`, Orca #17729) were 3-way merged and keep their local hunks. The recorder and `rpc-foundation/` were taken **at the commit rather than as a diff** — `recorderSha256` digests the whole recorder directory, so it also has to carry the two guard inversions main made between the two commits, which a path-scoped diff would have missed. Goldens 73 → 153. Port inventory: upstream's hunk verbatim — the boundary test named the same 13 stale source-control entries here as upstream, and `use-mobile-git-requests.ts` stays at 1 for the same reason (a `(method, params)` dispatcher five hooks feed action steps). 170 files → 157; pending references 414 → 370, upstream's 44. **One fork-local recorder edit:** `unhandled-recording.ts` casts `process.rawListeners`, which is `Function[]` — not callable — under the `@types/node` 22 this fork resolves, so `process.on` below it rejects every element. That one line is inside `recorderSha256`, so all 153 goldens had that single header field rewritten to `d31a6743…`; **no recorded observation was touched**, the run is still upstream's, and `compareGolden` re-checks every checkpoint field by field: 201 passed, 3 skipped. Outside the recorder, `mobile/vitest.config.ts` sets `testTimeout: 30_000`: the oracle transpiles product source through the TypeScript compiler API per scenario, so the first test in a cold process pays the warmup (`b1` records in under a second warm and timed out at 5277 ms cold), and it is CPU-bound across four files for half a minute, which starved `e2ee-base64` to 7.0 s and `agent-hud-launch-args` to 5.9 s in a full pass. Both measured clean alone; a timeout that fires only on a busy box names the wrong file |
| 50e752fc6 #20562 | pin each RPC golden to its own scenario input, not the whole manifest | **done, whole.** **Applied second, after #20563, because it is that commit's child in upstream's history — the PR numbers do not follow merge order, and both commits edit `pilot-scenarios.json` and every golden.** `recorderSha256` covered the recorder directory *plus* `pilot-scenarios.json`, so every golden's header was a function of every other family's scenarios and adding one family re-digested all 153. It now covers the recorder directory only; a new per-golden `scenarioSha256` pins the scenarios that golden was recorded from, derived through the new `derived-goldens.ts` so the digest and the recording agree by construction rather than by a restated expansion. `GOLDEN_FORMAT_VERSION` 3 → 4. Taken at the commit, every file whole: 4 new modules (`derived-goldens.ts` + test, `golden-header-digest.test.ts`, `scenario-digest.ts`), 6 edited (`family-recordings.test.ts`, `golden-recording.ts`, `pilot-recordings.test.ts`, `recorder-digest.ts`, `recording-runner.test.ts`, README), `pilot-scenarios.json` and all 153 goldens. After the #20563 port this fork's recorder was byte-identical to upstream's parent **except** `unhandled-recording.ts`, whose `process.rawListeners` cast is kept. Goldens 153 → 153, and `git diff -U0` over `mobile/rpc-foundation` filtered for the four moved header names leaves **zero** lines: `scenarioSha256` added (153 distinct values, upstream's own — the manifest is taken whole), `goldenFormatVersion` 3 → 4, `baseline` `5ec0b269…` → `e53f1557…`, and `recorderSha256`, this fork's `a9293486…` against upstream's `58a461db…` for the cast; the digest module reproduces upstream's value exactly over upstream's tree. No product file. Gate: tsc clean, vitest 730 files / 6069 passed / 17 skipped, oxlint clean; recorder suite alone 207 passed, 3 skipped — the 210 the README now states |
| e53f1557e #20563 | two known main bugs the RPC migration preserved | **done, whole.** Both are reply-side, no wire change. `hostReplyErrorTextOrFallback` passed a truthy non-string through under a `string` annotation, and its one caller is the in-band `git.commit` failure whose every consumer is display copy — `summarizeCommitFailure` opens with `raw.slice(…).replace(…)`, so an object error threw. It now reads a non-string as absent. `useNewWorkspaceRuntimeContext` read settings through `settingsRead`, whose reader preserves main's `boxed!.settings` throw, so a null or absent `settings.get` **or** `ui.get` reply threw a TypeError out of the effect and lost the trusted-hooks publish and the provider computation, not just the settings; it reads through `optionalSettingsRead` and degrades a null `ui.get` to untrusted hooks. Taken whole: `use-new-workspace-runtime-context.ts` + its new test, `rpc-refusal-message.ts` + its new test, the recorder README, `unhandled-recording.test.ts` (with the last unhandled-rejection golden gone, nothing else pins the capture), `pilot-scenarios.json` and all 153 goldens. Every parent file was byte-identical here **except** `settings-read-operations.ts`, which carries this fork's local `terminalCopyTrimsGutterRead` operation below upstream's two comment lines; the two-hunk patch applied around it and the local operation is untouched. Goldens 153 → 153: three move an observation — `matrix-hostedreview.create-intent-git.commit-1` (`inner-false-object-error`), `matrix-settings.workspace-context-settings.get-1` and `matrix-settings.workspace-context-ui.get-1` (`result-null`, `result-absent`) lose the TypeError effect and commit `providers: ["github"]` — and the rest move `baseline` and `recorderSha256` only. As on #20544, `recorderSha256` is this fork's `5ca89784…` rather than upstream's `70aa6f59…` because of the `unhandled-recording.ts` cast; the digest module reproduces upstream's value over upstream's tree, so the cast is the whole gap, and **no recorded observation was hand-edited**. Gate: tsc clean, vitest 728 files / 6064 passed / 17 skipped, oxlint clean; recorder suite alone 202 passed, 3 skipped |
| fc525c355 #20568 | the task workspace-creation domain through typed RpcOperations | **done, whole.** 22 operations over 20 methods in four new modules — workspace create, workspace source, task runtime, and the Smart picker's provider reads — plus the migrated call sites: 12 of `src/tasks/`'s raw-port files go to zero references. All 31 non-golden files taken whole **except** `unvalidated-rpc-request-port-inventory.ts`, which is this fork's own list (#20018 precedent) and was regenerated rather than copied: the boundary test named exactly the 12 stale `src/tasks/` entries upstream deleted, with the same counts, so the deletions match and only upstream's expanded `// src/tasks/` comment was taken verbatim — its three "cannot reach zero" files all exist here. 157 files → 145; pending references 370 → 334, upstream's 306. Every other parent file was byte-identical here, this fork's `use-mobile-tasks-*` workspace hooks and `mobile-tasks-refactor-parity.test.ts` included, so upstream's refreshed parity hashes pass unchanged. Recorder again taken **at the commit**: the two new task-workspace mount-adapter files, the three edited adapters/loader and `workspace-settings-mounts.ts`. Goldens 153 → 208 (55 added: 24 `matrix-*`, 31 `tw-*`/`settings-task-workspace-*`). The 153 pre-existing goldens move `baseline` (`e53f1557…` → `50e752fc…`) and `recorderSha256` and **nothing else** — filtering those two names out of `git diff -U0` leaves zero lines, and no `scenarioSha256` moved, which is exactly the property #20562 was built to give. `recorderSha256` is this fork's `d4284ed4…` against upstream's `5ec2faff…`, the cast again; upstream's value reproduces over upstream's tree. No `.oxlintrc.json` override was needed — the largest migrated file, `use-mobile-tasks-runtime-hydration.tsx` at 401 raw lines, is under the 400 `**/*.tsx` cap once blanks and comments are skipped. Gate: tsc clean, vitest 730 files / 6126 passed / 17 skipped, oxlint clean; recorder suite alone 262 passed, 3 skipped |
| c21c08322 #20535 | report auth callback failures instead of cancellation | **done, mobile half — it was already carried.** The fix itself is one branch in `src/main/orca-profiles/profile-cloud-pkce.ts`, the Electron loopback server for the Orca Cloud PKCE sign-in: a callback whose `error` is not `access_denied` now rejects as `orca_cloud_auth_callback_failed` and answers "sign-in failed" instead of "cancelled". That half, and its two desktop test files, are **skipped** — `ls src/` here returns `shared` only, this fork vendors no `src/main/`, and the phone pairs with a desktop rather than signing in to Orca Cloud. The commit's other 75 files are the mobile recorder: a lint-shaped rewrite of `family-recordings.test.ts` and `pilot-recordings.test.ts` (conditional `it()` registration replaced by a `continue` guard) and the `recorderSha256` refresh those edits force across 73 goldens. **Both test files already sit at their post-#20535 form here.** The #20544 port above is a descendant of this commit and took the recorder at that commit, so `pilot-recordings.test.ts` is byte-identical to upstream's post-fix file and the `if (!second) { continue }` guard is at `family-recordings.test.ts:121`. The golden headers likewise already carry this fork's own post-#20535 digest `d31a6743…` — upstream's is `9b12a8ad…`, the gap being the `unhandled-recording.ts` cast noted on #20544 — and `recorder-digest.ts` recomputes exactly that over the working tree; swapping either pre-#20535 test file back moves it to `021724db…`, which would fail every golden's header check. **Skipped goldens:** the 13 upstream `matrix-<family>.json` files — `matrix-legacy-inventory`, `matrix-linear-detail-barrier`, `matrix-project-explicit-false`, `matrix-settings-agent-read`, `matrix-settings-best-effort` and `matrix-settings.{bot-overrides,home-providers,repo-metadata,resume-metadata,task-hydration,task-workspace,workspace-context,workspace-submit}`. #20544 replaced one golden per family with one per matrix site, so this fork carries `matrix-<family>-<request>-N.json` and those 13 names do not exist here. Net diff on the port: none. Verified with the oracle itself — `family-recordings`, `pilot-recordings`, `recording-runner` and `probe-hole-witness` at 201 passed, 3 skipped |
| 44268d961 #20668 | the `github.*` PR surface and the diff-review loaders through typed RpcOperations | **done, whole.** 21 operations over 21 methods in four new modules (`github-pr-read-operations.ts`, `github-pr-mutation-operations.ts`, `mobile-diff-review-operations.ts`, `mobile-review-terminal-operations.ts`) plus `github-pr-mutation-outcome.ts` and `github-pr-repo-slug.ts`; `mobile-source-control-rpc-sender.ts` becomes `transport/rpc-operation-sender.ts` and `gitStatusProjectionReader` is exported from source-control for the branch-context read. All 31 non-golden files applied as upstream's patch: 29 were byte-identical to upstream's parent; `mobile-git-history.ts` (this fork's `commitTimestampToMs`, Orca #17729) took the two-line import/type hunk around its local hunk; `unvalidated-rpc-request-port-inventory.ts` is this fork's own list and lost exactly the 8 `src/session/` entries the boundary test named (the same 8 upstream deleted), 120 files → 112, pending references 296 → 258 (upstream's `src/session/` group alone: 47 → 39 files, 114 → 76 references). The 2 new adapters and `pilot-scenarios.json` (identical to upstream's parent) taken verbatim. Goldens 255 → 306 (51 new: 20 pilot, 30 reply-matrix sites, and `diff-review-notes-refused-before-compare`). **The 255 pre-existing goldens did not move**: recording rewrote only `lockfileSha256` (provenance, never compared; the committed headers predate the 6fa6638 lockfile), so they were restored to HEAD bytes, as on #20705. Parity proven the way upstream states it: the same 306 goldens recorded from a `git archive` of the pre-port tree with this recorder and manifest laid over it are byte-identical (modulo `lockfileSha256`) to the post-port recording. Gate: tsc clean, vitest 901 files / 7896 passed / 17 skipped, oxlint clean |
| 98784820d #20667 | the transport pairing and status domain through typed RpcOperations | **done, whole.** Three operations over three methods in `host-status-probe-operations.ts` and `mobile-relay-pairing-operations.ts` (`status.get` under a third policy, `status.transport-probe-or-skip`; `pairing.provisionRelay` and `pairing.getEndpoints` as `require-result-or-throw`), and 11 of the 18 raw-port references in `src/transport/` migrated: the protocol gate, the retrying capability probe, the candidate race, credential rotation, the direct-to-relay upgrade, startup recovery and first pairing. Recorder engine gains `native-mounting-substitutes.ts` (+ test) and `relay-pairing-fixtures.ts`, the loader consults the substitutes, and three adapters mount the seven new families. 18 of the 20 non-golden files were byte-identical to upstream's parent and took the patch verbatim; `runtime-capability-probe.ts` was 3-way merged — upstream's `UnvalidatedRpcRequestPort` parameter and `hostStatusProbe` send taken, this fork's second callback argument (the accepted `status.get` result, which `use-mobile-session-tab-reconciliation.ts` reads the host platform from) kept with a `CODE UI` note; the inventory's `src/transport/` group is upstream's new block verbatim except the probe's comment, which no longer claims a sender-only caller this fork lacks. 112 files → 106, pending references 258 → 248. Goldens 306 → 341 (35 new). The 306 pre-existing goldens move `recorderSha256` (the two engine files and the loader edit) and, on the 255 older ones, `lockfileSha256` — now every golden carries the 6fa6638 lockfile digest — and **nothing else**: filtering those two names out of `git diff -U0` leaves zero lines. Pre-port `git archive` recording with this recorder laid over reproduces all 341 byte-for-byte. Gate: tsc clean, vitest 902 files / 7934 passed / 17 skipped, oxlint clean |
| 2dfdbc865 #20685 | the task provider, detail and board domains through typed RpcOperations | **done, whole.** 56 operations over 58 methods in five new modules (`mobile-task-item-detail-operations.ts`, `mobile-task-list-operations.ts`, `mobile-task-item-comment-operations.ts`, `mobile-task-item-state-operations.ts`, `mobile-task-project-board-operations.ts`), 22 of `src/tasks/`'s 25 remaining raw-port files to zero (70 references). Recorder: eleven new task adapter modules, `recorder-fixture-shape.ts` (the checked `mountFixture` subset type, inside the fence by necessity), `adapter-load-deferral.test.ts`, and the b3 `order` mutant re-anchored at `linearIssueRead.request` — that hunk merged cleanly around this fork's `Reflect.get` anchor (Orca #20786 is Group C's). All 22 migrated hooks, `pilot-scenarios.json`, `github-project-host-routing-source.test.ts` and `mounted-operation-modules.ts` were byte-identical to upstream's parent and took the patch verbatim; the inventory's `src/tasks/` group was identical to upstream's parent too, so upstream's hunk (the 22 deletions and the three-file note) applied as-is: 106 files → 84, pending references 248 → 178. `mobile-tasks-refactor-parity.test.ts` re-pinned the way its comments say, with a dated note: hooks 350 and statements 417 unchanged in count, `semantics` 3449 → 3301 — a pure 148-line deletion verified by diffing the reader's output against the pre-port tree (70 `rpc:` signatures, 75 method literals, three duplicated discriminants, exactly upstream's), declarations, render tree and StyleSheets byte-identical; the hooks hash is upstream's own `7af4478d…` because the hooks are identical, the other two are this fork's. Goldens 341 → 453 (112 new). The 341 pre-existing move `recorderSha256` (for `recorder-fixture-shape.ts`) and **nothing else**; a pre-port `git archive` recording with this recorder laid over reproduces all 453 byte-for-byte. Gate: tsc clean, vitest 903 files / 8047 passed / 17 skipped, oxlint clean |
| d13034799 #20702 | the dictation, terminal, notification and browser domains through typed RpcOperations | **done, minus upstream's push and worker-takeover halves.** New operation modules `mobile-dictation-operations.ts`, `mobile-agent-history-operations.ts` (+ `resume-metadata-lists.ts`), `mobile-browser-command-operations.ts` and `mobile-terminal-operations.ts`; `interpretOrThrowRefusalMessage` in `rpc-refusal-message.ts`; `RpcSendArguments` admits an explicit `null` where the catalog declares no params. 22 files were byte-identical to upstream's parent and took the patch verbatim (dictation setup and the three dictation hooks, agent history, the five browser files, the viewport refit and its test, `native-mounting-substitutes.ts`, `rpc-operation.ts`). Merged: `mobile-terminal-query-reply.ts` (keeps this fork's `hostPlatform` win32 short-circuit) and `terminal-live-accessory-raw-send.ts` (takes `terminalInputSend`, but NOT the `reportWorkerTerminalUserInput` call: this fork has no `worker-terminal-takeover-report.ts` and no orchestration workers). **Not carried, with the reason:** `mobile-push-registration-operations.ts`, `push-registration-mount-adapters.ts` and upstream's `push-registration.ts` hunk — this fork's `push-registration.ts` is its own module and already sends through `push-registration-operations.ts` (`notifications.register-push`), and has no `sendRegister`/`sendUnregister` for the adapter to expose; `native-voice-settings-operations.ts` — this fork has its own settings screens; `workerTerminalTakeoverReport` dropped from the terminal operations module and the `terminal.takeover-report` mount from the terminal adapter. The manifest is upstream's minus the two push and two takeover scenarios, and `terminal-raw-input-reported` becomes `terminal-raw-input-accepted` with the `orchestration.workerTerminalUserInput` completion and the takeover site removed (`oxfmt` reproduces upstream's formatting, so the diff is exactly those edits). `mounted-operation-modules.ts` is upstream's whole file minus the push entry — the first hand-registration missed `exposes: agentHistoryMountExposures` and every `aiVault.history` recording settled its mount with `Cannot read properties of undefined (reading 'Provider')`, which is why the register is taken whole. Inventory: upstream's deletions and holdout notes, adapted where the fork differs (no push entries to delete; `mobile-notifications.ts` stays at 2 with its second reach named); 84 files → 74, pending references 178 → 141. Goldens 453 → 500 (47 new; upstream's 56 minus the 9 push/takeover goldens). The 453 pre-existing move `recorderSha256` (the substitutes edit) and **nothing else**; a pre-port `git archive` recording reproduces all 500 byte-for-byte. Gate: tsc clean, vitest 903 files / 8094 passed / 17 skipped, oxlint clean |
| 6c03bb6e8 #20874 | replace `Reflect.get` with typed property access in the mounting substitutes | **done, whole.** One hunk in `native-mounting-substitutes.ts`, byte-identical parent, verbatim. Engine file, so `recorderSha256` moved on all 500 goldens and nothing else; upstream's diff touches 509, the 9 push/takeover goldens this fork does not carry (#20702) being the difference. Gate: tsc clean, recorder suite 570 passed / 3 skipped, oxlint clean; full suite with #20895 |
| 783d8feab #20895 | merge duplicate type imports in the recorder adapters | **done, the half that exists here.** `agent-history-mount-adapters.ts` verbatim (byte-identical parent); `push-registration-mount-adapters.ts` is not carried (#20702). The commit also repins `baseline` to `e7206f62…`, taken with the manifest hunk: all 500 goldens move `baseline`, the 5 `aiVault.history` goldens also move `adapterSha256`, nothing else. Gate: tsc clean, vitest 903 files / 8094 passed / 17 skipped, oxlint clean |
| 005616171 #20873 | record the real worktree catalog snapshot result in the RPC recorder | **done, whole.** The `worktree.catalog-snapshot` action settled with the fetch's raw result, which nests the live client, so `captureValue` threw and every golden through that adapter baked an `unhandled-rejection` effect with the action pending. The adapter hunk and the new `golden-recorder-failure-absence.test.ts` (reads every golden, fails on `captureValue`'s refusal texts, with a positive control) taken verbatim. Failing first: on this fork's pre-port goldens the new test reported the same seven checkpoints upstream found, green after the re-record. Exactly the six worktree-catalog goldens move (`adapterSha256`, the seven effects become `[]`, each fetch settles with the projected catalog); the other 494 are byte-identical. Gate: tsc clean, recorder suite 571 passed / 3 skipped, oxlint clean; full suite with #20884 |
| 2fccacadb #20884 | mount screens, declared device stores and declared OS state in the RPC recorder | **done, minus upstream's push-dismissal proof.** Screen mounting (automatic JSX runtime, `screenMount` with a crash boundary reporting to the effect sink, `screen-native-substitutes.ts`, `inert-native-elements.ts`, `native-module-traps.ts` with the `__esModule` rule), declared `deviceStore`/`deviceState.notificationTray`, and the `sent` ordinal on effects; four holdout sites recorded to prove them. Nine engine/driver files byte-identical to upstream's parent and taken verbatim, eleven new files whole; `mutants/pilot-mutants.test.ts` merged (`device: scenario` inside this fork's loop shape, Orca #20755 being Group C's); `mounted-operation-modules.ts` is upstream's whole file minus the two push entries. **Not carried:** `push-dismissal-mount-adapters.ts`, the `push-dismissal-tray-reconciled` scenario and the one `declared-device-state.test.ts` case that reads it — this fork has no `push-dismissal-reconciliation.ts`; the unit case beside it pins the tray capability and the file says so. **One fork-local addition, by the table's own rule:** `TextInput` joins the react-native screen members, because this fork's file explorer draws a search bar (`MobileFileExplorerSearch.tsx`) and `files-explorer-readdir` settled its mount with `Unsubstituted native member: react-native.TextInput` without it — so the fork's file-explorer goldens record this fork's screen, search bar included. Goldens 500 → 509 (upstream's 11 new minus the push-dismissal pair). The 500 pre-existing move `recorderSha256`, and 247 gain `sent` on their effects and **nothing else** — resolving both value pools and stripping `sent` leaves zero bodies moved, the same 247 upstream reports. No product file changed. Gate: tsc clean, vitest 908 files / 8132 passed / 17 skipped, oxlint clean |
| 0869d997c #20891 | the rest of the session domain through typed RpcOperations (step 4) | **done, minus the worker-takeover report.** Five new operation modules (`mobile-session-{read,write,launch}-operations.ts`, `mobile-clipboard-image-operations.ts`, `mobile-diff-review-git-operations.ts`), ten new adapters, `expo-haptics`/`expo-clipboard`/`BackHandler`/`Keyboard` in the substitutes, three re-anchored or new mutants, and the port ratchet's offender floor 50 → 20. 22 files were byte-identical to upstream's parent and took the patch verbatim, 15 files are new whole. **12 session files 3-way merged**, each keeping its local hunk: `mobile-native-chat-send.ts` (the chunked `splitAgentTuiClearWrites` clear loop now sends each chunk through `nativeChatTerminalWrite`), `use-mobile-native-chat-stop.ts`, `mobile-native-chat-image-send.ts` and `mobile-native-chat-send.ts` take the operation but NOT `reportWorkerTerminalUserInput` (no orchestration workers here), `use-mobile-session-terminal-list.ts` (transcript-tail filtering and stray-tail closing over `response.value`), `use-mobile-session-close-actions.ts` (early-return close and `planSessionTabClose`'s `leafId` on `sessionTabClose`), `mobile-new-tab-agent-loader.ts` (the folder-workspace rule, Orca #16215, folded into upstream's `loadDetectedAgents`), `mobile-native-chat-image-attachment.ts` (no `contentFingerprint` import), plus five whose local hunks sat clear of upstream's. `mounted-operation-modules.ts` and the manifest are upstream's minus the push and takeover families; the three session scenarios that scripted an `orchestration.workerTerminalUserInput` completion (`native-chat-write-accepted`, `native-chat-write-typed-command`, `native-chat-stop-accepted`) lose that step, so their goldens record this fork's sends. `mobile-session-route-parity.test.ts` re-pinned per its notes: callback bodies (5 moved), nested functions (6 moved, count 13), runtime strings 677 → 671 — the same twelve method literals out and six in as upstream, checked by diffing the reader's output against the pre-port tree; every other hash and count unchanged. Inventory: the 29 deletions the boundary test named plus upstream's holdout notes, the fork-only session entries kept under a `CODE UI` note; 74 files → 45, pending references 141 → 79. Goldens 509 → 628 (119 new; upstream's 121 minus the two takeover matrix sites). The 509 pre-existing move `recorderSha256` and **nothing else** (resolved bodies compared); a pre-port `git archive` recording with this recorder laid over reproduces all 628 byte-for-byte, the fork's raw `sendRequest` session code and the migrated code recording identically. Gate: tsc clean, vitest 908 files / 8252 passed / 17 skipped, oxlint clean |
| ea7902cbe #20915 | the device-state holdouts through typed RpcOperations (step 4, wave 3) | **done, minus the push-dismissal half and the worker-takeover report.** `accounts.consumeCodexResetCredit` (new `codex-reset-credit-consume-operations.ts`), the repo reads of the accessory's connection lookup and the New Workspace dialog on the new-tab readers, and the composed send, live keystroke send and clipboard paste on `terminal.input-send`; the new `session-terminal-input-mount-adapters.ts` and two family mutants. Verbatim (byte-identical parents): `codex-reset-credit.ts`, `use-new-workspace-repositories.ts`, `mobile-session-read-operations.ts`, `agent-send-keyboard-dismissal-wiring.test.ts`. Merged: `use-mobile-session-terminal-send-actions.ts` and `use-mobile-terminal-paste.ts` take `terminalInputSend` and not `reportWorkerTerminalUserInput`; `use-mobile-session-accessory-selection.ts` (this fork's #12251 hold-repeat), `mobile-session-route-types.ts`, `mobile-terminal-operations.ts` and the mutants merged clean. **Not carried:** `push-dismissal-operations.ts` and upstream's `push-dismissal-reconciliation.ts` hunk (no such module here); the adapter's takeover-window resets and its doc says so. Manifest: upstream's minus the families this fork lacks; the three `session.terminal-clipboard` scenarios also lose the `settings.get#1` completion upstream's accessory hook sends on mount for the terminal copy gutter setting (Orca #20545, Group C's) — this fork's hook does not read it, so **whoever lands #20545 must re-record `session.terminal-paste` and `session.worktree-connection`** — and the three takeover completions. Inventory: upstream's deletions and rewritten notes (the gesture-input holdout is scope, not a recorder gap), `use-new-workspace-ssh-target-labels.ts` kept as fork-only; 45 files → 40, pending references 79 → 71. `mobile-session-route-parity.test.ts` re-pinned: callback bodies (2 moved), nested functions (`handleSend`), runtime strings 671 → 668 — the same one `repo.list` and two `terminal.send` literals upstream lost. Goldens 628 → 637 (9 new; upstream's 13 minus the two takeover and two gutter matrix sites); **no pre-existing golden moved at all** (no engine file touched), and a pre-port `git archive` recording reproduces all 637. Gate: tsc clean, vitest 908 files / 8263 passed / 17 skipped, oxlint clean |
| 615b1370f #20914 | own the request/cache lifecycle in `GenerationScopedRequestOwner`, piloted on the legacy file inventory (step 5) | **done, whole.** New `generation-scoped-request-owner.ts`, its compile fence and `lifecycle-owner.test.ts` (five named schedules and a loader-body fence); `RpcClient.getGeneration?` (the stable client already had it); the native-chat file search's three hand-rolled guards become one owner scoped to host, workspace and logical authority epoch; the `race` mutant re-anchored on the owner's compare. Two files byte-identical to upstream's parent and verbatim; `use-mobile-native-chat-file-search.ts` is upstream's new file with this fork's `limit` argument and `nativeChatFileSearchPending` state re-applied (the explorer's search bar reads both); `rpc-client.ts` adds `getGeneration` beside this fork's liveness-probe members; the mutant table merged clean. **No golden moves**: the candidate recorder suite reproduces every legacy-inventory golden against the rewritten hook, so nothing was re-recorded. Gate: tsc clean, vitest 909 files / 8275 passed / 17 skipped, oxlint clean |
| b8d4cde09 #20919 | six screen-mounted call sites through typed RpcOperations (step 4, wave 3) | **done, minus the settings push probe.** The explorer's `files.read-directory-or-skip` and `files.legacy-explorer-list-or-skip` (new `mobile-file-explorer-operations.ts`), `accounts.home-snapshot-or-skip`, `linear.select-workspace-or-skip`, the history panel's `worktree.ps` on `worktreeCatalogRead` and the tasks route's `repo.list` on `newTabRepoListRead`; four new adapters, `SectionList`/`RefreshControl`/`useSafeAreaInsets`/`useLocalSearchParams` substitutes (`TextInput` was already here from #20884), four mutants. Seven files byte-identical to upstream's parent and verbatim; `MobileFileExplorerPanel.tsx` (this fork's search bar), `mobile-tasks-filter-pickers.tsx` and the mutants merged clean; `screen-native-substitutes.ts` merged around the fork's early `TextInput`. **Not carried:** `mobile-push-delivery-test-operations.ts`, `notification-test-screen-mount-adapters.ts` and the `notifications.display-test-screen` family — this fork has no `settings/notification-display-test.tsx` (own settings screens). Inventory: upstream's five deletions and its rewritten holdout notes for `accounts.tsx`, `terminal-settings.tsx` (its line-193 `ms`-off-the-envelope defect is in this fork's file too) and `host-screen-overlays.tsx`; `refresh-account-usage.ts` stays as fork-only; 40 files → 35, pending references 71 → 64. `mobile-tasks-refactor-parity.test.ts` re-pinned: semantics 3301 → 3297, a pure four-line deletion (two `rpc:` signatures, two method literals) verified against the pre-port tree; hooks hash is upstream's own again; statements and render-tree hashes move (the picker's handler names an operation), all counts unchanged. Goldens 637 → 648 (11 new; upstream's 13 minus the probe's pilot and matrix). The 637 pre-existing move `recorderSha256` (the substitutes edit) and **nothing else**; a pre-port `git archive` recording reproduces all 648. Gate: tsc clean, vitest 909 files / 8291 passed / 17 skipped, oxlint clean |
| d4c19d5db #20920 | let the RPC recorder open a subscription and script its frames (step 6 capability) | **done, whole.** The scripted transport carries the real `RpcClientStreamRegistry` per session, a `frame` step hands a host response to the real `handleResponse`, `payloads` entries carry `sent`, the reply matrix drives frames as sites, `useFocusEffect` is substituted as `useEffect`; two families prove it (`session.live-worktree-name`, `worktree.host-refresh`, both product files byte-identical to upstream's) and four runner tests each kill a named mutation. Six engine files byte-identical to upstream's parent and verbatim, the new adapter whole; `recording-runner.test.ts` and `screen-native-substitutes.ts` merged clean around this fork's one-line differences; the register is upstream's minus the three modules this fork lacks. Goldens 648 → 660. The 648 pre-existing move `recorderSha256`, and 642 gain `sent` on their payloads and **nothing else** (resolved bodies compared with `sent` stripped: zero moved; the six with no payload are header-only, as upstream reports). No product file changed. Gate: tsc clean, vitest 909 files / 8308 passed / 17 skipped, oxlint clean |
| f78483ec2 #20954 | the subscription-gated holdouts through typed RpcOperations (step 6, migration 1) | **done, minus the desktop notification stream and the worker-takeover report.** `use-live-worktree-name.ts`'s `worktree.show` on the renamed `sessionWorktreeRecordRead`, native chat's older-history page on `nativeChat.read-session-page-or-skip`, the gesture flush on `terminalInputSend` and the menu's clear on the new `terminalBufferClear`; the recorder's `frame` now returns what a product listener threw and the runner records it as a `stream-listener-crash` effect; `rpc-subscription-inventory.ts` + its boundary test classify every `client.subscribe` (this fork's ten sites are the same ten). Six files byte-identical to upstream's parent and verbatim, two adapters and the inventory pair new whole. Merged: `use-mobile-native-chat-session.ts` (104 lines of fork divergence, clean), `use-mobile-session-terminal-input.ts` (takes both operations, not `reportWorkerTerminalUserInput`; **this fork's unpaced mouse-click send stays raw at 1 reference** with a note), `mobile-terminal-operations.ts` (gains `terminalBufferClear`, not the takeover report), `recording-runner.test.ts`. `isTerminalSendRpcAccepted`, which upstream deleted with its last caller, is **kept** with a note and a test case: this fork's Codex picker, document attachments, queue editor and draft mirror still read the envelope. The gesture adapter is adapted to this fork's gesture model (an in-flight count per handle, one sequence per 16 ms flush, no token bucket, `hostPlatformRef`) and its scenario loses the `reported` checkpoint the absent takeover would have named. **Not carried:** `desktop-notification-stream-operations.ts`, its adapter, its three scenarios and the `notifications.unsubscribe` hunk in `mobile-notifications.ts` — this fork's stream subscribes with no params, its catch-up read carries the stored epoch and cached delivered pushes, and its dismissals reconcile the tray, so upstream's recordings failed on the first frame's params (`Subscribe params mismatch`); the subscription inventory lists it as `unwritten-scenario` with the reason and the raw-port inventory keeps `mobile-notifications.ts` at 2. Inventory otherwise upstream's; 35 files → 33, pending references 64 → 60. `mobile-session-route-parity.test.ts` re-pinned: callback bodies (2), nested functions (`handleClearTerminal`), runtime strings 668 → 666, the same two literals upstream lost. Goldens 660 → 667 (7 new; upstream's 15 minus the stream family's 7 and the gesture takeover site). The 660 pre-existing move `recorderSha256` and **nothing else**; a pre-port `git archive` recording reproduces all 667, the fork's divergent paging hook included. Gate: tsc clean, vitest 910 files / 8326 passed / 17 skipped, oxlint clean |
| a28085adb #20950 | checked reply readers for the source-control domain (step 7 pilot) | **done, whole.** The 17 unchecked readers in `src/source-control/` become `rpcResultVariant(variant, schema)` over five new schema modules (git-status, git-compare, git-history, hosted-review, worktree-metadata; `z.looseObject`, required only where a consumer reads unguarded); `RpcIncompatibleReplyError` gets user-readable copy with the token on `code`/`name`; `openEnum` joins `src/shared/zod-salvage.ts` and `supportsHostedReviewCreation` takes a plain string (both re-vendored whole at this commit and pinned in `UPSTREAM.txt`); the new `unchecked-rpc-reader-inventory.ts` + boundary test ratchet the 201 unchecked readers; a screen-read adapter records the four reads that had no oracle. 24 mobile files byte-identical to upstream's parent and verbatim, 9 new whole; `MobileGitHistoryList.tsx` (pull-to-refresh) and `mobile-git-history.ts` (`commitTimestampToMs`, #17729 — the signature widens to `number | null | undefined` as upstream's did) merged around their local hunks. The reader inventory is **this fork's own list**, like the raw-port one: no push/probe/stream modules to list, no worker-takeover reader (`mobile-terminal-operations.ts` 3, not 4), and five fork-only operation modules added under `CODE UI` lines (project config ×2, MCP status, hunk revert, the capability gate) plus `host-screen-operations.ts` at 10 for the pinned-in-groups pair — 38 files, counts from the AST counter. Goldens 667 → 678 (11 new). This step is a behaviour change on malformed replies, and the oracle upstream states holds: recording the pre-port tree with this recorder and comparing resolved bodies moves exactly 13 — the six reply-matrix goldens upstream names (git.history, hostedReview.getCreationEligibility ×3, hostedReview.create ×2), the five new matrices for git.status/branchCompare/branchDiff/commitCompare/history, and `sc-prefill-unavailable-on-{refusal,rejection}`, whose prefill loses a `canCreate: false` the schema's optional member no longer invents — upstream's own goldens for those two moved the same way, though its message counts only eleven. **All 19 moved or new bodies are byte-identical to upstream's own recordings at this commit** (resolved value pools compared). The other 659 move `baseline` (label follows upstream's `9e01c50e…`) and `recorderSha256` only. Gate: tsc clean, vitest 912 files / 8361 passed / 17 skipped, oxlint clean |
| 9add08bb5 #21088 | recorder follow-ups: write ordinal, teardown streams, context anchor, salvage observation, provider pass-through, React draw | **done, whole.** One write ordinal orders requests, payloads and effects against each other (`sent` → `ordinal`); teardown records the streams a registry still holds (`streams-registered-at-teardown`, read after a drain); one host-client context exposure anchored on the product source, the seam test refusing a sixth copy; a `reply-salvage` effect for what a checked read dropped; React's lazy `Math.random` draw paid before the seeded run; the subscription suites split out of `recording-runner.test.ts`. Product half: `hostedReview.create`'s provider is an open string, so `src/shared/rpc-contract/hosted-review-params.ts` is re-vendored whole at this commit (pin moved from `c84007c54`; the phone reaches it through the params catalog, which the backlog's direct-import count missed) and `mobile-hosted-review-service.ts` drops its SAFETY-annotated cast. 14 files byte-identical to upstream's parent and verbatim, 6 new whole, `recording-runner.test.ts` merged clean; the probe adapter's hunk is not carried (#20919). Goldens: all 678 move `recorderSha256`, 21 move `adapterSha256` (upstream's 23 minus the probe adapter's two), and with `sent`/`ordinal` stripped **exactly 4 bodies move** — the two `runtime.clientEvents.subscribe` matrices of live-worktree-name and host-worktree-refresh on their non-`ready` partitions, which now carry the cancelled registration at teardown — the set upstream discloses. Gate: tsc clean, vitest 915 files / 8367 passed / 17 skipped, oxlint clean |
| ccb4d2044 #21083 | the last session-route raw-port calls as operations (step 6, migration 2) | **done, whole, adapted to this fork's hooks.** The startup effect's two `worktree.activate` sends reuse host-screen's `worktreeActivate`, the New Tab create gets `sessionTabCreateTerminal` (`require-result-or-throw-message`), the display-mode toggle `terminalDisplayModeSet`; three adapters, ten scenarios (three of them the empty-cell arms), the guard witness in the probe-hole suite. Four files byte-identical to upstream's parent and verbatim, three adapters new whole; six files 3-way merged: `use-mobile-session-terminal-create-actions.ts` keeps this fork's agent HUD launch config on the create, `use-mobile-session-terminal-stream-display.ts` keeps its queued, viewport-measuring `setDisplayMode` and still returns the accepted verdict (`interpret(...).accepted` is exactly the `response.ok` the floor release retries on), `use-mobile-session-startup.ts` (cached-tab seeding), `host-screen-operations.ts`, `mobile-session-route-types.ts` and the mutants merged clean. Three adapter adaptations for this fork's hooks, each noted in place: the display-mode adapter supplies `measureViewportOnce` as an effect sink (a never-shown terminal is measured before `auto`), the startup adapter supplies `sessionTabsRef`/`activeSessionTabIdRef` (the warm-start cache seed) and the fork's per-handle in-flight count, and the agent-launch create scenario scripts the `settings.get`/`status.get` pair the fork's HUD launch config reads and carries the resulting `launchConfig` in the create's params (generated from the fork's own resolver for a `darwin` host with no agent defaults). Inventories: upstream's hunks (`use-mobile-session-terminal-create-actions.ts` stays at 1 for the prompt `terminal.send`, `mobile-session-write-operations.ts` 8 → 10 readers); 33 files → 31, pending references 60 → 56. `mobile-session-route-parity.test.ts` re-pinned: callback bodies (`setDisplayMode`), effects (the startup activation pair), nested functions (`handleCreateTerminal`), runtime strings 666 → 662 — the same four literals upstream lost. Goldens 678 → 700 (22 new, the same 22 names as upstream); the 678 pre-existing move `baseline` (label follows upstream's `9add08bb5`) and **nothing else**; a pre-port `git archive` recording reproduces all 700. Gate: tsc clean, vitest 915 files / 8389 passed / 17 skipped, oxlint clean |
| 4b876758d #21089 | checked reply readers for the session domain (step 7) | **done, whole.** 51 unchecked readers across nine session operation modules become checked zod readers over eight new schema modules (clipboard image, GitHub PR mutation/entity/read/check, diff review, review terminal, session launch/read/write) with their unit suite; `github-pr-parsers.ts`, `github-pr-comment-parsers.ts` and `github-pr-value-readers.ts` (+ test) deleted; twelve call-site casts gone; a refused `github.prChecks` is contained to the checks section (`checksError`) instead of taking the sidebar down; ten new recording families plus a PR-sidebar adapter. 40 files byte-identical to upstream's parent and verbatim, 12 new whole, 4 deleted; `MobilePRSidebar.tsx`, `mobile-new-tab-agent-loader.ts` (folder-workspace rule), `use-mobile-native-chat-file-search.ts` (limit/pending), `use-mobile-session-markdown-actions.ts` and the mutants merged clean; `use-mobile-session-terminal-list.ts` keeps the transcript-tail filtering under upstream's new SAFETY note. The reader inventory loses the nine session files outright (38 → 29 entries, this fork's own list). `mobile-session-route-parity.test.ts` re-pinned: callback bodies (the markdown tab doc and `diffComments` casts gone) and nested functions (`handleCreateBrowser`'s cast gone); every count unchanged. Goldens 700 → 731 (31 new). Against HEAD the 700 pre-existing move `baseline` (label follows upstream's `ccd228af…`), `adapterSha256` on the eight review-action goldens (the send-sheet action) and 34 bodies; against a pre-port recording with this recorder laid over, **50 bodies move** — every one a reply-matrix golden's malformed or refused partition or a named malformed-reply scenario in the migrated families (github.pr-read ×7, pr-sidebar ×6, review-file-diff ×4, clipboard uploads, the local-agents and settings-agent reads, tab documents, markdown save, browser tab create, the triage and resume launches' invalid-tab arms…), the disclosed behaviour change; upstream reports 42 over its 730, the difference being the fork's own scenarios in these families. Against upstream's own goldens at this commit, the only two that newly differ are `session-tab-closed` and its `session.tabs.close` matrix, where this fork's tab close keeps `activeHandle` where upstream nulls it (the 2026-09-10 close plan; identical wire) — the 50 others that differ did so before this wave, for the fork reasons already recorded. Gate: tsc clean, vitest 915 files / 8460 passed / 17 skipped, oxlint clean |
| 01a1b6b02 #21169 | checked reply readers for the tasks item and list domain (step 7) | **done, whole.** 38 unchecked readers across the four tasks item/list operation modules become checked zod readers over five new schema modules (+ their unit suites); three call-site shape tests deleted; the null-collapse mutant registered in the new `mutants/family-mutants.test.ts`; the `gitlab.todos` fixture corrected to a real `GitLabTodo` row. All 24 touched product and recorder files were byte-identical to upstream's parent and took the patch verbatim, 11 files new whole; the two mutant files merged clean. The reader inventory loses the four tasks files (29 → 25). `mobile-tasks-refactor-parity.test.ts` re-pinned: `semantics` 3297 → 3275, the same 23-literals-out-one-in as upstream (the invented reaction arms, the mergeable/viewed/status arms, both "Invalid checks response" strings), verified against the pre-port tree; hooks hash is upstream's own again, statements and declarations move at unchanged counts, the render-token hash does not move. Goldens 731 → 733 (the two corrected todos goldens). Against a pre-port recording with this recorder laid over, **50 bodies move, every one a `matrix-tasks.*` malformed partition** — the disclosed behaviour change; against HEAD, `baseline` (label follows upstream's `5ec0430e…`), `scenarioSha256` on the 14 todos-family goldens and those bodies. **Against upstream's own goldens at this commit, no golden newly differs.** Gate: tsc clean, vitest 920 files / 8534 passed / 17 skipped, oxlint clean |
| 3e32b8352 #21176 | checked reply readers for notifications, components, terminal, transport, home, worktree and browser (step 7) | **done, minus upstream's notification modules.** 21 → here 16 unchecked readers across the browser, components (setup hooks, `ui.get` trust, Codex redeem), terminal (send verdict, viewport pair, buffer clear), transport (`status.get` under three named readers, the two pairing readers over the shared credential contract), home (stats), worktree (catalog union) and browser command operations become checked zod readers over seven new schema modules (+ suites); `terminalSendAcceptedSchema` moves to the terminal layer; the `push-test-envelope` mutant re-anchored; the reader ratchet's offender floor 20 → 10; three wrong fixtures corrected; the `worktree-catalog-unchecked-reader` mutant with its unreadable-catalog scenario. 26 files byte-identical to upstream's parent and verbatim, 15 new whole; 9 files 3-way merged, clean except `mobile-terminal-operations.ts` (no takeover report), `mobile-relay-pairing-operations.ts` (this fork's doc still says `method_not_found`, #19952 being Group B's) and `runtime-capability-probe.ts`. **Not carried:** `notification-reply-schema.ts` (+ test) and the `push-registration.ts` hunk — they serve upstream's four notification operation modules, none of which this fork has; `terminal-send-rpc-response.ts` (+ test), which upstream deleted, stays for this fork's four raw sends with a note. **One fork adaptation:** the probe's second callback argument (the accepted status, for the host platform) now comes from `readProbedHostStatus`, a `CODE UI` reader beside upstream's `readProbedHostCapabilities` that returns both in **one** interpretation — a first cut interpreted twice and the recorder faithfully recorded a second `reply-salvage` effect on `transport-capability-probe-non-string-capabilities-drop`, which is how it was caught. Reader inventory 25 → 16 (this fork's list). Goldens 733 → 734. Against a pre-port recording, **12 bodies move** — the pairing/catalog/history-screen malformed partitions, the non-string-capabilities probe (one `reply-salvage` now) and the new unreadable-catalog scenario; against upstream's own goldens **no golden newly differs**. Gate: tsc clean, vitest 928 files / 8581 passed / 17 skipped, oxlint clean |
| 6142657d7 #21246 | checked reply readers for the tasks domain's board, runtime, search and create (step 7) | **done, minus the `agent.launch` create route.** The board, runtime, source-search, workspace-source and workspace-create operations become checked zod readers over five new schema modules (+ suites; `ownerType` closed, SSH `status` open and degrading, tri-states preserved); the Linear hand reader and its test go; the b2 seed's `acceptance` mutant re-anchored at the schema and `pilot-recordings.test.ts` restates the seed; the reader ratchet's floor becomes `observed.size === inventory.length`. 38 files byte-identical to upstream's parent and verbatim, 10 new whole, 1 deleted; `worktree-create-capability.ts` and the mutants merged clean; `mobile-workspace-create-operations.ts` takes the four schema swaps but **not** `agentLaunchRun`/`agentLaunchReplayRun` (the `agent.launch` create route is Orca #19849/#20999/#21137, Group B's, not in this fork yet), `worktree-create-retry.ts` drops its create-receipt cast in this fork's pre-#21137 shape, and `blank-workspace-create.test.ts` gains this fork's version of upstream's break-branch test — "names an accepted worktree.create reply that carries no workspace id" — **red on the pre-port create files, green after**. The `worktree.agent-launch-create` family and its scenario are not carried (dropped by family, which the first pass missed by operation until the derived-goldens test named it); the create-retry adapter's `agentLaunch` argument stays verbatim and unused. Reader inventory 16 → 11 (the floor comment says 11 here). `mobile-tasks-refactor-parity.test.ts` re-pinned: `semantics` 3275 → 3271, exactly the four literals of the deleted inline cast type (verified against the pre-port tree); hooks hash is upstream's own; statements and declarations move at unchanged counts; render tree unchanged. Goldens 734 → 734 (upstream's two new are the launch family's). Against a pre-port recording, **46 bodies move, all `matrix-*` malformed partitions of the converted families**; against HEAD also `baseline` (label `a499dada…`), `recorderSha256` (the driver's seed restatement), `adapterSha256` on the 34 create-retry-adapter goldens and `scenarioSha256` on 12. **Against upstream's own goldens, no golden newly differs.** Gate: tsc clean, vitest 932 files / 8705 passed / 17 skipped, oxlint clean |
| 4a86b2dc5 #21269 | checked reply readers for files, dictation, host-screen and agent-history (step 7) | **done, whole.** The last upstream domains: seven new schema modules (+ suites) for file preview/tab-doc/explorer/ownership, dictation, host-screen and agent-history; `hostUnionArms` joins `src/shared/zod-salvage.ts` and `getRepoExecutionHostId` takes loose member types in `src/shared/execution-host.ts` (both re-vendored whole at this commit and pinned); `dictationMode` becomes the host's own string across the composer, overlay, view props and input actions; the repo icon and worktree row take the decoded catalog icon type; three new before-picture families and two vocabulary scenarios; `mutants/reply-schema-mutations.ts`; `repo-metadata-platform` re-anchored. 19 files byte-identical to upstream's parent and verbatim, 15 new whole, 16 files 3-way merged — clean but for five small conflicts resolved on the fork's side (the fork's `mobile-native-chat-view-props.ts` carries the widened `dictationMode`, the fork's preview test keeps its PDF-cache mock beside upstream's new import, the reader ratchet's floor comment). **Two fork-only writes converted the same way:** `worktree.set-archived-or-skip` and `worktree.set-unread-or-skip` in `host-screen-operations.ts` (this fork's archive/unread rows) read through `hostScreenUnreadReplySchema` like upstream's pin write, so host-screen leaves the reader list here too. The reader inventory is down to this fork's own four modules (project config ×2, hunk revert, the capability gate), with its header rewritten: upstream's countdown is over, these are the fork's backlog, nothing may be added back. `mobile-session-route-parity.test.ts` re-pinned: callback bodies (`readMarkdownTab`'s fallback cast retired), nothing else. Goldens 734 → 746 (12 new). Against a pre-port recording, **26 bodies move, all malformed partitions of the converted families**; against upstream's own goldens, the only newly differing goldens are the three new preview-family matrices, whose refusal partitions carry this fork's `Unable to load preview: <host message>` copy — the same error-detail behaviour the existing `files.preview-load` goldens already differed by. Gate: tsc clean, vitest 939 files / 8779 passed / 17 skipped, oxlint clean |
| db2ffe7af #21416 | default injected timers to receiver-free wrappers | **done, whole.** New `timer-scheduler.ts` (`ScheduleTimer`, `defaultScheduleTimer`, `defaultCancelTimer`), every transport module that took `typeof setTimeout` as an injectable now types it `ScheduleTimer` and defaults to the wrappers (endpoint lifecycle, supervisor contract and fakes, direct-return probe, background grace, direct-grace timer, lease rotation, reconnect controller, host-open retry, liveness watchdog, file-preview navigation); `global-timer-receiver-test-fakes.ts` and three new suites (`timer-receiver-census.test.ts` walks the module graph for a bare global timer parked as a default, `rpc-session-liveness-watchdog-default-timers.test.ts`, the host-open retry pair). 9 files verbatim (byte-identical to upstream's parent), 4 new whole, 11 3-way merged — only `mobile-endpoint-lifecycle.ts` conflicted, resolved keeping this fork's `networkType`, `networkIdentity` and `directReturnProbe` lines beside upstream's default timers. **One fork edit:** `defaultCancelTimer` is a null-guarded wrapper rather than upstream's bare arrow, because with `@types/node` 22 and the RN lib both in scope no `clearTimeout` overload accepts `null` and the arrow fails `typeof clearTimeout`; behaviour is the same (a null handle is a no-op either way). Failing-first: with HEAD's eleven product files swapped back under the ported tests, four fail (the census finds the parked globals, both default-timer suites cannot schedule without an injection), all pass restored. **Not carried:** the `tests-typecheck-baseline.txt` hunk — that file is Group C's #21298, which this fork does not have yet; whoever lands it should leave `mobile-relay-background-grace.test.ts` out of the baseline, since it typechecks here after this port. Oxlint: `mobile-relay-reconnect-controller.ts` gets a dated 301-line ceiling — upstream's sits at exactly 300, this fork's `user-send` retry (2026-09-10) is a line longer, and the `ScheduleTimer` import tips it. No RPC surface changed, no golden moved. Gate: tsc clean, vitest 941 files / 8792 passed / 17 skipped, oxlint clean |
| 6c3b97b95 #19952 | a scope refusal is not a missing method on the Relay pairing probes | **done, merged.** The desktop's mobile allowlist gate answers before its dispatcher, so an old desktop refuses `pairing.getEndpoints` / `pairing.provisionRelay` with `forbidden`, never `method_not_found`, and the "too old for Relay, stay on LAN" fallback keyed on absence alone never fired: in `upgradeDirectMobileRelay` the refusal threw into the controller's swallowing catch and the write-once journal (with its pending resume secret) was never retired. New `pairing-relay-rpc-unavailable.ts` (verbatim; its comment cites upstream's `docs/reference/remote-wire-compatibility.md`, which this fork does not carry) reads both codes at the three probe sites. Both tests verbatim (the files matched upstream's parent); both product files **hand-merged** because this fork still calls the probes through raw `client.sendRequest` — the #20667 operations module `mobile-relay-pairing-operations.ts` is Group D, not landed here — so the same three guard swaps, the `'relay-pairing-unavailable'` sentinel rename and the coordinator's breadcrumb log were applied onto the raw-call shape. `isMethodNotFoundRefusal` keeps its four other consumers. Failing-first: with the fix reverted both new tests reject with `forbidden: …` instead of resolving (2 failed / 14 passed), green after. Recorder untouched: no golden scenario drives the pairing probes |
| 5287c5cdb #20069 | stop a created tab from jumping when the host snapshot lands | **done, merged.** The strip appended a created tab and then jumped it: the request anchored on the active tab (`afterTabId`, host `splice(insertAfter + 1)`) while the optimistic paint appended, two placements that disagree by construction. New shared `session-tab-placement.ts` + test (verbatim, pinned 5287c5cdb) is the one rule both sides use; `use-mobile-session-foundation.ts` (verbatim, matched parent) binds `useHostProtocolGates` for `hostCapabilities`; `protocol-version.ts` takes the `SESSION_TABS_SPLIT_GROUP_PLACEMENT_RUNTIME_CAPABILITY` constant and its `RUNTIME_CAPABILITIES` entry as a **hand-applied hunk** on its d07c47593 pin (LOCAL-FILES.md updated). `use-mobile-session-terminal-create-actions.ts` **hand-merged** onto this fork's raw `client.sendRequest('session.tabs.createTerminal', …)` shape, which sits below the fork's HUD `launchConfig` resolution and the Claude-falls-through-to-terminal branch: one captured `afterTabId` feeds the request and the paint, the paint goes through `placeCreatedSessionTab(…, { afterParentGroup: true })`, and against a host that does not advertise the capability the phone paints nothing and lets the snapshot place it (an older headed host places after the split parent, an older headless one after the leaf). `mobile-runtime-client-capabilities.ts` + test hand-merged (this fork's list lacks the #19849/#19695 entries upstream's has). The create-actions test took upstream's six-case `optimistic placement` block verbatim on top of this fork's Claude-fallback cases. Failing-first: 3 of the 6 fail on the pre-port hook (`paints … after the anchor`, `paints after the active split parent`, `waits for an older host snapshot`: append vs anchor, and setSessionTabs called against an older host), green after. Route parity re-pinned: hooks 281 → 282 (`useHostProtocolGates`), `HEAD_HOOK_BINDING_SHA256`, `HEAD_NESTED_FUNCTION_SHA256` (`handleCreateTerminal`'s body); the same three upstream moved, dated notes in the test. **Recorder:** the ten upstream goldens and `session-terminal-create-mount-adapters.ts` are Group D's #20891 recorder mount, which this fork does not carry yet, and its `create-after-tab-id-null` mutant likewise; when that lands the mutant's `before`/`after` must match the `const afterTabId = …` line, as upstream's does |
| 6da72383d #19849 | one executor for agent launches, exposed as `agent.launch` | **done, shared half, in one commit with #19850** (its first and only consumer; the constant and the intent type have no reader until the create sheet routes through them). `agent-launch-intent.ts` vendored whole at 97aa5ff19 rather than 6da72383d, because #19850 is the next commit to touch it (moves `warning` to the top level of the receipt, makes `withoutReservedAgentCreateFields` generic) and landing the intermediate shape for one commit would pin nothing. `protocol-version.ts`: `AGENT_LAUNCH_RUNTIME_CAPABILITY` and its two array entries **hand-applied** on the d07c47593 pin (upstream's hunk context names `NOTIFICATIONS_REMOTE_PUSH_RUNTIME_CAPABILITY`, not vendored here). `rpc-params-catalog.generated.ts`: this commit's `RPC_METHODS_WITHOUT_SHARED_PARAMS` entry is superseded by #19850's catalog row, so only the net is applied (see that row). The executor itself, `src/main/agent-launch/`, and the `agent.launch` handler are host code this fork does not vendor |
| 97aa5ff19 #19850 | open native chat when a new worktree launches a default agent | **done, mostly verbatim.** Picking an agent on the create sheet always produced a terminal: every create path put `startupAgent` on `worktree.create`, which means "create agent-first", so the startup terminal IS the agent and the host's structured branch is unreachable. The blank, branch and new-branch creates now send the same payload through `agent.launch` when the host advertises `agent.launch.v1` on `status.get`, and let the host settle the surface; work-item creates stay on `worktree.create` (their issue/PR URL is an unsent `startupDraft`). **Verbatim (matched upstream's parent):** `use-new-workspace-create-submit.ts`, `blank-workspace-create.ts` + test, `source-workspace-create.ts` + test, `workspace-create-params.ts` + test (`agentLaunchCreateFields` → `startupAgentCreateFields`), `worktree-create-capability.ts` + test, `worktree-create-retry.ts`, `mobile-workspace-create-operations.ts` (`agentLaunchRun`), `active-session-tab.test.ts`, the recorder's `workspace-settings-mounts.ts` (`getAgentLaunchSupport: async () => false`, so the baseline stays on `worktree.create`); **new:** `agent-launch-worktree-create.ts` + test, shared `rpc-contract/agent-launch-params.ts` (pinned 97aa5ff19). **Hand-merged:** `NewWorktreeModal.tsx` (two one-line hunks; the file carries fork chrome), `mobile-runtime-client-capabilities.ts` + test (advertise `agent.launch.v1`; this fork's list lacks the #19695 turn-item entry upstream's has), the catalog's `agent.launch` row (LOCAL-FILES.md). **Not taken:** `unchecked-rpc-reader-inventory.ts` is Group D's #20950 and does not exist here. Failing-first: with `worktree-create-retry.ts` reverted to the pre-port file, 8 tests across `blank-workspace-create.test.ts` and `source-workspace-create.test.ts` fail (`expected ['worktree.create'] to deeply equal ['agent.launch']`, and the no-workspace reply throwing instead of returning "Failed to create workspace"); green after. **Recorder re-recorded:** 17 goldens move, the same 17 upstream's commit lists — 5 behaviourally (`tw-capabilities-*` and `matrix-worktree.runtime-capabilities-status.get-1` now project `agentLaunch: false` off the probe; `matrix-worktree.create-retry-worktree.create-1` turns three `TypeError` rejections off an unguarded `result.worktree` read into `{ error: 'Failed to create workspace' }`, the guard the launch route needs because a receipt can arrive without a `worktreeId`) and 12 `settings-*`/`matrix-settings.*` by `adapterSha256` only (the mount adapter gained the member). The other 238 moved `lockfileSha256` alone and were restored to HEAD bytes, as a0fb9f8 did; the wrapper's baseline fence names an upstream sha this repo does not hold, so the two recording drivers were run directly, as 54ee8cb did. **Allowlist ratchet:** `agent.launch` is not on the 1.4.205 fixture and the installed 1.4.205 bundle registers neither the method nor the capability, so the phone never sends it to that desktop. `orca-mobile-rpc-allowlist.test.ts` gains a fourth exception kind, `capability`, checked on the real pieces rather than a marker: the gate file must read the capability through its `protocol-version` constant (`capabilities.includes(AGENT_LAUNCH_RUNTIME_CAPABILITY)`; a copied string fails it, proven by ablation), and the downgrade reader must accept the gate's verbatim refusal and `method_not_found` while refusing `invalid_argument` |
| 4b87bc718 #20999 | redefine the `agent.launch` contract | **done, verbatim where it reaches.** Contract only, redefined in place because the method had no shipped client yet: the capability becomes `agent.launch.v2`, a launch warning lives at the top level of the receipt only, the prompt receipt becomes an outcome union (`journaled` / `handed-to-terminal` / `not-delivered`), the dead `customization` field goes, and the mode-reason union is declared once in shared. `agent-launch-intent.ts` re-vendored at 4b87bc718; `agent-launch-worktree-create.ts` + test applied verbatim (the reader drops the nested-warning fallback, and the test its two cases for it); `protocol-version.ts` hand-bumped to `v2` with upstream's comment on the d07c47593 pin. The `operation.id` / `clientOperationId` params half is `src/main/runtime/rpc/methods/agent-launch-schemas.ts`, host code; it reaches the shared params file in #21106. Refactor oracle: the touched tests are green and no golden moved (recorder 321 passed, 3 skipped) |
| 0bf815a48 #21106 | make a lost launch safe to retry | **done, the reachable half.** The host admits `agent.launch` through its durable operation ledger when the caller names the launch with `operationId`, and advertises `agent.launch.replay.v1` so a client may only treat a retry as safe once told. Taken, because #21137 reads them from the phone: `agent-launch-intent.ts` re-vendored at 0bf815a48 (`isAgentLaunchResult` and its three narrowers), `rpc-contract/agent-launch-params.ts` verbatim (optional `operationId`, refined through `parseAgentSessionOperationTimestamp`), and `AGENT_LAUNCH_REPLAY_RUNTIME_CAPABILITY` hand-applied on `protocol-version.ts`'s d07c47593 pin. **Not taken, with the grep:** `agent-launch-operation.ts` (new), `agent-session-operation-ledger.ts` (`claimAgentSessionOperation`, monotone settlement, the `launch` payload), `agent-session-mutation-envelope.ts` (`canonicalAgentSessionDigest`) and the `agent_session_operation_unknown` member of `AGENT_SESSION_RPC_ERROR_CODES` in `agent-session-host-authority.ts` — `grep -rln "agent-session-operation-ledger\|agent-session-mutation-envelope\|agent-launch-operation" mobile/src mobile/app` is empty, and `AGENT_SESSION_RPC_ERROR_CODES` has no mobile reader (`mobile-structured-agent-session-rpc.ts:143` compares the literal against a string, so the union does not reach it). The ledger, the claim and the handler are `src/main/`, host code. No mobile behaviour changes here: shipped mobile sends no `operationId` until #21137 |
| abc8386e1 #21137 | name a create's launch so a lost reply cannot build two workspaces | **done, mostly verbatim; ported after #19849/#19850/#20999/#21106, which it needs, rather than in the backlog table's slot.** Mobile retries a lost create by design, and a retried `agent.launch` built a second agent in a second workspace because no client sent the `operationId` the host's ledger admits. The retry loop now mints one durable id per create candidate (beside `clientMutationId`, never re-minted inside a candidate: the fingerprint folds the workspace name, so carrying one id across a collision bump would refuse `agent_session_operation_conflict`) and sends it through the distinct `agent.launchReplay` method whenever the probe saw `agent.launch.replay-required.v1`; a host that advertises `agent.launch` without the ledger gets the unnamed call as before. **Verbatim (matched upstream's parent):** `worktree-create-retry.ts`, `agent-launch-worktree-create.ts`, `worktree-create-capability.ts` + test (`agentLaunch` becomes `{ replay } | false`), `mobile-workspace-create-operations.ts` (`agentLaunchReplayRun`, a checked `rpcResultVariant` reader over `isAgentLaunchResult`), `blank-workspace-create.ts` + test, `source-workspace-create.ts` + test, `use-new-workspace-create-submit.ts`, `rpc-contract/agent-launch-params.ts` (`AgentLaunchReplay`); **new:** `session/structured-session-operation-id.ts` (the mint, in its own module so `tasks/` need not import the native-chat write graph) and `worktree-create-launch-operation.test.ts`. **Hand-merged:** `mobile-structured-agent-session-rpc.ts` and `mobile-structured-agent-session-launch.ts` (both diverged here; only the two mint functions move out to the new module — this fork's `structuredSessionOperationId()` had no `now` parameter, the shared mint already accepted one), `protocol-version.ts` (`AGENT_LAUNCH_REPLAY_REQUIRED_RUNTIME_CAPABILITY`, hand-applied on the pin), the catalog row. **One fork-local extraction:** the replay route's inline `method_not_found || forbidden || agent_launch_replay_unsupported` downgrade test became `isAgentLaunchReplayUnsupportedRefusal` in `agent-launch-worktree-create.ts`, behaviour-identical, so the allowlist ratchet's second `capability` exception (`agent.launchReplay`, gated on the replay-required capability) checks the reader the loop really uses. **Not taken:** `agent-launch-mobile-replay.test.ts` and `mobile-agent-launch-architecture.test.ts` mount the real host (`src/main/runtime/rpc/methods/agent-launch`, `AgentSessionRecordStore`, the dispatcher) under the phone's retry loop; this fork vendors no `src/main/`, so neither can run here — the end-to-end claim (one tap, one workspace, across a cutover and a legacy replacement host) is unproven on this fork and stays so until a host-side harness exists. `new-workspace/worktree-create-collision.ts` is only those tests' import. Failing-first: with `worktree-create-retry.ts` reverted to the pre-port file, 12 of `worktree-create-launch-operation.test.ts`'s 15 cases fail (no operation id minted, no `agent.launchReplay` sent, the name search restarted after a collision); green after. **Recorder:** the module loader is handed the real `zod` — `mobile-workspace-create-operations.ts` now builds a schema at module load, and the fork's loader (which predates upstream's substitute table from Group D's #20667) answered every non-relative import with a throwing proxy, so all seven `tw-create-retry-*` scenarios failed to send at all. That is a recorder-engine edit, so `recorderSha256` moved and all 255 goldens were re-recorded (headers only; `recording` and `values` byte-identical in every one — upstream's own invariant for this commit, which moves no golden). The 238 that still carried the pre-#19850 `lockfileSha256` picked up the current one on the way |
| 889c2b562 #21566 | say why a host is unreachable instead of "Connecting via Relay…" | **done, merged; 827 upstream files of which 788 are goldens this fork never copies.** The home host row and the host header said "Connecting via Relay…" for as long as the desktop stayed unreachable, even when every relay dial had ended with the cell's 4404 host-offline close (a user's export: 25 such dials over 25 hours), and the diagnostics report said "No single failure cause" because relay dial failures were not recognised and every app resume emptied the evidence window. Relay close codes now map to a closed `RelayHostReachability` verdict (`signed-out` / `host-offline` / `credential-refused` / `unreachable`, else `connecting`), latched after two consecutive identical dial failures and cleared only by an authenticated session; the old signed-out boolean becomes a member of it; `classifyConnection` renders each verdict as a label plus a detail line ("Host 1 is offline" / "Check it's awake, Orca is running, and you're signed in"); a failed dial carries its close code as a structured `relayCloseCode` on the log entry; and the analysis reports evidence from before the last resume or network change with a "Before …" qualifier instead of dropping it. **New (verbatim):** `relay-host-reachability.ts`, `relay-host-reachability-latch.ts`, `relay-recovery-evidence.ts` and their three tests, `connection-diagnostics-relay-dial.test.ts`. **Merged clean by `git merge-file` (upstream's hunks whole, fork drift untouched):** `mobile-relay-reconnect-controller.ts` + test, `stable-logical-rpc-client.ts`, `logical-client-connection-path.ts`, `types.ts`, `mobile-relay-session-establisher.ts`, `mobile-relay-diagnostic-log.ts`, `mobile-relay-recovery-log.ts`, `relay-credential-eligibility.ts`, `client-context-connection-metrics.ts`, `client-context.test.ts`, `relay-host-signed-out-*.test.ts`, `MobileHostCard.test.ts`, `home-host-connection-projection.test.ts`. **Hand-resolved (13 conflicts), every one against this fork's liveness probe or chrome:** the context contract, its state, `use-all-host-clients.ts` and the two test fakes swap `isHostSignedOut` for `getRelayHostReachability` and keep `isLivenessProbing`; `home-host-connection-projection.ts` takes upstream's one-record shape (`HomeHostConnections`) with `livenessProbing` as one more field on the entry, and `MobileHomeHostList.tsx` / `MobileHomeScreen.tsx` / `use-mobile-home-data.ts` read it off `connection?.livenessProbing`; `connection-health.ts` keeps the "Checking…" branch inside `connected` and adds upstream's verdict block below it; `MobileHostCard.tsx` adds the verdict's detail line as one more themed `Txt` caption under the fork's chrome; `host-screen-header.tsx` passes `hostName`. **`connection-diagnostics-analysis.ts` is a real merge, not a pick:** upstream's structure (`selectDiagnosticFailure` with the session/resume split and stale flag, `diagnoseFailure`, relay-first `newestFailure`) is taken whole; this fork's 401-over-503 rule (`hasRejectedCredential`, over the run the selected failure came from) sits at the top of `diagnoseFailure`; and the relay-dial reader is upstream's structured one with the fork's close-code copy folded in — the code is read from `relayCloseCode` when the log wrote one and from `relay_outer_NNNN` text otherwise (older exports), 4503 keeps the fork's "the Relay service itself" copy and its `orca-relay` reportability, the 503-upgrade text keeps its copy, 4404 and 4401 take upstream's (they pass both suites and match the host row's detail line), 4403 routes to the credential copy, 1006 keeps the fork's "either this phone cannot reach the Relay, or the Relay is down" line — upstream's `blames the phone's connection on a transport close` case is re-pinned to it, with the reason in the test — and the text fallback is gated to entries with no code at all, so a session close naming `relay_outer_4408` in prose reaches its own branch (upstream's ordering test caught the fork's old regex taking it). The fork-only `mobile-relay-diagnostic-log.test.ts` gains the two e2ee mocks upstream's latch test uses, because the log now reads `RelayOuterError` off the link. Failing-first: with `connection-health.ts` reverted to the pre-port file, 6 of `relay-host-reachability-verdict.test.ts` fail ("Connecting via Relay…" where the verdict names the offline desktop); green after. **Not taken:** `pilot-scenarios.json` (upstream's baseline pin), 788 goldens (fork re-records its own corpus; none moved here — the twelve upstream re-digested belong to screen-mount adapters this fork does not carry), the three Group D screen-mount adapters and `client-context.web.tsx` (Group E). Recorder 321 passed / 3 skipped, unchanged. **Second review (Opus, read-only, 16 differential inputs old-vs-merged):** one real regression and two small ones, each fixed with its own failing-first test in the same pass. (1) The 401-over-503 rule read the *current* window while the selector could pick a *stale* failure, so `[401 ×4, 503, app-resumed]` wrapped "keep Orca open, recovery will retry" in the stale qualifier — `selectDiagnosticFailure` now hands back the run the failure came from and the sweep reads that (`keeps the refused credential ahead of a 503 when both sit before the last resume`). (2) The 401/403 socket-text branch had ended up behind the numeric map, and a real dial entry always carries a close code, so an Android `'403 Forbidden'` upgrade refusal read as the 1006 copy; it now precedes the map like the 503 text does (`reads a 403 in the socket error text as a refused device`). (3) A comment said the liveness row "draws a spinner"; it prints "Checking…". Accepted as deliberate: a live session close carrying 4503 reads "closed unexpectedly" (reportable) until the next dial fails 4503, because upstream's ordering test requires the code gate |
| 85d1ffc07 #20450 | accept enterprise managed GitHub owner logins | **done, verbatim.** GitHub appends `_<shortcode>` to Enterprise Managed User logins, and three drifting owner regexes (main, renderer, mobile) all rejected the underscore, so an EMU user could not pick a project by `owner/N` or URL. New shared `github/owner-slug.ts` + test (pinned 85d1ffc07) is the one source; `tasks/github-project-reference.ts` and its test matched upstream's parent and took the hunk whole (`OWNER_RE` and the shorthand regex now come from shared). Failing-first: the new case `accepts Enterprise Managed User owners` fails on the old regex (`octocat_acme/1` parses to null), green after. The main and renderer halves are not vendored here |
| eabfbaab8 #21293 | drop the unreachable dispose-before-ready notifications arm | **done, merged.** `disposed` is set only on the disposer's first line and the next statement detaches the stream listener in every transport, so the `ready` handler's `if (disposed)` arm could never observe it. Removed here the same way (three lines, with upstream's comment) from this fork's much larger `mobile-notifications.ts` (epoch/watermark catch-up, 289 lines of drift). Upstream's two transport pins applied clean by 3-way merge: `mobile-relay-rpc-streams.test.ts` (matched parent) and `stable-logical-rpc-client.test.ts` (a live/cancelled differential against a physical session with an inert disposer, so only the logical guard can fence the late event). Upstream's two registry-driven notification cases — the real `RpcClientStreamRegistry`, dispose before and after the `ready` reply — are adapted to this fork's catch-up shape (no `requestNotificationCatchup` mock here; the assertion is that no request at all is sent) and live in a new `mobile-notifications-dispose.test.ts`, because the fork's `mobile-notifications.test.ts` sits at oxlint's 800-line ceiling. Refactor oracle: both new cases pass with the arm present (verified by swapping the pre-port file back in) and with it gone; no golden moved |
| c35a47958 #20303 | reuse computed project identity in projection | applied verbatim; `project-host-setup-projection.ts` matched upstream's parent. `createProjectFromRepo` now takes the already-computed `projectId` argument instead of recomputing it a second time via `getProjectId(repo)`. Mobile calls `projectHostSetupProjectionFromRepos` from `NewWorktreeModal.tsx` and `new-workspace-project-targets.ts` |
| b7a1aeda5 #20305 | avoid per-item arrays in journal projection | 3-way merged (`git merge-file`) onto this fork's many local hunks in `structured-agent-session-projection.ts` (Orca #19226, #18765, #19845, #19841, #19230, e80fae0c4's per-item render cache — see LOCAL-FILES.md). `projectStructuredItemsToNativeChat` builds its result with `forEach`/`push` instead of `flatMap` allocating a one-or-zero-element array per item; none of the local hunks touch that function, so the merge applied clean. Mobile reaches it through `mobile-structured-transcript-projection-reuse.test.ts`'s render-cache reuse guard |
| 0d4d059ae #20321 | stop building chat diff sections beyond preview limit | applied verbatim; `native-chat-diff.ts` matched upstream's parent (no drift since its ddc5b75ac vendor pin — see UPSTREAM.txt). The internal `patchTextFromToolInput` now breaks out of its section loop once the joined length passes `MAX_DIFF_CHARS`, instead of building every section through `flatMap` first and truncating after. Reachable through the exported `diffFromToolCall`, which `MobileNativeChatDiffCard.tsx` calls. Upstream's own `src/renderer/.../native-chat-diff.test.ts` addition is desktop-only and not vendored |
| 13c0c8b53 #20334 | select matching model IDs without sorting | 3-way merged (`git merge-file`); `native-chat-session-option-state.ts` carries this fork's local hunk (LOCAL-FILES.md: carry a reported-model's own non-reported option picks onto the newly reported model) below where this commit's hunk lands, so both merged clean. `matchNativeChatCatalogModelId` now walks `catalog.models` once, tracking the longest containing id, instead of sorting a copy of the array by id length on every call. `native-chat-session-option-state.test.ts` was byte-identical to upstream's parent; its new case taken verbatim |
| dcc1f9012 #20343 | count bot comments without a filtered array | applied verbatim; `pr-comment-audience.ts` matched upstream's parent (this file is at the vendor base, unlisted in UPSTREAM.txt because it was never re-vendored past it). `getPRCommentAudienceCounts` now counts with a `forEach` instead of `.filter(...).length`. Mobile calls it from `PRCommentsSection.tsx` |
| fbab61ec0 #20329 | skip unclosed suffixes when stripping review markdown tags | 3-way merged (`git merge-file`); `mobile/src/components/pr-sidebar/markdown-blocks.ts` carries two fork-local features upstream's parent lacks (a wrapped-list-item continuation join, and the per-cell table-delimiter check from #20317's port) — neither overlaps `stripHtmlTags`, so the merge applied clean. `stripHtmlTags` now regex-replaces only the prefix up to the text's last `>`, leaving any unclosed `<a ` suffix untouched instead of retrying the tag pattern against a suffix no tag can close in. New `markdown-tag-stripping-performance.test.ts` taken whole (a `runInNewContext` 250ms budget over a 20,000-token unclosed run, plus a differential fuzz against the original always-retry regex) |
| 2d1bd1eb4 #20332 | bound whitespace normalization for tool previews | `native-chat-tool-summary.ts` 3-way merged (`git merge-file`, base = upstream's parent, other = upstream's new): this fork sits one commit behind that parent because #20328's `countToolCalls` rewrite was skipped as unreachable (see Skipped), and that region does not overlap this commit's hunks, so the merge carried both changes without a conflict. New `native-chat-tool-preview-prefix.ts` (`collapsedToolInputPrefix`/`MAX_TOOL_PREVIEW_LENGTH`: a single bounded scan with a sticky-regex whitespace matcher, instead of `.replace(/\s+/g,' ').trim()` over the whole string) and its `native-chat-tool-preview-scan.test.ts` vendored whole; `summarizeToolInput`, `normalizedToolInputHasDetail` and `summarizeToolPath` now call the shared helper. Upstream's `config/scripts/tool-preview-whitespace-benchmark.mjs` was not taken — this fork carries no `config/scripts/` (same precedent as #20224) |
| f107499e4 #20786 | fix(lint): enable anti-slop/no-reflect-get | **code only, rule skipped.** This fork has no `config/oxlint-anti-slop.json` and no `config/oxlint-plugins/anti-slop/` directory — the `anti-slop` plugin does not exist here at all, so "enable the rule" has nothing to enable; recorded as skipped for that reason. The three mobile-reachable files this commit's fix touches were byte-identical to upstream's parent and applied verbatim: `settings-read-operations.ts` gained a named `settingsField(settings, key)` reader (boxes once, indexes a `Record<string, unknown>`) replacing two `Reflect.get(Object(settings), '...')` calls (for `prBotAuthorOverrides` and this fork's own `terminalCopyTrimsGutter`, ported at #20545 above), and the two `mobile/src/test-support/rpc-recording/` mutation-harness files (`mutants/operation-mutations.ts`, `recording-runner.test.ts`) update their anchor text to match, so the `bot-overrides-envelope` mutant still finds exactly one site |
| bfdec2635 #20781 | fix(lint): enable anti-slop/no-object-parameters | **code only, rule skipped** (same anti-slop absence as #20786). The six mobile files' `object`-typed parameters became named identity types (`Record<string, never>`, a branded `unique symbol`, or a real domain type), which this fork's local hunks in four of the six files did not touch, so `git merge-file` applied clean: `connection-diagnostics-screen-data.ts` and `host-client-acquisition-registry.ts` were byte-identical to upstream's parent (applied verbatim); `mobile-terminal-viewport-resubscribe.ts` (this fork's `emptySnapshotNeedsResubscribe`), `direct-rpc-client.ts` (this fork's liveness-foreground-profile arrow methods and the `deliveryUnknown`/`stateAfterClose` local hunks), `relay-dial-stage.ts` (this fork's `RelayDialTimings`/`formatRelayDialTimings`) and `rpc-session-liveness-watchdog.ts` (this fork's `isProbing`/`onProbingChange`/`setProfile`) 3-way merged. One follow-up fix this port required that upstream's own diff didn't touch: `app/connection-log.tsx` declared its own inline `routeKey: object` / `{ hostId, requestedHostId, routeKey: object }` shape instead of importing the screen-data module's types, so tsc refused `object` against the new `DiagnosticsRouteKey`; replaced with the exported `DiagnosticsHostSelection`/`DiagnosticsRouteKey` types directly |
| 231e805b1 #20785 | fix(lint): enable anti-slop/no-shape-in-symbol-names | **code only, rule skipped** (same anti-slop absence as #20786/#20781). The mechanical renames — nothing wire-visible, no exported symbol's name a caller keys off — were applied wherever they touch a file mobile reaches: `mobile/src/components/MobileRepoIcon.tsx` (a comment-only exemption note for lucide's own `Shapes` export) and `mobile/src/tasks/use-mobile-tasks-provider-view-projection.test.tsx` (`shape()` → `summarizeSections()`, its local test helper) applied verbatim, byte-identical to upstream's parent. Shared: `native-chat-ask.ts` (`parseQuestionsShape` → `parseCanonicalQuestionsInput`, internal, not exported) and the three RPC-contract type helpers `repo-update-params.ts`/`rpc-send-params.ts`/`ui-update-value-tolerance-params.ts` (`ZodRawShape` generic constraints spelled out as `Readonly<Record<string, z.ZodType>>`, internal type-parameter renames only) applied verbatim. `agent-session-record.ts` 3-way merged onto this fork's local `rewind`/`conversationName` fields (from the rewind port, ce4a3a418 #19235) — the renamed local `shapeValid` → `fieldsValid` variable sits inside `isAgentSessionRecord`'s validation chain, which the local hunk only extends with two more conjuncts, so the merge applied clean |
| 40b223050 #21298 | typecheck the test files on a ratchet, and pin the reply enums where tsc looks | **done, ratchet infrastructure only; the enum-pin half is skipped.** Ported: `mobile/tsconfig.test.json` (puts test files back into a tsc program, `typecheck:tests`) and `mobile/scripts/check-tests-typecheck-ratchet.mjs` + its test, both self-contained tooling with no product-specific logic — the `TESTS_OUTSIDE_PROGRAM` map was set to empty rather than upstream's four Node-side file names, none of which exist here (`grep -rl "from '.*/src/main\|shared/child-process'" mobile/src mobile/scripts | grep '\.test\.ts'` returns nothing), and `tsconfig.test.json`'s `exclude` matches (just `node_modules`) so the two stay in sync, which the vendored test itself verifies. **Baseline regenerated for this fork, not copied from upstream** (per the port brief): `node scripts/check-tests-typecheck-ratchet.mjs --init` against this fork's own 904 test files produced 197 grandfathered entries (upstream's is 126, a different number for a different test suite). One census failure the `--init` run surfaced and fixed: `MobileHostCard.test.tsx` was shadowed by `MobileHostCard.test.ts` the same way upstream's was (a wildcard `include` keeps only the higher-priority extension) — renamed to `MobileHostCard-truthful-status.test.tsx`, the same name upstream chose, since its own content (never asserting a count nothing proved) fits. Pulled forward from a **later** commit in this same group, f949d5fcc #21156 (confirmed an ancestor of 40b223050 in upstream's actual history, despite the backlog listing it after): `vitest.config.ts`'s `include` gained `'scripts/**/*.test.ts'`, 3-way merged onto this fork's local `resolve.alias`/`testTimeout`/`exclude` hunks — without it the new ratchet test would typecheck but never actually run under vitest. `mobile/package.json` gained the two scripts (`typecheck:tests`, `check:tests-typecheck`), byte-identical hunk. `mobile/README.md`'s Validation section describes the same mechanism with this fork's own 197 count instead of upstream's 127. **Skipped:** `src/shared/zod-salvage.ts`'s `openEnum`/`hostUnionArms` refinement and the five schema files it feeds (`new-workspace-reply-schema.ts`, `notification-reply-schema.ts`, `task-project-board-reply-schema.ts`, `task-provider-entity-reply-schema.ts`, `workspace-source-reply-schema.ts`) — none of `openEnum`, `hostUnionArms`, or any of those five files exist in this fork at all (confirmed absent; this fork's reply-schema layer for these domains, and the #21269 `hostUnionArms` helper this commit builds on, were never ported here — out of scope for this commit alone). `mobile/src/test-support/rpc-recording/README.md`'s corpus-count correction (368→393 scenarios, 727→778 goldens) also skipped: those are upstream-specific measurements of upstream's own corpus, not this fork's (this fork's own count moves with each RPC-migration port and is tracked in that port's own commit message, e.g. "Goldens 153 → 208"); whether this fork's README carries a similarly stale count is a separate, unaudited question left as a known gap. The 18-test-file mechanical fixes and the reply-schema pin moves were not replicated file-by-file — the ratchet's own baseline already captures whatever pre-existing type debt this fork's tests carry, which is the mechanism's whole purpose |
| 2b34255d9 #20755 | fix(ci): stop defining pilot mutant tests inside a conditional | applied verbatim; `pilot-mutants.test.ts` matched upstream's parent. Each pilot now pairs with its pinned mutant and reference state before the loops, so every iteration defines exactly one `it()` unconditionally instead of a variable count gated on a runtime `if`. Same 14 tests as before (11 mutant-kill, 3 `skipIf`-gated reference tests): 11 passed, 3 skipped, verified |
| f949d5fcc #21156 | ci(mobile): fail CI when the RPC recording pin leaves main's history or the corpus does not reproduce | **mostly skipped; one hunk already landed.** `vitest.config.ts`'s `include` gaining `'scripts/**/*.test.ts'` was pulled forward and applied as part of the #21298 port above (needed there for the new ratchet test to actually run under vitest — recorded on that row). The new `mobile/scripts/rpc-recording-pin-guard.mts` + its test (594 lines) are **skipped**: the guard's whole premise is that `mobile/rpc-foundation/goldens/*.json`'s `baseline` field names a commit reachable in `git log` on this repo's own main branch, so a squash-merge that drops the branch cannot silently orphan the pin. Checked: every golden's `baseline` (e.g. `c6a72169843ececf3a21da370ac50c5c5a4e6462`) is an **upstream Orca** commit sha that `git cat-file -t` confirms does not exist as an object in this repo at all — this fork's recorder inherited the field from vendoring rather than rewriting it to a Code UI commit, and each port's own commit message (not a CI ancestry check) is what actually tracks the baseline moving here (e.g. "baseline `e53f1557…` → `50e752fc…`", #20568's row above). This fork also has no PR-gating GitHub Actions workflow at all — `.github/workflows/` holds only `mobile-android-release.yml`, a tag-triggered release build, with no `pr.yml`/`main.yml` for the guard to run under. Porting the script would add ~600 lines of dead CI tooling with no home and a check that would always fail for the wrong reason (a foreign commit sha) rather than the one it exists to catch |
| cf7408a37 #19750, f31bfa8fb #20023 (revert), fa3d29fa1 #20029 (re-land) | session search query engine over the index | **done, one shared file's worth.** The engine itself (SQLite FTS index, `SessionSearchEngine`, the route ladder, benchmarks) is entirely `src/main/`, not vendored. What reaches the phone: `ai-vault-session-filters.ts`'s `parseVaultQuery` now delegates to a new shared tokenizer, `splitAiVaultSearchQuery` (new `ai-vault-search-query-operators.ts` + test, vendored whole — no external deps), instead of its own hand-rolled `tokenizeQuery` (removed, confirmed unreferenced: `grep -rln 'tokenizeQuery' mobile/ src/` returns nothing). `matchesAiVaultQueryOperators` was extracted so the panel and the (unvendored) engine share one definition of `repo:`/`path:`. Mobile calls `filterAiVaultSessions` → `matchesQuery` → `parseVaultQuery`/`matchesAiVaultQueryOperators` from `agent-history-sections.ts`'s Agent History search box, so this is a real behaviour change reaching the phone: quoted operator values (`path:"/Users/ada/My Project"`), case handling and the repo/path matching semantics now match the desktop exactly. `ai-vault-session-filters.ts` was byte-identical to upstream's parent (cf7408a37^) and to the re-landed fa3d29fa1's version (the revert-then-reland nets to the same file), so applied verbatim. The revert (f31bfa8fb) itself has nothing to port |
| 1f7655f3e #20277, a01027697 #20886 | public session search contract and transports; enable indexing on paired servers from a client | **done, catalog-only.** The session-search panel, settings UI, IPC/relay/web transports and the paired-server indexing toggle are desktop-only (`src/renderer`, `src/main`, `src/preload`, `src/relay`) and were not ported — mobile has no session-search UI. What reaches the phone: `rpc-contract/rpc-params-catalog.generated.ts` gained three `RPC_PARAMS_BY_METHOD` rows (`aiVault.searchSessions`, `aiVault.searchStatus`, `aiVault.setSearchEnabled`), applied verbatim from both commits' isolated hunks (the file was byte-identical to 1f7655f3e's parent; a01027697's own hunk to the same file is a clean, context-independent 2-line addition, taken directly without pulling in two intervening non-group commits — 46ed53b88 and 0d2f7bcea — that also touched files in this chain but are outside this port). Pulled in only because the catalog imports them (same precedent as c84007c54's rpc-contract dependencies): new `ai-vault-search-contract.ts` (hand-assembled from 1f7655f3e's version plus a01027697's isolated `AiVaultSetSearchEnabledParamsSchema` addition — verified as a clean, non-conflicting insert), its test, and new `ai-vault-search-limit.ts`. Both new schemas' dependencies (`AI_VAULT_AGENTS`, `AI_VAULT_SCOPE_PATHS_MAX_COUNT`) already exist in this fork's `ai-vault-types.ts`. This closes a gap `orca-mobile-rpc-allowlist.test.ts` had pinned since 2026-09-18 — the real installed desktop (1.4.205) already allowed these two `aiVault.search*` methods for a mobile-scope token, ahead of what the vendored catalog knew; that test's comment said "re-vendoring the catalog past them shrinks this list", and this port does exactly that (the expected catalog-gap list is now `[]`). Nothing on the phone sends any of the three methods (no session-search UI here), so no RPC allowlist exception was needed |
| 09073086a #19512 | inline terminal images via @xterm/addon-image (perf-first) | **done, one shared file only.** The feature itself — the lazy xterm addon, DA1 Sixel advertisement, Kitty/SIXEL/iTerm2 decode, the Settings toggle — is entirely `src/renderer/`/`src/main/`/`src/relay/` desktop code (an xterm.js addon has no counterpart on a React Native terminal view) and was not ported. `src/shared/terminal-query-reply.ts` applied verbatim (byte-identical to upstream's parent): a new `KITTY_GRAPHICS_PREFIX_RE` recognizes a complete `ESC _G ... OK/E...:...  ESC \` Kitty graphics acknowledgement as a query reply, so it bypasses user-input activity and the query-reply debounce the way a CPR or DECRPM reply already does. Mobile imports `isTerminalQueryReply`/`extractOnlyTerminalQueryReplies`/`needsCookedEchoSafeQueryReply` from this file (`mobile-terminal-query-reply.ts`, `terminal-webview-query-reply-routing.ts`, `use-mobile-session-terminal-input.ts`) to keep a PTY-emitted ack out of the visible transcript even though the phone never renders the image itself. `terminal-query-reply.test.ts`'s new case vendored (not run here). `default-global-settings.ts`/`global-settings-types.ts`/`terminal-image-protocol.ts`/`terminal-inline-images-settings.ts` are desktop-only, mobile imports none of them |
| 55b339201 #20545, #19770 | drop the agent gutter from copied selections | **done, mobile half.** New `src/shared/terminal-selection-gutter.ts` (+ test) vendored whole — `stripTerminalSelectionGutter` measures the run of leading spaces every non-blank selected line shares (CRLF-safe: the carriage return is split off a line before its indent is measured, so a blank CRLF row cannot read as a zero-indent content row and cancel the gutter on Windows) and trims only that. Mobile now mirrors the desktop's "Trim Gutter on Copy" setting instead of always stripping: new `settings-read-operations.ts` export `terminalCopyTrimsGutterRead` (3-way merged around this fork's already-reworded acceptance-comment hunks from #20563/b30debd — an unreachable read, since this fork never had the gutter-strip feature to begin with, so nothing was actually "always on" here before this port), and new `mobile/src/terminal/terminal-copy-gutter-preference.ts` (`useTerminalCopyTrimsGutter`: reads once per connection, defaults to on so an unreachable host or an older desktop still trims). `use-mobile-session-accessory-selection.ts` 3-way merged onto this fork's local accessory-repeat-fired hunk (Orca #12251, unrelated function) — the selection Copy button now trims through the shared helper when the mirrored setting is on. `mobile-session-route-parity.test.ts` re-pinned to **this fork's own computed hashes**, not upstream's (per the port brief): hooks 281 → 282, `HEAD_MAIN_HOOK_SHA256`, `HEAD_HOOK_BINDING_SHA256` and `HEAD_CALLBACK_BODY_SHA256` recomputed from the merged source. Desktop-only pieces not vendored: every terminal-pane/xterm clipboard seam, the Settings UI section, and `default-global-settings.ts`/`global-settings-types.ts` (mobile never imports the global-settings object directly, only through `settings.get`'s RPC projection) |
| f2af92b2f #19705 | show live background work and name each row by kind | **done, the wire and the phone's half.** Upstream's visible change is three `src/main/` trackers (Claude and Codex) that now publish foreground work while a turn is open, plus the desktop strip; the phone already names each row by kind and reads the roster off the wire (484323a), so what it needed was the contract: `stoppable?: boolean` on a task (absent means yes), equality that sees it, and the two capabilities the host gates on. `agent-session-background-task-wire.ts` vendored new at f2af92b2f — upstream split the task types out of `agent-session-wire.ts` there — and `agent-session-wire.ts` took the re-export hunk by 3-way merge (only the import line conflicted, on the rewind import this fork lacks). `remote-runtime-client-capabilities.ts` re-vendored whole at f2af92b2f: its whole base→f2af92b2f history is #19346's and #19705's one-line adds. `protocol-version.ts` hand-applied: `AGENT_SESSION_BACKGROUND_TASK_STOP_CAPABILITY` (#19346, never taken with 484323a — so an upstream host was nulling a Codex roster with `supportsStopAll: false` for this phone) and `AGENT_SESSION_BACKGROUND_TASK_ROW_STOP_CAPABILITY`. Advertising the second is what makes the host send foreground rows at all; `mobile-structured-background-tasks.ts` carries an explicit `stoppable: false` onto the projected row and `MobileBackgroundTasksSheet.tsx` withholds that row's Stop. Five tests written from the symptom, all red on the pre-port code (equality judged a withdrawn stop equal; the reducer dropped the republish; the projection lost the flag; the sheet drew `Stop Explore the repo`; the capability list lacked both), green after; the sheet case runs in both themes. The upstream reducer test is mirrored in `mobile-structured-session-background-tasks.test.ts` because `src/shared/*.test.ts` is never collected here. `docs/mobile-background-tasks.md` says how a row can now be unstoppable |
| 2626e2eca #19695 | make the structured turn lifecycle row durable so completed durations survive | **done, whole for the phone.** The turn record becomes its own journal item (`kind: 'turn'`, revised in place through running → completed / interrupted / unverifiable, never tombstoned) carrying the host's `startedAt`/`completedAt`, the provider's own `durationMs` and the `userItemId` that opened it; every frame carries `hostNow`, so a client attaching mid-turn anchors its live counter on the real start, and a settled "Worked for" comes off the journal instead of what this phone happened to clock. A host publishes the typed item only to a client advertising `agent-session.turn-item.v1`; the phone now does. **Shared, new and verbatim at 2626e2eca:** `agent-session-turn-record.ts` (one reader for both journal shapes), `agent-turn-lifecycle-text.ts`, `structured-agent-session-turn-timing.ts` (settled-turn selectors, the skew-free local anchor), `agent-session-background-task-state-equality.ts` (the reducer's roster comparator, moved out), and their four test files; `remote-runtime-client-capabilities.ts` re-vendored whole. **Merged** (3-way onto the local hunks, see LOCAL-FILES.md): `agent-session-journal-types.ts`, `agent-session-journal-schemas.ts`, `structured-agent-session-projection.ts`, `structured-agent-session-reducer.ts`, `agent-session-wire.ts` (`hostNow`), `protocol-version.ts` (the capability, hand-applied). **Not taken:** `agent-session-wire-refusals.ts` — the split exists for upstream's 300-line cap and imports `agent-session-rewind.ts`, which this fork does not vendor; the refusal codes stay inline in `agent-session-wire.ts`. **Completed on the way:** `structured-agent-session-live-turn.ts` and its test, partial since #19977 for want of the typed item, are now vendored whole at fab78c766 and the projection re-exports its two selectors, as upstream's does. **Mobile:** `use-mobile-structured-agent-turn-timing.ts` + test new verbatim; `use-mobile-structured-agent-state.ts` and `use-mobile-native-chat-turn-disclosure.test.tsx` verbatim (both matched upstream's parent); `use-mobile-native-chat-turn-status.ts` re-vendored at fab78c766, which is exactly this fork's copy plus the #19695 hunk; the disclosure hook, view props, view, overlay, both controller contracts, the controller and the structured session hook hand-threaded (`workingStartedAt`, `settledTurns`), keeping this fork's `thinking`/`turnActivity` props beside them. Upstream's extraction of the composer-lock settle hook into the lease file was skipped — this fork had already moved it to `use-mobile-native-chat-input-lock.ts` — and its new test came over adapted as `use-mobile-native-chat-input-lock.test.ts`. The fork-local `mobile-native-chat-turn-activity.ts` now finds the turn start through `readAgentJournalTurn`, so a typed row bounds the scan. **Failing-first:** reverting the twenty modified source files fails 8 tests — the two upstream disclosure cases, the capability pin, and five in the new `mobile-structured-session-turn-record.test.ts` (a typed row is not the live turn; it paints as a bubble; the previous turn's activity copy leaks into the live one; the reasoning check runs past it; no `hostClock`). Green after. Upstream's reducer/projection/turn-record/turn-timing suites are vendored but never collected here, so that file mirrors them. One fork-local fixture corrected: `mobile-structured-older-page-hole.test.ts` fed items with no `body`, which the reducer's submission retention now reads. `MobileNativeChatView.tsx` ceiling 410 → 412 for the two threaded props. Gate: tsc clean; vitest 904 files / 7864 passed / 17 skipped; oxlint 0 errors |
| 027acb4ef #19863 | settle a structured send on admission, not on the provider echo | **done, the phone's half.** Upstream's defect was the desktop outbox: the host held `agentSession.send`'s reply up to 10 s for the provider to echo the message, an echo that cannot come until the turn ahead ends, and the renderer read the timeout as "delivery unconfirmed" with a Retry that re-delivered (one message reached the model five times). The fix's dispatch and outbox halves are `src/main/` and `src/renderer/`; this phone never read the reply's dispatch state — an `ok` is "accepted" and its own pending-echo system draws the bubble until the journal's submission row lands — so it never showed the false banner. What it did pay was the wait: a mobile-scope client without `agent-session.pending-send-result.v1` still went through `waitForSendSettlement`, up to 10 s against this phone's 15 s send budget over the relay. The capability is now advertised (`protocol-version.ts` hand-applied, incl. the Electron list line for fidelity; `mobile-runtime-client-capabilities.ts`), so the host answers as soon as the message is written. `structured-agent-session-outbox.ts` re-vendored whole (it matched upstream's parent; the new `pending` arm of `reconcileStructuredAgentSessionOutbox` has no caller here), the projection's comment hunk merged, `rpc-client-capabilities.test.ts` verbatim — red before (the capability was not on the wire), green after. **Not taken:** `structured-agent-session-send-disposition.ts`, the renderer outbox's pure send-result classifier; nothing under `mobile/` imports the outbox reconciler or would import this (`grep -rn "reconcileStructuredAgentSessionOutbox\|SendDisposition" mobile/src` → 0). The Codex 10 s stopwatch upstream names as debt is host-side |
| 9b83f976f #19928 | describe slash commands from the provider's own report | **done.** The Claude session reports a description and argument hint for every command it can run; the catalog kept only the name, so the `/` menu described the handful the curated map covers and left the rest — `/goal` included — blank. The producer is `src/main/claude/claude-slash-command-catalog.ts` (not vendored; a host past this commit sends the fields). Shared: `AgentSessionSlashCommand.description` / `argumentHint` on `agent-session-wire.ts` and the `sessionSlashCommandSuggestions` change on `native-chat-slash-commands.ts` (a reported description wins, the curated one is the fallback for a names-only report; the hint carried through), both hand-applied — both files carry local hunks — and the test's two new cases by 3-way merge. Mobile: `MobileNativeChatComposerSuggestions.tsx` shows the hint beside the token in the muted mono tier, capped at 80 characters with whitespace collapsed, the same bound upstream's `native-chat-picker-items.ts` applies; the phone's own row already drew the description. Four tests, all red on the pre-port code: the two upstream shared cases mirrored in `mobile-native-chat-session-catalog.test.ts` (with this fork's own `/compact` text, since the curated catalog here is Claude Code 2.1.261's) and a new `MobileNativeChatComposerSuggestions.test.tsx` that renders the row in light and dark and pins the cap |
| 8999a0028 #20133, with e7206f62a #20868 folded in | give each structured dispatch state exactly one meaning | **done, whole for the phone.** `unknown` meant five things; now `pending` is written-and-awaiting, `accepted` the provider has it, `rejected` provably did not happen, `unknown` genuinely cannot tell — and Retry never re-delivers an `unknown`. The mobile half is the larger one: a send's operation id was released whenever the reply came back `unknown`, so the user's next send of the same text went out under a fresh id the host had no ledger row for — a second delivery of the one message the host might already hold — and a replayed `unknown` came back `ok`, which the phone called "accepted" and cleared the composer on. The send path now lives in `mobile-structured-agent-session-send.ts`: the id is kept in a durable AsyncStorage journal keyed by session and intent (`mobile-structured-send-operation-journal.ts`, bounded at the host's 4 096-row ledger limit and fail-closed at capacity), `mobile-structured-send-delivery.ts` reads the reply's `dispatchState` in one pure function (accepted/pending spend the id; rejected spends it and shows `structuredAgentSessionRejectionNotice` copy instead of an internal `provider_write_failed: …` token; unknown keeps it), and `use-mobile-structured-send-operation-reconciliation.ts` clears an ack-lost id once the journal settles its submission. `retryUnknown` is gone from the wire. Transport: a relay request is marked delivery-unknown only once its frame was written (`relay-pending-requests.ts` `written`), the direct client settles a correlated `unauthorized` before marking the rest unknown, and the stable logical client resolves a correlated response even when close or cutover wins the callback race (this fork's error-arm cutover wrap is kept). An uploaded chip carries a `contentFingerprint` so a re-pick of the same bytes is the same send. **Verbatim** (matched upstream's parent, or new): `mobile-structured-agent-session-rpc.ts`, the five new session modules and their three tests plus the send-result fixture, `use-mobile-structured-agent-session.test.tsx`, `mobile-structured-session-operation-retention.test.ts`, `use-mobile-structured-native-chat-send-bridge.ts`, `relay-pending-requests.ts`; shared `agent-session-host-authority.ts`, `structured-agent-session-mutation.ts` + test, `structured-agent-session-outbox.ts`, and new `structured-agent-session-dispatch-rejection.ts` and `structured-agent-session-send-disposition.ts` + test (the latter skipped at #19863 as unreachable; `mobile-structured-send-delivery.ts` imports its notice now). **Merged** by 3-way: the controller (`callerIdentity` from the device token), the session lane, both attachment hooks, the chip type (beside this fork's `kind`/`name`/`uploading`/`batch`), the session hook (its send arm replaced by the new module; the fork's rewind, cancel, background-task and `/` catalog wiring untouched), the composer-command test, the direct and relay clients and their tests. **#20868 folded in:** #20133 maps a cancel's `agent_session_operation_unknown` to `unknown`, and this fork's `mobile-structured-cancel-id-retirement.test.ts` — written against upstream's #20868 before the defect could reach here — went red on the port exactly as upstream's did; the `hostReportedOperationUnknown` flag and the cancel dispatcher's condition are #20868's hunks on this fork's `mobile-structured-agent-cancel.ts` (upstream's `mobile-structured-agent-session-cancel.ts` and its test did not exist here until the #20601 port below took both). **Failing-first:** reverting the 17 modified source files fails 28 tests across 8 files — the ack-loss id kept across two `unknown` replays, the journal's persistence/capacity/identity guards, the composer command retaining its id after a host failure, the chip fingerprint, the relay's pre-write failure staying definite, the direct client's auth-recovery marking, the stable client's committed response, and the cancel retirement. Green after. `use-mobile-native-chat-image-attachments.test.ts` ceiling 800 → 806 (this fork's copy carries its own batch and document cases). Gate: tsc clean; vitest 908 files / 7902 passed / 17 skipped; oxlint 0 errors **Fork-local divergence after the second review (2026-09-19):** an unreadable send-operation journal (not JSON, wrong shape, malformed id) starts over with a warning instead of refusing every later send; upstream's fail-closed test for that case is replaced by `starts over from an unreadable journal…`. A transient storage read/write error still fails closed, as upstream pins. Also fork-local: `useMobileStructuredAgentTurnTiming` hands the settled-turns map on only when its content changed, so settled rows stop re-rendering per token batch |
| c287a5d9b #20506 | provider-aware Fast mode | **done, the phone's half.** The producers — Claude's `list_models` support check and the `fastModeState` frame, Codex's `serviceTier` routing — are `src/main/`; a host past this commit reports `fastModeSupport`, `supportsFastMode` per model and `current.fastMode`/`fastModeState`, and the phone now draws and sets it. Shared: `structured-agent-session-option-codec.ts` (new, the one boundary that writes the boolean as `'true'`/`'false'` on the string-valued wire), `structured-agent-session-options.ts` (the Fast row appears only when the session AND the model say so; a boolean pick is admissible; a `fastMode` the host stops reporting is cleared), `native-chat-session-options.ts` (`currentValue` required on the boolean arm, `sessionOptionValueMarker`), `native-chat-session-option-defaults.ts` (`fastMode` joins the launch seed through the codec) — all four re-vendored whole with their tests, the defaults file moving past 852495d35's pure `narrowStructuredLaunchSeedOptions` extraction on the way; `agent-session-wire.ts` and `native-chat-session-option-snapshot.ts` took their hunks by 3-way merge onto the local ones. Mobile: `use-mobile-structured-agent-options.ts` merged (generation fencing, the pending-write guard, the post-write options refresh, the codec on the wire; this fork's rewind-support read kept beside it), `mobile-native-chat-session-option-labels.ts` merged (a boolean summary says On/Off and lets the sheet's marker carry provenance), and `MobileNativeChatSessionOptionRows.tsx` hand-written: upstream's On/Off radio pair becomes one `Switch` row that owns its label with a `Default` / `Not reported` marker, drawn from this fork's theme rather than upstream's static palette (the track and thumb take the palette every other switch in the app uses). Upstream's two new mobile tests came over verbatim (`MobileNativeChatSessionOptionRows.test.ts`, `use-mobile-structured-agent-options.test.ts`, 398 lines pinning the hook's concurrency contract and the boolean round trip); `MobileNativeChatSessionOptionRows.theme.test.tsx` is this fork's, pinning the row's colours in light and dark and the flip reaching the setter. **Failing-first:** reverting the eight modified source files fails 12 tests across the three files (one switch instead of two radios, the marker's two arms, the resolved value on the switch, the theme colours, the boolean round trip on the wire, the refresh after an accepted write and its fence gate, the superseded read, the overlapping-write refusal). Green after. Not verified against a live host: no desktop here is past c287a5d9b, so the Fast row has not been watched appearing; a host that reports nothing draws nothing, which is the pre-port view |
| 4634d2c03 #20518, a4c11f188 #20581 | let the provider reopen a Claude turn it resumed itself; stop a bounded tail read from moving the chat cursor past unapplied rows | **done as alignment; neither defect reaches the phone.** #20518 is `src/main/claude/` (a lifecycle row may key itself when provider output opened a turn with no user item); its one shared line is the `userItemId` doc comment on `AgentJournalTurnLifecycle`, merged, and the phone's `selectStructuredAgentTurnTimings` already accepts a row keyed to its own id (`itemIds.has(key)`). #20581's symptom — a pane latched "Working for N" after the agent finished — came from the desktop's `tail-page` refresh on window focus, a third reducer operation that took the cursor from the journal head and the items from a bounded page, so a send's `pending` submission survived past the acceptance row that would have settled it. **This phone never dispatched `tail-page`**: `use-mobile-structured-agent-state.ts` moves the cursor only through the live subscription and reads history only as `older-page` (`grep -rn "tail-page" mobile/src` → tests only). The reducer hunk is merged anyway so the vendored file tracks upstream: `history-page` replaces the page whole (roster, activity and catalog carried from state), `replacePage` takes a nullable fence, `shouldAdvanceStructuredResumeCursor` is gone (0 callers here). The fork's three reducer mirror tests that used `tail-page` to prove state survives a refresh now say `history-page` and still hold. The vendored `structured-agent-session-reducer.test.ts` is left at its pre-#20581 shape — seven conflicting hunks on a file this fork never collects. No failing-first test: nothing changed that a phone test can observe; the four mirror files stay green, which is the refactor's oracle |
| c6a721698 #20496 | hide activity while awaiting input | **done, adapted.** While a structured prompt (approval, question, ask) waits on the user, the live turn's "Working for N" row said the agent was working beside a card that said it was waiting. Upstream gates its list footer on `!hasPendingStructuredInteraction`; this fork draws the live row under the user message (and in the list header when unanchored), so the gate went where both readers get it — `awaitingInput` on `use-mobile-native-chat-turn-disclosure.ts` nulls `active` and the row's `turnStatus` while leaving `activeTurnIsWorking`, the timing state and Stop untouched. Withheld, not settled: the clock keeps counting and the row returns when the prompt resolves. The view computes the flag exactly as upstream (`structuredActivityUi && (ask ?? permission ?? question) != null`). Upstream's three-card test (`ChatAsk`, `ChatQuestion`, `ChatPermission`) came over adapted to this fork's row-level assertions, red on the pre-port code (three cases: the row still said working), green after. The commit's other twelve files are `src/main/codex/` prompt-registry settlement and the desktop renderer, not vendored. `MobileNativeChatView.tsx` ceiling 412 → 413 |
| 59d29af40 #20655 | a verified OMP native-chat mock scenario | **done, verbatim.** Both files matched upstream's parent byte for byte: `mobile/scripts/mock-server-native-chat-scenario.ts` gains `MOCK_CHAT_AGENT=omp` (an OMP tab with no `transcriptPath`, to exercise legacy-hook readability discovery, and four decoded transcript messages including a tool call and its result), and `mobile/README.md` documents the variable. Dev tooling only — the mock server is not part of the app or the gate; tsc and oxlint clean on the script |
| f55b7ba68 #20601, with 955051ded #20138's shared line | cancel pending prompts precisely | **done, whole for the phone.** A structured approval or question card gets an X that cancels the prompt itself: on a host advertising `agent-session.prompt-cancel.v1` the cancel names the item (`prompt: { itemId, expectedRevision }` on `agentSession.cancel`, a strict schema an older host would reject whole), and on any other host it degrades to the turn stop it always was. The capability comes off the same runtime probe as the other session surfaces (`use-mobile-session-feedback-capabilities.ts`, `use-mobile-session-tab-reconciliation.ts`, verbatim) and is threaded through the dictation hook, the controller and the session lane to `use-mobile-structured-agent-session.ts`. **New** `mobile-structured-agent-session-cancel.ts`, taken at e7206f62a (#20868) so it carries `hostReportedOperationUnknown` from the start, replaces this fork's own `mobile-structured-agent-cancel.ts`; `mobile-structured-cancel-id-retirement.test.ts` (this fork's #20868 witness) goes with it, and upstream's `mobile-structured-operation-id-retirement.test.ts` takes its place. `mobile-structured-agent-prompts.ts`, `mobile-structured-grouped-question.ts` and `mobile-native-chat-question.ts` verbatim (the projected card now carries its prompt identity); `MobileNativeChatOverlay.tsx`, the controller and its test, the session lane, the controller contract, `agent-session-wire.ts` (`AGENT_SESSION_ID_MAX_LENGTH`) merged; `protocol-version.ts` hand-applied. The cards are this fork's themed ones, so the X was hand-written on `MobileNativeChatPermission.tsx` and `MobileNativeChatQuestion.tsx` (muted glyph from the theme, disabled while a response is in flight), the prompt card and the view props threaded by hand, and upstream's two card tests came over adapted to run in both themes. Shared: `structured-agent-session-dispatch-rejection.ts` re-vendored at f55b7ba68, which also brings **#20138** (955051ded) — `DISPATCH_REJECTED_CODEX_QUEUE_FULL` is internal copy the notice must not print, a one-line shared change the backlog did not list because the file did not exist here when the backlog was cut — and #20601's `DISPATCH_REJECTED_CANCELLED`; `structured-agent-session-outbox.ts`, its disposition test and `rpc-contract/structured-agent-session-params.ts` (the `prompt` field on `CancelParams`) verbatim. **Failing-first:** reverting the 23 modified source files and restoring the old dispatcher fails 6 tests — upstream's four in `use-mobile-structured-agent-session-prompt-cancel.test.tsx` (the identity reaches the wire on a capable host, an old host gets turn-only, a question card cancels by identity, the rendered identity wins over a journal that moved) and the two card tests. Green after. Route-parity re-pinned: hooks 281 → 282, capabilities 5 → 6, and the four hashes upstream's own re-pin moved. Ceilings: the session hook 300 → 307, the dictation hook 300 → 302, the controller 496 → 502 and its test 800 → 805, the view 413 → 414. Gate: tsc clean; vitest 912 files / 7931 passed / 17 skipped; oxlint 0 errors |
| 533b0bd02 #21086 | count a turn from the send that opened it | **done, whole for the phone.** The live indicator switched on at the send but anchored its clock at the provider turn-open, so it climbed to "Working for 25s", reset to "Working for 0s" when the turn opened, and settled "Worked for 26s" — three readings of one turn from two instants (measured upstream on a real Claude session). The host now publishes `requestedAt` on the turn lifecycle row (both providers; omitted when no send can be named); readers take one origin, `requestedAt ?? startedAt`, for the live counter and the settled duration, exact host endpoints outrank the provider's own measure, and the live reducer keeps the earlier anchor so publication order can never run the clock backwards. New `structured-agent-turn-clock-anchor.ts` latches one host-to-local conversion per turn at first sight, so a later, jittered host sample cannot move the anchor later. Shared: `native-chat-turn-status.ts` + test, `structured-agent-session-turn-timing.ts`, the new clock-anchor module + test, all re-vendored whole (each matched upstream's parent); `agent-session-journal-types.ts`, `agent-session-journal-schemas.ts` and the turn-timing test by clean 3-way merge. Mobile: `use-mobile-structured-agent-turn-timing.ts` re-vendored whole (matched the parent) — it now drives the shared latch instead of its own anchor state; its test is unchanged upstream and still passes. **Failing-first:** `mobile-structured-turn-origin.test.ts` mirrors upstream's shared cases where the gate runs them; reverting the five modified files fails three of its four (the settled interval counted from the send, the live anchor on the request, the reducer's no-later rule); the latch case passes either way because the module is new. Green after |
| aad41b1a4 #21087 | render approvals from the harness presentation, not serialized tool input | **done, adapted.** Upstream's symptom had two halves. The unbounded detail — `JSON.stringify(input)` in an element with no height bound, pushing the buttons off the screen — this fork had already fixed on its own card (a reading area capped at 16 % of the window, a diff scroller, and choices that scroll), so that half was already solved here. The other half is new: the Agent SDK's own presentation (`displayName`, `description`, `decisionReason`, `blockedPath`, `matchedAskRule`) threaded through the journal item, so the card says why a request was raised, not only what it was. Shared: `agent-session-journal-types.ts` and `agent-session-journal-schemas.ts` (+ test) by clean 3-way merge onto their local hunks. Mobile: `mobile-structured-agent-prompts.ts` re-vendored whole (matched the parent), `mobile-native-chat-permission.ts` merged (the five optional fields), and `MobileNativeChatPermission.tsx` hand-written — upstream's card is the static-palette one, this fork's is themed and already scrolls, so the harness context is one more bounded scroller above the diff/prose, labelled `Reason:`, `Blocked path:` and `Ask rule:` in the theme's tones. Upstream's `keeps oversized provider context in a bounded scroller above the actions` came over adapted to this card's own bound and runs in both themes; `mobile-structured-agent-prompts-presentation.test.ts` pins the projection carrying every field and carrying nothing extra for an older host. **Failing-first:** reverting the five modified files fails both (the projection dropped the fields; the card had no context block). Green after |
| 68ea3b92e #21151 | stop a collapsed run claiming success when a tool call failed | **done, the half the phone can show.** Upstream's collapsed run drew its completion mark whenever nothing was `running`, so a run whose call `failed` took the mark. This fork's collapsed header (`MobileNativeChatToolRun.tsx`) never drew a completion mark, so the false claim did not exist here; what it shared was the silence — a run with a failed call read exactly like a clean one until expanded. The new `native-chat-tool-run-outcome.ts` (+ test) is vendored whole, `native-chat-tool-activity.ts` re-vendored for its two copy entries (it matched the parent), and the phone's header now says `N failed` in its muted member-arg type with a spoken `Failed tool calls: N`, counted over every call by the same composite test (a call's own `failed` verdict or an error result, a structured lane's two signals for one failure counted once). Text only, no danger tint, as upstream. Three tests on the header, red before (no mark at all), green after: the count in light and in dark, a legacy lane with no lifecycle state, and silence on a clean run |
| e42f7c00b #21090 | render a proposed plan as a plan, not a generic approval | **done, adapted.** A finished plan arrives as an ExitPlanMode approval whose detail is the tool input — thousands of characters of escaped JSON. The host now classifies it and carries a typed `subject: { kind: 'plan', text, filePath? }` on the approval item (the approval kind is kept on purpose: this phone filters pending approvals on it, and a new kind would have made the prompt vanish here silently). Shared: `agent-session-journal-types.ts`, `agent-session-journal-schemas.ts` (+ test) by clean 3-way merge. Mobile: `mobile-structured-agent-prompts.ts` re-vendored whole, `mobile-native-chat-permission.ts` merged, and `MobileNativeChatPermission.tsx` hand-written on this fork's themed card: a plan renders through `MobileMarkdown` in a scroller that takes the diff's share of the window (it reads line by line), with `Plan file: <path>` in mono below, and it replaces the raw detail — the proposed-edit preview, the description and the command are all skipped for a plan — instead of rendering beside it. Upstream's card test adapted to this card's own region and bound, in both themes; the projection test pins the subject carried. **Failing-first:** reverting the five modified files fails all three (the projection dropped the subject; the card drew the raw JSON). Green after |
| 434365d2d #21096 (one line of it) | offer to reconnect native chats that were working when Orca restarted | **done, the one phone-visible line; the feature is skipped.** The restart resume — teardown markers, `agent-session-restart-continuation.ts`, `agent-session-resume-marker.ts`, the `agentSession.restart*` RPCs and the `nativeChatResumeWorkOnRestart` setting — is the desktop's launch flow, `src/main/` and `src/renderer/`; the phone re-acquires a chat at its cursor by opening it and has no launch to resume from. What the commit also changed is `structured-agent-session-message-projection.ts`: a `rejected` submission's journal row is ledger evidence, not conversation history, so it leaves the visible transcript instead of standing as a message the agent never received beside the banner that says it was not sent. That file is re-vendored whole at 434365d2d, which also brings #19364's optional `projectItems` argument (skipped earlier as unused; the phone still calls the default). `mobile-structured-session-refused-send.test.ts` pins it — red on the pre-port projection (the refused row stayed), green after, with the no-refusal and no-items cases. **Skipped from the same commit:** `newestStructuredAgentSessionTurn` on the live-turn module and `latestStructuredAgentSessionUserItem` on the projection (`grep -rn` over `mobile/src mobile/app` → 0), the two new shared modules, the settings fields, and the params catalog's four `agentSession.restart*` rows — the generated catalog is re-vendored by whichever group carries the commit that adds a method the phone calls, and these it never calls |
| f1a901e97 #20672 | suggest verified OMP terminal commands | **done, adapted.** The `/` menu had no catalog for OMP, so it fell back to the two-entry common set. Upstream's `OMP_COMMANDS` (22 entries, its built-in registry) is hand-applied onto `native-chat-slash-commands.ts`, which carries this fork's own catalogs and cannot be re-vendored whole. One adaptation: the ten entries upstream describes as "in Terminal" (`model`, `switch`, `resume`, `fork`, `branch`, `tree`, `git`, `settings`, `extensions`, `hotkeys`) carry this fork's `opensOverlay` flag, so the phone shows the terminal for a selector the chat cannot drive — exactly what its Codex catalog already does through `slashCommandOpensOverlay`; upstream's desktop has no such lane and needs no flag. Upstream's mobile classification test hunk merged clean; the shared `native-chat-slash-commands.omp.test.ts` vendored (never collected here). Two tests on the phone, red on the pre-port catalog (OMP fell back to the common set, so `/switch` was an unknown token and every OMP command read as an overlay), green after: the classification case and one pinning which commands open the terminal and which keep the chat |
| 9641a1b54 #21348 (with f2ca3cbfb #21325) | serve the packaged mobile web bundle over RPC (OTA phase A, 3/5) | **done, shared half.** The two host methods (`mobileWeb.bundle.manifest`, `mobileWeb.bundle.chunk`), the asset reader, the read-admission cap and the bundle-root resolver are all `src/main/runtime/`, and the two packaging scripts are `config/scripts/` — none of it is vendored here; the stock desktop serves the bundle. What the phone needs is the contract: `src/shared/mobile-web-bundle/` (five files, from #21325 with #21348's `params: null` change) vendored whole at 9641a1b54, byte-identical to origin/main. `zod-salvage.ts` re-vendored at the same commit for `hostUnionArms`, which the contract imports — additive over the base and identical to origin/main. The generated catalog took its two entries and one import by hand (LOCAL-FILES.md): the fork's copy sits at eedd35645 and a whole-file re-vendor would carry Group B/D entries. Nothing under `mobile/` calls the methods until #21374; the mobile-scope allowlist ratchet is untouched here and takes its exception there. tsc clean; `orca-mobile-rpc-allowlist` and the `rpc-operation` suites green |
| b90837ee4 #21376 | advertise the bundle capability where a bundle ships (OTA phase A, 4/5) | **done, mobile half — merged.** The product change is `src/main/runtime/orca-runtime-get-status.ts` pushing `mobileWeb.bundle.v1` on `status.get` only when the install's bundle resolves, plus two runtime specs; not vendored, the stock desktop does it. The mobile side is one test, `host-status-capability-ignorability.test.ts`, pinning that the new string is inert on every released `status.get` reader. Upstream's copy drives `readHostStatusGates` / `readProbedHostCapabilities` / `hostAnsweredStatusProbe` from `host-status-probe-operations.ts`, which is Orca #20667 + #21176 (Group D) and does not exist here yet; the fork's transport probe is still `startRuntimeCapabilityProbe`, so the test drives that reader in their place and says so in its header — bring it to upstream's form when #20667/#21176 land. Load-bearing here: a gate that flips `floatingWorkspaceEnabled` on seeing the capability fails it (mutant applied and reverted, 2026-09-19) |
| 60a774c30 #21374 | client operations and dev probe for the desktop-served mobile web bundle (OTA phase A, 5/5) | **done, whole; ported third, in upstream's order, because #21411/#21409 edit its files.** New, verbatim: `mobile-web-bundle-operations.ts` (two `require-result-or-throw` / `on-settle` descriptors), `mobile-web-bundle-reply-schemas.ts` (+test), `mobile-web-bundle-fetch.ts` (+530-line test: paging, the four-worker cap, the mid-asset stop, the 32 MiB allocation bound), `use-mobile-web-bundle-probe.ts` (+test), `mobile-web-bundle-probe-row.tsx`, and the recorder's `adapters/mobile-web-bundle-mount-adapters.ts`. Verbatim hunks: `rpc-operation-compile-fence.ts`, `rpc-operation-test-families.ts` (both matched upstream's parent). Merged: `rpc-operation.ts` took the `runRpcOperation` tuple hunk **and** the `void extends` null-admitting arm of `RpcSendArguments` it relies on, which is Orca #20702's (Group D) — without it the two `null`-params call sites do not compile; when #20702 lands that arm is already here. `mobile/app/troubleshoot.tsx` mounts the row under `__DEV__` inline (this fork skipped #19675's `TroubleshootView` extraction, so there is no `developerRow` slot to fill). Recorder: the four scenarios / two families appended to `pilot-scenarios.json` (additions only, oxfmt-clean), the module registered, the `mobile-web-bundle-chunk-placement` mutant added and killed. **One fork-local recorder edit:** the fork's `operation-module-loader.ts` refused every non-relative import except `react`, and the bundle readers import `zod` and `@noble/hashes/sha256`; a two-entry `NATIVE_MOUNTING_SUBSTITUTES` map admits the real libraries — the two-line form of upstream's `native-mounting-substitutes.ts`, which arrives whole with Orca #20667 (Group D) and supersedes it. Goldens 255 → 264: all 255 existing move `recorderSha256` (that loader edit) and `lockfileSha256` (provenance, never compared) only, 0 bodies moved, 9 added (four pilot, five matrix); checked with a four-class delta script, not asserted. **Mobile-scope allowlist ratchet:** neither method is on the 1.4.205 fixture, and the literal scan could not see them because the operations name them through constants imported from `src/shared` — a hole the port opened, so the ratchet gained a `CONSTANT_NAMED_SENDS` table that pins the constant's value, the sender binding it, that the fixture still refuses it, and that no literal has appeared; the gate is `mobileWeb.bundle.v1` on `status.get` (#21376), which the shell walls on and a 1.4.205 host never advertises, and the dev row is `__DEV__`-only. README counts refreshed to this fork's corpus (134 / 264 / 266). Gate: tsc clean, vitest 905 files / 7902 passed / 17 skipped, oxlint 0 errors |
| 1e7a69710 #21411 | update wall for the desktop-served mobile web bundle (OTA phase B, 1/4) | **done, whole, verbatim.** All seven files were at upstream's parent (the four from #21374 had just landed), so the patch applied clean: `mobile-web-bundle-compat.ts` (+test) is the pure verdict — no bundle, unknown schema, shell too old, bundle incompatible on either side — `ProtocolBlockScreen.tsx` (+new test) renders the bundle walls beside the protocol one with `blockRemedy` deciding title, body and button once, and `mobile-web-bundle-reply-schemas.ts` reads an unknown `schemaVersion` through to the wall and types the protocol window. **One file brought early:** `transport/host-status-reply-schema.ts`, whose `HostStatusReply` the verdict takes a `Pick` of, is Orca #21176's (Group D) — a leaf module (zod + `salvagedOptional`), vendored verbatim at 1e7a69710 and identical to origin/main, so #21176 finds it in place; its doc comment names `host-status-probe-operations.ts`, which arrives with that port. Failing-first: the two new reply-schema cases go red on the pre-port `mobile-web-bundle-reply-schemas.ts` (unknown `schemaVersion` refused at the parse; a manifest with no window accepted) and green after. The block screen still reads the legacy static palette (dark-only), as it did before this port and does upstream; converted in the fork-local commit that follows |
| 002ff3ddb #21409 | per-host generation store for the mobile web bundle (OTA phase B, 2/4) | **done, whole, verbatim.** New: `mobile-web-shell/generation-store.ts` (+633-line test), `generation-store-file-system.ts` (the `expo-file-system` port, which this fork already depends on for the PDF cache), `host-cache-key.ts`. One verbatim hunk: `mobile-web-bundle-reply-schemas.ts` exports the manifest read schema, which the store re-parses a cached manifest with. First file under `mobile/src/mobile-web-shell/`. Pure addition with its own suite (29 cases, including the four-host ceiling, the mid-download host, the case-folded `Manifest.JSON` refusal and the `WeakSet`-tracked handles); no existing behaviour to go red on |
| b749091b6 #21417 | native shell view serving a mobile web generation from a private origin (OTA phase B, 3/4) | **done, whole, verbatim.** *Corrected 2026-09-23: this row said "the native halves unbuilt here", and that was wrong. The module is autolinked (`:orca-mobile-web-shell` is in `gradlew projects`), so `assembleRelease` compiles the Kotlin half into every APK. The shell screen stays behind `useMobileWebShellEnabled`, which is off by default. `:orca-mobile-web-shell:testReleaseUnitTest` ran 55/55 in the batch E review and again on c26c7f2a. The "not verified" note at the end of this row predates both runs.* `mobile/modules/orca-mobile-web-shell/` (33 files: the Expo module config, the TS surface `src/index.ts` + `load-state.ts`, 12 Kotlin sources + 6 JUnit suites, 8 Swift sources + the `swiftc` checks) and `mobile-web-shell/shell-load-state.test.ts` came over as-is. First entry under `mobile/modules/`, which Expo autolinking scans by default (`nativeModulesDir` = `./modules`); nothing imports the module by name, the TS is reached by path. **One dependency added:** `expo-modules-core ~57.0.16` in `mobile/package.json` — upstream lists it directly, this fork only had it through `expo`, and pnpm's strict layout left `requireNativeViewManager` unresolvable; pinned to the version already in the lockfile, so `pnpm install` added three lockfile lines and fetched nothing. tsc typechecks the module's TS (the fork's `tsconfig` includes `**/*.ts`, as upstream's does). What is NOT verified here: the Kotlin and Swift compile and their unit suites — the brief forbids an APK build and the machine's memory notes forbid a second NDK build; the JS/TS side (34 tests under `mobile-web-shell/`) is green |
| 8fc81182a #21432 | envelope contract for the web shell bridge (OTA phase C, C0.1) | **done, whole, verbatim.** Eight new files under `mobile-web-shell/bridge/`: `bridge-caps.ts` (the pinned wire numbers — 640 KiB frame, depth, nodes, the 8 MiB reply ceiling, in-flight and subscription caps), `bridge-envelope.ts` (one zod schema per message in each direction, `v`-gated, unknown keys dropped), `bridge-error-capture.ts` (a rejection crossing as a capture that keeps its delivery-unknown mark and survives a throwing getter), `bridge-reply-chunking.ts` (an oversized reply split after serialization, surrogate pairs kept whole, refusals remembered per id), each with its suite: 160 cases. Nothing existing touched; tsc and oxlint clean |
| 3aefee4a1 #21434 | native page-shell bridge in orca-mobile-web-shell (OTA phase C, C0.2) | **done, whole, verbatim.** All 16 files are the module's and every existing one was at upstream's parent (#21417 had just landed): `bridgeEnabled` prop, `onBridgeMessage` event and `postBridgeMessage` view function on `src/index.ts`; `MobileWebShellBridge` + `MobileWebShellAppliedProps` on both platforms (a `WKScriptMessageHandler` bound to our own frame, scheme and ASCII-folded session host; a `WebMessageListener` gated on the Chromium 88 feature query), with their JUnit and swiftc checks. As with #21417, the native halves are not compiled here; the TS surface typechecks and the shell suites are green (194) |
| 47d107cf2 #21435 | hybrid shell route, dark behind a dev-only flag (OTA phase B, 4/4) | **done, whole; 18 of 22 files verbatim, 4 merged.** New: `app/h/[hostId]/web.tsx`, `MobileWebShellScreen.tsx` (+test), the session reducer `mobile-web-shell-session.ts` (+717-line table test) and its vocabulary `mobile-web-shell-session-contract.ts`, the runner `use-mobile-web-shell-session.ts` (+test), the flag census, the reachability and route tests, `generation-store-file-system.test.ts`, and the `__DEV__` Troubleshoot toggle `diagnostics/mobile-web-shell-dev-row.tsx` (+test). Verbatim hunks: `app/h/_layout.tsx` (the `[hostId]/web` screen), `generation-store-file-system.ts` (`generationDirectoryPath`), `mobile-web-bundle-operations.ts`, `mobile-web-bundle-reply-schemas.test.ts`. Merged: `preferences.ts` / `.test.ts` took `loadMobileWebShellEnabled` (unreadable-is-off, and a release build never reads the key) and its three cases beside the fork's own preference set; `host-status-gates.ts` took `statusReadable` and `hostProtocolWindow` onto the fork's pre-#20667 hook — same five settle sites, same fields, the upstream parent's operation-based read is Group D's; `app/troubleshoot.tsx` mounts the shell toggle beside the bundle row under the same `__DEV__` guard. The flag census passes over this tree: the route reads the key, the dev row reads and writes it, and the key lives in `preferences.ts` alone. **Dark-only, by design and by fact:** with the flag off the route redirects to `h/[hostId]` and the screen is never constructed; it also still reads the legacy static palette (as upstream's does), which is acceptable for a dev-only surface no store build reaches and is noted for whoever lifts the flag. Gate: tsc clean, vitest 921 files / 8217 passed / 17 skipped, oxlint 0 errors |
| 381a3da46 #21449 | Route A, the phone's host routes bundled for the web, dark (OTA phase C, C0.7) | **done, mobile half; the builder is not taken.** Verbatim: `mobile/web-entry/` (the `ExpoRoot` entry, the typed `route-manifest.ts` the builder replaces, `web-overrides.json`), `app/h/[hostId]/web.web.tsx` (redirects on web so the shell never nests itself), `transport/host-device-token-store.web.ts`, and the shell module's CSP hunk (`style-src 'unsafe-inline'` for RN Web's runtime stylesheet, Kotlin + Swift + both checks; the four files matched upstream's parent). Merged: `client-context.web.tsx`, the placeholder page transport, drops `getGeneration` (this fork's `RpcClient` has not taken Orca #20914's optional counter) and answers the fork's own `isLivenessProbing` on the context value — both marked in the file; `unvalidated-rpc-request-port-inventory.ts` lists it as an owner, by hand, above the fork's own entries. **Dependency added:** `@types/react-dom ^19.3.0` (dev) — upstream's mobile `tsc` finds it in the Orca root's `node_modules`, which this fork has none of; the lockfile churn beyond the one package is radix/vaul's optional peer now resolving. **Skipped, with the reason:** `config/scripts/build-mobile-web-app-bundle.mjs` and its five siblings, `verify-mobile-web-bundle.mjs`, the CRLF `.gitattributes` pins, the three workflow edits and the root `package.json` script. They are the DESKTOP's build tooling: they run from the Orca root against the root's esbuild and `build-mobile-web-bundle.mjs` (phase A 2/5, not here either) and write `out/mobile-web-app`, which the desktop then packages and serves. This fork has no root `package.json`, no `out/`, and does not build the page bundle — the stock desktop builds it from upstream's `mobile/app` and the phone downloads it — so a copy here could not run and would drift. The page-side sources under `mobile/` are kept because vitest and tsc cover them and the later C-series commits build on them |
| f2be6299c #21459 | RN bridge host for the web shell page (OTA phase C, C0.3) | **done, whole; 10 of 12 files verbatim, 2 merged.** New: `mobile-web-shell/bridge-host.ts` (+735-line test), `bridge-host-subscriptions.ts`, `bridge-host-test-fakes.ts`, `use-mobile-web-shell-bridge.ts` (+test). Verbatim hunks: `MobileWebShellScreen.tsx` (+test) wires the host to B4's session, `bridge-caps.ts` (the desktop's viewport bound, read from the schema source rather than imported), `bridge-envelope.ts` (+test). Merged: `unvalidated-rpc-request-port-inventory.ts` takes the two owner entries by hand (the host is an owner, not a call site: it picks no method and reads no reply); `transport/rpc-client.ts` takes Orca #20914's six-line optional `getGeneration?: () => number` on the `RpcClient` interface — the `state` frame carries the generation, `StableLogicalRpcClient` here already implements the counter, only the interface lacked the member; #20914 (Group D) will find it in place |
| ddbb19458 #21467 | page-side RpcClient over the web shell bridge (OTA phase C, C0.4) | **done, whole; 15 of 18 files verbatim, 3 merged.** New under `bridge/`: `bridge-rpc-client.ts` (+two suites, 1,023 lines between them), `bridge-client-{connection-cache,errors,init-handshake,requests,subscriptions}.ts`, `bridge-port-pair-test-harness.ts` (one FIFO per direction, delivery on a microtask — what C0.5's replay needs), `bridge-screencast-binary.ts`, `orca-bridge-page-channel.ts` (+test); `transport/client-context.web.test.tsx`. Verbatim hunks: `bridge-host.ts`, `bridge-reply-chunking.ts`, `web-overrides.json`. Merged: `client-context.web.tsx` re-vendored whole at this commit plus the fork's `isLivenessProbing` line; the port inventory's owner entries by hand; and **one fork-local wire hunk** in `bridge-envelope.ts`: `BRIDGE_FOREGROUND_NUDGE_REASONS` gains `'user-send'`, this fork's own fourth `ForegroundNudgeReason` (0.2.92, a send on a cooled-down relay re-dials) — the list is documented as closing over that union and the page client compiles against it; upstream's page never sends it, this fork's shell accepts it and its native client already handles it. Marked `CODE UI` in the source |
| 209d2d8df #21475 | split the Route A page into per-route chunks (OTA phase C, C1.5) | **done, the two-line mobile half.** `web-entry/index.tsx` loses its suspense boundary (expo-router wraps every screen in its own) — applied verbatim. The other six files are `config/scripts/`: the lazy route manifest, esbuild splitting, content-addressed output names, the chunk and entry-bytes budgets, the render check's navigation fetch — the desktop's Route A builder, not taken for the reason on the #21449 row (no root `package.json`, no `out/`, the desktop builds and serves the page) |
| f24f38bd4 #21485 | certify the golden corpus through the page bridge (OTA phase C, C0.5) | **done, product and test changes; goldens re-recorded here, never copied.** New, verbatim: `bridged-parity/divergence-classes.ts` and `divergence-evidence.ts` (+tests), `bridge-port-pair-test-harness.test.ts`, `scripted-client-wrapper.test.ts`, and `rpc-recording-through-bridge.test.ts` (opt-in behind `RPC_FOUNDATION_BRIDGE=1` at this commit, as upstream's; one adaptation — `pilotMountAdapters(root)` without the `device` overlay of Orca #20884, Group D). Verbatim hunks: `bridge-port-pair-test-harness.ts` (generic over the shell client, `rewriteToPage`, `drainNow`), `bridge-rpc-client.test.ts`, `golden-recording.ts` (exports `firstDifference`). **Merged onto the fork's older recorder:** `scripted-rpc-transport.ts` takes the `wrapClient` seam and the names queue on its pre-#20920 class (no stream registry, no write ordinal — the second constructor parameter holds #21088's place as `undefined` so the three-argument call shape matches upstream's), `run-recording.ts` passes the wrapper through, and the port inventory's scripted-transport ceiling rises 5 → 7 with upstream's reason. `BRIDGED_PARITY_BASELINE` **re-pinned from a measured run over this fork's 264 goldens** (13 identical / 133 reply-meta-required / 111 result-absent-settlement / 0 observation / 7 params-undefined / 0 write-ordinal / 0 unclassified — upstream's 787 partition 24/372/338/7/33/13), with a dated note; `write-ordinal` is 0 because this recorder has no subscription lane. Nothing lands in `unclassified`, so the classifier's rules cover this corpus. Goldens 264 → 264: `recorderSha256` (the seam) and `lockfileSha256` (the `@types/react-dom` add) moved on every file, 0 bodies moved — the seam's own claim, checked by script. The `.github/workflows/mobile.yml` step is CI this fork does not carry |
| d253dd0e1 #21511 | the page bridge accepts every reply native accepts and settles what it refuses (OTA phase C, C0.8) | **done, whole; 11 of 13 files verbatim, 2 re-pinned.** Verbatim: `bridge-envelope.ts` (3-way merged clean around the fork's `'user-send'` arm — the reply reader is now `isRpcResponse` itself), `bridge-rpc-client.ts` and its new `bridge-client-inbound-frames.ts` / `bridge-client-diagnostics.ts` (a refused frame settles the exchange it answered; a refused stream frame cancels at the shell), `bridge-client-errors.ts` (`BridgeReplyRefusedError`, delivery-unknown at construction), `bridge-client-subscriptions.ts`, both frame suites, `divergence-evidence.ts` (+test; the `params-undefined` arm now reads the step the transport refused and the paths that moved). Re-pinned to this fork's corpus, with dated notes: `divergence-classes.ts` — the `_meta` widening took `reply-meta-required` 133 → 0 and `identical` 13 → 146; the pin is now 146 / 0 / 111 / 0 / 0 / 7 / 0 / 0 over 264, and `params-undefined` (7, nameable) is listed golden by golden. Upstream's three stream-shaped exclusions and members are parked in a comment because the classes are 0 here (the recorder has no subscription lane) and the ratchet holds a reasoned class to a non-zero count; the classes test trades a `params-undefined` member where upstream trades a `write-ordinal` one. `rpc-recording-through-bridge.test.ts` re-vendored whole plus the two fork adaptations. **Failing-first:** eight of the ported cases go red on the pre-port bridge — the six `_meta` agreements with the native predicate, the reply handed back verbatim, and `settles the request a reply it could not read was answering` (a 30 s hang) — and green after. The commit's own repin half (`baseline`, 787 headers) is not taken |
| f2fd18c82 #21533 | make the bridged recording-corpus replay a CI gate and pin the C1 page closure (OTA phase C, C1.6) | **done, whole; the two pins re-derived for this fork.** The replay now runs by default in `npx vitest run` and `RPC_FOUNDATION_BRIDGE=0` is the one value that skips it (`BRIDGED_PARITY_OFF`); `bridgedParityTallyDrift` holds every number in `BRIDGED_PARITY_BASELINE` exactly, `identical` included. `divergence-classes.ts` / `.test.ts` and the harness re-vendored whole at this commit and the fork's pins re-applied (the #21511 row's numbers; the tally test's `pinned 341, ran 340` line reads 111 / 110 here). **`c1-page-closure.ts` is this fork's own table**, derived the way upstream's doc says — the value-import closure of `app/h/_layout.tsx` and `app/h/[hostId]/index.tsx` with `.web.*` resolution (274 files), matched against each scenario's `sites` — and pinned golden by golden from a measured run: 16 families, 79 goldens, 42 byte-identical, 30 `result-absent-settlement`, 7 `params-undefined`. Upstream's 22 / 103 are these 16 plus six families whose recorder scenarios this fork does not carry yet (`host-worktree-refresh`, `transport.capability-probe`, `transport.host-status-gates`, `notifications.push-registration`, `components.new-workspace-repositories`, `worktree.agent-launch-create` — Groups B and D); they are named in the pin's comment so they join it when their goldens exist. The census test says 16 / 79. The `.github/workflows/mobile.yml` step removal is CI this fork does not carry. Gate: tsc clean, vitest 933 files / 8676 passed / 17 skipped (the replay adds 265), oxlint 0 errors |
| d043cfbbe #21500 | the page mounts on the shell's init, with the client injected and a fault boundary (OTA phase C, C1.1) | **done, whole; 33 of 36 mobile files verbatim, 3 merged.** New: `bridge/page-bootstrap.ts` (+test), `page-fault-boundary.tsx` (+test), `bridge-init-frame.ts`, `bridge-notify-grants.ts` (+test), `bridge-client-notifications.ts` (+test), `bridge-client-inbound-frames.test.ts`, `bridge-page-client-test-harness.ts`, `mobile-web-shell/bridge-diagnostic-log.ts` (+test), `bridge-host-errors.ts`, `mobile-web-shell-runtime.ts`. Verbatim hunks on 19 more (the host enforces the grants it issued and hears nothing before `ready`; the ready deadline; the frame suite split; the screen, session, runner and bridge hooks; `web-entry/index.tsx` owns the page's one client and mounts nothing until `onReady`). Merged: `bridge-envelope.ts` takes `BRIDGE_FAULT_GRANT` and the `fault` notify arm by hand beside the fork's `'user-send'` arm; `client-context.web.tsx` re-vendored whole (the placeholder and `BridgeTransportUnavailableError` are gone; the client is injected) plus the fork's `isLivenessProbing`; the port inventory drops the placeholder's owner entry by hand. `config/scripts/mobile-web-app-render.test.mjs` is the builder's render check, not taken (#21449 row). Bridged gate: 266 green, closure unchanged |
| b8f3b1ec0 #21501 | the shell names the screen, and the page routes to it (OTA phase C, C1.2) | **done, whole; 30 of 31 mobile files verbatim, 1 merged.** New: `bridge-host-contract.ts`, `bridge-host-init.test.ts`, `bridge-host-test-harness.ts`, `bridge/bridge-client-session.ts`. Verbatim hunks on 26 more: `init` gains an optional `route: { pathname, params }`, held to what a path may be; the page writes it into its history and hands it to `ExpoRoot` as `location` before its first render; a shell too old to name a route gets an update panel built outside React; both native load-state machines stop reading the document URL to decide a load finished (Swift only here — the Kotlin side had no such read); the shell route supplies the route, the screen passes it, the hook holds it for one host. Merged: `bridge-envelope.ts` re-vendored whole at this commit (`BridgeInitRouteSchema`, the four route caps) with the fork's `'user-send'` arm re-applied — that re-application is now a script, `scratchpad/envelope.py`, since every C-series commit touches this file. The builder's render check is not taken (#21449 row). Bridged gate: 266 green, closure unchanged |
| ac4dc6599 #21502 | the desktop lists a page route, the shell honours it or stays native (OTA phase C, C1.3) | **done, whole; 41 of 45 mobile files verbatim, 4 merged, the shared contract re-vendored.** New: `host-screen/HostScreen.tsx` (the list in its own module, mounted by the route, the wide sidebar and the route's new `index.web.tsx`), `mobile-web-shell/page-route-policy.ts` (+test), `mobile-web-shell-gates.ts`, `use-mobile-web-shell-enabled.ts`, `bridge-host-requests.ts`, `navigation/route-handoff.ts` and `route-handoff.web.ts` (+test), `route-href.ts`, plus verbatim hunks on 30 more (the `[hostId]` route mounts `MobileWebShellScreen` with a `fallback` when the flag is on, the session reducer's page-route negotiation, the flag census). Merged: `use-host-screen-controller.ts` swaps `useRouter` for `useRouteHandoff` on the fork's file (same two lines, the fork's surrounding hunks untouched); the port inventory moves the owner entry to `bridge-host-requests.ts`; `bridge-envelope.ts` and `client-context.web.tsx` re-vendored whole with the fork's two lines (`'user-send'`, `isLivenessProbing`). `src/shared/mobile-web-bundle/manifest-contract.ts` (+test) and `bundle-rpc-contract.test.ts` re-vendored at ac4dc6599 — `routes` on the manifest, `MobileWebBundleRouteSchema`, the two ceilings — and `UPSTREAM.txt` moved. The six `config/scripts` files (the builder emits `routes`) and the runtime test fixture are not taken (#21449 row). Route parity and tasks parity tests unchanged. Bridged gate: 266 green, closure unchanged |
| e2afb5eef #21503 | the page reads this host and keeps the app's pins (OTA phase C, C1.4) | **done, whole; 38 of 47 mobile files verbatim, 9 merged.** New: `bridge/page-async-storage.ts` (+test), `page-host-profile.ts`, `mobile-web-shell/page-storage-keys.ts` (+test), `use-page-host-snapshot.ts` (+test), `storage/mirrored-storage-keys.ts`, `transport/host-store.web.ts` (+test), `bridge-host-notifications.test.ts`, and the three host-screen action-error suites. Verbatim hunks on 24 more (the host hands the page its host profile and mirrored keys on `init`; `last-visited-worktree-repo.ts` gains `writeLastVisitedWorktree`, the one writer of that key; the keyboard-state effect calls it). Merged onto the fork's own files: `HostRouteNoticeBanner.tsx` takes the `failure` tone on the fork's themed banner (`colors.danger` from `useTheme()`, both schemes, where upstream's static banner uses `statusRed`); `use-host-screen-state.ts` / `use-host-worktree-actions.ts` / `use-host-worktree-catalog.ts` / `host-workspace-list.tsx` take `actionError` — a failed host removal is a dismissible line above the list instead of `Alert.alert`, which is a no-op on React Native Web; `preferences.ts` notes the pins write through `noteMirroredWrite` before persisting it; `host-workspace-list-action-error.test.tsx` asserts the red rule on the themed inline style rather than the static palette's array style; `bridge-envelope.ts` re-vendored with the fork arm. **Ratchet re-pinned:** `mobile-session-route-parity.test.ts`'s `HEAD_EFFECT_SHA256` moved 1961e639 → 0a93fb85 for the last-visited-worktree effect, with a dated note, the way upstream's own line moved. The four `config/scripts` files are not taken (#21449 row). Gate: tsc clean, vitest 950 files / 8860 passed / 17 skipped, oxlint 0 errors |
| 0817476b2 #21570 | follow-ups from the C1 stack review, one commit per finding (OTA phase C, C1.8) | **done, whole, verbatim.** All ten files were at upstream's parent: the host list route encodes the host id into the page pathname; `bridge-caps.ts` refuses a trailing-dot pathname segment; the fault report handles a store that refuses the write; the session runner re-arms a session the route rebuilt with host and gates unchanged; `writeLastVisitedWorktree` goes through `writeMirroredStorage`. Failing-first: four of the ported cases go red on the pre-port product files — the encoded host id, the trailing-dot refusal, the refused-write fault, the re-armed session — and green after. Route parity unchanged (the effect body still calls `writeLastVisitedWorktree`) |
| 1ef947394 #21582 | hand the page's dead Back button to the shell (OTA phase C, C2.2) | **done, whole; 19 of 21 mobile files verbatim, 2 merged.** New: `mobile-web-shell/use-shell-stack-pop.ts` (+test) — the shell's own stack pop and the module-scoped latch that keeps one `navigate-back` from becoming two, released on the committed route or on the holder's unmount — plus verbatim hunks on the host, the notify grants, the page-side route handoff (`back()` on the page hands the pop to the shell) and the screen. Merged: `bridge-envelope.ts` re-vendored with the fork arm (the `navigateBack` notify and its outcome); `use-shell-stack-pop.test.tsx` loads expo-router's shipped routing bytes against a stub `require`, and this fork is on expo-router 57 where upstream's 55 `routing.js` is a re-export shim — the loader now evaluates 57's `routingQueue.js` and `router.js` as shipped (the ref off `./store`, the queue handed to the router as the shim does), which is the upgrade the test says it exists to catch; noted in the file. The builder's render check is not taken (#21449 row). Bridged gate green |
| edd9e3125 #21589, 428558b94 #21591, 57fdf68ab #21597 | give the page its height and its long press (C1.9); let the shell's page paint a file preview (C3.0); let the page hand a link to the device through the shell (C2.3) | **done, the mobile halves; one commit for the three.** #21589: the Swift view turns WebKit's text interaction off so a long press reaches the page (verbatim); the height fix is the Route A document's inline reset, which lives in the builder — not taken (#21449 row), the desktop emits the document. #21591: the CSP hunks on both platforms and both checks, verbatim (`img-src` and `font-src` widened for a file preview's own bytes). #21597: 26 of 27 files verbatim — `platform/external-link.ts` and its `.web.ts` sibling (+tests; the page hands a link to the device through the shell, held to the three schemes a task source produces and the route-href cap), `openMobilePrUrl` becomes a re-export of it, the `openLink` notify with its grant on the host, client, caps and screen; `bridge-envelope.ts` re-vendored with the fork arm, which is now two lines instead of a wrapped literal because the vendored file sits at the 300-line ceiling and the wrapped form was the one counted line over. Failing-first for #21597: the eight `external-link` cases and the notify cases go red on the pre-port modules (the module did not exist; the host refused the notify) and green after. Kotlin and Swift not compiled here, as before |
| b7c06900e #21592 | give reanimated mapper hooks the inputs esbuild never writes (OTA phase C, C1.10) | **done, the mobile half, and the sweep it demands.** `reanimated-web-mapper-deps.test.ts` came over verbatim: a source-reading guard that every `useAnimatedStyle` / `useAnimatedProps` / `useDerivedValue` / `useAnimatedReaction` under `src/` and `app/` carries a dependency array naming each shared value its updater reads, because the web bundle is built without Reanimated's Babel plugin and a mapper with neither `__closure` nor deps listens to nothing. `DragReorderList.tsx` took upstream's hunk verbatim (it matched the parent). `RightDrawer.tsx` and `mounted-bottom-drawer.tsx` are fork-changed (reduced-motion opacity, the keyboard shift), so their arrays were written against the fork's bodies — `reduceMotion` joins upstream's lists — plus the sheet's `testID`. **The guard then named five fork-only sites upstream never had:** `ZoomableImage.tsx`, both hooks in `use-usage-progress.ts`, `ui/PressScale.tsx`, `ui/StatusPulse.tsx`; each got its array, which is the point of a guard over the whole tree. Red on the tree before the arrays (nine sites listed), green after. The two `config/scripts` render harnesses and the workflow edit are the builder's (#21449 row). Behaviour on native is unchanged: with the Babel plugin the closure still supplies the inputs |
| 211821dc1 #21596 | render agent session history from the desktop's bundle (OTA phase C, C5.1) | **done, whole; 12 of 15 mobile files verbatim, 3 merged.** New: `app/h/[hostId]/agent-history/[worktreeId].web.tsx` (the second page route's web sibling), `agent-history-back-handoff.test.tsx`, `agent-history-resume-writes.test.ts` (the three resume writes run through the bridge and against the same fake, verdicts compared), `worktree-navigation-actions.test.ts`, `host-route-href-encoding.test.ts`, `mobile-web-shell-agent-history-route.test.tsx`. Verbatim hunks: the native route mounts the shell for the listed route with the native panel as fallback; `route-handoff.web.ts` (+test) becomes tri-state — a target outside `pageRoutes` is never pushed locally; `worktree-navigation-actions.ts` encodes the host id; the flag census grows its first entry since C1.3; `web-overrides.json`. Merged onto fork files: `MobileAgentSessionHistoryPanel.tsx` swaps `useRouter` for `useRouteHandoff`; `host-screen-header.tsx` encodes the host id at its two `navigateFromHostList` targets (upstream's header has four; this fork's has two); `use-host-worktree-actions.ts` encodes `hostId ?? ''` at the session target. Failing-first: twelve of the ported cases go red on the pre-port files (the encoding at every site, the tri-state handoff, the Back handoff) and green after. `config/scripts` and the workflow are the builder's (#21449 row). Bridged gate and route parity unchanged |
| 62903b7cf #21649 | pin the agent-history page closure golden by golden (OTA phase C, C5.2) | **done, whole; the two pins re-derived for this fork, goldens untouched.** `page-closure.ts` (the one semantics both series read a run with: exclusions, totals, drift, the printed line) verbatim; `c1-page-closure.ts` (+test) re-vendored on the refactor and the fork's 16 / 79 table re-applied; the harness re-vendored with the fork's adaptations. **`c5-page-closure.ts` is this fork's own table:** the value-import closure of `app/h/_layout.tsx` and `app/h/[hostId]/agent-history/[worktreeId].web.tsx` (315 files), matched against each scenario's sites and pinned from the measured run — 17 families, 89 goldens, 47 byte-identical, 35 `result-absent-settlement`, 7 `params-undefined`. It is C1's 16 families plus `settings.resume-metadata`; upstream's C5 adds that one and four `aiVault.*` recorder families this fork does not carry yet (Orca #20277 and after, Group C), so the C5 test's census, totals, added-families and exclusion-count lines read 17 / 89, 47 / 35 / 7, one family and 42 (upstream: 27 / 134, 72 / 50 / 7 / 3 / 2, five families, 62), each with a dated note. The inheritance test still holds: every C1 family's pins are C5's, unchanged. No golden moved — this commit's goldens upstream are its repin, and this fork's corpus was last recorded at #21485. Gate: tsc clean, vitest 961 files / 8993 passed / 17 skipped, oxlint 0 errors |
| (merge) groups A/B/C ← group E, 2026-09-19 | the OTA branch integrated onto main 95a6519a | **Corpus re-recorded once on the merged tree**, not hand-merged: 264 goldens. Against main's 255: header-only on all 255 (`recorderSha256` — the group E seam and the folded loader — plus `lockfileSha256`, provenance), 0 bodies moved, the 9 `mobileWeb.bundle-*` goldens added. Against the group E branch's 264: `recorderSha256` on all, 12 `adapterSha256` (the three `settings.*task-workspace*` / `workspace-submit` families, whose adapter main's #19849/#19850 port edited) and 5 bodies — `matrix-worktree.create-retry-worktree.create-1`, `matrix-worktree.runtime-capabilities-status.get-1`, `tw-capabilities-{advertised,cutover-retried,legacy-idempotency}` — every one byte-equal to main's body, i.e. main's `agent.launch` re-record carried forward, nothing the merge introduced. Bridged replay re-run on the merged corpus: 146 / 0 / 111 / 0 / 0 / 7 / 0 / 0, unchanged. C1 and C5 page closures re-derived from the merged route tree: 16 and 17 families, same scenarios (the C1 root is the `index.web.tsx` sibling, since the native file now mounts the shell). Route-parity effect hash re-pinned once more (both sides' effects in one body). `client-context.web.tsx` answers main's #21566 `getRelayHostReachability` with upstream's own `'connecting'`. The loader's two zod admissions (main's branch, E's map entry) folded into the map. Gate: tsc clean, vitest 984 files / 9249 passed / 17 skipped, oxlint 0 errors |
| c22c442fd #21623 | answer native verbs on the shell, clipboard first (OTA phase C, C2.4) | **done, whole; 39 of 40 files verbatim, 1 merged.** New: `bridge/bridge-native-verbs.ts` (+test; the `native.` prefix and the typed verb table — `native.clipboard.write` / `native.clipboard.read` — whose name tuple `MOBILE_WEB_SHELL_GRANTS` spreads), `bridge/use-native-verbs.ts` (+test; the page's typed surface, refusing an ungranted verb before a frame is sent), `mobile-web-shell/bridge-host-native-verbs.ts` (+test; `readBridgeNativeVerbCall`, the whole decision, and a `native_verb_refused` code the desktop's vocabulary does not contain), `platform/native-clipboard.ts` (+test; over `expo-clipboard`, text only — image is declared and refused as out of scope), `mobile-web-shell-session-effects.ts`, `-session-grants.test.ts`, `-session-test-fixtures.ts`. Verbatim hunks on 24 more: the host fences `native.` in `forward()` and on subscribe, the session reducer carries a route's declared verbs into its grants, both routes and the agent-history route pass `serveNativeVerb`, `bridge-host-errors.ts`, `page-route-policy.ts`, and `src/shared/mobile-web-bundle/manifest-contract.ts` (+test) re-vendored at c22c442fd (`native.` grant names admitted), pins moved. Merged: `transport/types.ts` takes the one hunk — `_meta` optional on `RpcSuccess` / `RpcFailure`, with upstream's reason — onto the fork's file, which carries its own hunks elsewhere; nothing under `mobile/src` reads `_meta` outside test fixtures (grep). `platform/native-clipboard.ts` is a new module beside, not merged with, the fork's own `session/mobile-clipboard-image.ts`. Failing-first: fourteen of the ported cases go red on the pre-port host, client, session and route-policy files — the shell-answered write, the no-`_meta` reply, the desktop fence on request and subscribe, the manifest-declared grants — and green after. Gate: tsc clean, vitest 988 files / 9267 passed / 17 skipped, oxlint 0 errors; bridged replay and both closures unchanged |
| (merge) main ← group D, 2026-09-19 | the typed-operation and recorder branch integrated onto main 3a222039 (first 46222b13, then #21623 as it landed) | **Corpus re-recorded once on the merged tree**, not hand-merged: 759 goldens (branch 746 + main's 9 `mobileWeb.bundle-*` + 4 new). Against the branch's 746: header-only on all (`recorderSha256`, `lockfileSha256`; 19 `adapterSha256`, 5 `scenarioSha256`) and **21 bodies moved, every one main's product change now recorded**: the 7 `session-create-terminal-*` pilots and 2 `session.create-terminal` matrices (main's #20069 — a host without `session-tabs.split-group-placement.v1` gets no optimistic tab, so `sessionTabs` reads `[]`), the 5 clipboard goldens (`terminal-paste-*`, `terminal-worktree-connection-resolved`, their matrices — main's #20545 gutter setting, so the `settings.get#1` the branch's #20915 row said it dropped is scripted again), the 4 capability goldens (`tw-capabilities-*`, `runtime-capabilities` matrix — `agentLaunch: false` from main's #19849 route, byte-equal to main's own bodies again) and the 3 `native-chat-image-upload-*` (main's #20133 `contentFingerprint`). The 4 new: `tw-create-retry-agent-launched` and its `worktree.agent-launch-create` matrix (the family the branch's #21246 row dropped for want of the route), and the two clipboard `settings.get` matrices. Against main's 264, with the write ordinal (#21088) stripped: 53 bodies are Group D's own already-documented moves (the step-7 malformed partitions, `reply-salvage` effects), 5 `mobileWeb` matrices move only by #20950's `RpcIncompatibleReplyError` message and `code` (confirmed by stripping those fields), and the 4 capability goldens that differed on the branch are main's bytes again. **Bridged replay** re-derived on the merged corpus: 384 identical / 0 / 327 / 1 / 6 / 33 / 8 / 0 over 759 (main's 146 / 0 / 111 / 0 / 0 / 7 / 0 / 0 over 264; upstream's 396 / 0 / 341 / 3 / 6 / 33 / 8 / 0 over 787 — the two `result-absent-observation` short are the `notifications.desktop-stream` matrices this fork does not record). The replay now mounts a scenario's declared device store (upstream's `pilotMountAdapters(root, { device: scenario })`, which main's port predated), without which the five #20884 device-store goldens threw `Missing or completed request`; the three stream-class reasons main parked as a comment are upstream's own again, and `BRIDGED_PARITY_MEMBERS` names the stream classes as upstream does. **C1 / C5 page closures** re-derived from the merged route trees (value-import closure with `.web.*` resolution, matched against scenario sites): C1 16 → 20 families / 79 → 94 goldens (`host-worktree-refresh`, `transport.host-status-gates`, `components.new-workspace-repositories`, `worktree.agent-launch-create` joined; `transport.capability-probe` reaches upstream's closure only through its push modules), C5 17 → 25 / 89 → 125 (upstream's five AI Vault families, the four `aiVault.*` having arrived with #20702); verdicts for the new goldens taken from the run's own drift output. **Conflicts** (21 files + 255 goldens): five transport/task files resolved to upstream's tip verbatim because upstream itself carries both sides (`pre-profile-pairing-coordinator.ts`, `mobile-relay-direct-upgrade.ts`, `host-status-gates.ts` — #19952 and #21566 over #20667/#21176; `mobile-workspace-create-operations.ts`, `blank-workspace-create.test.ts` — #19849 over #21246); `worktree-create-retry.ts` keeps main's `isAgentLaunchReplayUnsupportedRefusal` (the allowlist ratchet reads it) with #21246's checked reader; `use-mobile-session-terminal-create-actions.ts` is the typed create plus main's #20069 placement plus this fork's HUD launch config; the terminal-list refresh reads through `sessionTerminalListRead` and closes a transcript-tail leftover (main removed the feature; nothing of it was in the ratchets); recorder engine files (`scripted-rpc-transport.ts`, `run-recording.ts`, `pilot-mutants.test.ts`) to upstream's tip, which is #21088's ordinal plus E's bridged seam plus #20755; the loader keeps `native-mounting-substitutes.ts` (main's two-entry map was its own placeholder for it); `getGeneration` declared once in `RpcClient`; two adapters take main's `getRelayHostReachability` for `isHostSignedOut`; `zod-salvage.ts` at main's later 9641a1b54 pin (folded in UPSTREAM.txt). Route parity re-pinned from the test's output (callback bodies, effects, nested functions, 663 runtime strings = main's 678 less the fifteen method literals the operations took). Port-inventory ratchet: pending unchanged at 31 files / 56 references, main's three bridge owners counted by the AST test. `oxlintrc` keeps both sides' ceilings. Gate: tsc clean, vitest 1023 files / 10656 passed / 17 skipped, oxlint 0 errors |
| 49274394f #21299 | put the branch-compare leg on the lifecycle owner, with a currency probe (step 5) | **done, verbatim, after Group D's #20914 landed on main** (first recorded as blocked on it). The compare kept three hand-rolled guards for one reply (`branchCompareGenerationRef`, `currentBranchCompareIdentityRef`, `mountedRef`) combined in an `isCurrentLoad()` four exit points had to remember; the owner replaces the first two — an attempt `reset()`s then `load`s, the reply is published only through `commit(lease, value)`, and what retires a compare is named at the call site (host, route identity, workspace). The three mid-flight state writes (no base ref, refused capability, unreadable reply) become a returned `BranchCompareOutcome`, which also puts the file under the loader-write source fence in `lifecycle-owner.test.ts`. The owner's loader gains a currency probe (`currency.isCurrent()`), so a superseded attempt that spans two round trips stops before its second request instead of being refused at commit — request-count parity with main, pinned by the new loaders test. All seven files matched upstream's parent after the main merge and applied whole: `use-mobile-source-control-loaders.ts`, `generation-scoped-request-owner.ts`, `generation-scoped-lease-compile-fence.ts` (two new `@ts-expect-error` fences: the probe cannot commit and exposes no generation), `lifecycle-owner.test.ts`; new `mobile-branch-compare-outcome.ts` + test and `use-mobile-source-control-loaders.test.ts`. Refactor oracle: upstream's own — no golden moves — holds here over the 759-golden corpus (recorder 1650 passed / 3 skipped, `git status` clean under `rpc-foundation/goldens`); no inventory count moved. Gate: tsc clean, vitest 1030 files / 10722 passed / 17 skipped, oxlint 0 errors |
| ae9c06c94 #20612 | discover and switch native-chat models on desktop and mobile (OMP) | **done, adapted.** An OMP tab's model sheet had no catalog at all. The host now reports the running `provider/id` on every OMP hook post (and re-posts on a switch), discovers the configured choices through `git.discoverCommitMessageModels` with `agentId: 'omp'` (on the mobile allowlist), and installs an `/orca-model <selector>` extension command because OMP's own `/model` only opens a TUI picker; it advertises that command on status so an older host stays read-only. **Shared:** `agent-session-option-catalog-omp.ts` (+ test) and `omp-model-list-probe.ts` (from 4dec64d53, its dependency) vendored new; `agent-session-option-catalog.ts` re-vendored whole (matched the parent); `native-chat-session-option-state.ts` re-vendored at ae9c06c94 by clean 3-way merge onto its local hunk (an empty-seed catalog keeps the report as the tracked row); `agent-status-types.ts` took the four `modelSwitchCommand` hunks by hand (cannot be re-vendored: #19807/#19645 sit in between). `agent-model-probe-spec.ts` and `pi-family-events.ts` are host-side, no phone importer. **Mobile:** `mobile-omp-session-catalog.ts`, `use-mobile-omp-model-discovery.ts` (+ test) new verbatim (the catalog helper widened to accept a readonly list, which this fork's Codex path hands it). Upstream's `mobile-session-option-records.ts` extraction NOT taken — this fork extracted the same records earlier into its own module with `forgetModelReportScope`. The controller, the option controller and the options hook were hand-merged onto this fork's diverged versions: for an OMP tab — and only that one — `reportedModel` is the tab's `agentStatus.model` with source `'live'`, because the OMP extension stamps it on every post and on a switch, which is exactly the launch-record-is-not-a-source rule in `reportedModelPair` does not apply to (it stays for Claude); `discoveredModels` is the OMP list where the agent is OMP and Codex's own otherwise; the catalog goes through `mobileOmpSessionCatalog` so a switch is offered only where `modelSwitchCommand === 'orca-model'`. The seed resolver takes an OMP report verbatim before the catalog match, whose longest-prefix rule would fold `deepseek/deepseek-v4-pro-new` onto `…-v4-pro`. Upstream's options-hook test merged clean and its discovery test came whole. **Failing-first:** reverting the seven modified source files fails 10 cases across the two files (discovery queries the workspace, choices selectable only once the extension advertises support, the hook model kept on failure and older hosts, no previous host's list after a host change, the OMP session under its reported model, the exact report kept beside discovered choices). Green after. Not verified against a live OMP host |
| e7da72c3d (no PR) | fix(omp): attach desktop and mobile images through file mentions | **done, the mobile half, merged.** OMP's TUI does not turn a pasted path into an attachment; it needs an `@file` mention (Orca #20389). New shared `agent-image-paste.ts` decides per agent: Claude, OpenClaude, Codex, Gemini, Cursor, Copilot, Droid and Grok get the raw path (`agentImagePasteWrites` keeps the framed-paste writes), anything else gets `@path`, `@"path"` / `@'path'` when it carries spaces, `@` or one kind of quote, and an `@"file://…"` URL when it carries both, since OMP's mention lexer cannot escape quotes but its resolver decodes file URLs. `file-uri-path.ts` fixed alongside: a POSIX path is no longer backslash-normalised, so `/tmp/a \quotes.png` survives the round trip. **Shared:** both new files vendored verbatim; `file-uri-path.ts` re-vendored whole (matched the parent). `agent-image-paste.test.ts` is vendored but never collected here, so it is mirrored in the new `mobile-agent-image-paste.test.ts`. **Mobile, 3-way merged onto this fork's diverged files:** `mobile-clipboard-image.ts` (`buildMobileImagePastePayload(path, agent)`), `mobile-image-attachment.ts` (the agent on its args), `mobile-native-chat-image-send.ts` (the fork's parked-draft `splitAgentTuiClearWrites` burst kept in front of upstream's `agentImagePasteWrites`), `use-mobile-image-attachment.ts` (agent threaded through this fork's `run()`-based `attachImage`), `use-mobile-terminal-paste.ts`, `use-mobile-session-attachments.ts` (the agent read off the tab's `agentStatus.agentType`, falling back to the native-chat agent, and handed to both the terminal paste and the image attachments; this fork's keyboard image paste through `@codeui/expo-rich-paste` and `attachImageFile` is untouched and rides the same agent), `use-mobile-native-chat-image-attachments.ts` and `use-mobile-session-image-attachments.ts` (clean). Four upstream mobile tests came whole (`mobile-clipboard-image`, `mobile-image-attachment`, `mobile-native-chat-image-send`, `use-mobile-terminal-paste`) and the hook test merged onto this fork's copy (`baseArgs` in its `.test-support.ts` gains `agent: 'claude'`; the ride-along case runs for claude and omp). Two oxlint ceilings moved with dated notes (`use-mobile-native-chat-image-attachments.ts` 300 → 303, its test 806 → 807). **Goldens:** at the port commit (da6cf5e7, on main 46222b13) this fork's recorder corpus had no clipboard-image scenario, so nothing observed the paste text and no golden moved. The group D merge (c6a4e320, taken in 6b94ef28) brought the `clipboard.image-attachment` and `nativeChat.image-paste` families in, and 18 recorder cases went red on the merged tree — the disclosed change, now observable. **Corpus re-recorded once from the merge tree** (`baseline` → 6b94ef28), never copied: 759 → 759, header-only (`baseline`) on 751; the 7 manifest `text` hunks taken from upstream's `pilot-scenarios.json` by scenario (the same six scenarios, `scenarioSha256` moves on their 8 goldens); **8 bodies moved, all of them the paste frame and nothing else** — `clipboard-image-attachment-{pasted,anonymous}`, `native-chat-image-paste-{single,two-images,stops-on-rejection,trailing-image}` and the two `matrix-nativechat.image-paste-terminal.send-*` — `/tmp/x.png` → `@/tmp/x.png`, and the first of two images gains the word-boundary space a mention needs (two-images, stops-on-rejection) while a lone trailing image keeps none. Proved by inlining every value pool into its checkpoint and blanking the frames: the 8 are then byte-identical to HEAD's, and **all 8 re-recorded bodies equal upstream's own recordings at e7da72c3d** (HEAD's equalled upstream's parent). `recorderSha256`, `lockfileSha256`, `adapterSha256` did not move. Route parity re-pinned from the test's own output: `HEAD_RUNTIME_STRING_SHA256` only, strings 678 → 679 at the port commit and 663 → 664 on the merge (the `'agentStatus'` key the attachments hook now reads). **Failing-first:** reverting the nine modified source files fails 9 cases across 4 files (OMP references separated from following text, the captured OMP format kept while the picker is pending, portable references for omp/null/undefined, omp images riding along before the text); reverting `file-uri-path.ts` alone fails the mirror's backslash round trip. Green after. Not verified against a live OMP host |
| b6e8b1a7b #21694 | serve the tasks screen from the page, with its seams (OTA phase C, C2.1 + C2.5) | **done, mobile half; 19 of 29 mobile files verbatim, 10 merged.** New: `app/h/[hostId]/tasks.web.tsx`, `tasks/MobileTasksScreen.tsx` (the screen moved out of the route, verbatim), `platform/clipboard.ts` + `.web.ts` (+tests; native is `expo-clipboard`, web is `native.clipboard.write` through `useNativeVerbs`), `pr-sidebar/MermaidDiagram.web.tsx` (the labelled source box the native component already falls back to), `protocol-block-screen-exit.test.tsx`, `tasks/mobile-tasks-external-link.test.ts`, `mobile-tasks-session-href.test.ts`. Verbatim hunks: the tasks route becomes the shell's flag switch and declares `navigate` / `storage` / `externalLink` / `native.clipboard.write`; the dependency barrel's `Linking` and `useRouter` become the seams; the workspace-create push moves onto `hostNewWorktreeSessionRoute`; the two clipboard writes onto the seam; `MermaidDiagram.tsx` shares its props; the flag census, both parity test-supports and `web-overrides.json`. Merged onto fork files: `MobileMarkdown.tsx` (prose runs, figures, chips untouched — only the link tap swaps `Linking.openURL` for `openExternalLink`); `ProtocolBlockScreen.tsx` (the themed file: `openExternalLink` and `useRouteHandoff` in place of `Linking` and the router singleton) and the two suites' `expo-router` mocks; `SmartWorkspaceSourceField.tsx` calls the seam directly — upstream threads an `onOpenExternalUrl` prop through `NewWorktreeFormSheet` / `NewWorktreeModalController` / `new-worktree-modal-types` / `host-screen-overlays`, a chain this fork never took, so those four have nothing to change here. **Parity ratchet re-pinned** from the test's own output with a dated note: hooks 350 → 351, statements 417 → 418, semantics 3 271 → 3 269 — the same three movements upstream records, render and style holding; the hook hash lands on upstream's own value. Upstream's census rules checked by hand on this fork's tasks page closure (423 files, 68 recorder families): no `react-native` `Linking` import, no `expo-clipboard` import, no raw `/h/${…}` template under `src/tasks`, no `react-native-webview` reach; the two seams are in the closure. Failing-first: seven of the ported cases go red on the pre-port barrel, block screen and workspace-create hook and green after. `config/scripts` (the closure census, render check, route list) and the workflow are the builder's (#21449 row). Recorder in candidate mode: no golden moved |
| 76063e7ab #21712 | certify the tasks page closure, 70 families and 266 goldens (OTA phase C, C2.6) | **done, the pin re-derived for this fork.** `page-closure.ts` (`pageClosureRunTotals`) and the harness's C2 block verbatim; `c2-page-closure.ts` composes C1's table with two halves, `c2-work-item-closure-families.ts` and `c2-task-source-closure-families.ts`, and **both halves are this fork's own tables**: the value-import closure of `app/h/_layout.tsx` and `app/h/[hostId]/tasks.web.tsx` (423 of this repo's modules), matched against each scenario's sites and pinned golden by golden from a measured bridged run over the fork's 759-golden corpus — 68 families / 257 goldens, 108 byte-identical, 115 `result-absent-settlement`, 29 `params-undefined`, 3 `result-absent-stream-release`, 2 `write-ordinal`. The 26 work-item and 22 task-source families are exactly upstream's; the two families short of upstream's 70 are C1's `notifications.push-registration` (no push feature here) and `transport.capability-probe` (not a recorded operation here yet), named in the docstring. The six wholly-divergent families are upstream's six, at upstream's counts. C1's 20 families (94 pins) are inherited verbatim and read back out of C1's file text; C5 agrees on all 20. The test's census, totals, C1-inheritance, C5-overlap, exclusion (149) and drop (256) lines carry the fork's numbers with dated notes; the wholly-divergent assertion is sorted by name since the tables here are emitted largest-first. `config/scripts/mobile-web-app-page-closure-families.mjs` (+test) and the workflow are the builder's (#21449 row). Bridged gate: 763 green, all three closures drift-free |
| ac024d4f0 #21710 | serve the files explorer and preview from the page (OTA phase C, C3.1) | **done, mobile half; 16 of 19 mobile files verbatim, 3 merged.** New: the two files routes' `.web.tsx` siblings, `files/mobile-file-shell-route.ts` (+test; both dynamic segments encoded, the path as a param, a first-value read of a repeated param), `use-mobile-file-preview-back.ts` (+test; the discard confirm as state rather than `Alert.alert`, which is a no-op on React Native Web), `files-router-seam-census.test.ts` (no module under `src/files` imports expo-router's router by value), `mobile-file-path-route-encoding.test.ts`, `mobile-web-shell-files-route.test.tsx`, `platform/haptics.web.ts`. Verbatim hunks: both native routes become the shell's flag switch, `mobile-file-preview-route.ts` (+test), the flag census, `web-overrides.json`. Merged onto fork files: `mounted-bottom-drawer.tsx` (the reanimated arrays and sheet `testID` from #21592 kept; the hardware-back effect is now native-only), `MobileFileExplorerPanel.tsx` (`useRouteHandoff`), `MobileFilePreviewScreen.tsx` — the back handling moves onto the hook and a `ConfirmModal` while the fork's reading positions, `resolveImage`, the PDF kind and the zoom viewer stay exactly where they were. Failing-first: twelve of the ported cases go red on the pre-port routes, screens and route helper (the shell switch, the encoded segments, the router seam census) and green after. `config/scripts` (the files render check, the external-link seam census, the route list) and the workflow are the builder's (#21449 row). Recorder in candidate mode: no golden moved; the three closures drift-free. Gate: tsc clean, vitest 1044 files / 10867 passed / 17 skipped, oxlint 0 errors |



The sweep on 2026-09-13 covered every upstream commit to `origin/main`
(fe4237cd4) whose subject carries `(mobile)`, plus the three mobile Markdown
and terminal fixes that do not. `UPSTREAM.txt` is unchanged by that sweep: all
twelve ports are mobile-only, and its pin list records `src/shared/` files.

The sweep on 2026-09-19 took `docs/upstream-port-backlog-2026-09-19.md`'s
Group A — the 29 native-chat commits on the structured lane from f2af92b2f
(#19705) to 7f5141ae2 (#20977), in the order the table lists them — plus
955051ded (#20138) and e7206f62a (#20868), two commits the backlog did not list
because the files they change did not exist here when it was cut. Seventeen
landed (rows above, each with its failing-first evidence), twelve are in the
Skipped table with the grep that proved no phone reader. Every landed port ran
`tsc`, its own tests red-then-green, and the full suite; the branch was gated
whole after the large ones (#19695, #20133, #20601) and at the end. `UPSTREAM.txt`
gained or moved 41 `src/shared/` pins, and `LOCAL-FILES.md` records every
hand-applied hunk. Not verified against a live host: no desktop here is past
c287a5d9b, so the Fast row, the prompt-cancel X's precise path and the
`stoppable` foreground rows have not been watched arriving; each falls back to
the pre-port view on a host that does not send them.

**This table is behind `main` for work outside the chat-rendering and
shared-path batches.** It was last rewritten at 588b199, and several ports have
landed since without adding a row — the pins in `UPSTREAM.txt` for
`native-chat-tool-activity.ts` (c1e15c400), `native-chat-turn-status.ts`
(b0c67eaf8), `structured-agent-session-*` (1ae7aa8bb, f1d854502) and the
per-session `/` catalog (bf4e27050) all name commits no row here mentions.
Whoever owns those batches should fill them in; they are not guessed at here.

| b6b974cb8 #21714, a445abadd #20949, ff934256a #20634, 605a4ef86 #20622, ee354a35d #21418 | v1.4.206 shared halves: clear stale agent identity after shell exit; bound CDP output; retain recorded transcript paths when resuming; OMP starts new tasks without auto-resuming; OpenCode 2 beta | **done, re-vendored at c62eca398 (v1.4.206)** in cdbf19e3. Every `src/shared` file these five touch and this fork vendors moved to the release whole (`agent-session-resume.ts`, `agent-process-recognition.ts`, `tui-agent-startup.ts`, `pi-agent-kind.ts`, the OMP `omp-draft-launch` / `omp-fresh-launch` / `omp-fresh-config` modules, `agent-kind.ts`, `tui-agent-config.ts`, `tui-agent.ts`, the hook-listener routing files, `opencode-launch-command.ts`, `opencode-database-name.ts`, …). `protocol-version.ts` carries local hunks and took #21418's `AGENT_SESSION_OPENCODE2_RESUME_RUNTIME_CAPABILITY` by hand (LOCAL-FILES.md). None touch `mobile/` upstream; the phone's agent catalog and resume paths read the shared tables. Upstream's own tests are vendored but never run here |
| 335d11126 #21715 | name the Back control in every page-served screen (OTA phase C, C2.8) | **done in c05e0101; 4 of 6 mobile files verbatim, 2 by hand.** Explorer, preview and agent-history hunks as written; the tasks chrome's Back took the role and label on this fork's `TasksButton` (which already carried the hit slop). The census (`page-served-back-control-a11y.test.ts`) is adapted twice: its coverage reads the switched route files under `app/h/[hostId]` — this fork does not vendor `config/scripts/mobile-web-page-routes.mjs`, the host sends page routes at runtime — and it reads `IconButton` / `TasksButton` as pressables, taking IconButton's fixed role from its source. Tasks parity pins moved by the two props (`semantics` 3,269 → 3,271; render tokens 35,291 → 35,299) |
| cef441611 #21729 | name the narrow host header's controls and gate the drawer's hardware back (OTA phase C, C2.9) | **done in 50394451; drawer half verbatim, header half already solved.** `RightDrawer.tsx` and its hardware-back test as written. This fork's header is one toolbar of `IconButton`s and `Chip`s, each named where declared and with the role fixed in the component, so the six upstream a11y-prop hunks had nothing to land on; the new header census keeps upstream's rules over that shape (each expected control found once, not once per toolbar) and reads the fixed role from the component source through `roleFixedByComponent` in the shared `pressable-control-source-reader.ts`, whose `PRESSABLE_TAGS` also names `IconButton`, `Chip` and `TasksButton` |
| 65cde9bb8 #21739 | name the source-control Back controls and split the dock's Close from Back (OTA phase C, C4.3) | **done in 760e2e6d; header test, CustomKeyModal and the census case verbatim; header and panel by hand** (the fork's panel already had the two-handler comment in place; the header now renders Close and Back as two controls with role and label) |
| f3bda1bf3 #21732 | seam moves and the shared shell route guard for the source-control domain (OTA phase C, C4.1) | **done in a6a59c61; 21 of 22 files verbatim, the two renames completed, 1 adapted.** `shell-screen-route.ts` moved from `files/` to `mobile-web-shell/` with its test; the PR sidebar openers go through the external-link seam and its copies through the clipboard seam; the router-seam census walk moved into the seam module and the files, tasks and new source-control censuses call it. `shell-screen-route-census.test.ts` ties its root to the route files that mount the shell instead of the desktop registry this fork does not vendor |
| 6a0200f41 #21724 | certify the files page closure, 28 families and 125 goldens (OTA phase C, C3.2) | **done in d93cef76; all 9 files as written, the five census numbers re-derived for this fork** as C1, C2 and C5 were: 26 families / 116 goldens (upstream's less the two C1 families whose recorder scenarios this fork lacks), identical 60, result-absent-settlement 44, C1 20 / 94, 20 shared with C2 and C5, 56 exclusions |
| ee61e3bd4 #21735 | measure the keyboard from visualViewport inside the page (OTA phase C, C4.2) | **done in 14225358; 18 of 20 mobile files verbatim, 2 merged.** New `platform/keyboard-occlusion` (+web sibling, +tests) and `platform/text-input-font-size` (+web sibling raising inputs to 16px so an iOS focus does not zoom the page). `SmartWorkspaceSourceField.tsx` merged clean. The fork's `MobileSearchField.tsx` is themed through `useTheme()` with inline styles, so its input takes `Math.max(type.body.size, TEXT_INPUT_FONT_SIZE)`: the phone keeps its themed size, the web clears the floor. Upstream's text-input and keyboard closure censuses are `config/scripts`, not vendored |
| f5d2d6e75 #21758 | carry browser screencast frames over the bridge as base64 (OTA phase C, C6.1) | **done in faaeb9b1; whole, verbatim.** All 20 mobile files were at upstream's parent: the grouped base64 encoder, the `screencastBinary` grant, an over-cap frame dropped and counted instead of ending the stream, the stream-frame rules split out of `bridge-host.ts`, and the dropped-frame total on the shell's dev facts. `src/shared/mobile-web-bundle/manifest-contract.test.ts` re-vendored at f5d2d6e75 |
| 6f0fb3fe3 #21754 | paint browser screencast frames through web siblings (OTA phase C, C6.2) | **done in 189adc5a; 20 of 21 files verbatim, 1 merged** (`mobile-browser-frameless-stream.test.tsx`, around the fork's `StyleSheet.absoluteFill` mock). Upstream landed this and #21758 in parallel, so two #21758 pins that expect a decoded frame without `b64` are red on this commit alone and green on the next |
| 4e9d5b577 #21769 | pin the decoded screencast frame with the base64 it now carries (OTA phase C, C6) | **done in 76ad54b1; both test files verbatim** |
| 96c1dd8b7 #21760 | budget the browser pane's frames and put its inputs on the web seam (OTA phase C, C6.3+C6.4) | **done in 7248af4c; 11 of 14 mobile files verbatim, 3 adapted.** `MobileBrowserAddressField.tsx` and `mobile-browser-pane-styles.ts` merged clean around the fork's `StyleSheet.absoluteFill`; the new address-field test mocks `absoluteFill` to match. The three `config/scripts` census files are not vendored |
| e6aa90ff3 #21777 | certify the browser pane's golden families and render it in a page (OTA phase C, C6.5) | **done in bcda9c47; 3 of 5 mobile files verbatim, the harness merged, the table noted; the pin needed no re-derivation.** Measured over this fork's 759-golden corpus the C6 case reads upstream's table exactly: 4 families, 15 goldens, 6 identical, 9 `result-absent-settlement`, recorded at the same two `src/browser` sites. Takes upstream's 0.56 worst-case JPEG constant. Left out: the page render check, the Chromium frame-budget sweep, the closure census and the CI change (`config/scripts`, `.github`) |
| e5181113c #21779 | add the media verbs the page's pickers and paste will use (OTA phase C, C7.4) | **done in 542765d0; whole, verbatim.** All 26 mobile files were at upstream's parent: `native.media.pick` / `read` / `release` in the shell's verb table with strict contracts (`bridge-media-verbs.ts`), the staged-handle registry bounded by release, a TTL from last touch, a live-handle cap and the session's end (`media-handle-registry.ts`, `use-media-handle-registry.ts`), the device half behind injectable calls (`native-media.ts`, `native-media-device.ts`), and the chunk budget moved to the leaf `mobile-clipboard-image-upload-chunk.ts`. `native.clipboard.read`'s image refusal retired. Inert until a route grants the verbs |
| e736a9f29 #21790 | take the session screen's inputs, links, clipboard and routers through the platform seams (OTA phase C, C7.2) | **done in ce484b0d; 15 verbatim, 9 clean merges, 6 hand-merged, 3 tests adapted.** Clipboard writes, reads and probes go through `platform/clipboard` (a refused write now toasts "Couldn't copy" / "Couldn't copy path" and buzzes), the three external URLs through `openExternalLink`, the routers through `useRouteHandoff`. The fork's themed inputs (`TextInputModal`, `MobileNativeChatAsk`, `MobileNativeChatQuestion`) stay inline on `useTheme()` and take `Math.max(type.body.size, TEXT_INPUT_FONT_SIZE)`, 15 on the phone and 16 on the web; upstream's static-palette split of the chat fields (`mobile-native-chat-input{-base,}-styles{,.web}.ts`) and its overrides entry are **not taken**, and the fork's composer already renders at 16. `text-input-font-size.test.ts` checks the three themed inputs for that binding; `session-router-seam-census` lists two holders, since this fork has no `use-notification-pane-navigation.ts` (upstream's push work; that hunk skipped); `QuickCommandRow.test` reads the buzz at `platform/haptics`, which `vitest.setup.ts` stubs globally. **Route parity re-derived:** hooks 289 → 294 and runtime strings 676 → 678, the same +5 / +2 upstream measured, and the same seven pins moved. The fork-only chat bubble copy in `MobileNativeChatMessage.tsx` still calls `expo-clipboard` directly |
| d5dc7b9cf #21785 | budget the terminal snapshot on serialized bytes and hold live output instead of ending the stream (OTA phase C, C7.3) | **done in 447b99e2; 16 of 19 files verbatim, 1 clean merge, 2 by hand.** `terminal.subscribe` takes an optional `snapshotByteBudget`, which only the page sends (`terminal-snapshot-byte-budget.web.ts`, the frame cap less the envelope); the native budget is `undefined`, so the phone's own subscribe params, both engines and their reconnect path are unchanged. The shell's bridge host holds and coalesces a page terminal's output (`bridge-terminal-output-backlog.ts`) instead of ending it on the 4 MiB window, ending only after 20 s with no ack or past 32 MiB. Shared: `rpc-contract/terminal-stream-params.ts` re-vendored at d5dc7b9cf and `terminal-stream-json-byte-length.ts` (+test) vendored new, recorded in UPSTREAM.txt. Web-overrides takes the budget entry only. Route parity: only the callback-body pin moved, as upstream. Reverting `bridge-host-subscriptions.ts` fails 6 held-stream cases. `src/main` snapshot publication not vendored |
| bd5177801 #21795 | put the page's pickers, paste and editor fallbacks on the media verbs (OTA phase C, C7.6) | **done in ecc76577; 15 verbatim, 2 clean merges, 11 by hand.** New `platform/media-picker` seam (+web over `native.media.*`), the clipboard image resize split (+canvas web sibling), the downscale leaf, and plain web fallbacks for the rich Markdown editor and the HTML preview. **The seam's contract carries this fork's picker types**, not upstream's two sources: camera, library, files and clipboard, with the lazy `load`, `name` and `mimeType`. Native `media-picker.ts` binds what each caller passed before (terminal attach: none; chat attach: the clipboard reader and the 2048 px resizer), and the web half maps clipboard onto the shell's source and refuses camera before anything is staged (two cases, red without the guard). Both attach hooks take their image pick from the seam; the fork-only document and held-file paths stay on the phone's picker. `mobile-worker-takeover-send-sites.test.ts` skipped (no worker-takeover report here) |
| d86d5cbbe #21799 | settle browser dialogs on the stream that reports them, map taps through the page scale, and sweep the frame budget on the real encoder (OTA phase C, C6.7) | **done in 19823041; all 10 files as written** (9 verbatim, the pane styles a clean merge). The dialog card stays until the host reports the page took the answer, and a tap on a page without a viewport meta maps through `pageScaleFactor` instead of landing at ~41% of the aim. Reverting `browser-touch-geometry.ts` and `use-mobile-browser-commands.ts` fails 8 cases. `src/main` dialog settlement and the `config/scripts` sweep not vendored |
| 9fbdfc592 #21804 | generate the terminal WebView document from typed modules (OTA phase C, C7.1) | **done in a251dc28; upstream's generator, instrument, tests and 37 modules taken, this fork's terminal work carried into them.** The fork's 42 hunks in the files C7.1 deletes (write hold and self-waking pump, paint-synced remainder and settle, overscroll bend, per-ms momentum and blend, cached surface metrics, theme-key skip, local DECRQM; from 53006758, 8372594a, 2a1d1dec, 93b0198a, fb1d11ac, 9a6507b6, 308beeda, fd006cde, 7097feea, f8c5fbed, c840bc19) each moved into the module now holding that code; the overscroll slice is its own module, `overscroll-bend`, at the fork's slice position. Proved with upstream's own instrument aimed at this fork: `terminal-document-pre-flip-script.txt` is the fork's script, extracted by the rule that reproduces upstream's committed copy byte for byte, and the flip test holds the modules to it token for token with the class counts measured here (qualified 695, scope declarations 85, rebindings 425, braced 321, unbound catches 39, Number 17, shorthand 4, unshadowed 17; upstream 609/73/373/279/36/17/4/7). Markup outside the script byte-identical; the fork's happy-dom scroll suite passes on the generated document and reds when the bend or the write hold is removed. Every migrated assertion kept (scroll-routing +1, its CSS case now reads the document's sheet past the inlined engine sheet). Payload 757055 -> 740521. config/scripts not vendored |
| ec8217313 #21809 | mount the terminal document in the page over its own modules (OTA phase C, C7.5) | **done in b042be82; upstream's seams, controller, page mount, rulings 20 and 21 taken, the fork's terminal work moved onto them.** The flip pin retires as upstream retires it (the proof stays at a251dc28); the byte golden is the fence. The fork's module-level state (indicator frame, theme key, deferred metrics, surface metrics cache, overscroll pull and spring) moves onto the scope, 22 fork fields in all plus `dragging`/`canPanX` on `scope.touchGesture`; every fork frame goes through `scheduleDocumentFrame`; `stopWriteQueue` (held-pump timer) and `stopOverscrollBend` (spring frame) join the page's stop sequence, and the parse-time census pins 11 stops (upstream 9). The page's resize refit invalidates the cached layout, and the fork's CSS moves into `document-style.ts`. Golden review: upstream's golden diff merges onto the fork's C7.1 golden with 14 conflicts, all at fork lines, and the residual is only the conversions above; markup byte-identical to C7.1; payload 740521 -> 743416. `document-scope.ts` gets a dated max-lines override (345; upstream sits at 300) and the stale TerminalWebView.tsx cap goes. `oxc-parser` 0.148.0 becomes a mobile devDependency (upstream resolves it from the desktop's root install). One upstream test narrows React Native's `number \| null \| undefined` cancel handle for the test typecheck |
| 169bccc54 #21727 | restore query-string's named exports under the 9.5.1 override | **done in 48ad7d6f; patch and test verbatim, the defect was live here.** This fork carries the same `query-string: 9.5.1` override (decode-uri-component advisory) and 9.x's entry exports only `default`, so expo-router's and React Navigation's namespace imports had no `parse`/`stringify`: upstream's test failed on this fork before the patch and passes after. The workspace entry sits among this fork's own patched dependencies, all of which survive; `pnpm install --offline` moved only the three query-string lock lines, with upstream's patch hash (8624f1be…) |
| c77a82f78 #21864 | fire the page's haptics over the bridge notify (OTA phase C, C7.10 E) | **done in e577006b; 24 of 26 files verbatim, `bridge-envelope.ts` and `web-overrides.json` merged clean.** `platform/haptics.ts` is byte-identical to upstream's parent, so the notify's five kinds are this fork's five. One adaptation: `vitest.setup.ts` here stubs `platform/haptics` for every test, so `page-haptics.test.ts` `vi.unmock`s it to assert the mapping through the real module. The route-grant census and render checks are config/scripts, not vendored |
| e9b180685 #21871 | render Mermaid diagrams on the page from one deferred engine artifact (OTA phase C, C7.10 B) | **done in 70841307; 18 of 21 files verbatim, 3 merged, one fork file moved onto the new walk.** `MermaidDiagram.tsx` was at upstream's parent, so the native document takes the shared `MERMAID_DIAGRAM_CONFIG` (with `suppressErrorRendering`) and its escaping as written; postinstall gains `build-mermaid-page-engine.mjs`. `rpc-params-contract-type-only-boundary.test.ts` keeps this fork's module-scope sweep and walks through `censusSourceFiles`. This fork's own `orca-mobile-rpc-allowlist.test.ts` held a tenth copy of the walk, which upstream's new fence names, so it imports the shared walk too. Diagrams stay dark-only on the static palette, as they were before on both sides |
| 5d13a70ea #21723 | keep an in-page hop local only when the session's grants cover it (OTA phase C, C2.9) | **done in 5eb47a73; 22 of 23 files verbatim, `bridge-envelope.ts` merged clean; `manifest-contract.ts` re-vendored at 5d13a70ea.** Reverting `navigation/route-handoff.web.ts` to upstream's parent reds three route-handoff cases; the fix greens all 35. The envelope counts 301 code lines against its 300 cap (upstream's sits at exactly 300, the fork's `'user-send'` arm costs one more), so a dated max-lines override at 301 records it |
| 4a3a32206 #21859 | the terminal document is a function of its host (OTA phase C, C7.5b) | **done in f115e25f; 42 of 45 files verbatim, 7 by hand.** The document becomes `createTerminalDocument(host)`, whose start and stop sequences are emitted from each module's own `start*`/`stop*` exports, so `page-document-modules.ts` retires and the fork's `stopOverscrollBend`/`stopWriteQueue` join the sequence by name. The viewport refit moves into `fit-scale`'s start with the fork's `invalidateSurfaceMetrics()` first (7097feea). `touchesInRoot` is applied to the fork's touch handlers, keeping canPanX, the write hold, `nowMs()` and the bend. The fork's smooth-scroll suite reads `generatedDocumentProgram()`. The terminal double gains the `parser` the fork's shape declares (DECRQM). Parse-time census 12 stops (upstream 10, plus the fork's two). Golden regenerated and checked by 3-way applying upstream's golden diff onto the fork's pre-port golden: the remainder is only fork lines. Payload 743416 → 746253. config/scripts not vendored |
| 8d42410e0 #21862 | render the HTML preview on the page in a sealed srcdoc frame (OTA phase C, C7.10 A) | **done in a9a572c5; 16 of 17 files verbatim, `MobileHtmlPreview.tsx` and `web-overrides.json` merged clean.** The native preview takes only the tab role and selected state for its Preview/Source pair. A cancelled top-frame navigation reaches JS as `onExternalNavigation`. The Kotlin and Swift halves came verbatim. *Corrected 2026-09-23: this said "unbuilt here", but the module is autolinked into the APK (see #21417). So this is a native change behind the off-by-default shell flag: `shouldOverrideUrlLoading` now refuses a main-frame navigation to the shell's own document, which it used to allow, and hands a gesture-started foreign URL to JS as `onExternalNavigation`. The batch E review compiled it and ran `testReleaseUnitTest`, 55/55, `MobileWebShellDroppedNavigationTest` 10/10. The a9a572c5 commit message repeats the old claim.* Theme: the native preview and its new web sibling are both on the static dark palette, as the fork's native preview already was, so no light theme was lost; giving it one is separate work. config/scripts not vendored |
| e6de7a4bb #21878 | thread the scope through the terminal document and bundle the native script (OTA phase C, C7.5c) | **done in e68ccb30; 49 of 84 files verbatim, 5 clean merges, the rest by hand.** Every module takes the scope as its first parameter and tsc is the check. The fork's modules are threaded the same way: smooth-scroll remainder and settle, write hold and wake timer, overscroll bend, cached metrics, the once-per-frame indicator, deferred keyboard metrics, the DECRQM answer and the theme-key skip. Frame callbacks passed by reference are wrapped. `create-terminal-document.ts` runs the fork's two stops in reverse module order. The script is an esbuild IIFE bundle. The byte golden, the identity test, the payload digest and the module-order list retire as upstream retires them. Pins re-derived: the bundle's input census is **48, not 47**, the one extra input being `overscroll-bend.ts` (diffed against upstream's own build of e6de7a4bb). The scroll-routing test's fork cases are rewritten against the module source, and the smooth-scroll suite boots the bundle (17/17). Red on mutation: the write hold off (2 cases), the bend at 0 (1 case), and `stopOverscrollBend` dropped from the sequence (the start/stop census). `document-scope.ts`'s dated lint override is removed (285 code lines now); `surface-touch-gestures.ts` gets one at 303 (upstream 258). config/scripts not vendored |
| (follow-up) | fail the gate when the terminal document is stale, and pin the local DECRQM answer | **done in 63d795bd.** `terminal-document-script-freshness.test.ts`: a fresh build of the modules must equal the generated script, and the failure says to run the generator. It fails with `overscrollBend` edited and the script not rebuilt (the batch C review had 79 terminal files passing in that state) and passes once the module is restored. #21878's bundle census checks the same fact, but inside a broader case with an unreadable diff. `terminal-webview-decrqm-answer.test.ts` (from the batch C review) boots the real xterm engine and the bundled document and checks the fork's local DECRQM answer (c840bc19): 3 of 4 fail when the handler does not answer, and 2 of 4 fail without the private-mode registration |
| 23207bfde #21957 | register the source-control and review page routes (OTA phase C, C4.4) | **done in f604e4cd; 20 of 21 files as written, 3 of them clean merges, 1 by hand.** The review route's body moves onto `MobileDiffReviewRouteScreen.tsx` and the handoff seam; both routes become shell switches with `.web.tsx` siblings and the native screen as the fallback, so with the shell off they render what they did. The page's bridge client refuses an oversized frame at its own send boundary under the reader's predicate (`isBridgeFrameWithinCap`), and a frame that cannot serialize stays inside the send contract. By hand: `session-router-seam-census.test.ts` takes the new holder and keeps this fork's absent `use-notification-pane-navigation.ts` out (three holders, not four). Left out: the route registration and its censuses (`config/scripts/mobile-web-page-routes.mjs`, the render check); the host sends page routes at runtime |
| 8297e1d38 #21962 | stop ignoring the retired document-factory artifact | **done in c1937dcf, verbatim** (the `.oxlintrc.json` line merged clean). No factory file exists here; the five generated files postinstall writes stay ignored and untracked |
| a91ca8b19 #21905 | dictation on the OTA page over native audio verbs (Phase C, C7.10 PR D) | **done in d2ad9273; 33 of 34 files as written, 2 clean merges, 1 test adapted.** The shell answers `native.audio.start\|read\|stop` and `native.wakelock.set`; `use-mobile-dictation.ts` (upstream's desktop-transcription hook, here the FALLBACK) takes its device calls through `platform/dictation-capture`, natively the same calls in the same order (`dictation-capture.test.ts`). **This fork's own dictation is untouched**: chat dictates with the phone's recognizer first (`useMobileLiveTranscription`, 6f89a882) and none of that path's files moved; new `native-chat-dictation-engine-wiring.test.ts` reads the wiring from code and reds if the desktop engine is forced or the live hook reaches the capture seam, the raw microphone or keep-awake. The fork's audio package differs from upstream's only in importing from `'expo'` (SDK 57); upstream's hunk there is a comment. `mobile-dictation-mic-control.web.test.tsx` is adapted to this fork's themed `MobileTerminalInputActions` (icons, theme hook, attach sheet and level meter mocked); reverting the refused-start `setStatus('idle')` reds it. Left out: the dictation closure census and session-closure re-record (`config/scripts`). No fork pin moved **Local fix on top, bfb72756:** upstream's seam opened the audio engine for a start cancelled while the OS microphone prompt was up (the permission and the open became one call, so the hook's generation check ran after `initialize`), taking exclusive audio focus on Android; `open()` now takes an optional `stillWanted` check asked between the two, found by the batch F review, red-first in `use-mobile-dictation-cancel-during-permission.test.tsx` **Second local fix:** a start cancelled at the prompt no longer calls `release()`; it opened no engine, and the late tearDown could land on the one a quick second start was opening (batch G review). |
| d9954000b #21969 | the rich editor's document becomes scope-threaded modules and a bundled factory (OTA phase C, C7.10 C1) | **done in e779bd1a, with #19675's editor half, which it builds on and which this fork had skipped.** The editor program becomes TypeScript under `src/components/rich-markdown/`, and the native document is an esbuild bundle from a fifth postinstall generator. The component takes upstream's controller split and keeps this fork's toolbar `hitSlop`. The fork's pre-#19675 string modules are gone with upstream's. **This fork's editor behaviour carried:** wrapped-prose reflow and wrapped list items move into the fork-only `rich-markdown/markdown-reflow.ts`, and `parseListTree` takes `isBlockStart` as a parameter so there is no import cycle, which the bundle test refuses. The fork's five reflow cases move from the string-extraction HTML test onto the modules in `markdown-reflow.test.ts`, plus one through the shipped bundle. Removing the carry reds four. **Pins re-derived:** the page around the script is `1013420c…`, 5,621 bytes, and reads the same on 1150a4e0's document and on this one; the bundle has 23 inputs (upstream 22 plus `markdown-reflow.ts`); and the bare-marker case expects `<p>#</p>` because the reflow drops a paragraph's trailing whitespace |
| 5d51bcaf6 #21950 | serve any manifest route through a [...page] catch-all (OTA phase C, C8) | **done in 0762c2e5; all 27 files as written, 2 clean merges.** `firstParam` moves to `src/navigation/route-param-reader.ts`, and this fork's own three route files (mcp-servers, permission-rules, project-memory) import it from there. **Native change:** on a store build (shell off), an unrouted host path now lands on `PageRouteUnavailableScreen` instead of expo-router's Unmatched. Upstream draws that screen from the static dark palette, and this fork draws it from the theme. `PageRouteUnavailableScreen.theme.test.tsx` pins both modes, and upstream's palette fails its light case |
| 6ed961b32 #21970 | scan for the params contract before parsing, so the boundary census fits its budget under load | **done in 44519bd2, whole.** This fork had worked around the same timeout by hoisting the sweep to module scope. Upstream's `ts.preProcessFile` filter removes the cost the hoist was dodging, so the hoist goes. 3/3 in 1.4 s |
| a3c6d4266 #21964 | admit https: images on the web shell's CSP (OTA phase C, ruling 27) | **done in c26c7f2a; 11 files as written, web-overrides a clean merge.** `img-src 'self' data: https:` in both native halves, and `http:` stays out. The two TypeScript hunks on native files are comment-only. The Kotlin half compiles into the APK behind the flag, and `:orca-mobile-web-shell:testReleaseUnitTest` passes 55/55 on this tree, `MobileWebShellCspTest` 4/4 **Correction (batch G review):** on Android the widening is inert — the shell's WebView sets `blockNetworkLoads = true` and `shouldInterceptRequest` refuses every off-origin request, so no https image loads there. The ported comments ("a provider avatar would load", "loads a remote image exactly as native does") and this row's title describe the CSP, not what an Android shell loads; upstream has the same interceptor. Left as written. |
| 83128ea7a #22054 | four pre-existing rich Markdown editor defects (OTA phase C, C1 follow-up) | **done in a15ecb7c; all 9 files as written, 2 clean merges.** With the six source files at the parent and the new tests kept, all four regression cases were red: one change per checkbox tap, one change for inline code over a selection, a code block holding a fence, and a table cell holding a pipe. Pin re-derived: 24 bundle inputs, upstream's 23 plus `markdown-reflow.ts` (25 since the intraword-underscore fix, which brings in `components/markdown-inline-token-rules.ts`) **Local fix on top:** upstream's table fix doubled every backslash in a cell on save and undid only `\\` and `\|` on read, so any edit rewrote a lone `\s+` in a table to `\\s+` (this repo's own docs included). `markdown-table-rows.ts` now follows GFM: a pipe with a backslash right before it is the cell's, the reader takes that one backslash off, the writer adds one before each pipe, nothing else. Red-first in `rich-markdown-table-backslash-round-trip.test.ts` (5 of 6 red on upstream's code); upstream's own pipe case now expects `c\\d` shown as written. |
| (wrap-up) | guard two fork terminal behaviours and the Mermaid fallbacks the port left untested | **done in 5099cb3b.** `terminal-document-fork-behaviour-guards.test.ts` covers the theme-key skip and the resize invalidation of cached surface metrics (7097feea): #21878 retired the byte golden that was their only guard, and each case fails with its behaviour removed. `MermaidDiagram.fallback.test.tsx` and `MermaidDiagram.web.fallback.test.tsx` are from the batch D review; the fork had no test for `MermaidDiagram.web.tsx` |

| 9fed61e5c #19313, ebb1acfa3 #19683, 47b6c756f #19966, d138c3278 #20526 | agents-sidebar search visibility as a pairing-local preference; structured sessions published into the hook server store; the unexpected-signout notice shown once per version, then once across versions | **done, re-vendored at c62eca398 (v1.4.206)** in one sweep after the v1.4.206 pass found them unrecorded: `constants.ts`, `pairing-local-ui-fields.ts` (+test), `persisted-ui-state-types.ts`, `agent-hook-listener/listener-event.ts`, `agent-status-ipc-payload.ts`, `agent-status-observation.ts`, `runtime-worktree-contracts.ts`, plus `agent-status-run.ts` (#20531's run-identity types) which the payload file now imports. Base→release delta on each is a few added lines; nothing on the phone reads the new fields yet |
| v1.4.209 shared halves: 4085e1cf6 #21734, ea5152f1c #21665, 646fa3645 #21577, 253f0e394 #21606, ac4d6b407 #21925, 663d67087 #21891, f07bf8544 #21863, 347d399bb #22073 | the release's commits that touch no `mobile/` file but change `src/shared/` files this fork vendors | **done, 29 files re-vendored whole at those hashes, 2 hand-merged.** Each file taken at the last of these commits that touches it, byte for byte (checked), plus the five modules they newly import (`agent-hook-listener/opencode-session-{correlation,registry}.ts`, `agent-status-{legacy-adapter,legacy-ingress-manifest,run-capability,serving-readiness}.ts`) and #20624's OMP spec in `commit-message-agent-spec.ts`, whose vendored test already expected it. `protocol-version.ts` takes #21606's capability by hand, and `ai-vault-search-contract.ts` #21863's sort constant (see LOCAL-FILES.md). **Skipped: 4feaaf5c5 #21904** — its `terminalDefaultShellArgs` sits beside `terminalDefaultShell`, which this fork's base-pinned `global-settings-types.ts` predates, and a whole re-vendor pulls settings fields `default-global-settings.ts` does not provide; nothing on the phone reads either. 98299d879 #21367 and 438744ca7 #21854 touch no vendored file. Nothing here changes what the phone does. Mobile gate green; the nine touched shared test files pass on their own (200 tests), with `commit-message-agent-spec.test.ts` green for the first time since the v1.4.206 port vendored it (the OMP spec and the Antigravity `--print` flags were missing) |
| 506446968 #22139 | a same-build cache hit persists the fresh manifest (OTA phase D1) | **done in f1ee697d, with #22237 (both rewrite the same reducer, effects and hook); verbatim except `use-mobile-web-shell-session.ts` and its test, merged around this fork's `pageReady` divergence.** A same-build manifest read writes the manifest beside the cached generation (`persistActiveManifest`, crash-safe `manifest-next.json` swap), except when walled; the reply reader refuses a `buildId` that is not the assets' digest; `readMobileWebShellReachability` moves to its own module. Fork fix: an identical manifest is not rewritten, because the swap's delete races the native view's mount-time read. Reverting the persist effect fails the manifest-persist tests |
| e47ef8cc2 #22141 | the shell tells a page which optional capabilities it has (OTA phase D, C8.1) | **done in 07e76e09; `manifest-contract.ts`/`.test.ts` re-vendored whole at e47ef8cc2, `bridge-host-init.test.ts` by hand (this fork lacks `BRIDGE_ROUTE_PARAM_CLEAR`), the rest verbatim.** Routes may declare `optionalGrants`; `effectiveRouteGrants` narrows them to what this shell implements while `grants` alone decides serving. Without `externalNavigation` the HTML preview renders an artifact's links as text |
| 6ae5ef2d0 #22182 | the page runs one Zod (OTA phase C, C6.5 follow-up) | **skip.** Touches only `config/scripts/{build,verify}-mobile-web-app-bundle*.mjs`, the desktop's page bundler, which this fork does not vendor |
| da1c322b0 #22193 | one build-time switch picks native or OTA, default native (OTA phase E1) | **done in 263c2c73, adapted.** `mobileShellBuildKind()` and `mobileWebShellFlagCanBeOn()` land fresh in `preferences.ts` (this fork predates upstream's flag-gate split); the gate joins this fork's own `app/troubleshoot.tsx`, which the flag census names in place of `TroubleshootView`; dev row and preferences tests verbatim. `mobile-android-release.yml` gains a `shell` dispatch choice (default `native`) passed to `assembleRelease` as `EXPO_PUBLIC_MOBILE_SHELL`; `scripts/prepare-android-release.mjs` decides the kind (a tag push, which carries no inputs, is native; an unknown kind fails) and never publishes an `ota` build, since the update card would offer it to every user. Tested; the workflow itself has not run in Actions. **Skipped:** desktop packaging (root `package.json`, electron-builder, `src/mobile-web/`) and the non-Android workflows |
| 3d76c22b5 #22237 | a refused update serves the cached generation while the host is up | **done in f1ee697d, with #22139; `MobileWebShellScreen.tsx`, `HostRouteNoticeBanner.tsx` (this fork's themed one) and their tests by hand, the rest verbatim.** A bundle refusal over a reachable host opens the generation on disk, judged by its own routes and compat, with a dismissible notice above the page. Restoring the old `onDownloadFailed` branch fails the fallback and refused-update tests |
| 0ab2ba348 #22241 | the page stops writing, importing and requesting what it cannot use | **done in 26924592 for the two reachable fixes.** `host-app-version-store.web.ts` stops the page writing a key its storage refuses; `push-token.web.ts` keeps `expo-notifications` (which throws on the WebView's null `localStorage`) out of the page. **Not applicable:** `desktop-notification-channel.ts` does not exist here. **Skipped:** the favicon fix, which patches a document built by a desktop script |
| b76bc79d7 #22244 | key Metro's transform cache on the shell build kind | **done in 2e89ac08, verbatim** |
| a8786c040 #22283 | the page never removes a host, and stops bundling push | **done in d40fd236 for the reachable bug.** `host-removal-lifecycle.web.ts` rejects with `PageHostRemovalUnavailableError` instead of reporting success, and `handleRemoveHost` shows its message; `AuthFailedBannerActions` moves out onto this fork's themed `Button`, and the page's sibling names the Orca app instead of three dead controls. **Not applicable:** the page-closure re-pins, since neither family was ever in this fork's closure |
| 841d06a96 #22099 | the rich Markdown editor mounts on the page (OTA phase C, C7.10 C2) | **done in ba77faef; 18 mobile files, 16 as written, 2 merged.** New: `MobileRichMarkdownToolbar.tsx` (+test), `rich-markdown/rich-markdown-web-document-mount.ts` (+test), `document-host-root.test.ts`, `page-stylesheet.test.ts`. `MobileRichMarkdownEditor.tsx` is upstream's: the toolbar moves into its own module, and this fork's one carry there, the toolbar button `hitSlop`, moves with it. `TextInputModal.tsx` keeps this fork's themed drawer and takes the new `onAfterClose` prop. The native document bundle is rebuilt from the changed modules, and `native-document-bundle.test.ts` passes on it. The toolbar draws from the static dark palette, as the editor it came out of already did. Fixed after the port (2026-09-24): the editor, its toolbar, the page mount and the document's stylesheet take `useTheme()`, and `document-style.ts` now takes the theme as an argument, so a re-port of it must keep that parameter and `richMarkdownEditorThemeVariables`. Left out: the editable-host font-size check, the rich Markdown render check and the terminal closure census (`config/scripts`). Not tried on a device or in the web shell |
| 35897da0a #22145 | serialize a list the engine nested inside a paragraph | **done in e2016e7a, as written (3 mobile files).** `html-list-markdown.ts` gains `holdsUnownedList`, and `html-block-markdown.ts` writes a `<p>` or `<div>` that holds such a list as the blocks around it, instead of one flattened line. Reverting the two source files fails all three new round-trip cases. Left out: the page render case (`config/scripts`) |
| 226f4a077 #22114 | two more table parsers hold a pipe in a cell (OTA phase C follow-up) | **done for the fix, not for the code.** f1313b38 took it as written: `pr-sidebar/markdown-blocks.ts` dropped its own row splitter for `splitTableRow` from `rich-markdown/markdown-table-rows.ts`. The review then found that the editor's splitter follows marked, which ends a cell at a pipe after an even backslash run, and GitHub does not: through `gh api markdown` (gfm, 2026-09-24) a pipe after two backslashes stays in its cell, inside a code span or not (the cases are in `markdown-blocks.test.ts`). PR comments are GitHub markdown, so "Split PR comment table rows the way GitHub does" puts this parser's own splitter back, with #22114's fix as one change to it: a trailing `\|` is cell text, not the closing pipe. Upstream's "ends a cell at the pipe following an escaped backslash" is replaced by GitHub's rendering of the same row; its trailing-pipe case is kept and passes. **A re-port of `markdown-blocks.ts` must keep the local splitter.** Upstream's `mobile-markdown-parser.ts` hunk does not apply: this fork's chat renderer reads tables through `marked`, and upstream's two `MobileMarkdown.test.ts` cases pass on it unchanged. Left out: the closure census (`config/scripts`) |
| 0b1567a7b #22111 (with 059ee59a48 #22118) | catch the diff-comments loader rejection at the effect (OTA phase C follow-up) | **done in e136d25a.** `use-mobile-session-diff-comments.ts` is upstream's byte for byte: the mount effect ends in `.catch(() => undefined)`. The recording is the test. `matrix-session.diff-notes-worktree.show-1` is re-recorded on its own (`RPC_FOUNDATION_RECORD=1 RPC_FOUNDATION_MODE=--record`, `-t` that case) and loses its three `unhandled-rejection` effects. Its body now equals upstream's golden; the header keeps this fork's baseline `ca51be06…` and its lockfile and recorder digests. Without the `.catch` the new golden fails ("field effects"). `HEAD_EFFECT_SHA256` in the route parity test is re-pinned with a dated note, still 25 effects. **Not taken:** #22118, which only moves all 788 goldens' baseline to upstream's main tip. Left out: the page render check (`config/scripts`) |
| bfcbc6fee #22179 (with 0dae510d04 #22185) | contain the older-history page's rejection at its own boundary, and re-record its family | **done in f1c3e297.** `loadEarlier` returns on an accepted reply that carries no result or a null one, before `'error' in` can throw, and a `catch` swallows a rejected page. The hunk lands unchanged beside this fork's live-update merge. `mobile-native-chat-page-rejection.test.ts` is upstream's. Reverting the hunk fails all 3 of its cases, and the re-recorded `matrix-session.native-chat-page-nativechat.readsession-1` too (48 checkpoints against the golden's 47; body equal to upstream's). The hook goes from 300 to 305 code lines, so `mobile/.oxlintrc.json` gives it a dated `max-lines` override of 305. **Not taken:** #22185, the baseline-only repin |
| v1.4.210 shared halves: 60c43695e5 #22108, d60043787b #22037, eb92222e7f #21705, cd59678394 #22082, 90d363afc9 #21555, 7650abe224 #21923, 2739246058 #21924, 91e6e1f355 #22029, 297cfe0cf3 #22073, ba742a86bb #22247, 88f2f01061 #19430, 15472cd4c6 #21397, 33ba1ff3df #21398 with c49b8cd534 #22058, 0677271709 #18790 with 6956f44d2b | the release's commits that change `src/shared/` and belong to no other v1.4.210 port | **done in c759933f: 17 files re-vendored whole, 3 merged by hand.** Every whole file is byte-equal to the pin UPSTREAM.txt records (checked against the mirror). **#22108, #22037:** `agent-launch-intent.ts` (paneKey; agentArgs, cwd and launchSource), and `rpc-contract/agent-launch-params.ts` with its new test. #22037's `agent-launch-operation.ts` is not vendored here. **#21705:** Antigravity in `agent-session-option-catalog.ts` and the new `agent-session-option-catalog-antigravity.ts`, its `preflightTrust` in `tui-agent-config.ts`, and its case in `tui-agent-startup-session-options.test.ts`. That test also takes **#22082**'s override case, beside `tui-agent-startup.ts`'s `sessionOptionsOverrideAgentArgs` passthrough. #22082's new `agent-startup-plan-inputs.ts` is the host's resolver and is not taken. **#21555:** `crash-reporting.ts` ('code-editor'). **#21923:** `daemon-adoption-telemetry.ts` (+test), `developer-permissions-types.ts`, `telemetry-daemon-event-schemas.ts`, `telemetry-event-registry.ts`. **#21924, partial:** `structured-agent-session-live-turn.ts` whole. `protocol-version.ts` takes the turn-completion capability and its list entry by hand, and `rpc-params-catalog.generated.ts` its `subscribeTurnCompletions` row. The `agent-session-wire.ts` types they gate are not taken: they need `AgentJournalTurnOutcome`, which this fork never forward-ported. **#22029, partial:** `native-chat-tool-preview-prefix.ts` and its new test whole. `native-chat-tool-summary.ts` takes the login-shell unwrap and keeps `countToolCalls` in its c1e15c400 form, now written down in LOCAL-FILES.md and not only in UPSTREAM.txt. The pairing, run-sentence and turn-fold modules are not vendored here. **Already:** #22073 is 347d399bb's change again under a new hash, and `codex-usage-types.ts` has no diff. **Skipped:** #22247 edits the child-process allowlist and pin, which count upstream's `src/main/`; this fork has none, and its copy sits at pin 158. #19430 and #21397 touch only `child-process/fork-process.ts` and `agent-session-restart-continuation.ts`, and neither is vendored. **Net zero:** #21398 and its revert #22058 cancel inside the range, and so do #18790 and its revert 6956f44d2b, including the two `mobile/` files that pair touches. So `tui-agent-config.ts` is pinned at the v1.4.210 tag (4a5afd70a3), which is eb92222e7f's file without #18790's Freebuff entry. The 20 touched shared test files pass on their own (327 tests, run with a temporary config that points `zod` at `mobile/node_modules`). Nothing on the phone reads the new fields yet |
| 86b93e02a #22072 | the microphone owns the wake lock, and the stop reply carries the tail (OTA phase C, ruling 36) | **done in c1c39631 with #22150. Of 38 mobile files, 25 as written (2 of them by way of #22150), 5 deleted as upstream deletes them, 8 merged around fork divergence; 19 goldens re-recorded.** `platform/microphone-screen-lock.ts` is the one screen lock. `native-audio-device.ts` holds it while the mic is open, on both hosts, and is the only `expo-keep-awake` importer (the census pins that). `native.wakelock.set`, the page's keep-awake owner and the Android foreground re-acquire are gone. `native.audio.stop` returns the ring's tail (`{stopped, base64, droppedBytes}`, defaulted for an older shell). A refused desktop start rolls back the capture it opened, but only while it still owns it. The fork's own dictation fixes (bfb72756: `stillWanted` and the cancelled check) stay in `use-mobile-dictation.ts`. **Goldens, all by record mode, none by hand:** 13 move `adapterSha256` only, because the dictation adapter module dropped its keep-awake owner. 2 lose their `keep-awake-*` effects (`speech-desktop-start-fulfilled`, `speech-desktop-start-recording-failed`) and are identical once those effects and the ordinals are dropped. 4 move by the dictation id alone, `...-dakoxjr8wun` to `...-8ig2henseon`: the seeded LCG's first draw used to go to the deleted owner's id, so the dictation id took the second. They are identical once the id is swapped back, and `pilot-scenarios.json` carries the literal in 4 steps. `lockfileSha256` is provenance, not compared, and moved in those 19 too; the 375 goldens where it was the only change were restored. The baseline was not repinned, because a full re-record of the 759 would conflict with the parallel v1.4.210 ports. **Repin the corpus once those have merged.** Not taken: the `config/scripts` dictation-capture census, closure pin and page-route grant rows |
| b9643365b #22132 | the Android audio engine forgets a stop issued while paused | **done in 4904c924, verbatim** (`AudioEngine.kt` byte-identical to upstream). A stop sent while the activity was paused hit an early return and left the resume flag armed, so the next resume reopened the mic with no JS owner. Every stop now clears that flag, and a focus request is abandoned before it is replaced. **Not compiled here (no Android project in the worktree). Needs a device check:** a dictation stopped while the app is in the background must not reopen the mic on return |
| 5769eb672 #22150 | a granted microphone tap launches no permission activity, and an aborted start says so | **done in c1c39631 with #22072; 5 of 6 files verbatim, `use-mobile-dictation.ts` merged around the fork's fixes.** `ExpoTwoWayAudioModule.kt` answers from the permissions manager when RECORD_AUDIO is already held (byte-identical to upstream; **not compiled, needs a device check** that a granted tap does not flash the permission screen). `abandonDictation` carries a reason, a stale abort no longer writes over the start that replaced it, and a late `capture.open()` rejection no longer wipes the closure it reported. Reversing the hook hunks reds 7 cases, among them `mobile-dictation-input-closed-mid-start` and `mobile-dictation-stale-abort` |
| 48bdbb8e2 #22172 | the host-scoping rewrite moves out of the terminal's HTML | **done in 5246d59f; every hunk as written, 4 of the 6 mobile files byte for byte.** `document-style-scoping.ts` moves to `src/style-scoping/`, and both page mounts import it from there: the terminal's and the rich Markdown editor's (on the page since 103530d4, #22099). The terminal-sheet cases stay beside `document-style.ts` as `document-style.test.ts`, the editor's are in `page-stylesheet.test.ts`, and the moved test holds the selector rules and the refused sheet shapes through both exports. The editor mount and `page-stylesheet.test.ts` also carry this fork's theme fix (311b8674), so there only the import line is upstream's. One fork line on top: `document-style.ts`'s comment names the new path (upstream's still names the old one). A pure move, so no failing-first case. The `config/scripts` closure pin is not taken |
| 9a25e318f #22189 | write live-input text through a seam the page can honour | **done in 44b5780f; 6 of 10 mobile files verbatim, 4 merged, the census ported.** The two live-input hooks write through `terminal-live-input-text-write.ts`, whose web sibling sets `value` on the DOM field. The terminal-settings scroll lock moves out of the route file into `terminal-settings-scroll-lock.ts`, whose web sibling uses an inline `overflowY`. Upstream's `config/scripts` census reads only `mobile/`, so it is ported as `src/terminal/set-native-props-web-siblings.test.ts` and runs in the mobile gate. With the hooks and the route at the parent, 3 of its 8 cases go red. Not taken: the live-input probe route, the render oracle and the closure pin (all `config/scripts`) |
| 66ade3057 #22252 | the page's live input stays above the soft keyboard | **done in d2c375dc with #22256; 9 of 13 files verbatim, 3 merged, the parity pins re-derived** (one comment corrected in cb8aebd9). Edge-to-edge makes `adjustResize` inert, so `MobileWebShellScreen` shortens the WebView by the IME, and the session screen reads its keyboard from the `keyboard-occlusion` seam instead of a `Keyboard.addListener` pair that never fired on RN Web. Reverting `MobileWebShellScreen.tsx` reds "shortens the view by the keyboard, which is the only side that can see one". The old keyboard hook, fed an RN Web `Keyboard` that never fires, reds both `use-mobile-session-keyboard-state` cases. **Needs a device check:** on the page route, the live input row must sit above the raised keyboard |
| 0748915ce #22256 | a refused dictation start says why | **done in d2c375dc with #22252; the hook and the new routing test by hand, the parity pins re-derived.** Both failure paths call one `reportDictationFailure`, so `voice_dictation_disabled`, `voice_model_not_selected` and `voice_model_not_ready:*` open the setup sheet whichever path saw them. The hook's oxlint `max-lines` override goes from 303 to 310. Reverting the hook hunk reds both setup-sheet cases in `mobile-dictation-refused-start-routing.test.tsx` |
| 3cfb07029 #21977 | register the session page route (OTA phase C, C7.7) | **done in ad4f4834.** The session route becomes a flag switch over the shell, with a `.web.tsx` sibling, and the screen body moves to `src/session/MobileSessionRouteScreen.tsx`, which the route parity family now roots at. The bridge envelope splits into `bridge-frame-fields.ts`, `bridge-notify-envelope.ts`, `bridge-init-route.ts`, `bridge-route-update.ts` and the host's `bridge-host-frames.ts`/`bridge-host-route.ts`. `paneKey` now rides a re-sent `init` and is erased through `route-param-clear`. The page storage allowlist gains the session keys, and `session-view-preferences.ts` writes through `persistMirrored`. **Hand-merged:** `BRIDGE_FOREGROUND_NUDGE_REASONS` keeps this fork's fourth arm, `'user-send'`. `QuickCommandsSheet`'s Back gets `accessibilityRole="button"`, while `MobileSessionHeader` needed nothing because the fork's `IconButton` already sets it. **Not taken:** the markdown actions' `hardwareBackPress` effect. This fork's only session back handler is in `use-mobile-session-view-switch.ts` (back from terminal to chat, 2026-09-11), and a second one there would have taken back from it. The upstream fix it carried, no `BackHandler` on web, is applied to that handler instead, and a web case in `use-mobile-session-view-switch.test.tsx` is red without the guard. Also not taken: `use-notification-pane-navigation.web.ts` and `notification-pane-tab.ts`, since the fork has no native hook for them to mirror, and the comment this commit adds in `use-mobile-session-diff-comments.ts` recording the loader's unhandled rejection as a known defect, because #22111 (above) fixed that defect and the file is upstream v1.4.210's. The route parity pins pass unchanged on this tree. `persistMirrored` arrives here, and two things were fixed after the port (01207026). Upstream chains its read-back onto the write, so a value AsyncStorage took, followed by a read that failed, rejected the save while the value sat on disk. It now resolves, notes the written value and logs the key; a refused write still rejects. **A re-port of `mirrored-storage-keys.ts` must keep that branch.** Reverting it fails 4 of the 5 cases in `mirrored-storage-read-back-failure.test.ts`. Upstream's write-path census, `config/scripts/mobile-mirrored-storage-write-path.test.mjs`, reads only `mobile/`, so it is ported as `src/storage/mirrored-storage-write-path.test.ts` and runs in the mobile gate. The second review found that census weaker than it read, and 2153af1b hardens it. It blanks comments before matching and lets a call wrap. It holds the owner to an exact export list, `writeMirroredStorage` to its one caller, and the rows to every `persistMirrored` caller. In each row's module every direct store write must name a key the row lists as not mirrored, and outside the rows no module under `src`, `app` or `web-entry` may spell a page key or write one a row exports. Fourteen planted bypasses each fail their own case, and a comment naming the call fails nothing. It cannot see a key assembled from pieces it does not spell, or a helper that writes whatever key it is handed, so `mirrored-storage-every-key.test.ts` calls the real saver for each of the 13 page keys and prefixes and checks that the map holds what the store took. Its first case holds the table to the allowlist. Reverting `saveTerminalAutocompleteEnabled`, `savePinnedIds` or `updateSessionViewOverride` to a bare `AsyncStorage.setItem` fails that key's case, and a new allowlisted key with no row fails the first |
| 9267423f2 #22080 | the bridge host mount reads one args ref, and the handshake fact lives in the session (OTA phase C) | **done in ad4f4834, as written.** `use-mobile-web-shell-session.ts` returns `pageReady`, `MobileWebShellScreen` passes it to the bridge as `sessionEstablished`, and `use-mobile-web-shell-bridge.ts` reads its arguments through one ref. The tests came with it |
| 6731f08c0 #22077 | hold every hybrid shell switch on a neutral state while the flag is unresolved (OTA phase C) | **done in ad4f4834, as written.** New `shell-switch-decision.ts` (`pending` / `native` / `shell`) and `ShellSwitchPendingScreen.tsx`. All nine route switches and the catch-all use it. `useMobileWebShellEnabled` starts at `false` rather than `null` where `mobileWebShellFlagCanBeOn()` is false, so a native store build never paints the neutral frame. That function came with #22193 (above), asked by the loader and the Troubleshoot row, and the hook is its third caller: `preferences.ts` takes upstream's text for it, and the flag census pins all three callers in upstream's one case. Treating an unresolved flag as `native` again turns `shell-switch-decision.test.ts` and `shell-switch-null-flag.test.tsx` red. Fixed after the port (87cba431): `ShellSwitchPendingScreen` draws from `useTheme()` where upstream draws the static dark palette, which flashed a dark frame before every light native screen on a development or OTA build. Its static palette fails the light case of `ShellSwitchPendingScreen.theme.test.tsx`. `HostProtocolGate`'s pending state above it is still the static palette, predates this port and is left for its own fix |
| faf7567df #22180 | the catch-all carries a deep link's query and reads a trailing rest-segment pattern (OTA phase C) | **done in ad4f4834, as written.** `catch-all-page-route.tsx` forwards the query, minus `hostId`, `page` and the fragment. `page-route-policy.ts` is upstream v1.4.210's byte for byte: it matches a trailing `[...page]`, names a refused pattern through `routePatternRefusal`, and `grantsForRoute` prefers a route with no rest segment over the catch-all and reads it through #22141's `effectiveRouteGrants`. The shared manifest contract already admits `[...page]`, so nothing is re-vendored, and upstream's case in `mobile-web-bundle-reply-schemas.test.ts` reads such a manifest on the phone's reader and on the vendored `MobileWebBundleRouteSchema` (it passes on the unfixed code too, since neither reader changed). Reverting the matcher or the query carry turns their tests red |
| 59c0d5585 #22201 | the shell swaps the page's client identity so its terminal reaches init (OTA phase C) | **done in ad4f4834, as written.** New `bridge/bridge-page-client-identity.ts`. `readClientIdentity` is threaded through the host contract, requests, stream frames and `use-mobile-web-shell-bridge.ts`, and it swaps the page's `orca-page-client` placeholder for the socket's own id only when the page declares the `page-client-identity` accept. `client-context.web.tsx` sends the placeholder only when the shell accepts it. Removing the swap turns `bridge-page-client-identity.test.ts` red |
| 3bb9a4e26 #22264 | keep a painted frame under the page until its first paint (OTA phase C) | **done in ad4f4834.** New `ShellWaitingFrame.tsx`, `shell-page-frame.ts`, `bridge-page-painted.ts` and `page-first-paint.tsx`. The session reducer learns `document-started`, `page-ready {reports}` and `page-painted`, the hook projects `pageFrame`, and `MobileWebShellScreen` covers an unpainted page. The iOS `MobileWebShellView.swift` goes transparent. `mobile-web-shell-session.ts`, `mobile-web-shell-gates.ts`, `mobile-web-shell-cached-generation.ts` and `mobile-web-shell-session-step.ts` are upstream v1.4.210's byte for byte, now that #22139 and #22237 (above) are in. `MobileWebShellScreen.tsx` and its test harness are upstream v1.4.210's byte for byte, now that #22252 (above) is in, and its test is upstream's but for one mock line. `StyleSheet.absoluteFill` replaces `absoluteFillObject`, which RN 0.86.3's types do not declare. `ShellWaitingFrame.tsx` takes the same substitution, since RN 0.86.3 does not export `absoluteFillObject` at all, and its test (otherwise upstream's) mocks `absoluteFill` to match. Under upstream's mock the cover spread nothing, so 2153af1b adds a case that it fills the page; the upstream mock or an `absoluteFillObject` cover each turn it red. Removing the cover turns three `MobileWebShellScreen.test.tsx` cases red. Projecting every ready page as painted turns three `shell-page-frame.test.ts` cases red. Kept as upstream designs it: the cover also hides a refused-update notice until first paint, because it fills the same root and paints after the banner, so a page that declares the report and never paints hides the notice for the life of the document, while its dismiss control stays tappable through the cover. **Unverified here:** the Swift change is not compiled (no iOS build), and `withRouteScreenPaintReport` has no caller in this repo. Upstream calls it from the page bundle builder in `config/scripts/`, which this fork does not vendor, so the painted report only arrives from a desktop-built bundle |

## Already solved here

| Commit | What | Evidence |
|---|---|---|
| c300913f9 #17731 | double-scaled commit timestamps | Code UI accepts both units, for older hosts too |
| bd860f34c #21687 | normalize OMP terminal momentum across refresh rates | the fork's momentum was already time-based and refresh-rate independent before this landed (93b0198a, 9a6507b6: `FRICTION_PER_MS` = 0.972 per 60 Hz frame, `vel * elapsed`, a 64 ms step cap measured on the S23, a time-weighted velocity blend and the `nowMs` clock). Upstream's version reaches the same goal with different constants (a 50 ms cap, a 16 ms reference), so its code half and its three source assertions are not taken; the fork's own assertions pin its form. The golden and payload do not move |
| 1dae024ab #19380 | xmldom security patch | the fixed versions are already pinned through workspace overrides |
| c33a44619 #20930 | clarify the notification opt-in screen | `NotificationOnboardingPreview.tsx`, the reduced-motion hook (`ui/use-reduced-motion.ts`) and the "finishes or is waiting" copy landed here with the 0.6.7 interaction work (bb17fce6), themed through `useTheme` where upstream's preview is dark-only; the hold-until-the-OS-answers rule is in the shared hook |
| dcee2c5df #21652 | update vulnerable transitive packages | `query-string: 9.5.1` (and more) already pinned in `mobile/pnpm-workspace.yaml` overrides |
| 5a1acfec1 #18712 | clickable document paths and links in chat | `mobile/src/components/markdown-file-path-detection.ts` (241 lines, 312 lines of test) already turns bare paths in prose and path-shaped code spans into tappable text, and `markdown-href-routing.ts` already routes a markdown link through `routeNativeChatHref` — file targets to the viewer, web and mailto to the system handler. Upstream's shared delta is only `createNativeChatFileHref` and its decode loop, an encoding internal to desktop's remark pipeline. Mobile has its own parser and no remark, so porting it would add an unreachable export to a vendored file |

## Skipped, with the reason

| Commit(s) | Why |
|---|---|
| 3160b54c6 #18554, reverted by d53cbed43 #19203 | upstream pulled its own push-notification feature |
| 74cc9b503 #19935, d33354cfd #19951, 341b13cf6 #20068, reverted in part by e187c8267 #20040 | the re-landed desktop/mobile push integration. Code UI has no push feature and no paired-desktop push transport; the desktop half is `src/main/` this fork does not vendor. Upstream itself pulled the rollout once already |
| 1d1bca2a7 #20661 | upstream's mobile app.json bump to 0.0.50; this fork versions itself |
| 71f3bdb70 #21335, 85576b636 #21382 | upstream's own Android versionCode / version bumps; this fork versions itself (`mobile/app.json`, the `mobile-android-v*` tags) |
| afce0c85c #19115 | `mobile/src/sync-runtime-graph/` does not exist here. The agent-status projection join it short-circuits is part of the runtime graph this fork does not carry |
| 4e1681338 #19675 | a 5,200-line pure refactor that moves settings, diagnostics and editor-document screens out of their routes. These are among the most diverged files in this fork — Code UI wrote its own settings, voice and connection-log screens — so the extraction has no common ground to land on and no behaviour to gain. *2026-09-23: the **editor-document half is now taken**, in e779bd1a with #21969, which builds on it: `use-mobile-rich-markdown-editor-controller.ts`, `mobile-rich-markdown-editor-contract.ts`, `-script-string.ts` and the component's controller split. The settings and diagnostics halves stay skipped for the reason above.* |
| 1798786d4 #19869 | transcript row windowing. Every visible file is `src/renderer/` (NativeChatMessageList and 14 new modules beside it); mobile draws its own list. Its `src/shared/` delta is two optional fields on journal types owned by another agent this session. Windowing the phone's transcript is a Code UI decision, not a port |
| 23df74d85, e628090ad, c37413271, ceafdcad2, 83b1558ec, 0ba7f8dc8, 643571def | the relay speed pass, reverted upstream by d74f8cb78 and d936d8da8 pending a smaller re-land. Code UI already has the dial-stage timing and the network-type gate |
| b51bbf3fc #12772, 4b4acf26a #19769 | iOS only; this app is Android |
| 821c8b7df, da48ad2b4, 2265fce59, 36d209f51 | version bumps and repo chores |
| 06a607a1d, 8f78c2824, 12f53da54, 852495d35, 1c1cb7115 | Orca's orchestration-worker feature, which this app does not implement |
| 51eed5a1b, 14e0d40e0, da836faee | host-side agent and terminal bookkeeping with no mobile surface |
| 2513e2139 #18776, c7bcfa750 #19137 | the host-published status feed for the DESKTOP sidebar. Its wire type (`AgentSessionStatusSummary`) and its capability are already vendored, because `agent-session-wire.ts` is pinned at f1d854502 and `protocol-version.ts` at d07c47593 — both later than these commits. What is left is `projectStructuredAgentSessionStatusSummary`, which nothing on the phone reads: mobile learns a structured tab's status from `sessionTabs` and from its own subscribe |
| fa5ef9988 #19122 | host-side restart settlement (`adjudicateAgentSessionRestart`). The symptom — a turn stranded working after a desktop restart — is mobile-visible, but the phone reads the journal and the host writes the settlement. `agent-session-lease-adjudication.ts` has no mobile importer |
| 39cbc68f1 #19040 | desktop launch routing. Its shared half (`resolveStructuredLaunchSeedOptions`, and the `agent-session-record.ts` line) is already vendored at 546fd9b21 / f1d854502; the phone builds its own create params and has no persisted launch seed |
| 149732df6 #18739 | a native `encodeInto` fast path in `isUtf8ByteLengthWithinLimit` for the desktop's session-write scrollback scan. The phone imports `utf8-byte-limits.ts` for `readUtf8CodePointAt` and `clampUtf8TextPrefix` only; nothing under `mobile/` calls the changed function (grep 2026-09-19, 0 non-test uses). Found while checking the v0.0.50 release notes against this table — the script lists the file, not the symbol |
| cb7f7dd11 #18756 | host-side fallback that retitles a structured tab for a client which does NOT advertise `agent-session.structured.v1`. Code UI advertises it, so it never sees the row. Its `protocol-version.ts` half is already vendored at d07c47593 |
| 6494f2a4f #18933 | resume from the desktop's Agent Session History panel, which this app does not have. The `reveal` capability and `isAgentSessionWireRefusalCode` are already vendored at d07c47593 / f1d854502 |
| ce4a3a418 #19235 | the rewind backend, by its own commit title. **Consumed, not vendored** (2026-09-18): the phone now calls `agentSession.rewind` from `mobile/src/session/mobile-structured-agent-rewind.ts` ("Rewind to here" on a sent prompt, structured lane only, conversation only — the host wraps no file restore). The reply type, the `rewind` field on `agentSession.options` and the `agent_session_rewind:<reason>` refusal shape are copied from upstream's `agent-session-rewind.ts` / `structured-rewind-refusal.ts` into that file and pinned by its test, because `agent-session-wire.ts` cannot be re-vendored past f1d854502 (see LOCAL-FILES.md). Verified against the desktop's own bundle: Orca 1.4.205 carries the method, the `agent-session.rewind.v1` capability and those strings. **Same day, later:** carrying it is not the same as letting a phone call it — 1.4.205's mobile-scope allowlist refuses `agentSession.rewind`, so the control is now probe-gated (see "Catalogued ≠ reachable" below) and does not appear on that host |
| 20eea184c #19130 | the link-action popover. Entirely desktop renderer: the popover component, `http-link-destinations.ts` and the chat link owner all live under `src/renderer/`, and the one `src/shared/` line is a doc-comment on `terminalLinkActionPopoverEnabled` — a setting `mobile/` never imports. The phone has no link-routing preference and no popover; giving it one is a Code UI product decision, not a port |
| 0252fe5c3 #18773 | Codex subagent activity. Upstream designed this so a client without a roster renderer needs nothing: the host freezes a plain-text twin (`Ran 3 subagents (1 failed)`) into the journal beside the block, and `native-chat-subagent-summary.ts` says in its own header that mobile shows exactly that sentence. So the phone already reads a new host correctly. The producer is ~1,400 lines of `src/main/codex/` this fork does not vendor, and `worker-transcript-text.ts` belongs to the orchestration-worker feature already skipped above. **Residual:** without #18773's `native-chat-tool-fold.ts` hunk, a roster row landing mid-turn ends the tool run it sits inside, so one run draws as two. Cosmetic, and the fix is that hunk plus the `subagent-group` block type |
| d15a6df22 #19230 (`update_plan` icon, in effect) | the one-line `native-chat-tool-icon.ts` map entry is vendored so the pin moves, but it draws nothing here. Mobile imports only `isShellActivityToolCall` from that file and picks between a terminal and a wrench; it never calls `nativeChatToolCategory` or `nativeChatToolRunIconName`, and the new entry does not change `isShellActivityToolCall`. The checklist's own `list-checks` glyph comes from the mobile component, not from the category map |
| a567e33bf #17795 | GitHub Projects roadmap timeline | desktop renderer plus `project-roadmap-timeline.ts`, which nothing under `mobile/` imports. The `fieldName` wire field on a date cell is unused here |
| 72befaf36 #18806 | Claude subagent activity on the shared carrier | host-side Claude translation, same residual as #18773. The shared delta is a test fixture for `subagent-group`. Mobile still shows the host's frozen sentence |
| fab78c766 #19977 | one live-turn indicator, and Thinking means reasoning | **done, partial.** Shared: `native-chat-turn-status.ts` re-vendored whole (adds `describeNativeChatActiveTurnLabel`/`formatNativeChatActiveTurnLabel` and the host-settled turn map, drops `nativeChatTurnHasResponse`); `structured-agent-session-live-turn.ts` ported partially for `isStructuredAgentSessionThinking`. Mobile: the live turn's `thinking` now comes from the journal's reasoning tail instead of "this turn has produced nothing yet", and the live label goes through the shared resolver. **Not ported:** upstream also moves the live row from the user row to the list footer — this fork's inverted list already owns that placement through its own header/footer, and the restructure is unrelated to the reasoning fix. The `src/main` halves (Claude `thinking` journalled as a `reasoning` message, Codex streamed-reasoning markers) and `src/renderer` are desktop, not vendored — see the note below on what the installed desktop can send |
| ef6ad2243 #19364 | renderer work plus one optional `projectItems` argument in `structured-agent-session-message-projection.ts`. The phone already calls that function with the default projector. (2026-09-19: the argument is now vendored anyway — the file was re-vendored whole at 434365d2d for #21096's refused-send line — and still unused here) |
| 85d7cf3cc #20280 | preserve delivery ambiguity across a transport cutover | hand-applied; `rpc-client.ts` and `stable-logical-rpc-client.ts` had diverged, the three test files matched upstream and took the patch verbatim. `migrateTo` no longer blanket-rejects its own pending set (the set and its `PendingRequest` type are gone) — `previous.close()` is now the only settlement path, and `sendRequest`'s error arm wraps a stale-generation rejection in `LogicalClientCutoverError(error)` so the physical error survives as `cause` and its delivery-unknown mark is copied onto the cutover error. `RpcClient.close()` gained the settlement-contract doc comment. Our local hunks kept: the success arm still rejects a post-cutover correlated response (upstream resolves it — an older divergence, out of scope here), and the `closed`/`requestGeneration` branches, the liveness-probe surface, and the existing close()/suspendActiveSession() "let the physical close settle" comments are untouched. Nothing skipped |
| 2fc84cb49 #20493 | one tail-follow owner so streaming stops jumping | **done, adapted.** Upstream's symptom was two tail-followers racing: a delayed *animated* `scrollToEnd` easing toward a stale endpoint while a non-animated content-size pin yanked past it. **This fork never had that race** — its inverted FlashList has no timer-driven scroll — but it had the same disease in another form: **three owners of the list's offset.** The view pinned on content growth, `use-mobile-chat-dock.ts` pinned on the dock's height, and `use-mobile-chat-scroll-handlers.ts` jumped. The hook is now vendored as `use-mobile-native-chat-tail-follow.ts`, **adapted to offset-0 tail semantics**: upstream owns a non-inverted list whose tail is a measured content height, so it pins with `scrollToEnd` / `scrollToOffset({ offset: measuredHeight })`; here the tail is the constant offset 0, so `pinToTailAfterContentResize` takes the measured height the list hands it and ignores it — pinning at that height would land the reader at the far end of history. It absorbed `use-mobile-chat-scroll-handlers.ts` (deleted, comments carried over) and composes `use-mobile-chat-following.ts`, which keeps the reader's hold, the long-press ownership and the text-selection flag upstream has no counterpart for; the follow gate moved inside it; `use-mobile-chat-dock.ts` re-pins by calling `pinToTail` instead of reaching for the list. **Newly ported in this pass:** single ownership — nothing outside the hook may move the list, pinned by *leaves the chat list with exactly one caller that can move it*; the intent/geometry split (`following` vs `atTail`, one writer each, metrics deciding only where a RELEASED gesture leaves us — `atTail` is a ref here, not state, because nothing renders off it); `detachFromTail` before a history page, which is inert under inverted geometry (older rows append past the far end, so the tail does not move) and is kept for the contract; and `onLayout={pinToTail}`, which an inverted list only needs as a re-assertion. The momentum fence and the one-frame settle landed earlier and are unchanged. Upstream's 80px slop measured up from the bottom of the content stays this fork's 40px measured from offset 0 — the value tuned on the device, against an offset that does not move. **Still NOT ported, each pinned by a test here:** `showJumpToTail = !following && !atTail` — this fork raises the control the instant a drag starts (*stops following on a drag, so nothing yanks the reader while they scroll* in `use-mobile-chat-following.test.ts`; *does not resume following during a drag near the live edge* in `MobileNativeChatView.test.ts`), and that same flag drives `maintainVisibleContentPosition` (*uses history anchoring only while the reader is away from the live edge*), so upstream's rule would also disable scroll anchoring for a reader who dragged but stayed near the edge. And upstream's `jumpToTail()` on send: this fork leaves a reader in history exactly where they are (*preserves the reading position when a new message is sent from history*) and lets the content growth that follows do the pinning, which is upstream's own "content growth owns tail-follow" principle |
| c21c08322 #20535 | report auth callback failures instead of cancellation. The whole fix is one branch in `src/main/orca-profiles/profile-cloud-pkce.ts`, the Electron loopback server for the Orca Cloud PKCE sign-in: a callback carrying `error != access_denied` now rejects as `orca_cloud_auth_callback_failed` rather than `orca_cloud_auth_denied`. This fork vendors no `src/main/` at all — only `src/shared/` — and does not sign in to Orca Cloud; the phone pairs with a desktop. Its `mobile/` portion is not this app: `mobile/rpc-foundation/` is upstream's RPC recorder harness, absent here, and its 72 touched files are `recorderSha256` fingerprint churn plus two recorder tests. Nothing to port |
| 383c543e0 #21065, 66a894d91 #21092, 1c4f27147 #21105, f2e4d2fdb #21123, 229dd62ca #21173, 6b426a862 #21254, 7e2ebac31 #21266, 0b1cde0e0 #21287 | the eight "repin the recording baseline/corpus to main" commits. Each one is goldens under `mobile/rpc-foundation/goldens/` plus the `baseline` line of `pilot-scenarios.json`, and nothing else (checked per commit with `--name-only`). **This fork re-records its own corpus and never copies a golden from upstream:** every migration wave here (#20668 through #21269, and the recorder commits between them) was followed by a re-recording of the fork's scenarios, with the oracle's claim proved against a pre-port recording rather than against upstream's files. The `baseline` label in the manifest is taken as upstream's own at each wave (currently `50525015`, from #21269's manifest), so it names the upstream tree the fork's manifest was last aligned with, not a commit of this repo. Nothing to port |
| 754134fd6 #21155 | deliver a launch prompt from the host. The host now commits a `submit` prompt to the structured session `agent.launch` just created, through its own `agentSession.send` path, and reports `journaled` with the row id instead of `not-delivered` always. That is `src/main/agent-launch/` and `src/main/runtime/rpc/methods/agent-launch-structured-prompt.ts`, host code. Its one shared hunk splits `structuredAgentSessionSendRequest` into a typed `structuredAgentSessionSendMutation` plus a wire-shaped wrapper; the phone imports only `StructuredAgentSessionAttachment` and `structuredAgentSessionSendBody` from that file (`grep -rn "structuredAgentSessionSendRequest\|structuredAgentSessionSendMutation" mobile/src mobile/app` → 0, 2026-09-19), and neither changed. The mobile create sheet sends no launch prompt today (`agentLaunchCreateParams` builds `agent` + `target` only), so nothing here is observable from the phone until it does |
| 2531dc9d5 #20053 | bound the connect phase against an unreachable host, at the transport. A host whose TCP SYN is black-holed left every `orca <cmd> --environment <unreachable>` silent for 60 s; the fix passes `handshakeTimeout` at the three Node-side remote-runtime WebSocket sites and the relay control socket, all `src/shared/remote-runtime-*` transports the phone does not use (it has its own `rpc-client.ts` with its own connect bound). The phone imports two of the nine files: `remote-runtime-client-capabilities` (unchanged) and `remote-runtime-tailscale-hint`, from which it takes only `isTailscaleEndpoint` — unchanged; the changed export there is `withRemoteRuntimeTailscaleHint` (`grep -rn withRemoteRuntimeTailscaleHint mobile/src mobile/app` → 0, 2026-09-19) |
| f7b2736d6 #20153 | block worktree removal when the archive hook fails. Host-side gate (`archive-hook-removal-gate.ts`, the `worktree.remove` handler) plus an `allowFailedArchiveHook` waiver on the params and an `archiveHookOverride` receipt on `RemoveWorktreeResult`, advertised as `worktree.archive-failure-blocking.v1`. The phone imports `SparsePreset`, `CreateSparseCheckoutRequest` and `SetupDecision` from `worktree/create-types.ts`, none of which changed, and its `worktree.remove-or-skip` operation reads the reply unchecked and offers no waiver (`grep -rn "allowFailedArchiveHook\|archiveHookOverride\|WORKTREE_ARCHIVE_FAILURE_BLOCKING" mobile/src mobile/app` → 0, 2026-09-19). A host with a failing hook now refuses the phone's removal with its own message, which the existing error path already shows |
| dd85e5fc8 #20653 | keep OMP terminals when folder workspaces become Git repos. Host bookkeeping: one optional `folderUpgradeGitRootPath` on `Repo` that the folder-upgrade path records so the original checkout locator stays stable. Nothing under `mobile/` reads or writes it (`grep -rn folderUpgradeGitRootPath mobile/src mobile/app` → 0, 2026-09-19); the phone's six `repo-types` imports take other fields |
| c45b2c94c #21291 | make worktree scan failures actionable. Adds `worktree-scan-failure.ts` and a structured `failureKind` on `DetectedWorktreeListResult`, read by the desktop's worktree scan UI. The phone never lists detected worktrees (`grep -rn "DetectedWorktreeListResult\|unavailableReason\|failureKind" mobile/src mobile/app` → 0, 2026-09-19) |
| 2ed89b878 #20588 | name an unfiltered empty project view instead of blaming a filter. Twelve renderer files (`ProjectViewStates.tsx` and the six locale files) and one doc comment on `GitHubProjectView.filter` in `project-types.ts`. The phone's tasks board does not carry the "No items match this view's filter" copy (`grep -rn "match this view" mobile/src` → 0), and it imports only `GitHubProjectTable` and `GitHubProjectSortDirection` from that file |
| c34b94413 #13664 | bind projects to a specific gh account. Desktop Settings UI plus `github.listBindableAccounts` / `github.validateAccountBinding`, a `ghAccount` binding on `Repo` and on `repo.update`'s schema. The phone's one `repo.update` sends `issueSourcePreference` only, and nothing under `mobile/` names the binding or the two methods (`grep -rn "ghAccount\|accountBinding\|listBindableAccounts" mobile/src mobile/app` → 0, 2026-09-19) |
| aee98ccaa #20767 | make the browser identity one process-wide choice. Removes `BrowserSessionUserAgentMode` / `ProfileCreate` from the browser contracts and adds `browser.identity.v1` with its own params file. The phone imports one browser module, `browser-screencast-protocol.ts`, which the commit does not touch (`grep -rn "userAgentMode\|BrowserSessionUserAgentMode\|browser.identity" mobile/src mobile/app` → 0, 2026-09-19) |
| 13ba649c2 #20825 | let a runtime-created Windows terminal BE the requested shell. Adds a `shell` override to `terminal.create`'s params, `terminal.create-shell-selection.v1`, and the canonical Windows shell-name map in `windows-terminal-shell.ts`. The phone's one `terminal.create` (the transcript tail) sends no `shell`, and its one import from that file is `resolveWindowsShellStartupFamily`, unchanged (`grep -rn "shell:" mobile/src/session/transcript-tail` → 0, 2026-09-19) |
| 241fb9ed9 #20463 | batch file-link checks on their owning host. `files.pathsExist` with `PATH_EXISTENCE_BATCH_MAX`, for the desktop terminal's link underlining. The phone's terminal routes taps through `files.resolveTerminalPath` one at a time and never batches existence checks (`grep -rn "pathsExist\|path-existence" mobile/src mobile/app` → 0, 2026-09-19) |
| 16d1ab81d #12314 | bound WSL installed skill discovery. Host-side: `names` / `sourceKinds` filters on `SkillDiscoveryTarget` and a CR fix in the metadata parser. The phone sends `skills.discover` with `{ worktreeId, refresh }` only and imports `DiscoveredSkill`, `SkillProvider` and `SkillDiscoveryResult`, none changed (`grep -rn SkillDiscoveryTarget mobile/src` → 0, 2026-09-19) |
| 1249be3c8 #20286 | perf: accept UTF-8 text that fits the worst-case byte bound. `isUtf8ByteLengthWithinLimit` in `utf8-byte-limits.ts` is the changed export; mobile imports only `readUtf8CodePointAt` and `clampUtf8TextPrefix` from that file, same finding as #18739's `149732df6` row above (`grep -rn 'isUtf8ByteLengthWithinLimit' mobile/` returns nothing, 2026-09-19) |
| 627b7432c #20312 | perf: find path basename without splitting all segments. `getRuntimePathBasename` in `cross-platform-path.ts` is the changed export; mobile imports only `isPathInsideOrEqual` and `isRuntimePathAbsolute` from that file (`grep -rn 'getRuntimePathBasename' mobile/` returns nothing, 2026-09-19) |
| 3d648003e #20328 | perf: count chat tool calls without a filtered array. `countToolCalls` in `native-chat-tool-summary.ts` is the changed export; mobile imports `createToolInputDisplay`, `truncateToolDetail`, `isShellActivityToolCall` and `MAX_TOOL_DETAIL_LENGTH` from that file but never `countToolCalls` (`grep -rn 'countToolCalls' mobile/` returns nothing, 2026-09-19). Left the file at its c1e15c400 pin; #20332's later hunks to the same file merge around this gap cleanly, see that row |
| 2569a71ce | fix(deps): update vulnerable dependencies without new overrides. Upstream's diff touches no `mobile/package.json` at all — only `mobile/pnpm-lock.yaml` (transitive re-resolution within existing declared ranges) plus `cloud/**` and the root lockfile, none of which this fork vendors. The named packages (react-native 0.83.10, expo 55.0.30, eslint 9.39.4, the jest/babel-preset-expo toolchain) are upstream's own dependency baseline, substantially different from this fork's (react-native 0.86.3 here). Checked whether the equivalent action applies to this fork's own tree: `pnpm audit` in `mobile/` reports "No known vulnerabilities found", and `pnpm install --lockfile-only` produces **zero diff** to `mobile/pnpm-lock.yaml` — nothing to re-resolve within the current declared ranges. Nothing to port |
| 1cd296450 #21311 | perf(mobile): build the two projected git enums once, not per parse. The two files this commit touches, `mobile/src/session/diff-review-reply-schema.ts` and `mobile/src/source-control/git-status-reply-schema.ts`, do not exist in this fork. This fork's git status and branch-compare reads (`mobile-git-status.ts`, `mobile-branch-compare.ts`) have no `conflictOperation`/`compareStatus` zod-schema layer to optimize — the `zod-salvage` reply-schema pattern is used elsewhere here (`workspace-session-schema.ts`, `rpc-operation-result-reader.ts`) but was never ported for git status. Confirmed: `grep -rln 'readProjectedCompareStatus\|readProjectedConflictOperation\|GIT_BRANCH_COMPARE_STATUS\|GIT_CONFLICT_OPERATION' mobile/` returns nothing, 2026-09-19 |
| 4408fe897 #19807 | native-chat subagents as desktop sidebar child rows. Eleven of its thirteen files are `src/renderer/` — the status bridge that maps agent-kind background tasks into the sidebar's `subagents`, and the per-connection `live`/`unverifiable` confirmation. The shared delta is `'unverifiable'` added to `AgentSubagentState`, accepted by `normalizeSubagentSnapshot`, and a renderer-local `subagentObservation` on `AgentStatusEntry` ("absent on hook rows", by its own comment). Nothing under `mobile/` names any of them: `grep -rn "AgentSubagentState\|subagentObservation\|normalizeSubagentSnapshot\|normalizeAgentStatusPayload" mobile/src mobile/app` → 0 (2026-09-19, tests included). The phone's one reader of `subagents[].state`, `liveSubagentRoster` in `mobile-background-tasks.ts`, keys on `!== 'idle'` and reads hook rows off `sessionTabs`, which the desktop never marks unverifiable. Not ported |
| 37603a6cf #19645 | derive `WellKnownAgentType` from `TuiAgent` instead of a hand-copied 22-member union that had drifted past `qwen-code`, `mistral-vibe` and `claude-agent-teams`. Type-only, and nothing under `mobile/` names the union: `grep -rn "WellKnownAgentType" mobile/src mobile/app` → 0 (2026-09-19, tests included). The phone reads `AgentType`, which stays an open string, and its agent ids come from `tui-agent.ts` directly. `agent-status-types.ts` here is also not at upstream's parent (it lacks #19807's `subagentObservation` and the fleet `attention` field), so a whole-file re-vendor would drag in unported changes for a type nothing reads. Not ported |
| 60d793956 #20724 | replace the raw question tool row with an awaiting-input row. Desktop renderer work (`NativeChatAwaitingInputRow.tsx`, the tool run and receipt) plus three shared additions: `native-chat-ask-row.ts` (the row's copy and subject, "read by mobile directly" per its header — but upstream's own `mobile/` never imports it, at ea02d9070 either), `parseAskFromToolInput` on `native-chat-ask.ts`, and a `message-square-more` glyph name on the tool-icon table. Nothing under `mobile/` names any of them: `grep -rn "parseAskFromToolInput\|native-chat-ask-row\|message-square-more\|nativeChatAskRunSubject\|hasNativeChatAskCall" mobile/src mobile/app` → 0, and the phone never reads `NATIVE_CHAT_TOOL_ICON_NAMES` (0). The phone answers a question through its own card above the composer (`MobileNativeChatAsk.tsx`) and draws the transcript's tool rows itself; whether it should also stand a question call in for an "Awaiting user input" row is a Code UI decision, not a port. Not ported |
| 1457d3966 #20502, f02d09c1b #20659 | release sessions after provider root exit; deliver queued messages while the chat pane is hidden. Both are the desktop's outbox — `source?: 'launch'` on an outbox entry, and `admitStructuredAgentSessionOutboxEntry` plus the refusal-index fix in `disposeStructuredAgentSessionSendResult`. The phone has no outbox: it imports `structured-agent-session-outbox.ts` for `structuredAgentSessionSendBody` and the attachment type only, and `structured-agent-session-send-disposition.ts` for `structuredAgentSessionRejectionNotice` only; `grep -rn "reconcileStructuredAgentSessionOutbox\|parseStructuredAgentSessionOutboxEntry\|StructuredAgentSessionOutboxEntry\|admitStructuredAgentSessionOutboxEntry\|disposeStructuredAgentSessionSendResult" mobile/src mobile/app` finds one hit, a comment in `mobile-structured-send-delivery.ts` naming the desktop function it mirrors. The rest of both commits is `src/main/` and `src/renderer/`. Not ported |
| 9ed561c1d #20921 | judge Stop against the turn the journal published. `src/main/claude/`; its shared line is `activeStructuredAgentSessionTurnIdBySequence` on `structured-agent-session-live-turn.ts`, a host-side reader over unordered items. `grep -rn "activeStructuredAgentSessionTurnIdBySequence" mobile/src mobile/app` → 0. Not ported |
| c702e77bc #20944, 7f5141ae2 #20977 | stop reading the terminal arguments field on the structured chat route; make the Agent Permissions toggle apply to Codex chat. Desktop launch routing: `tui-agent-launch-customization.ts` (never vendored here) gives way to `tui-agent-launch-command-override.ts`, and `tui-agent-launch-defaults.ts` gains `tuiAgentArgsBypassPermissions` / `resolvedTuiAgentArgsBypassPermissions`, which #20977 then makes tokenize the argv so quoted text and operands after `--` cannot authorize a structured session. The phone imports `resolveTuiAgentLaunchArgs`, `resolveTuiAgentLaunchEnv` and `isResumableTuiAgent` from that file, none of which either commit changes; `grep -rn "tuiAgentArgsBypassPermissions\|resolvedTuiAgentArgsBypassPermissions\|hasExplicitTuiLaunchCommand\|hasExplicitTuiLaunchCustomization" mobile/src mobile/app` → 0. The phone has no Agent Permissions toggle of its own; the structured create it sends carries no arguments field. Not ported |
| 7a1f55c52 #20519 | a failed Claude background task as a typed row instead of an opcode. The same shape as #18773 (skipped above): a new `background-task` block on the dual carrier, where the host freezes a plain-text twin beside the typed block for clients without the block type — `backgroundTaskFallbackText`'s own comment says "mobile renders only this". So a host past this commit already reads correctly here: the phone shows the provider's own sentence (or `Background command "x" failed`) where before it showed the wire opcode twice. The producer is ~5,000 lines of `src/main/claude/` this fork does not vendor; the phone's own background-task surface reads the host's roster (`mobile-structured-background-tasks.ts`), not transcript rows. Nothing under `mobile/` names the block: `grep -rn "isBackgroundTaskBlock\|native-chat-background-task-row\|type: 'background-task'" mobile/src` → 0 (a plain `background-task` grep hits only the app's own expo background-task modules), and `native-chat-types.ts` carries hand-applied hunks that keep it off a whole-file re-vendor. **Residual, as for #18773:** without the `native-chat-tool-fold.ts` hunk a task row landing mid-turn ends the tool run it sits inside, so one run draws as two. Cosmetic; the fix is that hunk plus the block type. Not ported |
| 990711756 #21278 | record an explicit provider outcome on every structured turn. A failed turn was journalled `completed`, identically to one that succeeded; the host now writes `outcome: success / failure / cancellation` on the turn record (absent means unknown, never success). Shared: the field on the lifecycle type and both zod shapes, and `readAgentJournalTurnOutcome` on the turn-record reader. Nothing on this phone, and nothing in upstream's own `mobile/` at ea02d9070, reads it: `grep -rn "readAgentJournalTurnOutcome\|AgentJournalTurnOutcome" mobile/src mobile/app` → 0. The phone's zod `turn` shape is a plain `z.object`, which strips the unknown key rather than refusing the row, so a host past this commit still parses. Drawing a failed turn as failed is a Code UI decision that would start from this reader; not ported |
| 4d3b0cca8 #20615 | publish parent transcript paths for native chat (OMP). Host-side: `extractAgentProviderSession` now reads OMP's `session_file` hook field into `providerSession.transcriptPath`, which the host publishes on the tab; the phone already reads that field off `agentStatus.providerSession` (`mobile-native-chat-eligibility.ts`) and needs no change to receive it. The other shared line is a comment on `nativeChatRequiresLocalTranscript`, whose body is unchanged (OMP stays gated on a local transcript). `agent-session-resume.ts` also differs from upstream's parent by an orchestration-worker field this fork does not carry. Nothing to port; the #20655 mock scenario's "current OMP hooks may report a path" is this commit |
| ea02d9070 #20620 | answer startup Kitty queries before renderer handoff (OMP). The host's PTY startup ingress (`pty-startup-ingress.ts`, the new `pty-startup-query.ts` and `pty-startup-color-query-answer.ts`) answers OMP's `ESC [ ? u` Kitty keyboard probe when the client's terminal declared support, negotiated by a new `agent-session.keyboard.v1` capability and a `terminalKittyKeyboardProtocol` flag on `terminal.create` / `agentSession.create`. This phone's terminal is xterm.js in a WebView, which does not implement the Kitty keyboard protocol, so it must not declare it; it sends no `terminalColorQueryReplies` either (`grep -rn "terminalColorQueryReplies\|terminalKitty" mobile/src` → 0 outside the generated engine). None of the ingress modules is imported here. Not ported |
| c263f5d09 #21402 | repin the RPC recording baseline to main after #21374. Pure repin: `baseline` in `pilot-scenarios.json` moves 5741d48e5 → 1e3795de9 and every one of the 787 goldens moves that one header line, nothing else. This fork re-records its own corpus (see the #21374 row: 264 goldens, re-recorded from this tree) and its `baseline` is its own pin, so there is nothing to take |
| fc3a5d732 #21510 | repin the recording corpus to main's tip (OTA phase C, C0.8). Pure repin: `baseline` moves and 787 goldens move that one header line. This fork re-records its own corpus against its own pin (see the #21485 row), so there is nothing to take |
| 5a208ce6f #21588 | repin the recording corpus to main's tip after #21566. Pure repin: `baseline` moves and 787 goldens move that one header line. This fork re-records its own corpus against its own pin (see the #21485 row), so there is nothing to take |
| 3ad6b7e46 #21690 | repin the recording corpus. Pure repin: `baseline` moves and the goldens move that one header line. This fork re-records its own corpus against its own pin (main's is 3234d157 since the 0.9.1 work), so there is nothing to take |
| e1c8df41e #22113 | declare `externalLink` on the two page routes that reach the protocol wall (OTA phase C). The whole change is in upstream's `config/scripts/`: the page route registry `mobile-web-page-routes.mjs` and four `.test.mjs` checks over it, none of which this fork vendors (see the #21732 row: its censuses root at the route files instead). Nothing under `mobile/` or the vendored `src/shared/` changes, so there is nothing to take |


## Pending

**Assess before porting** — f4c282116 #18652 moves the session journal onto
SQLite. That is host storage; confirm nothing on the phone depends on the old
shape before touching it. The #19822 verdict lives in
`scratchpad/19822-assessment.md`; its working-state half is already ported.

## Catalogued ≠ reachable: the mobile-scope RPC allowlist (2026-09-18)

**The RPC catalog says which methods the desktop registers. It says nothing
about which of them a phone may call.** Orca's WebSocket dispatch checks every
request from a `mobile`-scope device token — the only kind the phone ever
holds — against a hardcoded allowlist before dispatch (`if (u.scope ===
'mobile' && !iRa.has(s.method))` in the 1.4.205 bundle) and refuses the rest
with `{ code: 'forbidden', message: "Method '<m>' is not available to mobile
clients" }`. Nothing in `src/shared/rpc-contract/` records this, tsc cannot see
it, and there is no RPC that lists it. The only way to learn a host's answer is
to call.

That is how two of the day's ports and three editors shipped dead controls:
`files.write` ("Revert this hunk", and Save on MCP Servers / Permission Rules
/ Project Memory) and `agentSession.rewind` ("Rewind to here") are both
catalogued and neither is on the list, so every tap got the refusal. A third,
`skills.discover` (the `/` menu's installed-skills read), had been refused
silently since it landed and re-asked every 3 s.

- **The list, as read from the installed bundle:**
  `mobile/src/transport/fixtures/orca-mobile-rpc-allowlist-1.4.205.json` (289
  names, version in the file name; a newer Orca means a new fixture under its
  own name, never an edit to this one). `files.read`, `files.createFile`,
  `files.resolveTerminalPath`, `terminal.*`, `nativeChat.*`, `worktree.set` and
  the `agentSession.*` mutations the chat already uses are on it. The fixture
  also carries two `aiVault.search*` methods the vendored catalog predates.
- **The probe:** `mobile/src/transport/host-mobile-capabilities.ts` asks each
  gated method once per connection (keyed on `useLastConnectedAt`, like the
  reconnect refetch) with parameters the host must refuse without side effects,
  and reads the refusal: the gate's own text, or a host with no such method →
  hidden; any other refusal or a result → shown; a transport failure → logged
  (`[capabilities] probe failed`), the last settled answer kept if there is
  one, asked again next connection.
  The verdict is a property of the host, not the socket: a reconnect re-probes
  in the background while the last answer stands, so nothing blinks off for a
  round trip (a review of the first cut found it did, and a half-typed rule
  went with it). The four surfaces read `useHostMobileCapability(hostId, key)`
  and offer nothing tappable until it is true; the three editors read the
  tri-state verdict and draw neither Save nor a line until the host has
  answered, then become viewers with one line, "Read-only from the phone on
  this Orca version." The feature modules stay — they are correct for a host
  that allows the call. **The exception is per method, not per surface:**
  `useProjectConfigFile().save` and `revertDiffCardHunk` still send
  `files.write` ungated, the gate sits on the buttons, and a new caller of
  either helper passes the ratchet because the method is already excepted.
- **A fourth kind, `capability` (2026-09-19, with #19850):** a method the
  phone sends only after `status.get` advertised a runtime capability the
  fixture's host does not have, whose sender downgrades to the older method
  on the gate's refusal. `agent.launch` is the one entry: the 1.4.205 bundle
  registers neither the method nor `agent.launch.v1`, so that desktop never
  receives it. The ratchet checks the real gate and reader rather than a
  marker — the named file must read the capability through its
  `protocol-version` constant, and the reader must accept the gate's
  verbatim refusal and `method_not_found` while refusing `invalid_argument`.
- **The ratchet:** `mobile/src/transport/orca-mobile-rpc-allowlist.test.ts`
  walks every catalogued method literal in `app/` and `src/` and fails on any
  that is neither on the fixture nor an explicit, typed exception (probe-gated,
  fails-open with a sender that reads the refusal, or provably never sent —
  "provably" meaning every occurrence sits in a position that cannot send: an
  array element that is not itself a call argument, a type literal, a `case`
  label; a wrapper call fails the claim, because the send shapes the scan
  recognises are a floor, not the tree's whole set). A new operation on an
  unlisted method fails it until it is one of those.
- **Prior art the map did not read:** the phone already recognised this exact
  refusal in three "old desktop" fallbacks — `files/file-list-fallback.ts`,
  `source-control/mobile-git-status.ts`, `dictation/mobile-dictation-setup.ts`
  — each hand-matching `not available to mobile clients` for a method that was
  off the list on an older desktop. The shape was known; the rule was not
  written down. `transport/mobile-scope-refusal.ts` now names it; those three
  keep their own match because each also folds in `method_not_found` for a
  desktop that predates the method.
- **The rule for the next port:** before promising a feature on the strength
  of a catalog row, check the fixture. "In catalog: YES" is necessary, not
  sufficient; the feasibility map's column of that name was written as if it
  were sufficient, and it was corrected the same day.

## Known gaps left behind

- `skills.discover` is refused for every phone on Orca 1.4.205, so the `/`
  menu never lists the user's own skills there. The phone now asks once per
  client and stops (it used to re-ask every 3 s); the list is empty, not
  wrong. Nothing on the phone can change the host's answer.

- The background-task roster has **not been watched arriving from a live host**.
  The contract and the projection are tested; no real desktop has published one
  to this phone. The fallback is the safe direction: a host that publishes
  nothing leaves the tab on the transcript reader.
- The wire's per-task `totalTokens` is read by the equality check and then
  dropped, because the sheet has nowhere to show it.
- The Windows status line and Stop hook run for real under PowerShell 7 in
  tests, and have never run on Windows.
- Live Codex `projectNativeChatTaskListFrames` (a `plan` notification rewritten
  as an `update_plan` tool call so the checklist can draw it) is still desktop
  only. The phone already draws a real `update_plan` call.

## Ported comments that cite checks this fork does not run (2026-09-23)

This fork does not vendor upstream's `config/scripts/`. That means the desktop-side bundle builder,
its render checks in headless Chromium, and the closure and seam censuses written as `.test.mjs`.
Code ported from upstream keeps comments that name those checks as the proof of a claim, for
example "measured in `mobile-web-app-mermaid-render.test.mjs`". Those checks run on upstream's CI
and **never here**, so a claim resting on one is unverified in this repo unless a local test
covers it. The comments stay as upstream wrote them, because rewriting them would add a conflict
to every later port. This table is the correction. Regenerate it by grepping `mobile/src`,
`mobile/app` and `mobile/web-entry` for each mobile-related filename under upstream's
`config/scripts/`.

| Cited check | Where the fork's code cites it |
|---|---|
| `mobile-web-app-haptics-seam.test.mjs` | `src/mobile-web-shell/page-haptics.ts`, `page-haptics.test.ts`, `page-route-policy.test.ts`, `web-entry/web-overrides.json` |
| `mobile-web-app-mermaid-render.test.mjs` | `src/components/pr-sidebar/MermaidDiagram.web.tsx`, `web-entry/web-overrides.json` (the page's diagram sanitising and its byte match with native. Locally, `MermaidDiagram.web.fallback.test.tsx` covers the failure path only) |
| `mobile-web-terminal-engine-closure.test.mjs` | `src/terminal/terminal-webview-html.web.ts`, `web-entry/web-overrides.json` |
| `mobile-web-app-web-overrides.test.mjs` | `web-entry/web-overrides.json` |
| `mobile-web-app-frame-budget-sweep.test.ts` | `src/browser/browser-screencast-request.web.ts`, `browser-screencast-budget-at-the-shell.test.ts` |
| `mobile-web-app-page-closure-families.test.mjs` | `src/test-support/bridged-parity/c3-page-closure.test.ts`, `c6-browser-closure-families.ts` |
| `mobile-web-page-routes.mjs` | `src/mobile-web-shell/page-served-back-control-a11y.test.ts`, `shell-screen-route-census.test.ts` (the route registry; this fork's censuses root at the route files instead, see #21732) |
| `mobile-web-app-source-control-render.test.mjs` | `src/components/pr-sidebar/pr-comment-card-web-avatar.test.tsx` |
| `mobile-web-app-html-preview-render.test.mjs` | `src/components/mobile-webview-editor-web-fallbacks.test.tsx` |
| `mobile-web-app-clipboard-image-resize-render.test.mjs` | `src/session/mobile-clipboard-image-resize.web.ts` |
| `build-mobile-web-app-bundle.mjs` | `src/reanimated-web-mapper-deps.test.ts`, `web-entry/route-manifest.ts` (the builder itself, named as what consumes these files rather than as a proof) |

17 files, 20 citations. They are all on the OTA page's side (web siblings, the shell's page
seams, and censuses of the page closure). None of them backs a claim about the phone's native path.

## Claude `thinking` on the terminal-driven transcript lane (2026-09-13)

The reported bug — the model's summarized thoughts drawn as its reply — is
**not fixed by #19977 and cannot be fixed on the phone today.**

- Orca's transcript reader (`src/main/native-chat/transcript-record-blocks.ts`)
  turns a Claude `thinking` content block into a plain `{type:'text'}` block on
  an ordinary assistant message. `transcript-line-decoders-claude.ts` has a
  comment saying a thinking-only message becomes a `reasoning`-role message, but
  `claudeMessageRole` returns `assistant` unchanged, and the test named
  "marks thinking-only assistant content as a reasoning surface" only asserts
  the block, never the role. Verified at upstream `origin/main` fe4237cd4
  (2026-09-13) and in the installed Orca 1.4.200 bundle. So on this lane a
  thought and a reply are byte-identical to the phone: same role, same block
  type, same `source: 'transcript'`. Per this repo's rule, the phone shows the
  row as it arrives rather than guessing.
- #19977 fixes the **structured** lane only: `claude-structured-journal-translation.ts`
  stops appending a Claude thought as `kind: 'status'` and appends
  `kind: 'message', role: 'reasoning'` instead. The installed 1.4.200 still
  writes `kind: 'status'` (checked in the bundle), so even the structured lane
  cannot deliver reasoning as reasoning until the desktop is updated past
  fab78c766 (2026-09-11). No Orca tag contains that commit yet; `package.json`
  reads 1.4.197 both at fab78c766 and at `origin/main`.
